# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# NOTE: outputs/ (figures·renders) 와 assets/ 는 일부러 커밋합니다.
#       로컬에서 받자마자 결과 그림이 바로 보이게 하기 위함입니다.
#       다시 만들 수 있는 파일이라 용량을 줄이려면 아래 두 줄의 주석을 푸세요.
# outputs/
# assets/meshes/

# 장면 조립용 임시 OBJ (scene_build.drone_parts 가 씀 — 정본 자산 아님)
assets/meshes/**/_scene/

# 팀미팅 덱: 현재판(v8)만 버전관리. v5 는 한국어 메모가 남은 원본이라 로컬 보존만.
teammeeting_0714_v5.pptx
