# 재개 지점 — 2026-09-10 (저녁)

⛔**이 파일부터 읽는다.** 다음 `docs/CLAIM_GATE.md` → `CLAUDE.md` →
`work/sweep_0904/RESUME_0910.md`(오전, §4·§5 가 끝난 목록) → `RESUME_0909.md`.

⭐**팀미팅은 2026-09-14(월) KST.** 남은 시간 약 4 일.

---

## ⭐지금 상태 (2026-09-10 19:45 KST · 마지막 갱신)

| | |
|---|---|
| **덱** | `team_meeting/teammeeting_0910/_out_0910_v26.pptx` — **v26 이 발표에 쓸 판** (`eb2080d`) |
| **시오나** | `05b85ac3` 이후 · 원격과 같음 · 작업 트리 깨끗 |
| **큐** | 0922 진행 중 **48/144** · 워커 11 · 지킴이·감독자 각 1 |
| **사슬** | `jobs_0923.txt` → `jobs_0924.txt` 대기. `queue_chain_0910_done.txt` 가 **비어 있다** = 아직 0922 만 돈다 |
| **재빌드** | ☑**끝났다.** 낡은 발행면을 전부 다시 구웠다(§3 ①②) |

### ☑덱 판단도 닫혔다 — **v26 으로 구웠다**(사용자 결정 09-10 저녁)
6 쪽 「did not move **at all**」 → 「the **typical** count did not move」 ·
9 쪽에 「그 수는 광선 예산에 매달린다」 한 문장. ⛔그림 재굽기 없음.

### ⛔이제 남은 것은 **큐를 마르기 전에 채우는 것**뿐이다
09-13 새벽쯤 마른다(§2). 0922 결과가 앉는 중이니 그때 재고를 다시 세고 뽑는다.

---

## 0. 세 줄

1. **덱은 v25 가 최신이고 발표에 쓸 판이다** — `team_meeting` `7959788`.
   v23·v24·v25 를 오늘 연달아 구웠다(§1). ⛔새로 고칠 일이 생기면 **v26** 으로 굽는다.
2. **큐가 돌고 있다** — 0922 진행 중, 사슬에 0923 → 0924 대기. ⚠09-13 새벽쯤 마른다.
3. ⛔**샤드를 세션 내내 안 담고 있었다 — 사용자가 짚어 줘서 244 장을 담았다**(§4).
   앞으로 **세션마다 샤드를 담는다.**

---

## 1. ⭐덱 — 오늘 셋을 구웠다. 왜 그랬는지가 중요하다

| 판 | 무엇을 고쳤나 |
|---|---|
| **v23** | 9 쪽 노트가 **처리 차례를 거꾸로** 말했다. ⛔그림은 안 바뀜(코드가 맞다) |
| **v24** | 6 쪽 「씨앗을 바꿀 **설정이 없다**」 → 틀렸다. 솔버는 `seed` 를 매개변수로 받는다 |
| **v25** | ⛔**v24 의 내 수정도 틀렸다** — 「안 만들었다」가 아니라 **이미 8 판 흔들어 봤다** |

⭐**v25 의 참인 말** — `benchmark/raybudget_seed_ladder.py` 에 `--seed` 가 있고
`outputs/raybudget_seed_ladder.json` 에 씨앗 8 판이 있다(4e9 에서 레벨 표준편차 **1.833 dB**).
⚠단 그 판은 **40 m · el −15° · matrice4e** 한 자리라 덱 조건(15 m · el 0)이 아니다.
⇒ 「이 판에서는 안 흔들었다」 + 「다른 자리에서 흔들었더니 레벨이 약 2 dB 움직였다」가 참이다.

⛔**교훈** — 인용한 수만 검산하지 말고 **그 수가 사는 원장 전체**를 봐라.
v24 의 잘못은 `verify_raybudget_meaning.json` 의 `seed_vs_budget` 블록을 안 열어 봐서 났다.

### ⚠덱에 아직 안 넣은 것 — 사용자가 정할 일
6 쪽 「the count of copies did not move **at all**. Still three.」
· 「Still three」는 **일곱 칸 전부 참**이다(벌 수 중앙 3).
· ⛔그런데 「at all」은 아니다 — **낙차 자세 수가 광선을 탄다**:
  광선 1e8 → 낙차 **0** · 1e9 → **14** · 4e9 → **37** (경로중앙 31 → 240 → 1,015)
  원장 `outputs/read_dropladder_0910.json` · 판독기 `benchmark/read_dropladder_0910.py`
· ⛔「낙차가 광선의 산물이다」로 읽으면 안 된다 — 1e8 은 경로가 31 개뿐이라 구조가 성기다.
  「적으면 안 보인다」와 「많으면 생긴다」를 이 축으로 못 가른다.
⇒ 덱에 넣을지 말지는 **사용자 판단**이다. 넣는다면 v26.

---

## 2. 큐 (KST 14:04)

```
큐 26/144 · 워커 8
```
· 사슬 `runners/queue_chain_0910.txt` 에 `jobs_0923.txt` → `jobs_0924.txt` 대기.
  `runners/logs/queue_chain_0910_done.txt` 가 **비어 있다** = 아직 하나도 안 띄웠다.
· ⛔⛔**사슬이 소진되면 지킴이가 스스로 죽는다**(`queue_keeper_0827.sh:25`).
· 되살리는 법·30 초 점검은 `RESUME_0910.md` §1 에 그대로 있다.
  ⛔감독자에 `CUDA_VISIBLE_DEVICES=""` 를 붙이지 않는다.

---

## 3. ⛔다음 사람이 할 일

### ☑① 「어느 세대가 정본인가」 — **구웠다**(2026-09-10 저녁, 사용자 결정)

발행면이 제 입력보다 한 세대 낡아 있던 것을 전부 다시 구웠다. 커밋 `16fa1cdc` · `63b9f9e5` · `deee30e4`.

⭐**굽는 과정이 결함 다섯을 잡았다 — 이것이 재빌드의 값이다**
 · ⛔`geometry_benchmark` **D1 칸이 자기모순**이었다 — 근거는 「`sigma_anchor` 참조 **0회**」(박아 둠),
   같은 칸의 `grep_count` 는 **13**. 앵커는 2026-08-04(`e40b6aa8`)에 **배선됐다**(코드 9 줄).
   ⭐**결론은 그대로 선다** — 발행 격자에 `sigma_anchored_dbsm` 이 **없고 delta 스칼라만** 있다.
   ⇒ 근거를 **세어서**, 결론을 **격자를 열어** 쓰게 고쳤다(`_anchor_wiring_finding()`).
   이름도 「…σ 사슬 **밖에 있다**」 → 「**배선은 됐으나** 발행 격자에 **실체화되지 않았다**」.
 · ⛔같은 문장이 **두 군데 더** 있었다(`geometry_benchmark.py:1430` · `docs/PAPER_DRAFT.md:571`).
 · ⛔`D2` 도 같은 모양이라 **미리** 세어서 쓰게 바꿨다(지금은 0 — 맞다).
 · ⛔`report06` 의 「형상 정정 전 메쉬」 단서가 **공허해질 뻔했다** — 최솟값 기체가 mini5pro 로
   바뀌는데 mini5pro 는 `untouched_airframes` 에 있다(sha 동일). `_meshfix_clause()` 로 갈라 쓴다.
 · ⛔`outputs/physics_deck_repro_check.json` 의 `rerun_cmd` 가 없는 경로 —
   ⚠**굽는 코드가 저장소에 없어**(전수 grep) 손으로 고치고 그 사실을 원장에 남겼다.

⭐**전날 고친 것 둘이 여기서 값을 했다**
 · `geometry_benchmark.py:1157` 의 48 자 자르기 — 안 고쳤으면 「4 자리 단일값은 **거짓 정밀**이다(R11)」가
   잘려 나가고 그 네 자리 값만 실렸다. 이번 판엔 78 자 전부 실렸다.
 · `capability_matrix.py:945` 의 ⚠-만-보기 — 안 고쳤으면 R14 철회 경고가 **사라졌다**. ⛔ 2 개로 남았다.
 · `build_volumes.py:1292` 의 `continue` — 안 고쳤으면 **A_atlas.ipynb 가 다시 안 쓰여** 자국이 남았다.

**발행 수의 이동** (전부 입력 원장 09-06 재생성의 결과다. ⛔오늘 고침의 효과가 아니다)
 · 뒤집힘 폭 최솟값 1.298 → **4.044 dB** · 드리프트 여유 0.298 → **3.044 dB**
 · 그 폭을 정하는 기체 matrice4e → **mini5pro** · 공통모드 기울기 0.2462 → **0.2536**
 · `geometry_benchmark` 53 칸(검출거리 R90 아홉 줄 포함)
⭐결정성 확인 — `sigma_sensitivity.py:471` 이 `default_rng(20260731)` 로 씨앗을 고정하고,
  두 번 구워 시각을 뺀 해시가 같다. ⇒ 몬테카를로 이동은 무작위가 아니라 **자료가 바뀐 것**이다.

⛔**안 돌린 것** — `src/make_report11_2_two_channel.py`(챔버가 되살아난다) ·
  `benchmark/rename_outputs.py --apply`(큐가 도는 중이라 절대 안 된다. 예행만 돌렸다).

### ☑② `~/.venvs` 자국 — 굽는 것으로 사라졌다
손으로 쓴 문서 8 · 셸 4 · 재빌드로 사라진 노트북 3(`A_atlas` · `05_engine-physics` ·
`_parts/86_physics-deck-match`) · `docs/repro` 2.
⛔`reports/_parts/orig71.ipynb`(보존된 옛 셀) 3 자리는 **안 고친다**.
⛔남은 **63 자리**는 오래 전 원장의 «그때 무엇으로 돌렸나» **기록**이다 — 고치면 이력이 바뀐다.
  커밋 `78f8fb7a`(09-06)가 이미 기준선으로 깔아 두었고 「줄어들기만 한다」고 적었다.

### ③ 아직 안 본 것
`docs/PRECISION_REVIEW_0910.md` 는 전부 닫았다. 새 검토가 오면 `RESUME_0910.md` §4 의 방식대로
**21 판 검증(확인 + 반증)** 을 붙여라 — 오늘 그 방식이 지적의 과함 셋과 내 회귀 하나를 잡았다.

---

## ⭐3-b. 협곡의 «흔들림» 을 처음 읽었다 (09-10 저녁 · 0922 A 가 앉아서)

⛔같은 잣대(DEV=0.5)·같은 팔·같은 깊이·같은 자세(8,192)로 잰 것이다.

| 씬 / 앙각 | 사건 | 격자 3.8e9 | 격자 4.2e9 | 되풀이 |
|---|---|---|---|---|
| 우리 씬 el−60 | 82 | 0.8889 | 0.9195 | 1.0000 |
| **협곡 el−30** | 96 | **0.6031** | **0.6250** | 1.0000 |
| **협곡 el−60** | 128 | **0.7315** | **0.7740** | 1.0000 |

⇒ 같은 ±5 % 인데 **협곡이 훨씬 크게 갈린다.**
⭐**덱의 잣대(Hampel 51·5.0)로도** 쟀다 — 96 → 114/112 · 339 → 332/335.
  ⛔다시 구현하지 않고 `bake_outdoor.hampel_mask` 를 그대로 불렀다.
⇒ 이것이 **덱 v26** 의 9 쪽 문장이 됐다.
⛔⛔**2026-09-10 정정 — 이 값을 «띠» 로 합쳐 판정 문턱으로 쓰지 않는다.**
  · 두 수(0.6031·0.7740)는 **서로 다른 앙각·서로 다른 섭동 부호**에서 나온 점이다.
    앙각별 관측 구간은 el−30 0.6031~0.6250 · el−60 0.7315~0.7740 으로 **서로 안 겹친다** —
    둘을 «0.60~0.77» 로 합치면 어디서도 관측된 적 없는 구간이 띠 안에 들어온다.
  · 판독기 자신이 그렇게 못 박아 두었다 — `read_canyonnull_0910.py:186` 「두 점은 신뢰구간이
    아니다」 · `:20-21` 「expected_* 는 균일·독립 추출 가정의 참고값이다. 자세는 시각이고
    로터 위상 구조가 있다 — 유의성 검정이 아니다」.
  · 이것은 **같은 물체를 두고 광선 격자를 다시 썬 수치 민감도**다. 물리적 변경(다른 기체)의
    동등성 문턱으로 쓰는 것은 범주가 다르다. ⇒ `make_jobs_0923.py` C 묶음의 죽는조건을
    「레벨과 빈도만 읽는다」로 고쳤고 `jobs_0923.txt` 를 다시 만들었다(발주 줄은 불변).
  ⚠같은 뿌리가 이미 구운 `runners/jobs_0920.txt` 의 죽는조건 다섯 곳에도 있다(:15·20·29·30·55).
    그 묶음은 이미 돌았으므로 발주는 안 건드리고, 그 결과를 읽을 때 이 한정을 함께 적는다.
⭐협곡 el−60 의 `되풀이 2` 는 09-10 11:00 에 샤드가 채워져 **이제 읽힌다** — 다시 돌리니
  자카드 1.0000(덱 잣대로도 339, 1.0000)으로 들어와 읽은 칸이 앙각마다 5 개다.
판독기 `benchmark/read_canyonnull_0910.py` · 원장 `outputs/read_canyonnull_0910.json`.

## 4. 오늘 한 일 (커밋 순서)

| 해시 | 무엇 |
|---|---|
| `8d983b2f` | 정밀 검토 1~3 · 동체 사다리를 이미 사 둔 샤드로 다시 읽음 |
| `6a564dca` | ⛔결함 셋 — `--det` 가 `E_dedup` 을 틀리게 만듦 · 낡은 `n_trunc` · `comb_snr` 띠 |
| `5b7085e3` | ⑤ ⑦ — ⛔⑦ 은 검토의 제안문을 **안 썼다**(소스에 없는 기전이었다) |
| `9be91536` | 재개 문서 정리 |
| `60998889` | 덱 6 쪽 수를 샤드로 검산 · `read_dropladder_0910` 원장 |
| `31eec3ef` | 없는 경로(`~/.venvs`) — 손으로 쓴 문서 4 · 셸 4 |
| `739ab5a7` | ⛔`build_volumes` 가 조각 0 인 권(12권·A권)을 **아예 안 쓰고 있었다** |
| `6e74c40f` | ⭐**샤드 244 장**(29 MB) — 세션 내내 빠뜨리던 것 |
| `10342a5a` | 못 도는 빌더를 살림(`geometry_grid` multistatic) + 철회 경고를 잘라 먹던 자리 둘 |
| `b2df2eb7` | 큐 로그 둘 |
| `c33febfb` | 다시 굽기 전 막던 것 둘 — 자기모순 딱지 · 박아 둔 기체 이름 |
| `d968522`·`7959788` (team_meeting) | 덱 v24 · **v25** |

### 09-10 늦은 판 — 판독기 재검토 6 건을 검증하고 고쳤다

외부 검토 `docs/LATEST_READERS_REVIEW_0910.md` 6 건 + 보완 3 건을 **파일:줄 대조 + 샤드
재계산 + 교차반증**으로 검증했다. **인용은 6 건 전부 글자·줄 번호까지 정확했고(오기 0),
숫자도 전부 독립 재계산으로 재현**됐다.

| 해시 | 무엇 |
|---|---|
| `93cf904b` | 판독기 여섯 · 발주서 하나 — 지적 ②③④⑤⑥ · 보완 ⓐⓑ |
| `6e96ca61` | 인수인계 — 런북 2 단 구조 · `docs/HANDOVER_MAP.md` |
| `50a4dbaa` | 아틀라스 배율 보정 + 다시 굽기 (철회 **R30**) |

**등급을 바꾼 것**

| 지적 | 검토의 등급 | 검증 뒤 |
|---|---|---|
| ① 아틀라스 배율 | 발간값에 영향 | **그대로 — 다만 더 크다.** 리듬 몫 최대 −65 %p, **순위가 뒤집힌다**(R30) |
| ② 「둘 다 80」 | 결론이 과하다 | **양쪽 다 과하다.** 널이 없어 「구별된다」도 「구별되지 않는다」도 못 잰다 |
| ⑤ n_trunc | 잠재 결함 | 그대로. ⛔단 **처방을 그대로 하면 새 결함**이 생긴다(아래) |
| ⑥ 입력 검증 | 잠재 결함 | 그대로. ⛔협곡 게이트는 **두 곳**이고 낙차 판독기엔 **아예 없었다** |
| 보완 ⓒ 계단 | 보완 사항 | **접는다.** 발주서에 「물리」·「불연속」이 0 회이고, DEV 를 0.02~1.05 로
흔들어도 사다리가 [0.14, 0.999] 전 구간에서 안 움직인다 — 이 계단은 문턱의 산물이 아니다 |

⛔**⑤ 의 처방을 글자 그대로 하면 안 된다** — 저장된 `n_trunc` 첫 칸은 구울 때의 문턱으로 센
값이고 문턱이 0.999 → 0.99 로 내려간 적이 있다(`elevation_sweep_md.py:957-959`). 두 배열을
함께 가진 샤드 576 장 중 **14 장이 어긋나고 그중 12 장은 저장값 0 인데 지금 규칙으로는 전
자세가 «상한 근접 경고» 에 해당**한다. ⇒ 저장값은 `stored` 로만 남기고 저장된 `nret`·상한으로
지금 문턱을 다시 적용한다. ⛔「잘렸다」로 옮겨 적지 않는다 — `nret` 은 어림수다.

**내가 틀렸던 것(검토자가 잡았다)** — ⓐ 「el 0° 상위 4 팔이 전부 ps 팔」은 틀렸다(3/4 이고
`sionna` 가 이미 2 위였다. 모집단 51 팔을 함께 적는다) · ⓑ 「×0.4·×0.5 집합이 같으니 독립
관측이 아니다」는 성립하지 않는다(같은 결과만으로 독립성을 판정할 수 없다 — 「두 배율에서
집합이 같았다」까지가 관찰이다) · ⓒ 되풀이를 「정보 0」이라 한 것은 과하다(확률 표본은 아니어도
실행 재현성을 확인한다) · ⓓ 「새 세대로 중복 제거」는 검토 메모에 없는 처방이다(출처를 갈랐다) ·
ⓔ 창고의 `el −75°` 반쪽 자료는 낙차 판독기가 el 0°·8,192 로 고정이라 **실제로 들어간 적이 없다**
(완전성 검사의 필요성과 지금 피해는 따로 본다).


⭐**`geometry_grid.py` 가 살아났다** — `multistatic` 은 합본이 아니라 **기체별 샤드 다섯**에 있었다.
검산: 합본에 그 블록이 살아 있던 마지막 커밋 `26c3e1b1`(2026-07-31)의 값과
**β 일곱 칸 전부 최대 차 0.00e+00**. ⇒ `geometry_benchmark.py` 도 이제 돈다.

---

## 5. ⛔절대 어기지 않는 것 (전문은 `CLAUDE.md` · `RESUME_0910.md` §6)

- ⛔**말을 지어내지 않는다.** 오늘도 반증이 남의 제안문에서 **지어낸 기전**을 둘 잡았다.
- ⛔**샤드를 담는다.** `git add -A` 는 여전히 금지 — **np.load 로 온전성을 확인**한 목록만
  명시 경로로 담는다(2 분 안에 쓰인 것은 건너뛴다. `np.savez_compressed` 는 원자적이 아니다).
- ⛔**커밋 메시지는 heredoc + `git commit -F`** — `printf` 는 `%` 에서 잘라 먹는다(오늘 물렸다).
- ⛔덱은 매번 새 판. ⛔노트북·원장은 손으로 안 고친다(빌더를 고치고 다시 굽는다).
- ⛔`pgrep -f` 금지 · CPU 작업은 `CUDA_VISIBLE_DEVICES=""` + `taskset`.
- ⛔실기 계측 대조는 **0 건**이다.
