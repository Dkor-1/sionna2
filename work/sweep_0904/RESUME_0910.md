# 재개 지점 — 2026-09-10 (낮, KST 11:30)


> ⛔**더 최신 재개 지점이 있다 — [`RESUME_0911.md`](RESUME_0911.md) 를 먼저 읽는다.**
⛔**이 파일부터 읽는다.** 그 다음 `docs/CLAIM_GATE.md` → `CLAUDE.md` →
`work/sweep_0904/RESUME_0909.md`(어제, **09-10 에 §0·§① 을 크게 고쳤다**) → `RESUME_0908.md`.

⭐**팀미팅은 2026-09-14(월) KST.** 09-10(목)에서 미뤄졌다(사용자: 금 09-11 또는 월 09-14 →
「월요일이라고 가정하고 추가 실험 진행하자」). **남은 시간 약 4 일.**

---

## 0. 세 줄

1. **덱은 끝났다** — `team_meeting/teammeeting_0910/_out_0910_v23.pptx` (11 장) 커밋·푸시 완료
   (`team_meeting` `d9bdcac`). ⛔새로 고칠 일이 생기면 **v24 로 새로 굽는다** — 옛 판을 안 덮는다.
   ⚠v22 이하는 다 남아 있다. 발표에 쓸 것은 **v23** 이다.
2. **큐가 돌고 있다** — 0922(144 샤드) 진행 중, 0923(14 샤드) 이 사슬에서 대기.
   ⚠**09-12 저녁쯤 마른다.** 그 전에 다음 판을 넣어야 한다(§3).
3. ✅**외부 검토 세 판을 다 닫았다** — 09-09 두 판(9+10 항목) · 09-10 판(7 항목) 전부.
   ⭐09-10 낮에 ④~⑦ 과 **미수정 결함 셋**을 21 판 검증(확인 7 · 반증 14)으로 닫았다(§4·§5).
   ⛔그 과정에서 **덱을 v23 으로 새로 구웠다**(9 쪽 노트가 차례를 거꾸로 말했다).

---

## 1. 지금 도는 것 — 30 초 점검

```bash
cd /workspace/sionna
tail -3 runners/logs/sup_jobs_0922.log          # 감독자: 「큐 i/144 · 워커 n」
tail -3 runners/logs/queue_keeper_0910.log      # 지킴이: 다음 판을 띄우는 쪽
ps -eo etime,args | grep '[w]orker_supervisor'  # 감독자가 살아 있나
ps -eo etime,args | grep '[q]ueue_keeper'       # 지킴이가 살아 있나
ls outputs/elev_sweep_shards/*.npz | wc -l      # 09-10 11:00 기준 7,304 장
```

⛔`pgrep -f` 를 쓰지 않는다 — 내 명령줄이 스스로 걸려 셸이 죽는다. `ps -eo args | grep '[x]…'` 만.

**09-10 13:00 상태** — 0922 감독자 진행 중(`큐 21/144`) · 워커 10 · 샤드 **7,313 장**.
지킴이 살아 있음. 사슬에 **`jobs_0923.txt` → `jobs_0924.txt`** 둘 다 대기(아직 하나도 안 띄웠다 —
`runners/logs/queue_chain_0910_done.txt` 가 비어 있다). ⇒ 0922 가 끝나면 지킴이가 알아서 잇는다.

### 죽어 있으면 이렇게 되살린다

⛔**지킴이가 띄우는 것과 글자 그대로 같게 띄운다**(`runners/queue_keeper_0827.sh:7-12`·`:31`).
⛔⛔**감독자에 `CUDA_VISIBLE_DEVICES=""` 를 붙이지 않는다.** 지킴이가 안 붙이기 때문이고,
  감독자는 카드를 스스로 골라 워커마다 `CUDA_VISIBLE_DEVICES=<카드>` 를 **직접 넣어 준다**
  (`worker_supervisor.py:413`). ⚠붙였을 때 정확히 무엇이 깨지는지는 **안 재 봤다** —
  「지킴이와 똑같이 띄운다」가 근거이지 기작을 아는 것이 아니다.
  (`CUDA_VISIBLE_DEVICES=""` 는 **CPU 전용 도구**에만 쓴다 — dry-run·판독기·검사기.)

```bash
cd /workspace/sionna
export PYTHONPATH=src:benchmark
export DRJIT_LIBOPTIX_PATH=/workspace/.venvs/optix/libnvoptix.so.1
export LD_LIBRARY_PATH=/workspace/.venvs/optix:${LD_LIBRARY_PATH:-}

# ① 감독자가 없고 큐가 안 끝났으면 — 0922 를 이어서
SIONNA2_MAX_TOTAL=9 setsid nohup \
  /workspace/.venvs/py312/bin/python runners/worker_supervisor.py \
  runners/jobs_0922.txt runners/logs/sup_jobs_0922.log >/dev/null 2>&1 &

# ② 지킴이가 없으면 (인자 없음 — 사슬 경로가 스크립트에 박혀 있다)
setsid nohup bash runners/queue_keeper_0827.sh >/dev/null 2>&1 &
```

⚠**감독자는 시작할 때 큐를 통째로 메모리에 읽는다.** 도중에 `jobs_0922.txt` 를 고쳐도
지금 도는 판에는 안 먹는다. 새 줄은 **새 파일**로 만들어 사슬에 적는다.
⚠되살린 감독자는 **큐를 처음부터 다시 훑는다.** 이미 구운 샤드는 워커가 건너뛰므로
  안전하다 — `benchmark/elevation_sweep_md.py:650`·`:772` 의
  `if os.path.exists(f) and not a.overwrite:` 다. (소스에서 확인했다.)
⛔**단 `--overwrite` 가 붙은 줄은 다시 굽는다** — `jobs_0923.txt` 의 D 두 줄(샤드 넷)이 그렇다.
  그 줄이 겨누는 08-24 판 두 장은 `outputs/_backup_0910/` 에 복사해 뒀다. ⛔그 복사본을 지우지 않는다.
⚠지킴이는 한 번 띄운 사슬 줄을 `runners/logs/queue_chain_0910_done.txt` 에 적고 **다시 안 본다.**
  같은 판을 일부러 다시 띄우려면 그 파일에서 해당 줄을 지운다.

---

## 2. 큐에 무엇이 들었나

| 판 | 샤드 | 어림 | 무엇을 사나 |
|---|---|---|---|
| `runners/jobs_0922.txt` | 144 | 537 일꾼시간 | A 협곡의 널 · B 뒤쪽 반구 방위 · C 두 씬 깊이 사다리 · D 협곡 물리 팔 |
| `runners/jobs_0923.txt` | 14 | 33 일꾼시간 | A 정지 부품 나눔 · B 우리 건물만 앙각 0 · C 협곡 × mini5pro · D 앙각 −60 기준선 감사 |
| `runners/jobs_0924.txt` | 30 | 50 일꾼시간 | A 동체 ×1~×2 사이 · B 같은 사다리를 앙각 −30 에서 · C 격자 참고선을 두 점 → 네 점 |

발주서 머리글에 묶음마다 «물음 · 죽는 조건 · ⛔안 답함» 이 적혀 있다 — **결과를 읽기 전에 그 줄부터 본다.**
⛔거기 적힌 것은 «물음» 이지 «답» 이 아니다.

**어림 마르는 때** — (537 + 33 + 50) ÷ 워커 10 ≈ **62 시간 벽시계 → 2026-09-13 새벽**.
⚠워커 수는 남의 GPU 점유에 따라 감독자가 실시간으로 움직인다. 이 어림은 워커 10 고정 가정이다.

⛔⛔**사슬이 소진되면 지킴이가 스스로 죽는다** — `queue_keeper_0827.sh:25`
  (`say "사슬 소진 — 지킴이 종료"; exit 0`). 0924 가 끝나면 지킴이도 없다.
  ⇒ 새 판을 사슬에 적은 **뒤에 지킴이를 다시 띄워야** 한다(§1 의 ② 명령).

### 0924 는 무엇이고 왜 샀나

09-10 의 동체 사다리 판독(§4)이 **직접 연 칸**만 산다. 발주서 머리글에 근거가 다 적혀 있다.
- **A** — 줄이는 쪽은 ×0.4 까지 전부 80 인데 ×2 만 64 다. **×1~×2 사이를 한 번도 안 샀다.**
  `--body-scale` 1.2 · 1.5 · 3 을 사서 **계단인지 비탈인지** 가른다.
- **B** — 같은 사다리를 앙각 −30 에서. ⚠빈 하늘 짝은 **이미 있어서** 씬 쪽만 산다.
- **C** — 「격자를 갈았을 때의 흔들림」을 3.8e9·4.2e9 **두 점**으로만 재고 있다. 3.6e9·4.29e9 를 더한다.
  ⛔⛔**이 축은 한쪽으로만 열려 있다** — 광선 수는 **32 비트 천장 4,294,967,295** 를 못 넘는다
  (Sionna 표본기 `wavefront_size` 가 uint32). 4.4e9 로 짰다가 dry-run 이 막았다.
  규약값 4e9 는 천장의 93 % 라 **위로는 7 % 뿐**이다. ⇒ 대칭 띠를 만들 수 없다.

⭐**30 줄 전부 dry-run 「없음」** 을 확인하고 사슬에 넣었다(겹쳐 사는 것 0).

---

## 3. ⛔다음 사람이 할 일 (급한 차례)

### ⭐① 09-13 전에 큐를 다시 채운다
마르면 GPU 가 논다 — 09-08·09-10 에 두 번 그랬다(14 분 · **2 시간**). 둘 다 **사슬을 미리
안 채워 둔 것**이 원인이다. ⛔새 판은 **파일을 먼저 만든 뒤** `runners/queue_chain_0910.txt` 에
적는다. ⛔손으로 띄운 판은 사슬에 적지 않는다(두 번 뜬다). ⛔발주 전 `runners/filter_jobs.sh`.
⚠**무엇을 새로 살지는 지금 정할 수 없다** — 0921 재고 세기의 목록이 소진됐다.
0922 결과가 앉은 뒤 **재고를 다시 세고** 거기서 뽑는다.

### ⭐② `~/.venvs` 자국 — **진단은 끝났고 고치는 일만 남았다** (09-10 낮)
발표된 문서 **11 개 + 리포트 노트북 5 개**의 재현 명령이 **없는 경로**를 가리킨다.
따라 치면 전부 실패한다(⛔`~/.venvs` 는 없다 · 파이썬은 `/workspace/.venvs`).

⭐**빌더는 11 개 전부 이미 `/workspace/.venvs` 를 굽는다** — 낡은 것은 **구워진 판**뿐이다.
```
✅빌더가 돈다 (6 + 노트북 4)   → 다시 구우면 낫는다
   PAPER_DRAFT.md            ← benchmark/capability_matrix.py
   MEASUREMENT_PLAN.md       ← src/build_part11_measurement.py
   OUTPUT_NAMING.md          ← benchmark/rename_outputs.py
   INJECTION_PRECEDENT.md    ← benchmark/build_report00_po_case.py
   docs/repro/part07·part11  ← src/extract_part_docs.py   (json·os·re 만 — 가볍다)
   reports/05_engine-physics · _parts/86_physics-deck-match
                             ← src/build_part13_engine_physics.py (GPU 안 씀)
                                ⚠자국의 뿌리는 outputs/physics_deck_repro_check.json 의
                                  _meta.rerun_cmd 다 — 원장을 다시 구우면 낫는다
   reports/A_atlas           ← benchmark/build_atlas_toc.py (:653-654 는 이미 옳다)
⛔빌더가 못 돈다 (2)           → 다시 못 굽는다 (아래 ③ 을 먼저 고쳐야 한다)
   GEOMETRY_BENCHMARK.md · PAPER_POSITION.md ← benchmark/geometry_benchmark.py
—빌더 없음 (3)                → 손으로 쓴 문서라 손으로 고치는 것이 맞다
   MD_RANGE_SWEEP_CITATIONS.md · REPORT13_DESIGN.md
   outputs/partial/das_fleet_0803/README_PIDS.md
```
⛔`reports/_parts/orig71.ipynb` 은 **보존된 옛 셀**이다(`extract_part_docs.py:12` 가 「옛 셀 24
코드」라고 적는다) — **기록이라 고치지 않는다.**
⛔다시 굽지 말 것 셋(`work/sweep_0904/RESUME.md:89-90`) — `make_report11_2_two_channel.py`
(챔버가 되살아난다 · `SIONNA_ALLOW_CHAMBER=1` 없이 멈춘다) · `geometry_grid.py` · `geometry_benchmark.py`.

### ⭐③ `geometry_grid.py` 를 살릴 수 있다 — **고칠 자리를 찾았고 검산까지 했다** (09-10 낮)
`benchmark/geometry_grid.py:485` 가 **합본** 원장을 읽는다:
```python
ms = _read("report13_sigma_grid.json", "multistatic", default={}) or {}
```
⛔그런데 `multistatic` 블록은 **기체별 파일**에 있다(`report13_sigma_grid.matrice4e.json` ·
`.mavic4pro` · `.mini5pro` · `.phantom4` · `.s1000plus`). 합본에는 **git 25 판 전부에 없다** —
「블록을 잃었다」가 아니라 **애초에 거기 없었다.**

⭐다섯 파일을 합쳐 넣고 `:486-505` 를 그대로 흉내 내 돌려 봤다(⛔빌더는 안 돌렸다):
```
   β      칸   Δσ rms 중앙   Δσ p95 중앙
   0      15        0.0          0.0     ← 주석이 「β=0 이면 Δσ ≡ 0」이라 적은 그대로다
  15      15     6.3938      12.1553
  30      15     6.8444      13.1912
  45      15     7.4727      13.7191
  60      15      7.511       14.866
  75      15     8.0604      15.3802
  90      15      8.806      16.1033
   ⇒ 7/7 칸이 찬다 (칸마다 5 기체 × 3 대역).  지금 코드로는 0 칸 → 빈 원장.
```
⛔**빌더를 돌리기 전에** 산출물을 백업해라 — 08-27 에 이 빌더가 원장을 비워 되돌린 적이 있다.

### ④ ⬜남은 잔가지
`work/wf/queue_design_0921.js` 밖의 큐 기록(`jobs_0918`·`0921`·`0923`)은 **원문을 두고 정정
줄만** 붙인다 — 이력을 바꾸면 「무엇을 믿고 무엇을 샀나」가 흐려진다.

### ⛔⑤ 도구 함정 하나 (09-10 에 물렸다)
커밋 메시지에 `%` 가 있으면 **`printf` 가 거기서 잘라 먹는다**(`0.04~0.94 %` 에서 10 줄로
잘렸다). ⇒ 긴 메시지는 **heredoc 파일에 쓰고 `git commit -F <파일>`** 로 넣어라.

## 4. 외부 검토 세 판 — 어디까지 했나

| 판 | 항목 | 상태 |
|---|---|---|
| 09-09 ①(9 항목) | 「기본 환경에서는 문제가 없다」 출발점 외 | ✅ 검증·수정·커밋 |
| 09-09 ②(10 항목) | 소스·원장 후속 검토 | ✅ 검증·수정·커밋 `614a669f` |
| **09-10 ③(7 항목)** | `docs/PRECISION_REVIEW_0910.md` | ✅**1~7 전부** (④~⑦ 은 09-10 낮, 21 판 검증 뒤) |

### 오늘 고친 것 (09-10 ③ 의 1·2·3)

`work/sweep_0904/RESUME_0909.md` §0 · §① 을 다시 썼다. **셋 다 맞는 지적이었고 직접 재확인했다.**

- ⛔**① 0.86 은 «동체만» 이 아니었다.** `outputs/read_0914_0908.json` 의 그 칸은
  「C 깊이/깊이 3 ∩ D 크기/동체 ×0.5」로 **손잡이 둘이 섞인** 짝이다.
  기준선과의 진짜 동체 짝은 ×0.5 = **0.9524** · ×2 = **0.6809**.
- ⛔**② «우연이면» 열은 자카드가 아니라 «기대 공통 개수»**(`n_a·n_b/N`) 였다.
  0.889 옆에 0.88 을 놓아 「우연 수준」으로 읽혔는데 **정반대** — 기대 자카드는 **0.005** 라
  0.889 는 그 **170 배**다. 개수와 비율을 한 열에 놓지 않는다.
- ⛔**③ «널» 이 아니다.** 초기 광선은 **씨앗 없는 결정적 피보나치 격자**다
  (`sionna/rt/utils/ray_tracing.py:24-30`, `x = ns/φ mod 1` · `y = ns/(N−1)`).
  광선 수를 바꾸면 **모든 y 좌표가 함께** 바뀐다. 같은 설정 되풀이는 자카드 **1.000** 이라
  확률적 널이 **아예 없다**. ⇒ 「격자를 갈았을 때의 민감도」로 부른다. 두 점은 신뢰구간이 아니다.

⭐**결론이 바뀌었다** — 옛 판은 「동체는 널 안이다 ⇒ 동체는 무관하다」였는데,
고친 수로 보면 동체 ×2 는 82 중 **18 개**를 바꾸고 격자 흔들림(**2 개**)의 **아홉 배**다.
⇒ 「프롭·프레임이 목록을 정한다」까지만 받쳐지고 **「동체는 무관하다」는 안 받쳐진다.**

✅**덱은 안전하다** — `자카드`·`0.86`·`널` 중 어느 것도 v22 에 없다.
   (`make_v22.py:293` 의 「자카드」는 **금지어 문지기 목록**이다.)
✅이 잘못은 `RESUME_0909.md` 밖으로 **안 번졌다** — 리포트·발주서·덱 전부 0 건. grep 으로 확인했다.

### ⭐그리고 사다리를 다시 읽었다 — **새로 산 것 없이**

`benchmark/read_bodyladder_0910.py` · 원장 `outputs/read_bodyladder_0910.json`.
같은 조건의 `bs` 샤드가 0.4·0.5·0.6·0.8·2 로 **이미 구워져 있었다.** 판독기가 둘만 읽고 있었다.

| 동체 배율 | ×0.4 | ×0.5 | ×0.6 | ×0.8 | ×1 | ⭐×2 | *격자 3.8e9* | *격자 4.2e9* |
|---|---|---|---|---|---|---|---|---|
| 82 중 남음 | 80 | 80 | 80 | 80 | 82 | **64** | *80* | *80* |
| 경로 수 중앙 | 1,602 | 1,726 | 1,896 | 2,232 | 2,705 | **6,841** | *2,646* | *2,899* |

⇒ **줄이는 쪽은 2.5 배를 훑어도 전부 80** — 격자를 갈았을 때와 구별되지 않는다.
  바뀌는 것은 **×2 한 칸뿐**이다.
⛔**단조가 아니다**(80·80·80·80·82·64). 「키울수록 흩어진다」로 못 읽는다.
⛔⛔**×2 에서는 경로 수도 2.5 배 뛴다** — 어느 쪽이 목록을 옮겼는지 **이 축으로는 못 가른다.**
  ⚠경로 수만으로도 못 읽는다: ×0.4→×1 도 경로가 1.7 배 느는데 목록은 2 개만 바뀐다.
⛔`bs` 는 **모형 축**이다 — 비구조 산란면만 키우고 팔·모터·기어·다리와 허브·프롭은 고정
  (`src/articulated_fast.py:126`·`:145`). 줄이면 동체와 팔 사이에 틈이 생긴다.
⇒ 이 결과가 **0924 A·B 를 낳았다**(§2).

### 남은 4~7 번 (아직 안 함)

- **④ 프레임 배율은 «허브만» 의 대조가 아니다** — 정적 프레임 정점 전체가 함께 커진다
  (`src/articulated_fast.py:141`·`:169`). ⇒ §① 에 한정 문구는 **넣었다.**
  ☑**훑었다(09-10).** 저장소 전수 + 덱까지 봤다.
    · `RESUME_0909.md:237` — §① 제목대로 「프롭·프레임 기하」로 고쳤다(:238 의 「널」 지시도 함께).
    · `work/wf/queue_design_0921.js:47` — 발주 설계 이력이라 **원문을 두고 정정 줄**을 붙였다.
    · ✅**덱 v22 는 0 건** — 「허브·hub·프롭·남은 후보」 어느 것도 없다.
      ⛔옛 판(v14)·발표된 0903 판에는 있지만 **옛 판은 고치지 않는다**(규약).
    · ⬜남긴 것: `jobs_0918`·`0921`·`0923` 의 발주 기록. 이미 돌아간 큐 이력이라
      원문을 바꾸면 「무엇을 믿고 무엇을 샀나」가 흐려진다. 정정은 위 두 자리로 족하다.
- ☑**⑤ 노치가 «신호 불변» 은 아니다 — 고쳤다(09-10).** 네 자리.
  · `clutter_parts_ladder_0824.py:76` — 「신호 불변」 → 「날개 박자 126.7 Hz 는 이 대역 밖이다」
    (⛔`cs_eca` 와의 이름 고리는 남겼다 — :151-155 가 그 이름을 일부러 유지한다).
  · 같은 파일 `:162` — 「신호를 안 건드린다」 → 「**박자 빗살 칸은** 하나도 안 지워진다」 + 한정.
  · `clutter_methods_0901.py:8` · `src/build_report12_outdoor.py:571`(→ 리포트 12 재빌드 완료).
  ⭐직접 다시 잰 것 — 노치가 지우는 83 칸에 든 몫은 통짜 팔 AC 의 el 0 에서 **0.04~0.94 %**,
    el −30·−60 까지 넓혀도 최대 **1.16 %**. 빗살 칸 손실은 **정확히 0.0000 %** 다.
  ⭐그래도 사다리가 내는 `rhythm_pct` 는 **안 바뀐다** — raw→eca 가 전 팔 동일하고
    움직이는 것은 `body only` 13.42→12.86 하나뿐이다(원장으로 확인).
  ⛔검토가 인용한 0.012~0.389 %는 **다른 실험의 자유공간 팔**에서 잰 값이다 — 수는 맞지만
    이 주석이 명시한 조건(el 0 · matrice4e · 8,192 자세)과 자리가 다르다.
  ⛔el −90 에는 손실 %를 안 적었다 — 그 칸은 `metric_valid=false` 로 **수치를 안 내는** 칸이다(:201).
- ☑**⑥ 보간과 노치의 순서 — 고쳤다(09-10). ⛔그림은 안 바뀌었다.**
  ⭐**코드의 차례가 맞다** — `bake_outdoor.py:464-466` 이 원본에서 걸러 갈아끼운 뒤 뺀다
    (`cs_eca(Of)`). `:465` 주석이 「⚠순서 — **갈아끼우고 나서 빼야** 한다」로 못 박아 뒀다.
    ⇒ `f_outdoor_shipped.png` 는 **한 픽셀도 안 바뀌었고** 베이커도 안 건드렸다.
  ⛔어긋난 것은 **덱 9 쪽 발표자 노트 한 문단**이었다 — 「3 칸에서 보이는 그 줄들을 빼냈다」로
    읽혔다. **덱 v23** 으로 새로 구웠다(`team_meeting` `d9bdcac`). 문지기에
    「before I subtracted anything」을 걸어 다시 어긋나면 빌드가 죽게 했다.
  ⚠말만 다른 문제가 아니었다 — 거꾸로 하면 4 칸 그림이 중앙값 **8.17~10.26 dB** 달라진다(실측).
    빼기는 전역 FFT 라 갈아낼 자세의 몫이 **다른 자세로 새고**, 먼저 빼면 그 몫을 못 걷는다.
  ⭐96 / 339 는 그대로 맞다 — 걸러낸 자세는 어느 차례로 해도 **같다**(대칭차 0).
- ☑**⑦ 「표본 수가 가중치에 들어간다」 — 고쳤다(09-10). ⛔단 지적의 제안문은 안 썼다.**
  ⛔**지적이 겨눈 문장 자체는 과한 인용이었다.** `verify_critique_0909_result.json:46` 은
    「예산 대조가 부당하다」고 말한 적이 없다 — 「경로수 일치만으로는 같은 추정량이 되지
    않는다」는 **더 좁은 말**이고 그 좁은 말은 성립한다. 그 자리는 안 고쳤다.
  ⭐고친 것은 그 말이 **발주서 머리글로 굳어 번진 세 자리**다
    (`make_jobs_0924.py` 한계 ④ · 같은 파일 C 안답함 · `read_bodyladder_0910.py` limits_ko).
  ⛔⛔**검토가 제안한 문구는 쓰지 않았다 — 소스에 없는 기전을 넣는다.** 반증 둘이 잡았고
    내가 설치본을 열어 확인했다:
    · `field_calculator.py:447-448` 의 `1/q` 가 상쇄하는 것은 **재질이 정한 상호작용 표집
      확률**이지 `1/samples_per_src` 가 **아니다**(:214-220 주석이 그렇게 적는다).
    · `:313` 이 「If a diffuse reflection is sampled, then it is set to 2PI」로 입체각을
      되돌리므로 `4π/spp` 를 타는 것은 경로의 **첫 상호작용**뿐이다.
    ⇒ 「상쇄되니 괜찮다」는 **지어낸 기전**이다.
  ⛔「결함이 아니다」로도 안 적었다 — 원장이 spp 를 흔들면 prop 채널 진폭 ∝ spp^0.51,
    all 채널 기울기 0.075 로 갈리는 것을 보았고 그 까닭을 **「모른다」**로 적었다
    (`verify_critique_0909_result.json:43`). 「모른다」인 축에 인증을 박으면 검증된 것으로 읽힌다.
  ⇒ 적은 것은 「단일 변수가 아니다」 + 「일부러 넣은 정규화다」 + 「어떻게 남는지는 재 보지
    않았다」 셋뿐이다. ⭐「경로수 맞춤 외삽의 한정」은 **유지**했다.
  ✅`jobs_0924.txt` 를 다시 구웠고 **잡 줄 30 개가 한 글자도 안 바뀌었다** — 큐에 영향 없다.

---

## 5. ☑결함 셋 — 전부 고쳤다 (2026-09-10 · `6a564dca`)

- ☑`elevation_sweep_md.py:907-927` — `--det` 가 `E_dedup` 을 **틀린 값**으로 만들었다.
  정렬한 사본으로 `_t` 를 덮어써서, 중복제거가 얻은 **정렬 전 색인**이 엉뚱한 경로를 골랐다.
  ⭐반례 상대 어긋남 0.7695 → **0**. 무작위 3,000 판 중 옛 코드 2,505 판 틀림 → 고친 뒤 **0 판**.
  ⚠구운 `_det` 샤드 16 장 중 **8 장의 `E_dedup` 이 틀렸다**. ✅인용한 리포트·덱은 **0 건**.
  ⚠`E`·`npaths`·`nret`·`n_dup` 은 순열에 무관해 멀쩡했다.
- ☑같은 파일 `:1081-1102` — 합치는 고리가 **읽는 도중에 알게 된 상한**만 믿었다.
  ⭐**4 칸이 `truncated` false→true 로 뒤집힌다**(거칠기 S0.3·S0.7 × el+0·−30) —
  그 칸들은 자세 **8,192/8,192 가 상한에 붙어 있다.**
  ✅결론은 이미 맞게 서 있었다(그 축은 이미 접어 뒀다). 이 고침은 **원장이 그 사실을 말하게** 한다.
- ☑`comb_snr.py:123-124` — 띠가 프롭 배율을 안 따라갔다. 어긋남이 정확히 **1/ps 배**였고
  전수조사 팔이 **전부** 그 갈래로 떨어졌다. ⭐고친 뒤 두 식이 **모든 팔에서 0.999 로 일치**.
  ⚠`_ps` 없는 옛 팔은 **한 비트도 안 바뀐다**(확인).

⚠셋 다 지금 도는 큐에 안 물렸다(발주에 `--det` 0 건) — 워커가 도는 중이라
**임시 파일 + `os.replace`(원자적 교체)** 로 넣었다.

---

## 6. ⛔절대 어기지 않는 것 (전문은 `CLAUDE.md`)

- ⛔**말을 지어내지 않는다** — 레포트·문서·커밋·사용자에게 하는 말 전부.
  09-09 에 「레이다가 드론과 같은 높이라 지면을 거의 안 본다」를 지어냈다가 걷었다.
  PathSolver 는 **등방으로 쏜다** — 앙각과 무관하게 지면은 늘 조명된다.
- ⛔**우리끼리 쓰는 말을 덱에 넣지 않는다** — 그림 안 글자·축 이름·범례까지.
- ⛔「우리 커널이 맞고 PathSolver 가 틀렸다」로 **결론짓지 않는다** — 둘 다 근사.
- ⛔**실기 계측 대조는 0 건**이다. 「검증됐다」로 읽지 않는다.
- ⛔확산(F 비트)은 **모든 팔에서 항상 켠다**. ⛔프로펠러 단독 없음. ⛔뮌헨 없음. ⛔챔버 없음.
- ⛔σ(dBsm) 인용 금지 · 두 엔진 절대 레벨 비교 금지.
- ⛔상한이 결과를 정하면 그 축은 접는다 — `npaths` 를 먼저 본다.
- ⛔노트북·원장을 손으로 고치지 않는다 — **빌더를 고치고 다시 굽는다.**
  (⚠`RESUME_*.md` 는 손으로 쓰는 메모라서 예외다. 빌더가 없다.)
- ⛔레포트는 **주피터 노트북**이다 — 아티팩트로 내지 않는다.
- ⛔`git add -A` 금지 — GPU 큐가 `outputs/elev_sweep_shards/` 에 샤드를 계속 떨군다.
  `git add -u` + 명시적 파일. (⚠`team_meeting` 저장소는 큐가 안 건드리므로 `-A` 가 안전하다.)
- ⛔팀미팅 자료는 `/workspace/team_meeting/` 에서만. 만들면 **즉시 commit + push.**
- 파이썬은 `/workspace/.venvs/py312/bin/python` (⛔`~/.venvs` 는 없다).
- CPU 전용 작업은 `CUDA_VISIBLE_DEVICES=""`. 무거운 CPU 작업은 `taskset -c` 로 코어를 묶는다.

---

## 7. 마지막 커밋

| 저장소 | 해시 | 무엇 |
|---|---|---|
| `/workspace/sionna` | `5b7085e3` | ⑤ ⑦ 을 닫는다 — ⛔⑦ 은 검토의 제안문을 **안 썼다**(지어낸 기전) |
| `/workspace/sionna` | `6a564dca` | 결함 셋(`--det` · `n_trunc` · `comb_snr`)을 고치고 ④ 를 닫는다 |
| `/workspace/sionna` | `8d983b2f` | 정밀 검토 1~3 · 동체 사다리를 이미 사 둔 샤드로 다시 읽는다 |
| `/workspace/team_meeting` | `d9bdcac` | **0910 덱 v23** — 9 쪽 노트가 차례를 거꾸로 말하던 것 수정 |

⚠`5b7085e3` 은 메시지만 amend 하고 `--force-with-lease` 로 밀어 넣은 판이다(사용자 승인).
  파일 내용은 `29223f95` 와 **한 글자도 다르지 않다.** 단일 작성자·단일 브랜치라 안전했다.
