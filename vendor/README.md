# vendor — 밖에서 받아 온 것

`wheels/` : 설치용 wheel 59 개(약 2.8 GB). ⛔**git 에 담지 않는다**(`.gitignore`).
2026-09-02 에 저장소 최상위에서 옮겼다 — 최상위가 wheel 로 덮여 리포트가 안 보였다.

```bash
/workspace/.venvs/py312/bin/pip install vendor/wheels/<name>.whl
```
