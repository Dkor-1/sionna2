# 0932 반쪽 칸 채우기 (2026-09-14) — ⭐**판을 올리면 영영 못 채우는 칸**
#
# 원장이 「자세가 빠졌다」고 적은 7 행을 전수로 훑어 나온 것이다.
#   · 2 행은 원장이 낡은 것이었다 — 06:35 병합 뒤 06:53·07:04 에 샤드가 들어왔다.
#     ⇒ 다시 굽는 것이 아니라 **다시 병합**하면 된다(fc3502·fc3510 앙각 −60).
#   · 3 행은 **오늘 일과 무관하게 이미 못 채운다** — 메쉬 수리·날 법칙 기본값이 그 뒤로 바뀌어
#     지금 구우면 다른 팔이 된다(ours_mini5pro 날 법칙 legacy · ours_…_shift1 · sionna_p4000000000).
#     ⛔판 갈이가 나쁘게 만든 것이 아니다. 앞선 세대 변경이 이미 닫아 둔 문이다.
#   · 남는 2 행이 이것이다 — 지금 기본값으로 이름이 그대로 나오고(dry-run «없음» 확인),
#     발간 노트북 reports/A_atlas_B.ipynb 가 인용한다.
#
# 2.0.1 짝의 실측 소요: 81 분 · 86 분 ⇒ 어림 2.7 일꾼시간.
# ⛔반드시 **판을 올리기 전에** 끝나야 한다. 올린 뒤에 구우면 이름에 _rt210 이 붙어
#   짝이 안 맞고 그 칸은 영영 반쪽으로 남는다.
--engine sionna --spp 4000000000 --sw R0D0E0F1 --range-m 15 --n-poses 512 --env outdoor01 --max-paths 12000000 --env-scat 0.1 --max-depth 2 --els=-30 --shard 1 --nshards 2
--engine sionna --spp 4000000000 --sw R0D0E0F1 --range-m 15 --n-poses 512 --env outdoor01 --max-paths 12000000 --env-scat 0.5 --max-depth 2 --els=-30 --shard 0 --nshards 2
