# ⭐2026-08-31 — 한 파일로 합쳐 잡 전환 틈을 없앤다(사용자 지적).
# ⛔range_spp 는 뺐다 — spp 36e9·144e9 가 **uint32 한계(2^32=4.29e9)를 넘어** 아예 못 돈다.
#   sb_candidate_generator.py:110 `sampler.seed(seed, num_samples)` 가 TypeError 를 던진다.
#   4e9 가 이미 천장의 0.9 배다. ⇒ 「광선을 r² 로 올려 거리 교란을 가른다」는 설계는 막혀 있다.
runners/jobs_0831_merged2.txt

# ⭐2026-08-31 추가 — 깨진 prop-scale 대조를 고친 판.
#  ■ 왜 — prop 만 키우면 이웃 프롭이 관통한다(matrice4e 틈 5.9 mm → x1.4 에서 −103.7 mm).
#    x1.4·x2.0 의 낮은 dB 는 «프롭 크기 무관» 이 아니라 «관통면이 서로를 가림» 일 수 있다.
#  ■ ⇒ frame_scale 을 신설해 허브·동체를 함께 키운다. 세 축이 갈린다:
#      prop=frame=k  기체 전체를 k 배      ← 「크기가 설명하나」
#      frame=k only  프레임만 벌림          ← 「프롭이냐 프레임이냐」
#      prop=0.7 only 프롭만 줄임(이미 있음) ← 겹침 없는 유일한 prop-only 점
runners/jobs_0901_scale.txt

# ⭐2026-08-31 — 10 시간 분량 계획(사용자 요청). 한 파일로 합쳐 전환 틈을 없앤다.
#  A 동체만 줄이기(54) ⭐기전을 직접 시험 — 「정면에서 동체 거울이 날개를 묻는다」
#  E 균일 배율 양끝(24) — 크기 축을 0.7·1.0·1.4·2.0·3.0 다섯 점으로
#  D 기체 × 앙각(68, 36 은 기존) — 6 장의 «기체 의존» 이 각도를 따라 어떻게 사라지나
#  C 실외 앙각 확장(30) — future work 를 «첫 결과 + 각도 스윕» 으로
#  F 반사 깊이 3(30) — 「모서리는 무동작」 이 깊이 1 밖에서도 성립하나
#  ⛔B 로터 수 4→8 은 뺐다 — rotor_deg·rotor_r_mm 이 사양에 박혀 있고 대체 배치로 가면
#    CAD 프레임에서 IndexError 가 난다. 9/3 전에 손대기엔 위험하다.
runners/jobs_0901_merged.txt

# ⭐2026-09-01 — 세션이 오래 끊겨도 마르지 않게 이어 붙인다.
#  D-2 기체 × el −45·−75 · A-3 동체 축 × el −15·−45·−75.
#  둘 다 기존 메쉬·손잡이만 쓰므로 설계 위험이 없다.
runners/jobs_0901_more.txt
