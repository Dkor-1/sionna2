# -*- coding: utf-8 -*-
"""적대적 검산 라운드가 확인한 «치명» 항목만 정본 원장에 직접 반영한다.
   판단이 갈리는 항목은 안 건드리고 audit 블록에 [남김] 으로만 적는다."""
import json, io, collections
P = "/workspace/sionna/outputs/material_canon_0816.json"
d = json.load(open(P), object_pairs_hook=collections.OrderedDict)
log = []

def setv(path, val, note):
    cur = d
    for k in path[:-1]:
        cur = cur[k]
    old = cur.get(path[-1])
    cur[path[-1]] = val
    log.append({"where": "/".join(str(x) for x in path), "note_ko": note,
                "before": (old if not isinstance(old, str) else old[:220])})

mp = d["metric_protocol"]

# ── [고침 1] 정지 성분(DC) 밴드가 표에 아예 없었고, 기체 갈래는 el 0 값 6.613 을 el −30 에 썼다
mp["dc_grid_band_db_by_el"] = collections.OrderedDict([
    ("+0", 6.613), ("-15", 1.592), ("-30", 2.177), ("-45", 2.064),
    ("-60", 6.902), ("-75", 0.192), ("-90", 0.291)])
mp["dc_band_fix_ko"] = ("⭐[적대적 검산 고침] 정지 성분(DC) 밴드가 이 표에 없었다. "
    "기체 교차 갈래(material_canon_0816_airframes.bands.dc_db)는 **6.613 을 el −30 에 썼는데 "
    "그것은 정면(el 0) 값**이다 — el −30 의 실제 DC 밴드는 **2.177 dB** 다. "
    "이번 라운드의 어떤 판정도 뒤집히지는 않는다(s1000plus −0.15 은 두 밴드 다 안, "
    "matrice4e −13.81 · mini5pro −15.60 은 두 밴드 다 밖). 그러나 표에 없으면 다음 사람이 "
    "또 정면 값을 빗각에 쓴다.")
log.append({"where": "metric_protocol/dc_grid_band_db_by_el", "note_ko": "DC 밴드 앙각별 표 신설(없었음)", "before": None})

# ── [고침 2] 리듬·상한 위 밴드가 앙각별인데 한 값으로만 실려 있었다
mp["rhythm_grid_band_pp_by_el"] = collections.OrderedDict([
    ("+0", 11.852), ("-15", 0.100), ("-30", 21.819), ("-45", 12.824),
    ("-60", 15.914), ("-75", 2.442), ("-90", 16.450)])
mp["above_ceiling_grid_band_pp_by_el"] = collections.OrderedDict([
    ("+0", 1.1839), ("-15", 12.5549), ("-30", 9.256), ("-45", 1.5406),
    ("-60", 1.2948), ("-75", 0.1818), ("-90", 0.003)])
setv(["metric_protocol", "rhythm_grid_band_pp"], 21.819,
     "값은 그대로 두되 이름표를 고친다 — 이것은 «앙각별» 이 아니라 «전 앙각 최대(대체) 밴드» 다")
mp["rhythm_grid_band_pp_name_ko"] = "전 앙각 최대(대체) 밴드 — 앙각별 값은 rhythm_grid_band_pp_by_el"
setv(["metric_protocol", "above_ceiling_grid_band_pp"], 12.5549,
     "같은 이유로 이름표 정정 — 전 앙각 최대(대체) 밴드")
mp["above_ceiling_grid_band_pp_name_ko"] = "전 앙각 최대(대체) 밴드 — 앙각별 값은 above_ceiling_grid_band_pp_by_el"
mp["band_table_scope_fix_ko"] = ("⭐[적대적 검산 고침] «밴드는 앙각마다 다르다» 라고 적어 놓고 리듬·상한 위 몫만 "
    "한 값(21.819 · 12.5549)으로 실려 있었다. 그 두 값은 **전 앙각 최댓값**이고, 각도 갈래는 실제로 "
    "앙각별 값으로 채점했다(그리고 el −15 리듬 밴드 0.1 %p 같은 머리카락 밴드에는 최댓값을 대체로 썼다). "
    "표에 앙각별 값을 실어 둔다 — 최댓값만 보면 el −15 처럼 밴드가 좁은 앙각에서 «판정 불가» 로 잘못 내린다.")

# ── [고침 3] 튀는 자세(이상값) 전수 무결 주장이 사실이 아니다
setv(["metric_protocol", "headline_trim_ko"],
    ("이번 정본의 **헤드라인 칸**은 원판(안 솎음) 또는 갈아끼움 k=1 이다 — 두 값이 요동 축에서 0.01 dB 안에 "
     "같고, 사다리·각도·기체 갈래의 헤드라인 칸에는 튀는 자세(이상값)가 없다(고립도 1.001~1.056, 문턱 2.0). "
     "⚠[적대적 검산 고침] 그러나 **«네 갈래 어느 칸에도 없다» 는 사실이 아니다.** ⓐ H5(굴절) 갈래의 "
     "**정면 el 0 · 굴절 × 얇은 칸**은 요동이 사실상 자세 6 개에 몰려 있고(유효 자세 수 6.0 / 8192, "
     "상위 8 자세가 99.93 %, 최대÷중앙 1364) H5 갈래 자신이 «읽을 수 없다» 로 판정했다. "
     "ⓑ H5 갈래 el −30 · 정본 1.43 mm 의 굴절 팔은 고립도가 **1.227** 로 위에 적은 범위 밖이다. "
     "ⓒ 고립도(최대÷둘째)는 **여럿이 함께 튀는 칸을 못 잡는다** — el 0 굴절 칸의 고립도는 1.000 인데도 "
     "여섯 자세가 전부다. 고립도만으로 «튀는 자세 없음» 을 선언하지 말고 **유효 자세 수·상위 8 자세 몫**을 "
     "같이 본다."),
    "네 갈래 전수 «튀는 자세 없음» 주장 정정 — H5 갈래 두 칸이 반례")

# ── [고침 4] 슬래브 열 혼용: 40.2° 라고 적고 45° 열의 수를 붙였다
setv(["metric_protocol", "slab_column_rule_ko"],
    ("⭐**슬래브 예측을 인용할 때 어느 열을 쓰는지 규약으로 못 박는다.** "
     "ⓐ 정렬된 거울면(정반사·az45_el+0 같은 0.5° 안 정렬 삼각형)은 정의상 수직입사이므로 **수직입사 열**. "
     "ⓑ 빗각의 확산 표적 축(우리 헤드라인)은 **적합된 유효 입사각 열** — el −30 에서 40.2°(RMS 잔차 0.145 dB), "
     "el −60 에서 21.9°. ⛔**각도평균 열은 표적 축 인용에 쓰지 않는다**(정본에서 4.93 dB 어긋난다). "
     "⭐[적대적 검산 고침] **열과 수를 섞지 마라.** 정본 1.43 mm · el −30 에서 "
     "40.2° 열의 예측은 **−12.89 dB(잔차 0.13 dB)** 이고, **−12.71 dB(잔차 0.31 dB)** 는 **45° 열**의 수다. "
     "⚠그리고 40.2°·64.5° 는 **측정에 맞춘 역산값**이지 기하로 유도한 입사각이 아니다 — "
     "«예측과 0.1 dB 로 맞는다» 를 검증으로 읽으면 순환논증이다. 빗각 el −30 의 순진한 기하 입사각은 "
     "약 60° 이고, 그것과 맞는 쪽은 오히려 확산 바닥의 64.5° 다."),
    "40.2° 라고 적고 45° 열의 예측·잔차를 붙인 혼용을 고치고, 역산값 단서를 넣는다")

# ── [고침 5] J3 본문의 같은 혼용
for a in d["adjudications"]:
    if a["id"] == "J3":
        a["ruling_ko"] = ("⭐실제로 잰 날개 에코 변화는 el −30 에서 **−13.02 dB** 이고, 맞는 슬래브 열은 각도평균이 "
            "아니라 **유효 입사각 40.2°** 다 — 그 열의 예측은 **−12.89 dB, 잔차 0.13 dB**(⚠[적대적 검산 고침] "
            "앞서 여기 적혀 있던 «예측 −12.71 · 잔차 0.31 dB» 는 **45° 열**의 수였다. 사다리 갈래 원문은 "
            "«45° 예측과 0.31 dB · 적합 40.2° 에서 RMS 0.145 dB» 로 옳게 갈라 적었다). "
            "⇒ 값을 **−13.0 dB**, 문장을 «78 → 약 **91** dB» 로 고친다(이 수는 측정값이라 열 혼용과 무관하다). "
            "익사 간격은 계획보다 **약 4.9 dB 더 벌어진다**(결론 방향은 같고 강해진다). "
            "⚠덱 문장이 각도 무관 서술이므로 **el −30 꼬리표**를 같이 넣거나 문장을 빗각 한정으로 좁힌다.")
        log.append({"where": "adjudications/J3/ruling_ko", "note_ko": "40.2° ↔ 45° 열 혼용 정정", "before": "…(예측 −12.71, 잔차 0.31 dB)…"})

# ── [고침 6] 기체 갈래 헤드라인이 «판정 불가» 를 «안 움직인다» 로 미끄러뜨렸다
bd = d["branch_summary"]["D_기체_교차"]
setv(["branch_summary", "D_기체_교차", "headline_ko"],
    ("«프로펠러가 손잡이» 는 세 기체 모두 성립(날개선 −16.99 · −14.51 · −15.08 dB). 그러나 "
     "«확산 바닥이 프롭을 탄다» 는 **s1000plus 에서 성립하지 않는다**(−0.07 dB — 그 앙각 바닥 밴드 9.30 dB "
     "**안**이라 그 자체로는 «판정 불가» 이고, 동시에 −12.6 dB 라는 크기와는 **양립하지 않는다**). "
     "정지 성분도 −0.15 dB 로 **판정 불가**다(밴드 2.18 dB 안 — ⚠[적대적 검산 고침] 앞서 «안 움직인다» 로 "
     "단정돼 있었다. 짝지은 비교라 사실상 0 이라는 읽기는 기체 갈래 원문에 있지만, 그 근거는 밴드가 아니라 "
     "짝지음이므로 문장에 함께 적는다)."),
    "«−0.15 dB 로 안 움직인다» → «판정 불가» + 근거(짝지음) 명시. 바닥도 «반증» 단정을 크기 양립불가로 정밀화")

# ── [고침 7] 각도 갈래 헤드라인이 원 갈래의 «약한 쌍» 단서를 떨어뜨렸다
setv(["branch_summary", "C_각도_확장", "headline_ko"],
    ("두께 효과는 앙각을 탄다 — 요동 축에서 −30° 가 가장 크고(−16.99 dB @0.9 mm) **비단조**다. "
     "⚠[적대적 검산 고침] «그 각도 차이는 밴드 밖» 을 세 쌍 전부에 붙이면 안 된다 — 각도 갈래 원문은 "
     "−30↔−15(3.92 dB, 밴드 합의 2.3 배) · −30↔−60(2.26 dB, 5.8 배)만 세고 **−15↔−60(1.65 dB, 1.23 배)은 "
     "«겨우 밖 — 약하다»** 로 적었다. 게다가 −30·−60 밴드는 0.374·0.018 dB 로 **머리카락 밴드**라, "
     "이 원장 자신의 규약(밴드 <0.5 dB 면 빌려 온 시드 밴드 3sd 5.5 dB 도 넘어야 유의)을 각도 **차이**에 "
     "적용하면 세 쌍 다 5.5 dB 를 못 넘는다. 정면 0° 는 정본에서도 0.000 dB."),
    "각도 의존 «밴드 밖» 을 원 갈래의 쌍별 판정으로 되돌리고 머리카락 밴드 단서를 붙인다")

# ── [고침 8] 껍데기 서사의 «통째로 DC» 는 기제 과장 — J6 과 같은 정정을 대칭으로 적용
ws = d["why_shell_untouched"]
ws["one_paragraph_ko"] = ws["one_paragraph_ko"].replace(
    "셸은 기체에 붙어 같이 움직이지 않는 정지 구조물이라 그 에코가 통째로 DC 로 간다",
    "셸 에코는 **거의 전부** DC 로 간다(⚠[적대적 검산 고침] «통째로» 는 과장이다 — 아래 residual_is_buried_not_absent 참조)")
ws["residual_is_buried_not_absent_ko"] = ("⭐[적대적 검산 고침] J6 이 정면 0° 의 프롭에 내린 정정(«안 잡았다» 가 아니라 «묻혔다»)을 "
    "**껍데기에도 똑같이 적용해야 한다.** 셸 0.75 팔과 기본판의 차(잔차)를 el −30 에서 직접 재 보면 그 잔차는 "
    "정지 성분이 아니라 **날개 리듬을 그대로 탄다** — 리듬 몫 **49.9 %**(백색 널 12.6 %) · 빗살 대비 **+32.6 dB** · "
    "잔차 자신의 요동이 자기 정지 성분보다 53.0 dB 아래. 즉 셸 에코도 회전하는 프롭에 가려지며 변조된다. "
    "다만 그 몫이 기본판 요동보다 **59.4 dB 아래**(셸 0.5 팔은 55.7 dB 아래)라 표적 축 잣대에 안 뜬다. "
    "⇒ 옳은 문장은 «셸은 표적 축에 배선이 없다» 가 아니라 «**셸의 표적 축 몫은 59 dB 아래라 묻힌다**» 다. "
    "결론(비로 서는 주장은 셸 정정에 면역)은 그대로 살고, 기제 문장만 바뀐다.")
ws["evidence"]["shell_only_el-30_residual_rhythm_pct"] = 49.94
ws["evidence"]["shell_only_el-30_residual_comb_db"] = 32.56
ws["evidence"]["shell_only_el-30_residual_below_base_ac_db"] = -59.39
ws["evidence"]["shell0.5_el-30_residual_below_base_ac_db"] = -55.74
ws["evidence"]["waveform_rho_full_precision"] = 0.9999994473
log.append({"where": "why_shell_untouched", "note_ko": "«에코가 통째로 DC» 기제 과장 정정 + 잔차 실측(리듬 49.9 %·빗살 +32.6 dB) 추가", "before": "…정지 구조물이라 그 에코가 통째로 DC 로 간다…"})

# ── [고침 9] 정본 두께 잔차 범위의 기체 범위 오류
setv(["canon", "sionna_prop_mm", "residual_ko"],
    ("⚠D2 규약대로 이 값은 «우리 메쉬의 두께» 이지 DJI 실물 두께가 아니다. 실물과의 차는 **메쉬 층 결함**으로 "
     "남고 재질 손잡이로는 안 닫힌다. ⭐[적대적 검산 고침] 그 차의 크기를 **+1.2~+1.7 dB 로 적으면 안 된다** — "
     "그 범위는 감사 원장(mesh_audit_0816_prop_geometry §F)에서 **DJI Mini 2 공식 CAD 와 직접 견준 값**이고, "
     "**matrice4e 는 프롭 두께 실측이 저장소에 없다**. 같은 감사가 matrice4e 에 준 정직한 범위는 "
     "**+0.3~+3.3 dB**(추정 두 개가 1.41 배 벌어지고 어느 쪽인지 모른다)다. "
     "⚠추가로, 정본 1.4302 mm 는 **메쉬를 잰 값이 아니라 코드 상수에서 해석적으로 계산한 값**이고, "
     "실제 메쉬는 1.4559 mm(+1.8 %, |Γ| 로 +0.13 dB)다."),
    "matrice4e 에 Mini 2 범위(+1.2~+1.7 dB)를 붙인 범주 오류 정정")

# ── [고침 10] 파급 목록에 빠진 것 추가
d["impact"].extend([
 collections.OrderedDict([
  ("id","I16"),
  ("target_ko","⭐반사 깊이 축 판정 — 8/18 덱 30 장 Future work 의 데이터"),
  ("files",["outputs/depth_axis_verdict_0816.json","outputs/switch_factorial.json(깊이 절)"]),
  ("current_value_ko","깊이 1 ↔ 3 판정 전체 — 전부 셸·프롭 100 mm 위에서 잰 값"),
  ("direction_ko","⚠**모른다 — 그리고 지금 원장으로는 잴 수 없다**"),
  ("size_estimate_ko","두께 팔 **11 개가 전부 깊이 1** 이라 «얇은 재질 × 깊이 3» 칸이 하나도 없다 — 두께 축과 깊이 축의 "
    "교집합이 **공집합**이다. 그런데 H5 갈래가 낸 결과가 바로 «두께가 굴절 채널을 크게 바꾼다» 다(굴절 팔의 경로 수가 "
    "382 → 14~21 로 줄고 요동이 −14~−19 dB). 굴절·투과는 깊이 축의 주된 내용물이므로 **깊이 판정이 두께에 둔감하다는 "
    "근거가 없다.** ⇒ 정본 두께로 깊이 3 을 다시 돌리기 전까지 깊이 판정에는 «셸 100 mm · 프롭 100 mm» 꼬리표를 붙인다. "
    "⚠덧붙여 depth_axis_verdict_0816.json 은 병합 **이전** 원장(16:36 KST · 337 행)을 읽었다 — 최종 병합본은 349 행이다."),
  ("action","tag_now_recompute_after_thin_depth3"),
  ("basis_ko","H5 갈래 structural_evidence.every_thickness_arm_is_depth1 · 원장 두께 행 전수(17 칸 모두 max_depth 1)")]),
 collections.OrderedDict([
  ("id","I17"),
  ("target_ko","⭐PathSolver 팔의 판독거리·탐지확률 — **오늘 바로 다시 잴 수 있다**"),
  ("files",["outputs/detection_curves.json (arms sionna_el+0 · sionna_el−30 · sionna_el−60)"]),
  ("current_value_ko","sionna R50(빗살/대역) = el+0 2884.7/4204.8 · el−30 824.2/614.9 · el−60 924.9/731.4 m — 전부 100 mm 위"),
  ("direction_ko","el 0 불변 · el −30 사실상 불변 · **el −60 은 크게 늘어남**"),
  ("size_estimate_ko","⭐I1 이 «오늘은 안 바뀐다(D3 동결)» 로 적은 것은 **우리 커널 팔에만** 맞다. 이 파일의 여섯 팔 중 "
    "**셋이 PathSolver 팔**이고, 그 셋이 쓰는 세 앙각(0 · −30 · −60)에는 **정본 1.43 mm 칸이 전부 있다** — 즉 "
    "지금 원장만으로 다시 계산된다. 움직이는 몫으로 환산하면 el 0 0.00 dB(불변) · el −30 −0.08 dB(824.2 → 약 820 m) · "
    "el −60 **+5.74 dB**(924.9 → 약 **1287 m**, +39 %). ⚠단 앵커는 el −30 한 칸에서 뽑히므로 앵커도 함께 다시 잡아야 한다(I3)."),
  ("action","recompute"),
  ("basis_ko","moving_share 절(이 원장) · 원장 두께 칸 목록(prop1.43 이 el 0 · −30 · −60 에 존재)")]),
 collections.OrderedDict([
  ("id","I18"),
  ("target_ko","리포트 앙각 편 그림 다섯 장의 PathSolver 칸"),
  ("files",["outputs/ch1_elevation_figdata_r15.json","outputs/figures/ch1_f1_maps_r15.png … ch1_f5_raybudget_r15.png"]),
  ("current_value_ko","PathSolver 칸이 전부 sionna_p4000000000_r15_n8192_d1 (100 mm) — 정정판 칸은 이 원장에 안 들어가 있다"),
  ("direction_ko","그림의 PathSolver 지도·스펙트럼 레벨이 내려간다(빗각)"),
  ("size_estimate_ko","el −30 에서 요동 −13.02 dB · 정지 성분 −12.83 dB. 무늬 자체는 산다(맵 상관 0.903). "
    "⇒ 그림을 다시 그리기 전까지 **캡션에 «셸 100 mm · 프롭 100 mm» 꼬리표**가 필요하다."),
  ("action","tag"),
  ("basis_ko","ch1_elevation_figdata_r15.json 이 100 mm 팔만 담고 있음(문자열 검사)")]),
 collections.OrderedDict([
  ("id","I19"),
  ("target_ko","물리 켬(ps_phys) 팔 — 정정판이 **원리적으로 없다**"),
  ("files",["outputs/noise_distance_frame.json (ps_phys 47/63 m)"]),
  ("current_value_ko","ps_phys 무늬 한계 47 m · Pd90 63 m"),
  ("direction_ko","⚠**모른다**"),
  ("size_estimate_ko","I4 가 ps_off · ps_refr 의 정정 크기를 냈지만 **ps_phys 는 빠져 있다**. 원장의 두께 칸 17 개가 "
    "**전부 physics=False** 라 물리 켬 팔의 정정판이 하나도 없다. 회절·산란이 켜진 팔에서 두께가 무엇을 하는지는 "
    "**안 쟀다** — ps_phys 수는 꼬리표만 붙이고 방향을 적지 않는다."),
  ("action","tag"),
  ("basis_ko","원장 두께 행 전수 physics=False")])])
log.append({"where":"impact","note_ko":"빠진 파급 넷 추가(I16 깊이 축 · I17 PathSolver 판독거리 · I18 리포트 그림 · I19 물리 켬 팔)","before":"I1~I15"})

# ── [고침 11] 열린 질문 보강
d["open_ko"].extend([
 "⭐**두께 축과 깊이 축의 교집합이 공집합**이다 — 두께 칸 17 개가 전부 깊이 1 · 물리 끔이다. "
 "깊이 3 · 물리 켬에서 두께가 무엇을 하는지는 한 칸도 없다.",
 "⭐**날 시위(코드) 법칙이 이미 반증돼 있다**(mesh_audit_0816_prop_geometry §D·§K) — 실물 DJI 프롭 3 종이 "
 "r/R 0.7 에서 우리보다 27~39 % 넓다. 기체별 정본 두께 세 값(0.80 · 1.43 · 1.99 mm)은 서로 독립 측정이 아니라 "
 "**한 법칙 × 프롭 지름**이다(세 값 전부 지름 × 0.005220). 법칙이 바뀌면 세 값이 함께 움직인다.",
 "**고립도(최대÷둘째)만으로는 여럿이 함께 튀는 칸을 못 잡는다** — el 0 굴절 × 얇은 칸이 고립도 1.000 인데 "
 "유효 자세 수 6 이다. 이상값 등급에 유효 자세 수·상위 8 자세 몫을 넣어야 한다."])

# ── 검산 블록
d["adversarial_recheck_0816_2049kst"] = collections.OrderedDict([
 ("who","적대적 검산자(별도 세션) — 저장된 원장만 읽고 numpy 로 독립 재계산. GPU·git 미사용."),
 ("when_kst","2026-08-16 20:49 KST"),
 ("what_reproduced_ko",
  "잣대 식을 build_md_atlas.py 에서 베끼지 않고 **다시 구현해** 독립 재계산했다(표본 30 칸 이상). "
  "사다리 8 칸(요동·정지·확산 바닥·빗살·리듬)이 원장과 0.001 dB 안에서 일치 · H5 DiD 세 칸 일치 · "
  "기체 교차 세 칸 일치 · 앙각별 격자 밴드 21 개 값 전부 일치 · ITU-R 슬래브 표(8 두께 × 5 각) 전부 일치 · "
  "el 0 잔차(리듬 89.81 % · 빗살 48.44 dB · −54.68 dB) 일치. **수치에는 오류가 없다.**"),
 ("independent_robustness_ko",
  "규약이 요구하는 균일 재표집(앞반·뒷반·짝수·홀수)을 직접 돌렸다. 정본 1.43 mm · el −30: "
  "요동 −12.91~−13.09 · 빗살 −5.78~−6.31 dB — 헤드라인 강건. s1000plus 바닥 −0.07~−0.46 dB · "
  "정지 성분 −0.147 dB(네 재표집 전부) — 그 갈래도 강건. "
  "⚠단 mini5pro 빗살은 짝수·홀수 재표집에서 −4.55 · −4.75 dB 로 그 앙각 밴드(4.616) **안**으로 들어온다 — "
  "기체 갈래의 «mini5pro 빗살 유의» 는 강건하지 않다."),
 ("fixed_here_ko", log),
 ("left_open_ko", [
   "[남김] 정본 1.43 mm · el −30 의 빗살 −6.21 dB 는 밴드의 **1.34 배**이고, 균일 재표집 최솟값(−5.78)에서는 "
   "**1.25 배**로 각도 갈래 자신의 «약한 유의» 문턱에 걸린다. «유의» 로 적되 «겨우» 를 붙일지는 판단이 갈린다.",
   "[남김] 빗살이 el −60 에서 «부호가 뒤집힌다» 는 문장 — 값 +1.65 dB 는 밴드 4.05 **안**이라 그 자체로 "
   "«판정 불가» 다. 안전한 문장은 «−60 에서는 빗살 감소가 확인되지 않는다» 이고, «뒤집힌다» 는 밴드 안 값을 "
   "적극적 주장으로 쓰는 셈이다. 결론(빗각 전부에 붙이지 마라)은 어느 쪽이든 같다.",
   "[남김] R50 브래킷이 엔진을 섞는다 — current_R50_m 723.7 은 **우리 커널** 값인데 "
   "measured_sionna_arm_* 행은 **PathSolver** 의 AC/DC 균형으로 계산한 이동을 그 위에 얹는다. "
   "ours_balance_* 행이 따로 있어 둘 다 읽을 수는 있지만, 표만 보면 720.3 m 를 «정정 뒤 R50» 으로 읽기 쉽다.",
   "[남김] 격자 밴드의 주인이 우리 커널이라는 사실(open_ko 1 번)이 헤드라인 문장에는 안 붙는다 — "
   "PathSolver 에는 격자 개념이 없고, 이 원장의 «유의» 판정은 전부 빌려 온 눈금 위에 서 있다.",
   "[⑧ 말투] 갈래 원장 셋에 내부 축약어 «튐» 이 남아 있다 — "
   "material_canon_0816_ladder.json /outlier_forensics/procedure_warning_ko, "
   "material_canon_0816_angles.json /method_finding/reads_ko[0] · /method_finding/outlier_grades_ko, "
   "material_canon_0816_airframes.json /verdict/outlier_forensics_ko. "
   "«튀는 자세(이상값)» 로 고쳐야 한다(정본 본문에는 안 남아 있다).",
   "[정리] 기체 갈래 원장(material_canon_0816_airframes.json /verdict/outlier_forensics_ko)은 아직 "
   "«헤드라인은 −3.43 dB(판정 불가)로 적는 것이 규약에 맞다» 라고 적고 있다 — J1 이 **기각한** 권고다. "
   "그 원장만 따로 읽는 사람은 기각된 수를 인용하게 된다.",
   "[⚠] 이 정본의 _meta.written_at_kst 가 «20:5x KST» 인데 파일 실제 기록 시각은 **20:27 KST** 다.",
 ])])

json.dump(d, open(P, "w"), ensure_ascii=False, indent=1)
print("OK  fixes:", len(log))
