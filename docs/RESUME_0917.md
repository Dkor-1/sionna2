# Current status — 2026-09-17, 17:45 KST (post-snapshot notes to 18:05 KST)

> The in-repo handover. `/workspace/RESUME_NOW.md` points here. The previous edition,
> [`RESUME_0916.md`](RESUME_0916.md), is historical; its unscoped statements were corrected on 09-17.
> Python is **/workspace/.venvs/py312/bin/python**. CPU work:
> `CUDA_VISIBLE_DEVICES="" MPLBACKEND=Agg nice -n 19 taskset -c 8-15 /workspace/.venvs/py312/bin/python …`.
> Repo conventions: `../CLAUDE.md` (language policy, claim gate), [`HANDOVER_MAP.md`](HANDOVER_MAP.md),
> [`NEW_FILE_RULES.md`](NEW_FILE_RULES.md).
>
> ⭐**Current central question:** [`MOBICOM_PIPELINE_PLAN_0916.md` §9](MOBICOM_PIPELINE_PLAN_0916.md#9-central-question-and-evaluation-contract)
> (detection and tracking; the evaluation contract is fixed before the campaign).
>
> ⭐**Result readout for queues 0948-0956:** [`READOUT_0917.md`](READOUT_0917.md), generated by
> `benchmark/readout_0917.py` → `outputs/readout_0917.json` (+ `_stems.json`). It landed at 17:55 KST (generated
> 2026-09-17T08:55:02Z, not yet committed). At that generation every value of §3.1-§3.3 below that also appears in its
> tables 3(a)-(d) is the same there (checked row by row in the 09-17 review of this file). Cite `outputs/readout_0917.json`, not this file; if the
> readout is rerun, the readout is canonical and §3 here is the dated copy.

**Snapshot.** Shard counts and the worker list are as of 2026-09-17 17:42 KST (08:42 UTC). Every count is **saved
shards on disk** (`outputs/elev_sweep_shards`), matched to queue lines through `benchmark/elevation_sweep_md.py --dry-run`
— not the supervisor's `큐 <launched>/<total>` counter, which counts launches (including killed ones). The counts were
recounted with the same dry-run method at about 17:55 KST and had not changed.

**After the snapshot (17:55 KST).** At the user's request («GPU 1,2번에 작업 1개씩만», 17:56 KST) the main session
started the last two lines of `jobs_0955` (street canyon, el -30°, cap 32e6, shards 00 and 01) **by hand on cards 1 and
2** (`runners/logs/manual_gpu{1,2}_0955C_20260917T085500Z.log`; key `manual_one_each_gpu1_gpu2_0917` in
`runners/GPU_HOLD.json`). The hold stays `[0, 1, 2]`; nothing else goes on cards 1 or 2 (§1).

## 0. One line

Seven supervisors (0950-0956) share cards 3 and 4 with six workers (plus, from 17:55 KST, the two hand-started 0955 C
shards on cards 1 and 2). Queues 0948 and 0949 have finished; their cells are read in `READOUT_0917.md` 3(f). The antenna pattern × pointing comparison has **no complete 2×2 at one cap yet** — the
first (ground only, cap 2e6) completes when the running tr38901/device shard lands. Nothing needs a human except the
deck questions in §7.

## 1. GPU rules — read this before launching anything

`runners/GPU_HOLD.json` has `"gpus": [0, 1, 2]` — held. **New launches go to cards 3 and 4 only** (the user's standing
rule since 2026-09-15; do not ask again). The supervisors re-read the file every round (status lines show G0-G2
`⛔보류`). ⚠The file's `note` field still describes the 09-16 loan of card 2; the `gpus` list is what counts.

* A hold blocks new launches only. Taking a card back from running work is `runners/evacuate_gpu.py <card> --go`,
  and that needs the user's word; it writes the interrupted lines to `runners/jobs_REDO_gpu<card>_<UTC>.txt`.
* 09-17 12:43 KST: the six 0950 ground lines lent to cards 1 and 2 were stopped at the user's request
  (`runners/jobs_REDO_gpu1_20260917T034251Z.txt`, `runners/jobs_REDO_gpu2_20260917T034253Z.txt`) and re-queued
  unchanged as 0951. They are the six `rc=-15` lines in `runners/logs/sup_0950.log`.
* 09-17 17:55 KST, the one exception in force: two hand-started workers, `jobs_0955` lines 17-18 (street canyon,
  el -30°, cap 32e6, shards 00/01) on cards 1 and 2, at the user's request for exactly one job per card (GPU_HOLD key
  `manual_one_each_gpu1_gpu2_0917`). ⚠The worker writes no lock file, so if `sup_0955` reaches lines 17-18 while these
  two are still running, the shard does not exist yet and the line would be launched a second time on card 3 or 4 —
  check `ps` for the two pids (75010, 75009) before that happens; do not edit the queue from a sub-task.
* Never `pgrep -f` (it matches your own shell); never kill a supervisor. Sub-tasks do not edit `GPU_HOLD.json` or
  `runners/jobs_*.txt` — the main session edits queue headers.

## 2. Queues 0948-0956

State words: **queued** (lines in a jobs file, no shard) · **shards saved** (on disk, not read into an in-repo
ledger) · **analysed** (read into an in-repo ledger) · **scope checked** (recomputed from the shards by the 09-17
adversarial verification of the external review, and recounted again from the shards for this document and its review).
`READOUT_0917` (17:55 KST) reads every complete cell of 0948-0956; its manifest (§2.1 there) gives the same saved-shard
counts as this table.

| Queue | Purpose (from its header) | Lines | Saved shards | State |
|---|---|---|---|---|
| `jobs_0948_survivors_and_angles` | re-buy of the aimed-antenna cells interrupted on 09-16; aimed tr38901 at other angles; the 8e6 rung at other angles (ground -15/-45/-75°, canyon -15/-45°); canyon at -75°, canyon 16e6 at -30°, canyon rep1 at -60° | 23 | 23/23 (supervisor ended 15:10 KST, 0 failures) | **analysed** in `READOUT_0917` 3(f) (canyon rep1 also in 3(d)); `outputs/dropout_knobs_0916.json` predates them |
| `jobs_0949_antenna_by_angle` | aimed tr38901 in the canyon at -15/-45/-75°; canyon 8e6 at -75°; ground seed 4 and rep3; canyon seed 2 at -15/-45° | 16 | 16/16 (ended 13:15 KST, 0 failures) | **analysed** in `READOUT_0917` 3(f); ground rep3 and seed 4 also in 3(d); rep3 in `outputs/repeat_spread_0916.json` (§3.5) |
| `jobs_0950_pattern_x_pointing` | the two missing cells of pattern × pointing (iso pointed at the drone, tr38901 at the default orientation), el -60°, ground only and street canyon, caps 2e6 and 16e6 | 16 | 7/16 | three complete cells **analysed** (`READOUT_0917` 3(a)) and **scope checked** (§3.1) |
| `jobs_0951_redo_from_gpu12` | the six 0950 ground lines taken off cards 1/2 | 6 | 3/6 — **the same files as 0950**, not additional | see 0950 |
| `jobs_0952_open_sky_0deg_max_paths` | open sky at 0°, max_depth 2, max_num_paths_per_src 1e6-32e6 plus one repeat | 12 | 8/12 | 2e6, rep1, 8e6, 32e6 **analysed** (`READOUT_0917` 3(b)); 2e6, rep1, 32e6 **scope checked** (§3.2); 8e6 saved 17:29-17:30 KST and recounted twice (§3.2) |
| `jobs_0953_zero_deg_rt210_models_depth_az180` | 0° on 2.1.0: mini5pro, mavic4pro, max_depth 1 (2e6, 32e6), az 180° | 10 | 1/10 (mini5pro shard 00) | queued; first half-cell saved |
| `jobs_0954_aim_offset` | tr38901 tilted 5/10/20° toward the ground, el -60°, ground only and street canyon | 12 | 0/12 | queued (one running) |
| `jobs_0955_rt210_recheck_and_canyon_ladder` | A: our scene at -30/-60°, buildings only at -30°; B: angle-window edges az 0.11/0.12/0.2°, el -0.15/-0.16°; C: canyon -30° at 32e6 | 18 | 0/18 | queued (one running on card 4; the two C shards started by hand on cards 1 and 2 at 17:55 KST, §1) |
| `jobs_0956_open_sky_aim_offset_refs` | open-sky partners of 0954 at the same offsets (needed for «scene minus open sky») | 6 | 0/6 | queued, none launched |

Across 0950-0956: 80 lines, 74 unique shard names (0951's six are 0950's), 16 saved.

Running at the snapshot (launch line without an end line in `runners/logs/sup_095*.log`):

| Card | Queue, launch | Shard | Since (KST) |
|---|---|---|---|
| 3 | 0951 #4 | ground only, tr38901 / device orientation, 2e6, el -60°, shard 01 | 16:54 |
| 3 | 0953 #2 | mini5pro, open sky 0°, shard 01 | 17:29 |
| 3 | 0954 #1 | ground only, tr38901 aim +5°, el -60°, shard 00 | 16:36 |
| 4 | 0950 #11 | street canyon, tr38901 / device orientation, 2e6, el -60°, shard 00 | 16:36 |
| 4 | 0952 #9 | open sky 0°, 16e6, shard 00 | 17:30 |
| 4 | 0955 #1 | our scene (all elements), el -30°, shard 00 | 17:01 |

Durations from the queue headers: ground-only shard about 95 min, street-canyon shard about 225 min. Check with
`tail -2 runners/logs/sup_095*.log`. Before adding a queue: `runners/filter_jobs.sh` (NEW/DONE/STALE/BAD); and every
scene cell used in a scene-minus-open-sky readout needs its open-sky partner at the same antenna setting (missed twice:
0938, 0954).

## 3. Recounted results — scope on every line

Common scope unless a line says otherwise: Matrice 4E, 15 m, 4e9 rays, `R0D0E0F1` (diffuse on; refraction and
diffraction off), **sionna-rt 2.1.0** (`solver_build` read from every shard used), max_depth 2, 8,192 consecutive pulses
at 19.7 kHz, two shards per cell. Metric names are in §4.
Provenance for §3.1-§3.3: external review `/workspace/sionna_review_2026-09-17_queues_figures.md`, its adversarial
verification, and two further recounts from the shards (for this document and for its review; rule of
`benchmark/dropout_knobs_0916.py`; `team_meeting/teammeeting_0910/bake_ground2.contrast_db` and
`bake_outdoor.hampel_mask`). **This file is not a ledger** — cite `outputs/readout_0917.json` (`READOUT_0917.md` 3(a)-(d)).

### 3.1 Antenna pattern × pointing (queue 0950), el -60°

| Scene | Pattern / orientation | Cap | isolated_20xmedian | contrast_raw |
|---|---|---|---|---|
| ground only | iso / target | 2e6 | 90 — the same pose indices as iso / device at 2e6 | 0.14 dB |
| street canyon | iso / target | 2e6 | 339 — the same pose indices as iso / device at 2e6 | 0.25 dB |
| ground only | tr38901 / device | 16e6 | 22 — the same pose indices as iso / device at 16e6 | -0.50 dB |
| ground only 2e6 · ground only 16e6 · street canyon 2e6 | tr38901 / target (the existing aimed cells) | as listed | 0 · 0 · 0 | 29.28 · 29.39 · 29.56 dB (≈29 dB) |

* **No complete 2×2 at one cap yet.** Ground only 2e6 misses tr38901/device shard 01 (running on card 3 since 16:54).
  Street canyon 2e6 misses tr38901/device (shard 00 running since 16:36; shard 01 not launched). Ground only 16e6
  misses iso/target (not launched).
* Do not put counts from different caps into one 2×2: isolated_20xmedian moves about 4× between 2e6 and 16e6 in the
  same arm (ground only iso/device: 90 → 22). Contrast can be compared across caps; counts cannot.
* The iso contrast_raw differences (±0.1-0.2 dB) are below the solver-seed spread and are not read as effects: ground
  only iso/device 2e6 gives -0.03 dB for the plain run and rep1-rep3, and 0.44 / 0.43 / 0.11 dB with seeds 2 / 3 / 4;
  street canyon el -60° gives 0.34 dB plain and 0.06 dB with seed 2.
* tr38901/device 16e6 has the same 22 positions as iso/device 16e6 but a normaliser (median of \|E − m\|) about 158×
  smaller (1.58e-9 against 2.50e-7), so a count of 22 there is not the same event size as 22 in the iso cell. Report
  the normaliser, \|m\| and the median normalised distance next to every count. This cell puts the drone in the
  tr38901 back lobe by design (0950 header) — it isolates the pattern and is not a candidate setup.
* For tr38901 aimed at 2e6 (ground only): isolated_20xmedian 0, hampel_w51_k5 0, env_field_drop_halfmedian 82 (against
  open sky tr38901 aimed, el -60°) — the field-drop count does not follow pointing.
* For tr38901, orientation changes pointing and the polarisation basis together; they are not separated.

### 3.2 Open sky at 0° (queue 0952), iso, no environment

| Cap | S (positions with copy_k_ratio below its modal value) | Set | contrast_raw | contrast_dedup |
|---|---|---|---|---|
| 2e6 | 37 (copy_k_ratio 1: 1, 2: 36, 3: 8,155; modal 3) | — | 0.45 dB | 27.86 dB |
| 2e6, rep1 (same seed) | 37 | identical indices → **d = 0** | 0.45 dB | 27.86 dB |
| 8e6 | 12 (recounted for this document and again in its review; also `READOUT_0917` 3(b)) | subset of the 2e6 set | 0.98 dB | 27.86 dB |
| 32e6 | 2 — positions **2630, 7064** | subset of the 2e6 set | 0.05 dB | 27.86 dB |
| 1e6, 16e6 | not saved yet | | | |

* copy_k_ratio = n_dup + 1 at every position of these four cells.
* **contrast_raw is at its floor while S ≥ 1** (≤ 1 dB in all four cells); its value there depends on which positions
  are short, not on how many. Do not read 0.45 → 0.05 dB as a direction or a loss. contrast_dedup does not move.
* Tolerance in the 0952 header is max(5, 3·d); with d = 0 it is a **pre-set working threshold of 5 positions** from a
  same-seed replay, not a seed spread. A change within 5 reads «threshold not crossed», not «no effect». The header's
  outcomes (a)-(d) are not named until the 1e6 and 16e6 cells are also read (8e6 is in).
* 2.0.1 comparison: two 2.0.1 runs exist at this setting — the `_az0_` run (09-04: 36 = 35 at copy count 2 + 1 at 1)
  and an untagged run (09-06: 37, adds 7043). Name the file whenever a 2.0.1 count sits next to a 2.1.0 one; the 0952
  header's «36» is the `_az0_` run and is not a miscount. Neither file stores `solver_build`; «2.0.1» rests on the bake
  date (before the 09-14 install, [`SIONNA_UPGRADE_0914.md`](SIONNA_UPGRADE_0914.md)).

### 3.3 Isolated positions, ground only, el -60°, iso/device (the 09-16 question)

* Cap 2e6: isolated_20xmedian 90/8,192 (1.1 %) · hampel_w51_k5 98 · env_field_drop_halfmedian 82 (against open sky
  iso 2e6, el -60°). Nested: 82 ⊆ 90 ⊆ 98. The 8 isolated positions that are not field drops sit +0.95/+0.96 dB above
  the cell.
* Cap ladder 1e6 / 2e6 / 8e6 / 16e6 / 32e6 — env_field_drop_halfmedian **153 / 82 / 26 / 14 / 7**;
  isolated_20xmedian 161 / 90 / 34 / 22 / 15; hampel_w51_k5 166 / 98 / 41 / 30 / 22. Same-seed repeats rep1-rep3 at 2e6:
  82 field drops, 90 isolated. Every rung's env_field_drop_halfmedian is taken against open sky iso at cap **2e6**,
  el -60° (no open-sky cell exists at the other caps). Use the field-drop ladder when the topic is the missing ground
  reflection, and call it a field-threshold count: path lists back it only at the sampled positions (next bullets).
* At 32e6 the 15 isolated positions are **7 falls** (1604, 2277, 4386, 4557, 5798, 7597, 8015) + **8 rises** (176,
  2356, 3446, 4536, 4873, 5626, 6716, 7806); the same 8 rises appear at every cap. Path lists exist for 6 of the 15
  (5 rises + 8015). **Never diagnosed:** falls 1604, 2277, 4386, 4557, 5798, 7597; rises 3446, 5626, 7806.
* env_path_missing has not been counted for the 82. Path lists confirm the missing env_ground specular only at the
  sampled positions (`outputs/dropout_paths_0916.json`, indices ≤ 1414; `outputs/dropout_hash_vs_buffer_0916.json`).
* Other cells, cap 2e6, isolated_20xmedian: street canyon el -60° 339 (4.1 %); street canyon el -30° 96; open sky
  el -15/-30/-45/-60° 0 at max_depth 2, and 0 in the other open-sky 2.1.0 cells on disk (max_depth 1 el -30/-45/-60°,
  max_depth 3 el -30/-60°); open sky
  el 0° 37 — a different signature (one of three copies of the drone echo missing, §3.2).

### 3.4 Hash counter vs candidate buffer — `outputs/dropout_hash_vs_buffer_0916.json`

Scope: ground only, el -60°, 2e6 production cell; 12 selected isolated positions (6 chosen because the stored cap-32e6
cell no longer isolates them, 6 still isolated there) plus 24 neighbours and 6 controls = 42 positions; one diagnostic
process on card 2, 09-16.

* Buffer fixed at 2e6 (at most 1.43 % full at every grid point): raising only the hash counter restored the env_ground
  specular at **5/6 (hash 8e6) and 6/6 (hash 32e6)** of the positions chosen because cap 32e6 had recovered them. The
  one sampled position that lacked the path and stays isolated at cap 32e6 (8015) still lacked it at hash 32e6. The
  other 5 sampled still-isolated positions never lacked it.
* Hash fixed at 8e6, buffer 2e6 → 8e6: no incremental env-path recovery or loss on the 12. Its only path-count change
  (8015, +1 path, 1.97e-5 of |m|, the cell's component-wise median) is at a position where the same process's
  production-setting re-solve differs from the stored field by one path. The buffer was moved at one hash size only.
* Not established: that hash collisions cause the loss. Nothing counts collisions or ties a lost candidate to the
  missing path; a causal claim needs the specular-chain counters instrumented at 8015 and at one recovered position.

### 3.5 Repeat spread — `benchmark/repeat_spread_0916.py` → `outputs/repeat_spread_0916.json` (regenerated 09-17)

* **Comparator corrected.** Commit 5920e303 called the production-setting re-solve «same positions, same run, same
  card». It is the stored whole-sweep production output (GPU 1, 09-14, jobs_0931) vs a fresh diagnostic re-solve
  (GPU 2, 09-16, 42 poses). The script now reads these back from the shards, the split ledger and
  `runners/logs/sup_bridge_0931.log` into `_meta.comparator` (all six checks true). Card and process change together;
  nothing attributes the difference to the card.
* rep3 (bought by 0949) added. Identical whole-sweep runs, ground only el -60° 2e6, run 1 against rep1 / rep2 / rep3:
  the returned path count differs at **182 / 171 / 186 of 8,192 positions (2.1-2.3 %)**, 33 / 30 / 29 of them moving the
  field by more than 1e-5 of run 1's median |E|; 65 positions differ in all three. At the 42 contrast positions: 0
  path-count differences in 3 repeats, against about 0.9-1.0 expected per repeat at the whole-cell rate. Because the
  same positions flip again and again (65 in every pair), that 0 is too little to call those positions stable.
* `verdict_code` unchanged: `same-size-as-a-re-solve`. The open test is still a balanced same-process repeat of
  (buffer 2e6, hash 8e6) against (buffer 8e6, hash 8e6), with a production-setting re-solve at its start and end.

## 4. Metric names

Use these names in queue headers, readouts and docstrings. **Canonical definitions: [`READOUT_0917.md` §1](READOUT_0917.md#1-metric-definitions-used-under-these-names-everywhere-below).**
Short form only:

| Name | Short form | Code |
|---|---|---|
| isolated_20xmedian | d = \|E − m\|, m = component-wise median of E; count d > 20·median(d) | `benchmark/dropout_knobs_0916.py` |
| hampel_w51_k5 | Hampel marks on \|E\|, window 51, k 5 | `team_meeting/teammeeting_0910/bake_outdoor.hampel_mask` |
| env_field_drop_halfmedian | \|scene − open sky at the same antenna setting\| below half its median | readout (P1) |
| env_path_missing | positions lacking an environment path, from path lists only | `benchmark/dropout_paths_0916.py` |
| copy_k_ratio | round(\|E\| / \|E_dedup\|) | queue 0952 header |
| copy_k_diff | 1 + \|E − E_dedup\| / \|one copy\| | `team_meeting/teammeeting_0917/bake_repeat_count.py` |
| n_dup | stored per position in each shard | `benchmark/elevation_sweep_md.py` |
| contrast_raw / contrast_dedup / contrast_interp | `bake_ground2.contrast_db` on E / E_dedup / drop_outliers(E) | `team_meeting/teammeeting_0910/bake_ground2.py` |

## 5. Decks — `/workspace/team_meeting/teammeeting_0917/`

* `teammeeting_0917_v17.pptx` carries **the user's page numbers** (slide-number fields on 17 of 18 slides, read from
  the slide XML). They were added to the .pptx by hand; `make_0917_v17.py` cannot reproduce them.
* `teammeeting_0917_v18_kor.pptx` (`make_0917_v18_kor.py`) is v17 with the presenter script in Korean, but it **lacks
  the user's page numbers** (no slide-number field on any slide; slide 1 keeps only the cover template's empty number
  placeholder).
* The main session is building **v19** (page numbers in the generator). Never overwrite v17 or v18_kor. After the last
  pptx write, re-render the preview and check page numbers in the XML, not in the preview.
* Review items for a new deck version (through the make script; v17 slide numbering): the build tag «sionna-rt 2.0.1»
  in the leads of the 2.0.1 result slides (2, 4, 5, 6, 10, 12, 13); slide 4's lead should name the 60 ms excerpt;
  the repeat-count figure draws 0° at 0.012° on a log axis without a break mark and its step line implies unmeasured
  angles (`bake_repeat_count.py`). No line under any figure.

## 6. Open items from the 09-17 review

Package names are the owners assigned by the main session on 09-17.

1. **Readout (P1)** — `READOUT_0917.md` landed 17:55 KST with the metric table, normaliser columns, the 0952 wording
   and the nesting 82 ⊆ 90 ⊆ 98 by metric name. Still open: rerun it when the running ground tr38901/device 2e6 shard 01
   lands, which fills the first same-cap 2×2 (ground only 2e6); the other scene/cap rows keep their interaction marked
   cross-cap or not computed until ground iso/target 16e6 and street canyon tr38901/device 2e6 finish.
2. **Queue headers (main session)** — 0950: a dated line under «Kill / fold» (:19-20) saying the result is read as a
   2×2, leaving the original rule visible. 0953 and 0955: a one-line stop/extend rule, written before any 0953 cell
   completes (mini5pro shard 01 is running) or any 0955 shard lands. 0952 («36») and 0955 («same copies at the same
   positions»): name which 2.0.1 run is meant (§3.2).
3. **0955 B readout** — `bake_repeat_count.py` cannot see `_rt210` stems; load them explicitly (or count from n_dup + 1)
   and confirm the build string of every file read.
4. **0954 readout** — compute scene minus open sky only when both shards of the matching 0956 cell exist; call it
   «field difference due to the environment»; scope to «vertical tilt toward the ground at 60° below, +5/+10/+20°»;
   the single tr38901 model does not stand in for the real antennas.
5. **Ground core (P4)** — re-anchor `benchmark/dropout_paths_0916.py` and `benchmark/dropout_hash_vs_buffer_0916.py`,
   then diagnose the 9 undiagnosed positions of §3.3 (the 6 falls first) in a card 3/4 window; report a status per
   position and keep «why omitted» open.
6. **OFDM pilot (P5)** — CPU receiver on synthetic channels: per-frame reference, explicit null-subcarrier rule,
   Doppler-carrying multipath, separate calibration and evaluation seeds, truth used only for scoring.
7. **Reports (P3)** — missing script paths in `REPRODUCE.md`, one canonical rebuild order, the observability summary
   (instantaneous position rank vs accumulated constant-velocity state), and the README headline following the
   detection-and-tracking framing with a link to plan §9.
8. **Spread** — one or two repeats on the one-run side of comparisons that could flip (open sky 0° at 32e6; ground only
   iso/target); a different-seed tr38901 repeat only if an aim effect near 1 dB is to be reported; a max_depth 1 seed
   run only if a depth-1 change near 5 positions matters.
9. **Plan** — port §9-§9.3 of `MOBICOM_PIPELINE_PLAN_0916.md` into `benchmark/build_isac_plan_0915.py` and rebuild the
   Korean plan notebook.
10. **Untracked review files** — `benchmark/review_results_queue_0917.py`, `benchmark/review_repository_0917.py` and
    their notebooks are not committed; decide commit or merge.

## 7. Waiting on the user — do not act without a word

* Deck: whether the v17 Future Work sub-line («Updated this week: … on 2.1.0») should read as planned — the 0955 A/B
  cells do not exist yet. Any change to the user's scripted notes (e.g. the «listed once» reading outside the angle
  window, the «abrupt values» = Hampel marks wording) needs a yes first, and then goes into a new version. Do not bring
  the angle slide back into the main deck without asking.
* Carried over from `RESUME_0916.md` §5, **not re-checked today**: git history cleanup (`refs/original`, reflog), the
  deferred cleanup units, the HW1 deck cover.
