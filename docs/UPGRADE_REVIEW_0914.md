# Sionna 업그레이드 후 점검

시작 HEAD `1cc0a37dc037869b737601b45e760654c5822d4f` · 종료 HEAD `1cc0a37dc037869b737601b45e760654c5822d4f` · 2026-09-14T11:22:25Z.

설치와 버전별 이름 분리는 확인됐다. 현재 다리 큐는 기본 모드끼리의 버전 비교다. 새 결정 모드는 별도 대조가 필요하며, 일부 사후 검사와 구버전 발간 동기화에 보완점이 남았다.

## 확인된 현재 상태

- 설치 묶음: {'sionna': '2.1.0', 'sionna-rt': '2.1.0', 'mitsuba': '3.9.1', 'drjit': '1.5.0'}.
- 버전 관문 exit 0. 신규 도장·이름 일치, 등록되지 않은 묶음 차단, 실행 모듈 버전 대조가 실행됐다.
- 저장 샤드 7741개 중 새 버전 도장 1개. 원장 2434행 중 새 버전 이름은 0행.
- 고도 표집률 반례: [('base_actual_prf_differs', 0), ('ledger_prf_differs', 0)]. 정상 합성 쌍은 수용했다.
- 복소 상관은 실수부·크기를 함께 반환한다. 주기 신호 반례의 결과는 [-0.0, 1.0]다. 기존의 신호 부재 결론을 내리는 문구는 내려갔다.
- 동시 발간 중 정리 함수 호출: 살아 있는 임시 파일 삭제 0개, 보존 1개. JSON과 글 모두 새 값으로 발간됐다.
- 실제 판독기 main 4개가 임시 출력에서 완료됐다. 장면·대역·고도는 생성 시각을 제외하고 발간 내용과 일치한다. 파형의 차이는 아래에 별도로 적었다.
- 추가 관문 종료값: {'check_arm_names': 0, 'check_reader_gate': 0, 'check_new_file_rules': 0, 'check_report_links': 0, 'check_row_pointers': 0}. freeze 종료값 1; 창고 수 증가가 보고됐다.

## 새 결정 모드의 실제 CPU 대조

조건: llvm_ad_mono_polarized · CPU 1스레드 · 평판 576장/삼각형 1152개 · 산란 계수 0.7 · 반송파 3.5 GHz. 솔버 입력은 원장 checks.cpu_modes.settings에 보존했다.

| 결정 모드 | 반복별 경로 수 | 첫 반복 합산 전계 크기 | 반복별 정렬 경로 배열 해시 일치 |
|---|---|---:|---|
| False | [8059, 8059, 8059] | 0.000172420207068 | True |
| True | [8058, 8058, 8058] | 0.000157959342668 | True |

결정 모드 켜기 전후 합산 전계 크기 차이는 -0.760857 dB다. 복소 전계 차이를 기본 모드 크기로 나눈 값은 0.084582다. 같은 버전 안에서 옵션만 바꾼 비교이며 드론 다리 큐의 예상 변화량이 아니다.
기존에 저장된 구버전 CPU 결과와 경로 수는 비교할 수 있지만, 그 원장에는 같은 복소 전계 배열이 없어 버전 간 전계의 비트 일치까지 확인한 것은 아니다. 이번 반복은 한 스레드 조건이며 다중 스레드·GPU 재현성을 대신하지 않는다.
공식 근거: [PathSolver API](https://nvlabs.github.io/sionna/rt/api/paths_solvers.html), [릴리스 노트](https://github.com/NVlabs/sionna-rt/releases/tag/v2.1.0).

## 보완할 점

### 1. 신버전 기본 모드와 결정 모드를 분리해서 읽어야 한다

**분류: 현재 생산 설정과 새 실험 결과**

생산 코드는 PathSolver()를 생성한다. 설치본의 생성자 서명은 (deterministic: bool = False)이므로 현재 다리 큐는 결정 모드를 켠 시험이 아니다. 기존 --det는 반환된 경로의 합산 순서를 정렬하는 하네스 옵션이다. 실제 CPU 1스레드·합성 평판 576장·광선 40000000에서 모드별 반복은 각각 3회 일치했지만, 경로 수는 8059→8058, 합산 전계 크기는 -0.760857 dB 달랐다. 서로 다른 경로 집합의 물리적 정답을 이 결과로 고르지는 않는다.

**수정 방향:** 버전 비교의 현재 기본 모드는 보존하고 결정 모드를 별도 조건으로 추가한다. 생성자 옵션·샤드 도장·팔 이름·재개 판정을 함께 연결한다. --det의 설명은 합산 순서 정렬로 좁혀 새 생성자 인자와 구별한다. 지금 돌고 있는 큐 중간에 모드를 바꾸지 않는다. 기본 모드는 고정 씨앗에서도 재현성을 보장하지 않으므로, 버전 사이 관측 차이와 실행별 차이를 구별할 동일 버전 반복을 일부 조건에 추가한다.

원장 키: `checks.cpu_modes`

- [benchmark/elevation_sweep_md.py:793](../benchmark/elevation_sweep_md.py) — `_solver = RP.rt.PathSolver()`
- [benchmark/elevation_sweep_md.py:2162](../benchmark/elevation_sweep_md.py) — `ap.add_argument("--det", action="store_true",`

### 2. 창고 도장 검사는 전체 패키지 묶음을 대조하지 않는다

**분류: 합성 입력으로 확인한 검사 범위의 공백**

쓰기 전 build_tag는 장부의 패키지 묶음을 검사하지만 창고 검사 check_shards는 sionna-rt 부분만 읽는다. 정상 도장과 drjit만 다른 도장, 의존성 이름을 생략한 도장 모두 검사 오류 수가 [0, 0, 0]였다. 운영 창고에서 그러한 잘못된 도장을 발견했다는 뜻은 아니다. 정상 작동하는 쓰기 전 관문과 저장된 자료의 사후 검사를 구분해야 한다.

**수정 방향:** 저장 도장을 구조화해 필수 패키지 필드·전체 장부 값과 비교한다. 기록 없는 구세대는 별도 상태로 보존한다. 현재 등록된 묶음과 다른 스탬프를 넣는 사후 검사 반례를 추가한다.

원장 키: `checks.stamp_gate_cases`

- [benchmark/check_solver_build.py:235](../benchmark/check_solver_build.py) — `want = None if rt == "2.0.1" else rt.replace(".", "")`
- [benchmark/elevation_sweep_md.py:1128](../benchmark/elevation_sweep_md.py) — `if want != SOLVER_BUILD:`

### 3. 고도 버전 검사는 원장을 보지만 원자료 도장을 확인하지 않는다

**분류: 이전 지적이 일부 남은 입력 경로**

원장의 버전 값이 같은 상태에서 같은 칸의 샤드 도장을 서로 다르게 만들면 main이 1쌍을 발간했다. 비교 쌍의 실제 도장이 다른 경우도 1쌍이 통과했다. 표집률 차이는 이제 거절한다. 따라서 표집률 수정과 실제 도장 검사의 완료 여부는 서로 다르다. 이번 운영 창고의 신버전 자료가 잘못 섞였다고 판정한 것은 아니다.

**수정 방향:** 선택한 샤드의 도장 집합을 반환해 원장과 대조하고, 비교 쌍과 빈 하늘 대조까지 같은 계약을 적용한다. unknown과 known이 함께 있는 상태는 실제 서로 다른 버전이 입증된 상태와 구별한다.

원장 키: `checks.altitude_main_cases`

- [benchmark/read_altitude_0914.py:259](../benchmark/read_altitude_0914.py) — `_ba, _bb = r.get("solver_build"), rb.get("solver_build")`
- [benchmark/read_altitude_0914.py:112](../benchmark/read_altitude_0914.py) — `n0, prf, why = check_shards(fs)`

### 4. 거절할 대조군이 대역 비교의 기준 표집률을 선점한다

**분류: 새 입력 검사에서 재현한 순서 의존성**

불완전한 대조 행을 먼저 두면 그 행의 10000 Hz가 ref_prf에 기록된다. 나중에 해당 행을 버리면서도 기준은 남아 정상 행까지 거절한다. 같은 행 집합의 순서를 바꾸자 비교 주파수 수가 0→2개, 대역 모양 변화가 계측 불가→0.0 dB로 바뀌었다. 현재의 실제 C 자료에서 같은 잘못이 발생했다는 지적은 아니다.

**수정 방향:** 기준 표집률은 선언된 기준 팔의 검증된 행에서 정한다. 후보 행의 모든 검사가 통과하기 전에는 공유 기준을 갱신하지 않는다. 거절된 행의 앞뒤 순서를 바꿔도 결과와 제외 사유가 보존되는 시험을 둔다.

원장 키: `checks.budget_rejected_row_order`

- [benchmark/read_bandflat_0913.py:239](../benchmark/read_bandflat_0913.py) — `elif ref_prf[0] is None:`
- [benchmark/read_bandflat_0913.py:240](../benchmark/read_bandflat_0913.py) — `ref_prf[0] = float(_pr)`

### 5. 유효한 JSON 안의 잘못된 값은 GPU 보류를 해제한다

**분류: 운영 규약의 잠재 결함 · 임시 파일에서 재현**

직전 보류 [4]를 설정한 뒤 gpus 값을 잘못된 문자열로 바꾸면 보류가 []로 바뀐다. 조건부 보류의 문턱을 문자열 nan으로 바꿔도 해제된다. JSON 문법 자체가 깨진 경우는 직전 보류를 유지하므로, 파싱 실패와 값 검증 실패가 다르게 처리된다. 실제 운영 설정은 현재 정상이며 GPU 보류가 유지되고 있다.

**수정 방향:** 명시적 빈 목록과 값 오류를 구분한다. GPU 번호·문턱의 유한성·범위를 먼저 검증하고, 유효하지 않은 설정은 마지막 정상 보류를 유지한다. 시험은 임시 설정 경로에서 수행한다.

원장 키: `checks.gpu_hold_cases`

- [runners/worker_supervisor.py:222](../runners/worker_supervisor.py) — `out = set(_as_gpu_list(d.get("gpus"), "gpus", warn))`
- [runners/worker_supervisor.py:242](../runners/worker_supervisor.py) — `_hold_last["v"] = set(out)`
- [runners/worker_supervisor.py:218](../runners/worker_supervisor.py) — `thr = float(str(d.get("external_mb", 5_000)).strip())`

### 6. 마지막 구버전 병합을 파형 발간물이 아직 따라가지 못했다

**분류: 현재 발간 동기화 문제**

현재 파형 원장은 입력 원장 2426행 기준으로 521행을 싣는다. 현재 입력 2434행을 실제 main으로 읽으면 527행이다. 공통 행의 값 변화 0개, 새로 들어갈 행 6개이며 모두 구버전 실외 표집률 비교다. 신버전이 들어와서 생긴 차이가 아니다. freeze는 이 차이를 보고하지 않고 신버전 연기 시험 샤드 수 증가만 보고했다.

**수정 방향:** 현재 구버전 원장을 기준으로 파형 판독과 이를 인용하는 산출물을 다시 생성한다. 생성기 해시·입력 원장 해시·행 정체성으로 최신 여부를 확인하고, 동결 통계 통과와 발간 최신성을 별도 조건으로 둔다.

원장 키: `checks.wf_publication`

- [benchmark/read_wfsurvive_0912.py:364](../benchmark/read_wfsurvive_0912.py) — `def main() -> int:`
- [docs/SIONNA_UPGRADE_0914.md:186](../docs/SIONNA_UPGRADE_0914.md) — `움직였다면 뜻은 하나다 — **2.1.0 팔이 2.0.1 판독에 섞여 들어갔다.**`

## 기존에 알려진 메쉬 갤러리 문제 재확인

현재 메쉬로 다시 계산한 외접반경을 저장된 갤러리와 대조했다. 기체 7종 중 허용 상대차 0.5%를 넘는 것은 1종이다.
- phantom4: 저장 반경 0.31095306 m · 현재 반경 0.30446183 m · 상대차 2.1320%.

이 문제는 메쉬 입력·기하 원장·보고서의 일치 문제로 별도 정정해야 한다. 이번 검사만으로 솔버 업그레이드가 원인이라고 판정하지 않는다. 기존 진단과 변경 계획은 [메쉬 갤러리 메모](MESH_GALLERY_STALE_0914.md)에 있다.

## 파형 발간에 추가될 구버전 행

기존 521행 → 재실행 527행. 공통 행 변경 0개. 아래 행은 현재 발간물에 들어갈 자료이며, 이번 감사는 기존 발간물을 덮지 않고 비교 결과를 보존했다.

| 팔 | 앙각 | 표집률 Hz |
|---|---:|---:|
| sionna_p4000000000_swR0D0E0F1_r15_n8192_prf157600_envoutdoor01_mfixbatteryi5_blperairframe_d2 | -60 | 157600 |
| sionna_p4000000000_swR0D0E0F1_r15_n8192_prf157600_envoutdoor01_mfixbatteryi5_blperairframe_d2 | -30 | 157600 |
| sionna_p4000000000_swR0D0E0F1_r15_n8192_prf39400_envoutdoor01_mfixbatteryi5_blperairframe_d2 | -60 | 39400 |
| sionna_p4000000000_swR0D0E0F1_r15_n8192_prf39400_envoutdoor01_mfixbatteryi5_blperairframe_d2 | -30 | 39400 |
| sionna_p4000000000_swR0D0E0F1_r15_n8192_prf78800_envoutdoor01_mfixbatteryi5_blperairframe_d2 | -60 | 78800 |
| sionna_p4000000000_swR0D0E0F1_r15_n8192_prf78800_envoutdoor01_mfixbatteryi5_blperairframe_d2 | -30 | 78800 |

## 다리 큐의 상태와 가치

관측 2026-09-14T11:18:01Z (UTC). 발주 24줄 = 12칸, 팔 7개. 시작 8줄, 종료 0줄, 실패 종료 0줄.
[09-14 20:17:42] 상태 G0:3/3(상한3·남0G) G1:3/3(상한3·남0G) G2:1/1(상한2·남21G) G3:1/1(상한2·남50G) G4:0/0(상한0·남37G·⛔보류) · 큐 8/24 · 워커 8 · RAM 7.2G · CPU 0.58

구버전 짝 존재 24/24, n_dup 계측 존재 24/24. 읽은 구버전 짝의 배열·자세 분할·설정 오류 0개. 구버전 짝 소요 합은 27.16810 일꾼시간이며 새 버전의 남은 시간 예측으로 확정하지 않는다.
이 큐는 가치가 있다. 기존 자료를 보존하면서 스택 변경에 따른 차이를 같은 설정의 짝으로 잴 수 있다. 회절 비교를 제외한 것은 사용자가 새 버전에서만 회절을 쓰기로 한 결정과 맞는다. 현재 설계는 선택된 환경·스위치·깊이·앙각 표본이므로 모든 팔이나 회절의 새 모형 정확도로 일반화하지 않는다.
완료 후에는 같은 칸의 복소 전계·DC/AC 전력·반환 경로 수·중복 경로 진단·상한 근접 여부를 함께 비교한다. 평균 크기가 같아도 자세별 위상이나 일부 경로 변화가 남을 수 있다. 결정 모드를 넣을 때는 기존 기본 모드 자료를 별도 이름으로 보존한다. 현재 다리의 단일 실행끼리의 차이에는 기본 모드의 실행별 차이도 포함될 수 있다. 같은 버전의 반복 결과를 함께 보존해 차이의 재현성을 확인한다. 그 반복을 광선 격자 민감도의 신뢰구간으로 부르지는 않는다.

## 문면에서 함께 낮출 표현

- 업그레이드 문서의 “동결 값이 움직이면 뜻은 하나, 버전 혼합”은 판정 규칙으로 과하다. 새 구버전 자료 편입·선택 규칙·판독기 수정·미완결 자료 완성도 변화 원인이므로 행과 입력 출처를 추적해야 한다.
- 도장 없음과 알려진 도장이 섞인 것은 출처 기록의 혼합이다. 실제 서로 다른 솔버 판을 사용했다고 입증된 상태와 구별한다.
- 고도 판독기의 “로터 반응이면 두 지연 모두 상관이 서야 한다”는 설명은 신호 모델의 추가 가정을 요구한다. 서로 다른 로터 속도·위상·성분이 섞이는 상황에서는 상관 통계를 관측량으로 기술하는 것이 안전하다.

## 범위와 재현

운영 입력·발간물 내용 변화 감시: []. 소스 해시 변화: []. 인용 13건을 줄과 문자로 확인했다.
운영 큐·설치 환경·생산 코드는 변경하지 않았다. 새 감사 생성기·원장·Markdown·Jupyter를 남겼다. 이번 전체 창고 검사는 버전 도장과 이름에 초점을 뒀으며 모든 과거 배열의 CRC를 다시 전수 검사한 것은 아니다. 새 버전 연기 시험과 구버전 다리 짝은 배열도 확인했다. 실기 정확도의 확인은 이번 범위 밖이다.

```bash
/workspace/.venvs/py312/bin/python benchmark/review_upgrade_0914.py
```

검사 캐시에서 문서만 다시 생성할 때는 --publish를 붙인다. 외부 문서 관측 날짜는 external_sources에 기록했다.
