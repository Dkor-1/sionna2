# 최적화·적대적 검증 현황판 — 2026-08-20

> 이 파일은 **진행하면서 갱신**한다. 마지막 갱신 시각을 맨 위에 적는다.

**마지막 갱신: 16:22 KST**

---

## 0. 한눈에

```
적대적 검증  ████████████████████  100 %   4 라운드 전부 완료
최적화 반영  ████████████████░░░░   80 %   남은 것 아래 §3
검증(커버리지) ██████████████░░░░░░   70 %   13/16 · 재시작으로 중단
커밋          ░░░░░░░░░░░░░░░░░░░░    0 %   오늘 작업 전부 미커밋
```

---

## 1. ✅ 적대적 검증 — 4 라운드 완료

| 라운드 | 대상 | 결과 |
|---|---|---|
| ① | 개선 커널(test.tar) | 치명 결함 **8 개** — K≥2 가 `--nshards` 를 탄다 · 판정선 0.06 dB 근거 없음 · 가드 2 개 누락 · 검증도구가 라디안↔도 혼동 |
| ② | 자세 루프 한 줄 감사 | ⭐**병목은 코드가 아니라 워커 수**(같은 코드로 19 배 차이) · 디스크 우회로 3,000 배 |
| ③ | 러너·감독자 | 치명 결함 **6 개** — 스레드 캡이 **안 걸림** · CPU 브레이크 16 배 헐거움 · RAM 이 호스트를 잼 · SIGTERM 처리 없음 |
| ④ | 반영 검증 | ⛔**내 A14 가 샤드 32 칸을 버렸다** · ⛔GPU 없으면 **조용히 CPU 로** 떨어진다 |

### ⭐ 관통하는 교훈 — 같은 실수를 **다섯 번** 했다

「**선언은 했는데 안 걸리는 안전장치**」 — 스레드 캡 · CPU 브레이크 두 번 · GPU 커널 가드 둘 ·
A14 의 상수 오타.
⇒ **고쳤다고 말하기 전에 «정말 걸리는지» 일부러 위반해서 시험한다.**

---

## 2. ✅ 반영 완료

### 성능

| # | 무엇 | 효과 | 검증 |
|---|---|---|---|
| **A1** | OBJ·디스크 우회로 제거(`--inmem`) | **2.2~2.4 배** | 정점·면 **비트 동일** 81 건 · 하류 잣대 차이 **0** |
| **A2** | 씬 메쉬 재사용(정점만 교체) | 위에 포함 | 같음 |
| A3·A4·A6 | 솔버 루프 밖 · 스크래치 제거 · doppler 안 받기 | −0.9 ms | |
| **B1** | 우리 커널 GPU 이식(`--engine ours_gpu`) | **3.66 배** | σ 최대차 0.0009 dB · 상대패턴차 격자밴드의 1/1,300 |
| **A10** | 병합 glob 3,146 → **1 회** | **1,400 배** | 원장 키 대조 — 차이 0 |
| **A11** | 같은 STFT 두 번 → 한 번 | −16~27 s/병합 | 같음 |
| **A13** | 원장 `savez_compressed` → `savez` | −2.9 s | 비트동일 |
| **R0~R2** | 워커 스레드 캡(68→17) · CPU 브레이크 cgroup · 상한 8 | 줄서기 **50 배** 제거 | 실측 |

### 정확성·안전

| # | 무엇 | 크기 |
|---|---|---|
| **D3** | 결측 자세(0)를 `level_db` 평균에서 제외 | 원장 **30 칸** 교정, 최악 **+24.08 dB** |
| **A18** | 소수 앙각 **거부**(−52.5 → `_el-52_` 뭉갬 차단) | 잠복 버그 차단 · **일부러 위반해 확인** |
| **⛔A14 철회** | 조기 건너뛰기가 별표로 `_sw`·`_d1/_d2` 를 삼켜 오판 | **샤드 32 칸(≈20 워커-시간) 손실** 원인 제거 |
| **⭐변종 가드** | GPU 안 열리면 **조용히 CPU 로** 떨어지는 것 차단 | 원장에 두 엔진 섞임 방지 · 발화 확인 |
| **C-a·C-b** | GPU 커널 죽은 코드 + **거짓 주석** 제거 | 주석이 «7.97 dB 어긋남» 을 «정확히 같다» 고 적고 있었다 |
| 러너 6 종 | SIGTERM 배수 · fork 실패 복구 · `HARD_TOTAL` · `Popen` 보관 · 워커 stdout 로그 | 실전 시험 **고아 0** |
| 도구 | `proc_audit.py`(고아·좀비) · `footprint_monitor.py`(30 초마다) · `thread_guard.py` | |

---

## 3. ⏳ 남은 것

| # | 무엇 | 상태 | 이유 |
|---|---|---|---|
| 1 | **B3·B4** — 옛 커널 마스크 9 장 → `which` 한 번 · `exp` 를 `lit` 에만 | 미반영 | 비트동일 증명은 됨(1,056 자세) |
| 2 | 커버리지 나머지 3 칸 (`R1D1E1F1` el −30·−60·−90) | 중단 | 컨테이너 재시작 |
| 3 | GPU 커널 커버리지 (앙각 4 점) | 미착수 | |
| 4 | 감독자 회귀 시험 22 개 | 미착수 | 같은 버그 4 번 재발 방지용 |
| 5 | **커밋** | 미착수 | 오늘 작업 전부 |

### ⏸ 일부러 보류

| 무엇 | 왜 |
|---|---|
| **B1'** 옛 커널 씬 재사용 | 안전이 전적으로 가드에 달렸는데 지금 가드가 «엉성한 지문» 이라 그룹 재라벨에 **1.361 dB 조용히 틀린다** |
| **C3~C5** 겉·속 메쉬 분리 후 씬 재사용 | 되살리려면 ①메쉬 분리 ②`id()` 서로소 어서션 ③앙각당 자기시험 순서가 필요 |
| **A1'** `build_scene`·`place` 하역 | 큐가 이 파일을 쓰는 중 — 새 워커가 패치판을 물면 원장이 갈린다 |

### ⛔ 버린 것

- 개선 커널 **K≥2** — 값이 `--nshards` 를 탄다(0.118~0.328 dB)
- `pose()` 의 `v.copy()` 제거 — probes 64 개가 마지막 자세로 붙는다
- 반올림을 float32 로 — 20,952 중 99 원소 갈림
- 프레넬 `which` gather — **오히려 0.06~0.58 ms 느리다**
- 「118 배」 표현 — 그건 K=64 값. 지금 판은 **3.66 배**

---

## 4. 큐

| | |
|---|---|
| 남은 샤드 | **131** (자세 536,576) |
| 그중 **matrice4e**(덱 대상) | **20** — 워커 9 개로 2~3 시간 |
| 회절 팔(`R0D1E1F1`·`R1D1E1F1`) | 62 (절반) |

---

## 5. 서버 예절 (담당자 지시 반영)

⛔**kill 금지** — «종료하는 것도 클로드로 명령하지 마십시오. 좀비가 생깁니다»
⇒ 줄여야 하면 **배수(drain)**: 큐 줄을 무력화하고 돌던 것은 끝나게 둔다.
정말 급하면 성은님께 컨테이너 재시작 요청.

| 지표 | 지금 | 아침 |
|---|---|---|
| 워커 | 9 | 87 |
| 워커당 스레드 | **20** | 322 |
| 총 스레드(우리) | ~180 | ~28,000 |
| CPU | 0.78/12 (cgroup 이 천장) | 12/12 + 줄서기 |


---

## 6. 🔌 세션 끊김 대비 (16:22 KST)

### 지금 도는 것 — 전부 `setsid` 라 세션과 무관하게 산다

| | 개수 | 확인 |
|---|---|---|
| 계산 워커 | **10** | 스레드 20/개 · GPU 정상 |
| 감독자 | 1 | `runners/logs/sup_0820_restart.log` |
| 발자국 감시기 | 1 | `runners/logs/footprint.log` |
| **좀비** | **0** | |
| 스레드 폭주 | **0** | |

**새 샤드가 실제로 나오고 있다** — 진행률 예: `el+0 sh1: 1536/4096 13.6 분 ETA 22.7 분`.
⇒ 자세당 약 **530 ms**(경합 상태). 샤드 하나 ≈ **36 분**.

### 돌아오면 이 순서로

```bash
cd /workspace/sionna
python runners/proc_audit.py                          # ① 고아·좀비·스레드
tail -2 runners/logs/sup_0820_restart.log             # ② 감독자 상태
tail -5 runners/logs/sup_0820_restart.log.workerout   # ③ ⭐워커 진행률
tail -2 runners/logs/footprint.log                    # ④ 서버 발자국
ls -lt outputs/elev_sweep_shards/*.npz | head -3      # ⑤ 새 샤드
cat docs/OPT_STATUS_0820.md                           # ⑥ ⭐이 현황판
```

### ⛔ 끊긴 사이 안전선

예산 **8** · 절대선 **12** · 스레드/워커 **2** · CPU 상한 0.85(할당 12) · RAM 문턱 6 GiB(천장 32).
감독자가 이 안에서만 새로 띄운다. **kill 은 절대 안 쓴다** — 배수(drain)만.

### 큐가 끝나면 (또는 멈추면) 다시 띄우는 법

```bash
nohup setsid bash runners/start_supervisor.sh \
  runners/jobs_resume_0820b.txt \
  /workspace/sionna/runners/logs/sup_$(date +%m%d_%H%M).log \
  >/dev/null 2>&1 < /dev/null &

nohup setsid bash /tmp/fpmon.sh >/dev/null 2>&1 < /dev/null &   # 감시기
```

⚠`/tmp/fpmon.sh` 가 없으면(컨테이너 재시작 뒤) 이렇게 만든다:
```bash
printf '#!/usr/bin/env bash\ncd /workspace/sionna\nexec setsid nice -n 19 \
/workspace/.venvs/py312/bin/python -u runners/footprint_monitor.py \
< /dev/null >> /workspace/sionna/runners/logs/footprint.out 2>&1\n' > /tmp/fpmon.sh
chmod +x /tmp/fpmon.sh
```

### ⭐다음에 할 일 (§3 에서 뽑은 순서)

1. **B3·B4** — 옛 커널 마스크 9 장 → `which` 한 번 · `exp` 를 `lit` 에만 (`src/rcs_sbr.py:1165-1187`)
   ⛔반영 전 **1,056 자세로 `E.tobytes()` 비트 비교**할 것
2. **커버리지 3 칸** — `R1D1E1F1` el −30·−60·−90
   `python benchmark/coverage_verify_0820.py --quick --gpu 0`
3. **GPU 커널 커버리지** — `--mode kernel`
4. **감독자 회귀 시험 22 개** — 같은 버그 네 번 재발 방지
5. **커밋** — 오늘 작업 전부 미커밋

⚠**커밋 전에 반드시** `git status` 로 `outputs/` 아래 샤드가 섞여 들어가지 않는지 볼 것.
