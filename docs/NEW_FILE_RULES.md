# 새 파일·새 메모를 만들 때

> **왜 이 문서가 있나.** 2026-09-04~06 에 저장소를 한 줄씩 전수조사해 **900 건**을 확정하고
> 전부 고쳤다. 그 900 건을 근본 원인으로 묶으니 **16 가지**가 나왔고, 그 16 가지가 900 건을
> 거의 다 덮는다. 곧 **새로 만들 때 이 16 가지만 안 하면 그 900 건이 다시 안 생긴다.**
> 아래 규약은 전부 **실제로 걸린 것**이고, 각 항목에 그 사고를 그대로 적어 두었다.

관문: `benchmark/check_new_file_rules.py` 가 기계로 잡히는 것을 잡는다(기준선 뒤로 새 것만).
원자료: `work/sweep_0904/rootcauses_0906.json`

---

## ⭐먼저 — 파일을 저장하기 전에 이 여덟 줄

1. 이 파일에 적은 **수**가 전부 원장 키에서 f-string 으로 왔나? 손으로 친 수가 하나라도 있나?
2. 수마다 그 수를 낳은 **칸**(기체·앙각·방위·거리·주파수·표본 수)이 **같은 문장에** 있나?
3. 「없다·유일·최초·가장·전부」를 썼다면, **센 모집단**이 같은 문장에 있나?
4. 「때문이다·A 가 아니라 B 다」를 썼다면, 그 변수를 **껐다 켠 대조 팔**이 원장에 있나?
5. 「실측·검증됨·독립」을 썼다면, 그게 **외부 계측**인가? (이 저장소의 실기 계측 대조는 0 건이다)
6. **제목·머리기사·캡션**이 본문보다 세지 않나? 본문의 가장 약한 문장을 잘라 왔나?
7. 이 파일이 `outputs/` 에 있다면, **레포 안 실재 스크립트**가 한 명령으로 다시 굽나?
8. `{}`·`?`·TBD·nan 이 남아 있지 않나?

---

## 열여섯 가지 — 되풀이가 많은 순

### 1. 발행되는 수를 원장에서 읽지 않고 사람이 자판으로 쳐서 문장·독스트링·캡션·라벨에 박았다 — 원장이 다시 돌아도 그 수는 따라오지 않고, 같은 파일 안에서도 두 값으로 갈린다

**152 건.** 기계 검사 가능

⭐**규약** — 새 파일에 적는 모든 수는 원장 키에서 f-string 으로 찍는다 — 손으로 친 숫자(예시로 드는 수·표본수·라벨의 파라미터 포함)는 문장에 넣지 않는다

**실제로 난 일** — benchmark/mesh_cert_symmetry_derived_0816.py:239 「부피×밀도 질량이 공표 TOW 의 1.1~5.0 배다(감사 I9, 이 라운드가 재확인)」 — 같은 파일 190행과 원장은 5.2(5503/1063=5.18)인데 «재확인» 이라는 재보증까지 붙어 나갔다. 같은 꼴로 src/report_registry.py:39 「본편 11 권 + 별편 8 편, 곧 노트북 23 개다」가 디스크의 33 개·정본 13권/7편과 갈렸다

<details><summary>기계 검사</summary>

빌더 .py 의 f 접두사 없는 문자열 리터럴에서 `[-+]?\d+(\.\d+)?\s*(dB|dBsm|%p|%|mm|m|Hz|GHz|배|편|권|칸|기체|개)` 히트 → 실패(상수 정의 줄만 예외). f-string 뒤에 붙은 리터럴 꼬리 `f"[^"]*\{[^}]+\}[^"]*\d+\.\d+` 도 실패. 원장 산문 필드의 수가 같은 JSON 의 수치 필드 어디에도 없으면(rel_tol 1e-3) 경고, 한 파일에서 같은 이름·같은 단위의 양이 두 값이면 실패. 개수·대소·단조는 len()·max()·sorted() + assert 로 뒷받침

</details>

### 2. 원장 JSON·수기 문서를 레포 안 스크립트 없이 손으로 쓰거나 생성기를 없는 경로(scratchpad·/tmp)로 적었고, 근거로 건 파일:줄·§ 도 실재하지 않았다 — 정정이 되돌아올 자리가 없다

**148 건.** 기계 검사 가능

⭐**규약** — outputs/ 에는 레포 안 실재 스크립트가 한 명령으로 다시 굽는 파일만 두고(_meta.generator 에 그 경로), 문서에 거는 모든 참조(파일·줄·절·빌더)는 적는 자리에서 실재를 확인한다 — 못 하면 그 수는 적지 않고 notes/ 로 보낸다

**실제로 난 일** — outputs/reference_library.json 의 meta.builder 가 가리키는 두 파일이 레포 전체 find 에서 없는데, 그 원장에서만 나온 「표준 관행」·「유일한 게재 논문」·「이 라이브러리에 없다」 세 건이 docs/REFERENCE_LIBRARY.md 로 그대로 흘러갔다. docs/EXPERIMENT_BACKLOG.md:247 은 이미 삭제된 .bak 판의 RESUME.md:386 을 근거로 든다

<details><summary>기계 검사</summary>

outputs/**.json 마다 `_meta.(generator|producer|builder|generated_by)` ① 존재 ② 그 안의 모든 `\S+\.py` 가 repo_root 기준 실재 ③ `^(scratchpad/|/tmp/|/home/|/Users/)` 아님 ④ 그 스크립트가 이 JSON 경로를 여는지(grep) — 하나라도 깨지면 UNBUILDABLE 목록에 넣고 그 파일을 인용한 src·benchmark·docs·reports 줄을 실패로. 문서 쪽은 `([A-Za-z0-9_./-]+\.(md|py|json|ipynb))(:(\d+))?` 실재·행수 이내와 `§[\d.]+` 헤딩 존재를 대조

</details>

### 3. 훑은 모집단·범위를 안 적고 「없다·유일·처음·가장·전부·아무도」로 문장을 닫았다 — 우리가 읽은 몇 편의 미발견이 문헌 전체의 부재로 올라갔다

**139 건.** 기계 검사 가능

⭐**규약** — 「없다·0 편·유일·최초·가장·전부·아무도」는 같은 문장에 센 모집단(무엇을 몇 편·몇 칸·어떤 검색어·언제까지)과 안 본 구멍을 적을 때만 쓴다 — 못 적으면 「우리가 훑은 N 안에서는 못 찾았다」로 낮춘다

**실제로 난 일** — docs/REFERENCE_LIBRARY.md:107 「표적 산란을 스스로 계산하고 그 위에 검출을 세운 드론 논문이 없다」 — 실제로는 라이브러리 94편·정독 64편 안의 공백인데 범위구가 없다. outputs/reflib_sweep_sionna.json:1471 은 「…가 이 분야의 기본값이다」라고 적지만 같은 파일 counts 는 1/137 이다

<details><summary>기계 검사</summary>

`(없다|없음|전무|0\s*편|유일|처음|최초|가장|최고|제일|전부|모두|아무도|어디서도|한 번도|표준 관행|only|never|nobody|the first)` 히트 문장(마침표 단위)에 모집단 토큰 `(\d+\s*(편|건|칸|기체|종|점|papers)|아카이브|코퍼스|우리가 (훑|찾|읽|센)|범위에서|\d+\s*/\s*\d+|as of \d{4})` 이 없으면 실패. 제목·⭐·headline*/tldr/claim 필드에서는 예외 없이 실패. 같은 파일에 counts·n_papers 키가 있으면 그 값 병기를 강제. 면제는 ⛔/⚠ 로 시작하는 정정 줄과 인용부호 안 남의 문장뿐

</details>

### 4. 값·판정을 내려 놓고 그 문구가 사는 다른 자리(빌더 f-string·쌍둥이 md·요약표·절 제목·주석)를 따라가지 않아, 재빌드하면 철회된 문장이 되살아났다

**114 건.** 기계 검사 가능

⭐**규약** — 값이나 문장을 내릴 때는 그 문자열을 레포 전수 grep 해 나온 자리를 같은 커밋에서 전부 고치거나 ⛔+날짜를 박은 뒤에만 «내렸다» 고 적고, 새 문장을 쓰기 전에 RETRACTION_LOG 와 ⛔ 목록을 문자열로 먼저 대조한다

**실제로 난 일** — reports/_parts/82_el-nadir-floor.ipynb c10:1 「## 사각지대는 반각 몇 도짜리 원뿔이다」 — 같은 파일 c0 이 2026-09-04 에 ⛔로 철회했고 빌더 주석(src/build_part12_elevation.py:925~932)에도 철회가 적혔는데 제목 문자열(:1320)만 안 고쳐 재빌드마다 되살아난다. atlas/00_since_deck.html:253 의 「세 두께가 소수점까지 같은 리듬 80.5 % 를 낸다」도 R29(docs/RETRACTION_LOG.md:1318)가 내린 값인데 꼬리표가 없다

<details><summary>기계 검사</summary>

RETRACTION_LOG 항목마다 «내린 문구»를 따옴표 필드로 필수 등록 → 원장의 `⛔|must_fix|인용 금지|refuted_ko` 문구와 합쳐 banned_strings.json → `grep -rF -n` 전수 → 히트 줄 ±3 줄에 `⛔|⚠|철회|정정|R\d+|20\d\d-\d\d-\d\d` 가 없으면 실패. 생성물만 고치고 src/**.py·benchmark/**.py 리터럴에 같은 문자열이 남으면 «재빌드 부활» 로 별도 실패. 산출 JSON 의 self_check 에 passed=false 가 하나라도 있으면 빌더를 비0 종료

</details>

### 5. 같은 판정 문장을 여러 자리(신판·옛 빌더·생성물 노트북·쌍둥이 md·한/영 짝·계획 JSON 사본)에 손으로 복제해 두어, 한 곳을 고쳐도 나머지가 옛 문면으로 남았다

**110 건.** 기계 검사 가능

⭐**규약** — 같은 문장을 두 곳에 두지 않는다 — 문면은 한 곳(원장 키 또는 빌더 상수)을 정본으로 정하고 나머지는 그것을 읽어 찍으며, 부득이한 복제본에는 「출처: 파일#키」를 달고 한정어·⚠ 를 양쪽에 똑같이 넣는다

**실제로 난 일** — outputs/paper_kit.json 의 「취약성을 정하는 것은 기체 크기가 아니라 밴드 간 σ 로브 산포다」가 같은 파일 5785·5794·5803·5812·13529 다섯 자리에 있고, 현행 빌더 src/build_part10_results.py:1239 는 이미 「⛔ 옛 제목」으로 반대 문면을 쓴다 — 인용하는 쪽이 어느 판을 옮길지 갈린다

<details><summary>기계 검사</summary>

src·benchmark·outputs·docs·reports·atlas·prior_work 전수에서 40자 이상(또는 12어절 이상) 한글 문장을 공백·숫자·조사·강조기호 정규화 후 해시해 2곳 이상이면 실패로 목록화(중복 문면 대장). `src/build_part*.py` ↔ `src/make_report*.py`, `prior_work/src/make_pw*.py` ↔ `prior_work/*.ipynb`, HTML 렌더 ↔ readme 렌더 짝은 무조건 검사. `*_ko`/`*_en`, `headline_*`/`verdict` 짝은 숫자 토큰 집합과 한정어 토큰 수가 같아야 통과. 예외는 원장에서 주입되는 f-string 한 곳

</details>

### 6. 안 돌린 판·안 잰 양·시뮬 출력·우리 커널 자체검산을 「실측·측정됨·검증됨·독립·확증」이라 부르거나, 옆 값·추론으로 메우고 잰 것처럼 단정했다

**99 건.** 기계 검사 가능

⭐**규약** — 「실측·측정·계측·measured·검증됨·독립」은 외부 계측기·문헌 측정곡선에만 쓰고, 우리 출력은 「원장 재계산(파일:키)」·「우리 커널 계산」·「자기일치 바닥」으로 부르며, 안 돌린 조건은 「아직 안 쟀다」를 같은 문장에 적는다

**실제로 난 일** — outputs/das_fleet_ours.json:43 「평판 실측이 β≤60° 에서 0.3 dB 안으로 그 예측과 맞는다」 — 인용한 outputs/sbr_defect_fixes.json 은 우리 SBR 로 돌린 PEC 평판 자체검산이고, 이 저장소의 실기 계측 대조는 0 건이다. reports/12_outdoor-scene.ipynb c9:14 「제거는 박자를 되찾는다」가 인용한 comb_repaired 키는 노치(cs_eca)를 아예 거치지 않는다

<details><summary>기계 검사</summary>

`(실측|측정된|측정으로 확인|계측|검증(됐|되었|완료)|확증|\bmeasured\b)` 히트 줄의 근거 경로가 outputs/*.json·benchmark/*.py 이면 실패(실기 계측 화이트리스트는 현재 0 건). 원장 confidence 가 medium|low 이거나 값에 「미확정·추론·못 찾」이 있는 키에 «확인» 도장이면 실패. `_meta.env.gpu == false` 인 파일이 GPU 엔진 결과를 단정하면 실패. 동사↔키 대응표(제거·노치·ECA ↔ comb_repaired/fill 금지)로 위배를 잡고, 「독립·따로 쟀」은 두 빌더의 import 그래프가 겹치면 실패

</details>

### 7. 제목·머리기사·한 줄 요약·그림 캡션·표 칸 같은 짧은 층을 본문과 따로 손으로 써서, 조건과 ⚠·한정어는 떨어지고 본문에 없던 최상급·인과가 새로 붙었다 — 청중이 유일하게 읽는 줄이 본문보다 세다

**83 건.** 기계 검사 가능

⭐**규약** — 제목·머리기사·한 줄 요약·그림 캡션은 새로 쓰지 않고 본문에서 가장 약한 문장을 그대로 잘라 오며, 조건(기체·앙각·거리·밴드·표본 수)과 한정어가 안 들어가는 수·판정은 그 자리에 올리지 않는다

**실제로 난 일** — src/build_part12_elevation.py:934(TITLE_82) 「나딧 잔여의 64 % 는 광선 격자 표본화 잡음이고」 — 원장 outputs/refute_nadir_mechanism_final.json 의 caveat_ko 는 «이 나눗셈은 어림이고 64 % 는 하한» 이라 적었고 값은 10 m 판인데, 제목엔 하한도 거리도 없다. 이 제목이 README·색인 여섯 곳에 본문 없이 홀로 실린다

<details><summary>기계 검사</summary>

얼굴 문자열만 추출: `^#{1,4} `, 문서 첫 40 줄, «한 줄 요약/TL;DR» 블록, 노트북 md 셀 첫 줄, 표 첫 열, 키 `^(headline\w*|title|one_line|so_what|reading|verdict|summary|answer|paper_caption|replace_with|citable\w*|can_write\w*)$`, 빌더의 `TITLE_\w+`·`set_title|suptitle|supxlabel|fig\.text|annotate` 인자. ① 본문 어느 문장의 부분문자열도 아니면 «새로 쓴 요약» 표시 ② 수치가 있는데 조건 토큰 2개 미만이면 실패 ③ 같은 파일에 ⚠·⛔·한정어가 있는데 얼굴에는 없으면 실패 ④ 최상급·전칭·인과·「A 가 아니라 B 다」가 얼굴에 있으면 실패

</details>

### 8. 관찰을 판정으로 굳히는 닫는 낱말(뿐·유일·완전·절대·불가능·정답·검증된·A 가 아니라 B 다)과 도구 우열 판정을 썼다 — 실측 대조 0 건인데 우리 근사가 채점자로 섰다

**83 건.** 기계 검사 가능

⭐**규약** — 닫는 낱말과 도구·방법을 주어로 한 우열·불가능 판정은 쓰지 않는다 — 「이 설정(기체·앙각·거리·팔)에서 본 것은 …까지다」로 적고, 구현 한계는 「현재 구현으로는」으로, 판정은 발표자 노트로 내린다

**실제로 난 일** — docs/PRIOR_WORK_COMPARISON.md:197 「Ziganshin 의 HH/VV 편파 분리. **물리적으로 불가능**하다: `grep -ci polari` = 0」 — 우리 구현이 스칼라라는 사실을 물리의 성질로 승격했다. docs/RETRACTION_LOG.md:1058 「⇒ 탐지 축에서 PathSolver 는 경쟁자가 아니다」도 같은 꼴로, 상시 규약 「시오나가 틀렸다고 하지 않는다」 정면 위반이다

<details><summary>기계 검사</summary>

금칙어 정규식을 커밋 훅에서 차단: `뿐이(다|고)|유일(한|하게)|완전(한|히)|절대 (못|안)|불가능|정답|검증된|틀렸다|이긴다|보다 낫다|앞선다|우월|경쟁자가 아니|[^.]{2,30}(이|가) 아니라 [^.]{2,30}다` + 엔진 전용 `(Sionna|스톡|PathSolver|SBR|PO|우리 커널)[^.]{0,40}(틀리|부정확|과대|과소|못 (낸|한|따라)|불가능|지배력이 없)`. 문자열 '물리적으로 불가능' 은 조건 유무와 무관하게 무조건 실패(→ '현재 구현으로는'). 예외는 인용부호 안 남의 말과, 대조 짝(밴드·참값·반례 칸)과 원장 경로가 같은 문장에 있는 경우

</details>

### 9. 수를 적으면서 그 수를 낳은 칸(기체·앙각·방위·거리·주파수·표본 수·판)을 문장에서 떼어, 한 칸에서 잰 값이 조건 없는 상수·물리처럼 읽히게 했다 (폭이 있는 값은 최댓값이 대표값으로 굳었다)

**82 건.** 기계 검사 가능

⭐**규약** — 수가 든 문장에는 그 수를 낳은 칸(기체·앙각·방위·거리·주파수·표본 수)과 원장 키를 같은 문장에 붙이고, 폭이 있으면 원장의 min/max 를 그대로 폭으로 적는다 — 못 붙이면 그 수는 쓰지 않는다

**실제로 난 일** — outputs/outofband_power.json:922 「⭐격자를 얼리면 대역밖 절대 전력이 λ/12 에서 13.1 dB, λ/32 에서 20.1 dB 내려간다」 — 같은 파일 _meta 는 matrice4e·az 0°·el −15°·3.5 GHz·PRF 19700 한 칸인데, 조건 없이 reports/02_kernel.ipynb 로 그대로 옮겨졌다. docs/STANDARD_FRAME.md:60 「PathSolver 최선의 판(빗살 52 dB)」도 거리축을 흔들면 40.5~52.0 dB 로 움직인다

<details><summary>기계 검사</summary>

`[-+]?\d+(\.\d+)?\s*(dB|dBsm|%p|%|배|m/s|Hz|kHz|GHz|ms|m)\b` 가 든 문장에 조건 토큰이 하나도 없으면 차단. 조건 토큰 = 기체명(src/drones.py DRONES 키에서 자동 생성) | `el\s*[-−+]?\d|앙각` | `az|방위` | `\d+\s*m\b|거리` | `GHz|MHz` | `n\s*=\s*\d|\d+\s*(칸|자세|점|셀|편|종|판)`. 원장 JSON 은 `headline*/verdict*/so_what*/reading/text_ko` 값에만 걸면 오탐이 적다. 원장에 min/max 가 함께 있는 키를 한쪽 끝만 인용하면 실패

</details>

### 10. 그 변수를 껐다 켠 대조군 없이 원인·기전·반사실을 관측처럼 단정했다 — 잰 양 한 줄에서 「무엇 때문이다 / A 가 아니라 B 다」로 곧장 건너뛰었다

**76 건.** 기계 검사 가능

⭐**규약** — 「때문이다·탓이다·우연이 아니다·였다면·A 가 아니라 B 다」는 그 원인을 껐다 켠 대조 팔이 같은 원장에 있을 때만 쓴다 — 없으면 관측과 해석을 두 문장으로 갈라 「…로 읽는다 — 원인은 아직 안 쟀다(후보 기전)」로 적는다

**실제로 난 일** — src/build_report18_switch_grid.py:462 「정면에서는 … 유한한 광선으로 재느라 생긴 계산 흔들림이 상한 위를 백색으로 채워 놨기 때문이다」 — 이 판(15 m·자세 8192·el 0)에는 광선 수를 내려 본 짝이 0 개다. benchmark/das_fleet_ours.py:512 도 같은 파일 357행이 「일 수 있고」로 적은 것을 「그것은 …이다」로 굳혔다

<details><summary>기계 검사</summary>

`때문이다|탓이다|덕분|라서 |우연이 아니|였다면|이었다면|의 성질이다|기전(은|이)|원인(은|이)|이 아니라 .{0,20}(다|이다)|because|caused by|driven by` 히트 → 같은 원장에 대조 키 `(control|null|ablation|swap|_off\b|with_|without_|run_id|대조)` 가 없으면 실패(문구를 「후보 기전」으로 강제). 반사실 어미(`였다면|이었다면`)는 근거가 붙어 있어도 사람 검토 큐 — 대조군이 실제로 지어졌는지는 기계가 못 본다. 집계값 줄(`중앙값|평균`)에 `n=|칸 중|산포` 가 없으면 경고

</details>

### 11. 자유 파라미터(창 반폭 hw·격자 div·광선 예산·문턱·가드 폭·분모 정의)가 정하는 수를 손잡이 없이 물리량처럼 적고, 검출 바닥·널·문턱 아래를 「없다·불변·0·비트 동일」로 닫았다

**63 건.** 기계 검사 가능

⭐**규약** — 수를 쓰기 전에 그 수를 만든 손잡이를 한 칸 흔들어 보고, 결론이 바뀌면 크기를 쓰지 않는다 — 쓸 때는 손잡이 값을 같은 줄에 적고, 바닥·널·문턱 아래는 「이 잣대로는 못 읽는다(바닥·문턱 값 병기)」로만 적는다

**실제로 난 일** — benchmark/r12_azimuth_harvest.py:606 이 리듬 몫 63 % 를 「익사하지 않는다」 판정 근거로 썼는데, 같은 손잡이에서 hw 2 Hz 면 9.9 %, f_above 를 기본값으로 두면 98.9 % 다(docs/RETRACTION_LOG.md:1318 R29). benchmark/report15_null_control_v2_append.py:24 는 ptp 0.0045 dB 를 「변조가 0 이다」로 적었다

<details><summary>기계 검사</summary>

원장 항목에 `knobs`(hw·f_above·div·spp·seed·budget·THRESH·PROM·HALF_WIDTH·가드) 와 `shaken`(흔든 폭) 필수 — 산문이 그 항목을 인용하면서 knobs 를 안 적으면 실패. 등급·자격 문자열(`A_convention_free|headline_eligible`)은 리터럴 금지, knobs_varied 차집합이 빌 때만 부여. 분모 퇴화(`f_tip==0|cos(el)==0|n_independent==1|is_saturated`)면 배수 출력 차단. 널·바닥: `(p99\.?9?|분위|null|바닥|floor|SNR\s*\d|p\s*=|\de-0\d)` 와 `(없다|불변|죽은 파라미터|0 ?개|정확히 0|비트 동일|무시가능|못 넘는다)` 가 같은 문장이면 실패(「못 읽는다·검출 바닥 아래」 병기 시 통과)

</details>

### 12. 한 칸(기체 1·앙각 1·자세 1·논문 1·점 2~3 개)에서 본 것을 부류·도구·분야 전체의 성질이나 법칙·경향으로 올렸다

**57 건.** 기계 검사 가능

⭐**규약** — 문장의 주어를 표본과 같은 크기로 쓴다 — 기체 1 대면 그 기체 이름으로 적고, 점 3 개 이하이거나 차이가 산포 안이면 경향어(일수록·비례·단조·정하는 것은)를 쓰지 않고 값만 나열한다

**실제로 난 일** — benchmark/adv_rf_layer_0816.py:279-282 「el −30 에서는 n≈0.9 라 20log10 은 정확히 2배 과대」 — 같은 원장 K2b 는 기체마다 1.802 / 1.654 / 0.881 / −1.197 인데 matrice4e 한 대 값을 기체 표시 없이 일반화했다. benchmark/sigma_sensitivity.py:652 는 기체 다섯 대·자기 상관계수 +0.091 위에 「취약성을 정하는 것은 …이다」를 세웠다

<details><summary>기계 검사</summary>

원장 항목마다 `drones`·`n_samples` 필드를 필수로 두고, `n_samples==1 or len(drones)==1` 인데 인용 산문에 복수 주어 `(모든|전 기체|기체별로|공통 성질|들은|every|all|분야|문헌 전반)` 이 있으면 무조건 실패. 경향어 `(일수록|비례|반비례|단조|커질수록|줄어들수록|경향|법칙|정하는 것은|지배한다)` 는 근거 점 수 ≥ 4 이고 상관계수 |r| ≥ 0.5 일 때만 통과. 결론 필드에 전칭어가 있으면 그 산출물의 축 길이(cells·rows·기체 수)를 함께 인쇄하도록 강제하고 그 수가 1~2 면 실패

</details>

### 13. 원장·원문이 값에 붙여 둔 한정(caveat·note·n·confidence·범위·예외·⚠·인용 금지)을 옮기지 않고 값만 인용했다 — 단서를 값과 따로 두어 꼬리표가 떨어져 나갔다

**46 건.** 기계 검사 가능

⭐**규약** — 원장 키나 남의 문장을 인용하면 그 값에 달린 한정(caveat·note·n·confidence·범위·예외·⚠)을 같은 문장에 함께 옮긴다 — 「다른 절 참조」로 대신하지 않고, 조건을 떼거나 말을 보태면 인용부호를 빼고 우리 판정으로 표시한다

**실제로 난 일** — docs/S2R_JEPA_POSITION.md:171 「Trinh 2026 이 배경 정렬 하나로 chance 50 % → 97 % 를 만들었다」 — 같은 문서 §9 가 «확인 전 인용 금지» 라 적어 두었지만 값 자리에는 표시가 없고, docs/SIM2REAL_PLAN.md:294 가 그대로 물려받았다. outputs/psolve_adopt.json:830 은 「616 ms」를 옮기며 같은 파일 433행의 «LUMI 8노드(MI250X 32장)» 를 벗겨 냈다

<details><summary>기계 검사</summary>

원장 값은 형제 키가 아니라 값 객체 안(`{"value":…, "caveat":…}`)에 단서를 두도록 규약화하고, `caveat|note|note_ko|warning|but|limits|confidence|precision|held_fixed|_what` 를 가진 키를 인용한 문장에 그 문면의 핵심 낱말(하한|상한|어림|예외|한정|미확정|⚠|⛔)이 없으면 실패. 인용부호(«…»/「…」/"…") 안 문자열은 원문 quote 필드와 공백·조사 정규화 후 완전 일치 대조 — 불일치면 차단. 남의 수가 든 줄에는 하드웨어·표적 토큰 최소 하나 필수

</details>

### 14. 축(단위·기체·앙각·밴드·적합 구간·지표 정의·창·표본 독립성)이 다른 두 수를 한 문장·한 표 행에 나란히 놓고 ✅·「범위 안·일치·N 배」로 판정했다

**45 건.** 기계 검사 가능

⭐**규약** — 뺄셈·범위·배수·「범위 안·일치」는 축이 모두 같은 칸끼리만 하고, 하나라도 다르면 「대조 불가」와 그 이유를 적는다 — 밴드·적합구간·지표 정의·표본 독립성 네 칸을 먼저 채운다

**실제로 난 일** — docs/INJECTION_PRECEDENT.md:153 「밴드 기울기 6-18.2 GHz | ✅ 0.264 dB/GHz — 실측 0.21~0.315 범위 안」 — 우리는 6~18.2 GHz 부분적합, 문헌은 1.8~18.2 GHz 전대역 단일 적합이라 같은 구간에서 재면 2.07 배다. outputs/deck0811_redundancy.json:77 「레벨차 60 dB 는 확정」도 면적분[m²]과 자유공간손실 포함 채널진폭을 뺀 값이다

<details><summary>기계 검사</summary>

원장의 모든 수치 항목에 축 태그(`unit, drone, el_deg, band, fit_range, arm, window, metric, n`) 필수. 두 수를 잇는 산문(`대|vs|보다|→|차이|같은 크기|배|범위 안|일치|✅|INSIDE|steeper`)이 태그가 다른 두 키를 인용하면 실패. 비교 문장에 구간 표기 `[0-9.]+\s*[-–~]\s*[0-9.]+\s*GHz` 와 표본 표기 `\d+\s*(기체|점|편)` 가 둘 다 없으면 실패. 「그중」으로 잇는 앞뒤 수의 단위 명사(칸·행·편·팔)가 다르면 실패, 합산 키(`or|plus|합계|total|_and_`)를 단독 명사로 인용해도 실패. margin/ratio 는 value·budget 을 같은 튜플로 받는 함수로만 만들고 분모 퇴화 시 배수 출력 차단

</details>

### 15. 상시 규약(환경은 실외만·챔버 금지·우리끼리 쓰는 말 금지)을 새로 쓴 발표 문단·용어풀이에 그대로 어겼다

**10 건.** 기계 검사 가능

⭐**규약** — 새 파일·발표 문단·용어풀이에 챔버·실내 통제 기하를 우리 설정으로 쓰지 않고, 우리끼리 쓰는 말·줄임말을 만들지 않는다 — 도구는 환경이 아니라 하는 일로 그 자리에서 한 줄에 푼다

**실제로 난 일** — prior_work/src/make_pw03.py:105 「우리는 NVIDIA Sionna RT 로 챔버 전파를 담보하고(선행 Deterministic-Modeling·SimART 와 같은 용례)」 — 바로 다음 줄 헤더가 스스로 «§4. 정직한 한 문단 (외부에 말할 때)» 라 밝힌 발표용 자리이고, 귀속한 선행 두 편도 원장상 실외 사례라 «같은 용례» 가 성립하지 않는다

<details><summary>기계 검사</summary>

`grep -rn '챔버|무반향|anechoic' <신규·변경 파일>` → 우리 설정을 서술하는 문장(주어 `우리|본 연구|our_row`)이면 실패. 발표 자리(제목·한 줄 요약·용어풀이 표·«외부에 말할 때» 절)에서는 문헌 출처가 있어도 실패, 문헌 설명 인용은 같은 줄에 `arXiv|doi|[A-Z][a-z]+ 20\d\d` 가 있을 때만 통과. 조어 금지 사전과 도구 이름(Sionna·PathSolver·SBR·PO)을 발표 자리에서 grep 해, 같은 줄에 한 줄 풀이가 없으면 실패

</details>

### 16. 수가 채워지거나 검산되기 전에 결론 문장을 먼저 확정해, 자리표시자·실패한 계수가 결론과 함께 발간됐다

**6 건.** 기계 검사 가능

⭐**규약** — 결론 문장은 그 안의 수가 채워지고 검산된 뒤에 쓴다 — `{}`·`?`·TBD·nan 이 남거나 계수·수집이 실패하면 그 문장을 아예 내보내지 않는다(수 없는 결론 금지)

**실제로 난 일** — docs/DECK_FACTS.md:407 (benchmark/deck_facts.py:1100) 「'rcs' 라는 단어가 ?회, 'radar_cross_section' 이 ?회 나온다 — 즉 산란적분도 σ 출력도 없다」 — 같은 항목 numbers 는 {"error": "No module named 'sionna'"} 인데 부재 판정 문장은 남았고 등급이 computed-by-us 라 청중은 이번 실행에서 센 것으로 읽는다

<details><summary>기계 검사</summary>

빌드 후 `grep -nE '\{[A-Za-z_0-9.]+\}|%\.?[0-9]*[dfs]|\?\s*(회|개|dB|%|Hz)|TBD|N/?A|None|nan|\{\}' outputs/*.json docs/*.md reports/**/*.ipynb` 히트가 0 이어야 통과. `numbers` 에 `error` 키가 있는 항목의 claim 은 발행 금지. 손으로 한 환산(「N 분의 일·N 배」)은 옆 괄호에 계산식이 있어야 통과하고, 파장·주파수가 한 줄에 같이 있으면 λ=c/f 로 자동 재계산해 오차 10 % 초과 시 실패

</details>

---

## 이 문서를 고칠 때

⛔이 문서는 **손으로 늘리지 않는다.** 새 항목은 실제로 걸린 사고가 있을 때만 넣고,
그 사고를 「실제로 난 일」에 파일:줄과 함께 적는다. 근거 없는 규약은 지켜지지 않는다.

