# 리포트 1~12 적대감사 findings (2026-07-22, wf w4gurtd2o)

12에이전트 적대감사. report07은 스키마 실패로 미감사(이번 세션에 이미 대폭 수정됨). **18개 high/med — 전부 적용 완료(커밋 `70ba2e5`)**.
확인: report12 의 −19.7 하드코딩 제거(JSON 주입), report10 '8,000장'→10,000 4곳, report08 '십배'→'약 5배'.

## 🔴 HIGH (반드시 수정)
- **report01** `make_notebook01.py:116,188,214,239,292` [overclaim] — "벽·천장 흡수·바닥만 유일 반사" 척추 서사가 자기 RT 데이터와 모순: 천장 흡수체 −9.82dB > 바닥 −14.68dB, absorber_front 2bounce −11.63dB가 같은 지연빈서 3dB 강함. **실제 통제는 물리적 단일반사 아니라 ECA 정적클러터 제거(report09).** 서사 정정.
- **report12** `make_notebook12.py:276` [stale] — 방위평균 σ "−19.7 dBsm" 하드코딩=구엔진값. report2_waveform_rcs.json 현행 −18.4(밴드3.5) or 밴드평균 −17.1로 갱신(JSON 동적주입 권장).
- **report12** `make_notebook12.py:412` [contradiction] — M['sigma_dbsm']=−28.0(이 자세=널)을 "밝은 상단"에 대입=포락선 최하단인데 상단이라 모순. "밝은 상단"엔 방위평균(−17.1/−18.4) 쓰고, 자세값(−28)은 "이 자세는 널=하단"으로 분리.

## 🟠 MEDIUM
- **report01** `:315-325` [overclaim] — 세기 0.0005dB 일치=항등식(같은 프레넬)이라 독립검증 아님 → "일관성 확인"으로 격하, 진짜 독립증거는 지연(기하).
- **report01** `:209,309` [physics] — Γ≈0.26은 TM전용, TE는 0.52(수직입사 0.39보다 큼) → 편파조건 명시.
- **report02** `:247-248` [contradiction] — "광선 맨앞서 멈춤" 정당화가 penetrate=True(셸투과)와 모순 → "불투명면만 멈추고 반투명셸 투과, 면기반 적분이라 부피중복 없음"으로 재서술. + F_overlap 0.6~35%→0.66~37.4%(상한).
- **report03** `:74,139` [overclaim] — "네 기체 ±1dB 이내"가 Phantom4 −1.25로 자기위반 → "약 ±1dB"로 헤지. + :356 "보수적" 제거(밝은상단과 상충).
- **report05** `:77-79,158,385` [overclaim] — "규격 일치" 과대(교차검증은 우리OFDM↔SionnaOFDM 정합만, 규격정확성 아님) → "OFDM 변조수학·뉴머롤로지 정합"으로 축소(§3 caveats와 통일).
- **report06** `make_notebook06.py:85-92,404-416` [stale] — 재질별 σ분해가 구엔진(report3_rt.json 07-14)=금속<전체, report08 신엔진=금속>전체로 **부호 뒤집힘**. C_metal 분해를 report2_waveform_rcs.json materials 블록에서 재주입.
- **report08** `:280` [physics] — 7.1dB를 "십배"라 함(=5.1배). "약 5배"로 정정.
- **report08** `:285 vs 529` [contradiction] — §2 "광학영역·파장둔감" vs §6 "few-λ 공진영역·저주파12dB하강" 모순 → §2를 "측정 좁은대역서 스윙 3.2dB 작음"으로 한정.
- **report08** `:529` [overclaim] — 외삽 진값 −25~−28 vs 우리 −18 격차(7~11dB)를 "수 dB 낙관적"으로 축소 → "최대 ~7~10dB"로.
- **report09** `:268` [overclaim] — 기준클러터 −26dB를 "보수적으로 낮춤"이라나 실은 RT실측(−14.7)보다 11~16dB 과소 → "과소평가지만 +40dB 스윕이 실측 덮어 결론 무관"으로.
- **report09** `:217-220` [overclaim] — "지연·세기 눈금까지 겹침" 단정이나 같은 19.3ns에 탭 2개(−11.6,−14.7); 닫힌형은 −14.7과만 일치, −11.6 미규명 → 명시.
- **report10** `:150,186,302,495` [stale] — "파형당 8,000장"이 실제 10,000 → 4곳 교정(C['meta']['n_maps_chain'] 동적).
- **report11** `:416-448` [stale] — §3/§4 σ·SCR·Pd 전량 구엔진(9f26cee 이전) 스냅샷 → 재생성 or caveat.
- **report11** `:405-407,20x` [overclaim] — "pyAPRiL 3/3 모드 검출→검증된 체인"이나 detected_at_truth=false(CFAR 표적셀 미발화) → "CAF 봉우리 거리빈만 정답일치, CFAR 검출선언 아님"으로.
- **report12** `:196` [contradiction] — "SSB 20ms→무모호 1m/s"인데 "표적 느려서 어느 PRF서도 안 접힘" 자기모순(표적 3m/s>1) → "이 실험의 높은 PRF서는"으로 한정, 실 SSB선 접혀 G1 더 불리(헤드라인 강화).

## 적용 분류
- **narrative-only(재실행 불필요)**: r01·r02(서술)·r03·r05·r08·r09·r10·r11(overclaim)·r12 — 대부분 여기.
- **재실행 필요(stale σ)**: r06(재질σ 재주입)·r11(σ/SCR/Pd 재생성)·r12(σ라벨) + sbr_field 미관측(이미 코드반영) → **ONE 배치 재실행**.
- **mesh fix**: mini5 높이(별건).

---

## 2026-07-22 후속 라운드 — 메쉬 fix + 감사 플래그 정산

### 적용한 메쉬 수정 2건 (`drones.py` · `drone_cad.py`)
1. **엔벨로프 의미론 버그 (mini5pro)** — DJI 의 언폴드 91 mm 는 **프롭 포함**값인데 `frame_fit_scale` 이
   **프롭 제외 프레임**을 그 값에 맞춰, 프롭이 위로 더 얹혀 **전체 106 mm(+16.5%)** 가 되어 있었다.
   → `DroneSpec.env_props_included` 신설, 프롭 포함으로 z 배율을 다시 풀어 **전체 91.0 mm**.
   나머지 4종의 공식 높이는 프롭 제외라 변경 없음.
   - 독립검증: 강제하지 않은 L/W 의 **프롭 디스크 외곽 305.0 × 381.2 mm** vs 공식 304 × 380 (+0.3%).
   - `frame_envelope_mm` · `viz_report1` · `viz_verify_sbr` 도 **공식값과 견줄 대상**을 전체 드론으로 교정
     (안 고치면 mini5pro 가 −15.5% 오차로 잘못 뜬다).
2. **프롭 허브–블레이드 간극 (5종 전부)** — 허브 반경 0.085R 인데 블레이드 루트가 0.14R 부터 시작해
   **간극 4.2~10.5 mm**(3.5 GHz 에서 λ/20~λ/8). 프롭 1개가 **3개 분리바디**였고, 공중에 뜬 루트
   모서리가 산란에 가짜로 기여했다. → `root_frac=0.070` 으로 허브에 물려 **1개 솔리드**(전 기종 watertight).

**σ 파급(3.5 GHz, az 0~360° 2° 격자, el 0°)**
| 대상 | before | after | Δ |
|---|---|---|---|
| mavic4pro (헤드라인) | −16.71 | −16.61 | **+0.10 dB** |
| mini5pro | −20.18 | −21.60 | −1.42 dB |
| 프로펠러 단독(PO/SBR) | — | — | +1.1 ~ +1.3 dB |

→ **헤드라인 결론은 무영향**(+0.1 dB). mini5pro 는 높이 축소분만큼 어두워졌다.

### ⭐ 파생 발견 — σ 디스크캐시가 메쉬 변경을 몰랐다
`benchmark/channel.py` 의 `sigma_sbr_cache.json` 키가 `(드론, fc, az, el)` 뿐이라 **CAD 를 고쳐도
옛 σ 를 조용히 재사용**한다 — 링크버짓·SCR·Pd 가 전부 옛 형상 위에서 계산되는 사고가 된다.
→ 키에 `_mesh_fp()`(메쉬 정점 md5 8자)를 넣어 **메쉬가 바뀌면 자동 무효화**. 650 엔트리라 재계산 저렴.
⚠ 옛 키(지문 없음) 650개는 **지우지 않았다** — 영구 미스라 무해하지만 파일에 남는다(현재 740개 = 650 사문 + 90 신규).
  용량이 문제되면 `sigma_sbr_cache.json` 을 통째로 지우면 된다(재계산될 뿐이다).

### 감사 플래그 2건 — 둘 다 **내 오탐**이었다
- `mesh_verify.json` "부재" → **`report_mesh/outputs/` 에 실재**(27 kB). 리포트의 참조는 파일 로드가
  아니라 **본문 각주 인용**이었다. `outputs/` 만 훑어서 생긴 오탐.
- `verify_pyapril.json` "생산자 불명" → **`benchmark/verify_pyapril.py`** 가 생산자. `src/` 만 검색해서 생긴 오탐.
- 교훈: 생산자 추적은 `src/` 외에 **`benchmark/`·부록 `report_mesh/`** 까지 훑을 것.

---

## 2026-07-22 기록물 전수 정산 (json + 산출 이미지 + 죽은 코드)

전수조사를 **기계로** 돌렸다 — `benchmark/audit_outputs.py`. 각 `outputs/*.json` 에 대해
(1) 그 파일을 **쓰는** .py 를 찾고(모듈상수·argparse default·`os.replace(tmp,OUT)` 관용구까지 추적),
(2) 그 생산자가 σ/메쉬 모듈을 (간접이라도) import 하는지 보고,
(3) 파일 mtime 을 **엔진 대공사(9f26cee)** · **메쉬 수정** 시각과 비교해 세대를 판정한다.

### 삭제한 것 (전부 git 추적중 → 이력으로 복구 가능)

| 분류 | 수량 | 근거 |
|---|---|---|
| 고아 json | 4 | `beamform_demo` `report4_detection` `rt_chamber_env` `report3_microdoppler` — 생산자도 소비자도 없음 |
| 미참조 이미지 | 153 (153 MB) | 저장소 **전체**(sionna2 + team_meeting + 그룹미팅 + group_meeting)에서 파일명 인덱스를 만들어 교차검증한 뒤 삭제 |
| 죽은 report7 고리 | 4 | `rt_pipeline.py` → `report7_rt.json` → `viz_report7.py` → **아무도 안 읽는 그림**. 리포트 재편 때 소비자가 사라진 닫힌 고리 |
| 죽은 그림 생산자 | 2 | `viz_articulation.py` `viz_montage.py` — 산출물이 어느 리포트에서도 참조되지 않음 |

⚠ 삭제하지 **않은** 것: `viz_radar.py`·`viz_report2_photo_compare.py` 는 import 되지 않지만
**standalone 실행형**이고 산출 그림이 리포트에 실린다. `verify_server.py` 는 RT/OptiX 환경 진단 엔트리다.
→ import 그래프만으로 죽은 코드를 판정하면 **살아있는 생산자를 지운다**.

### 세대 판정 결과 (정리 후 26개)
- 🟢 최신 7 · ⚪ σ무관 2 · 🟠 메쉬이전 1 · **🔴 엔진이전 16**
- 🔴 16개는 전부 **07-14~07-20 산출 = RCS 엔진 대공사(07-21) 이전**이다. 즉 메쉬 수정과 무관하게
  **이미 낡아 있었다**. 실제로 재생성해 보니 링크버짓 단일자세 σ 가 최대 **+12 dB**, 마이크로도플러
  |DC|/AC 가 최대 **−15 dB** 움직였다 — 지터평균이 격자널을 메우고 셸투과가 내부 금속을 되살린 결과다.
- → `benchmark/regen_mesh_dependents.py` 에 5단계로 고정하고 순서대로 재생성 중.


### 하위 산출물 세대 — md 감사(2026-07-22)가 짚은 것
- `outputs/detection_rx_sweep.json`(07-20) = **엔진 대공사 이전**인데 report12 의 **헤드라인 출처**다 → 재생성 대상.
- `report_mesh/outputs/mesh_verify.json`(07-20) = 메쉬 수정 이전. mini5pro `fit_scale=[1,1,1.9788]` 과
  "H 측정 91.0" 을 담고 있는데, 그 91.0 은 **프레임 높이**다 — 정확히 이번에 없앤 버그다.
  현재는 `fit_scale=[1,1,1.679]`, 프레임 76.9 mm, **전체 드론 91.0 mm**. → `report_mesh/src/` 로 재생성 필요.
- 문헌 노트(`refs/drone_papers/*.md`)의 우리쪽 σ 는 **숫자를 빼고 JSON 키를 가리키도록** 바꿨다 — 다시 낡지 않는다.

---

## 2026-07-23 적대검증 반영 라운드 (과잉수정·날조·잔존 3종 검사 → 판정·정정)

선행연구 감사 정정 라운드(리포트 01~12) 결과에 적대적 검증 3건(과잉수정 / 날조·오류주입 /
교차 잔존)을 걸었다. 지적을 **전부 코드·JSON·원문 PDF 로 재검증**한 뒤 타당한 것만 고쳤다.
`outputs/` 는 이번에도 **읽기만** 했다(재생성 없음). 12/12 재빌드 성공.

### A. 고친 것 — 근거와 함께

**A-1. report08 헤드라인의 크기 이중잣대 (가장 심각)**
`_size_pair()` 가 대각 최근접으로 무조건 짝을 지어 s1000plus(1045 mm)↔Inspire 1(560 mm)를
**비 1.87** 로 묶고 있었다. 같은 절의 Semkin 행은 비 1.94 를 이유로 재질 이득 인용을 거부하는데,
헤드라인에는 같은 보정을 적용하지 않았다 — 자기 규칙 위반.
→ 헤드라인 범위를 **대각비 ±10 % 안인 3행**(mavic4pro·matrice4e·phantom4)에서만 잡도록 바꾸고
(`+8.0~+15.8` → **`+8.1~+15.8 dB`**, 실질 불변), **짝짓기 원장 표**를 신설해 대각비·Δpeak·
면적 스케일링 보정(20log₁₀)·보정 후 값을 전부 노출했다. 벗어난 2행은 ⚠ 로 표시하고
"보정해도 최솟값 **+2.6 dB** 로 부호 유지" 를 병기 — 결론이 짝짓기 규약에 의존하지 않음을 보였다.
전부 런타임 계산(손으로 적은 값 없음).

**A-2. pyAPRiL `detected_at_truth` — 깨진 필드를 근거로 쓴 문장 (규칙 3 위반)**
`pyapril/caCfar.py CA_CFAR.__call__` 은 `(hit_matrix, rd_snr)` **튜플**을 돌려주는데
`benchmark/verify_pyapril.py:94` 가 `np.asarray(cfar(mag))` 로 통째로 받아 shape 가 `(2,7,64)` 가
되고, `:119` 의 거리축 슬라이스가 **빈 배열**이 되어 `.any()` 가 구조적으로 항상 False 였다
(`n_fired` 21k~23k 가 448셀 맵에서 나올 수 없는 값인 것도 같은 진단을 지지).
report09·report11 이 이 필드를 "CFAR 가 발화하지 않았다" 는 근거로 새로 인용하고 있었다.
→ 두 곳에서 필드 인용을 **삭제**하고 report10 의 문구("봉우리 위치 판정이며 CFAR 발화 여부는
이 기록으로 확정되지 않는다")로 통일. **코드도 고쳤다**(아래 D절).

**A-3. 손계산 대조의 등급이 리포트마다 갈림**
report01 은 "지연도 독립 증거가 아니다(Sionna 자체가 image method)" 로 강등했는데 report09 는
같은 대조를 "독립 대조 / RT 와 독립하게" 로 유지하고 있었다.
→ report09 의 spine.verify·§2 를 report01 §4 와 같은 등급(구현 일관성)으로 맞췄다.

**A-4. σ 절대값 결론이 리포트마다 반대**
report08 이 "실측 범위 안" 을 철회하고 "+8~16 dB 위로 치우침" 으로 뒤집었는데, report07 caveat 과
report12 §6 은 여전히 "포락선(−28~−16 dBsm)에 든다 / 정합하되 밝은 상단" 이었다. 같은 σ 원장에
정반대 판정이 실려 있었다.
→ 두 곳을 report08 결론으로 동기화하고, "−28~−16 은 문헌이 보고한 값이 아니라 우리가 유도한
2차 산물이라 판정 근거로 쓰지 않는다" 는 경고까지 함께 이식했다.

**A-5. '바닥만 반사한다' 가 자기 ⚠ 와 충돌 (3편)**
report01 §2 는 천장 흡수체 1회 반사(−9.8 dB)가 바닥(−14.7 dB)보다 세다고 수치로 적으면서 같은 셀의
그림 캡션은 "바닥만 반사한다" 였다. report09 §1 의 경로 3갈래 열거, report11 용어풀이도 같은 단정.
→ 셋 다 "**설계상 반사면으로 남긴 것이 바닥뿐**" 으로 한정하고, report09 §2.1 에는 "위 세 갈래가
경로의 전부가 아니며 이 대조는 RT 가 분리해 준 바닥 탭 하나에 대한 것" 이라는 ⚠ 를 신설했다.

**A-6. "위상·편파를 보존한다" — 코드에 편파가 없다**
`src/rcs_sbr.py:436` 의 `E` 는 복소 **스칼라**이고 재질 반사도 스칼라 |Γ| 다(`grep -ci polari` = 0).
report08 은 이미 "스칼라 Γ 라 편파를 분리하지 않는다" 고 적는데 report06·07 은 편파 보존을 광고했다.
→ 두 곳을 "위상을 보존한다(⚠ 편파는 보존하지 않는다)" 로 정정.

**A-7. Sagitta(arXiv:2604.09243) "같은 방법" 강도 불일치**
report06 만 원문 식(7)의 PEC·모노스태틱 전용 선언을 근거로 "같은 계열 + 적용범위 다름" 으로
낮췄고 report03·07·08·11·12 는 "같은 방법" 그대로였다.
→ 5편 전부 "**같은 계열**(적용범위 차이는 report06 §4)" 로 통일. 같은 맥락에서 report07·08 의
"우리도 Sionna-RT 확장 계열" 도 report06 의 메커니즘 대조(그들=솔버 확장, 우리=Mitsuba 위 PO)로 교체.

**A-8. report09 의 '스냅샷 100 % ↔ 궤적 48 %' 가 처리 설정이 다른 두 실행**
`verify_floor_ghost.cd_ghost()` 기본값 **M=32** vs `verify_ghost_impact.json config.M` **M=48** 로
도플러 빈 폭이 다르다. 정정 라운드가 이 사실을 `directive_errors` 에만 적고 본문에는 안 썼다.
→ spine·caveat·§4 정리 3곳에 M 값을 병기하고 "위치 효과만의 감소로 읽지 말 것" 을 명시.
M=32 는 JSON 에 없으므로 **생산자 코드의 기본값을 정규식으로 읽어** 주입한다(손으로 적지 않음).

**A-9. report07 subdivision invariance — 회귀검사 가치까지 지웠다**
"정의상 그렇다" 로만 강등해 원본 코드가 남긴 의미(`verify_mesh_suite.py:376`: 파이프라인이
테셀레이션 밀도에 의존하지 않음을 실측으로 못박는다)가 사라졌다. 면별 가중·지터가 섞였다면
깨지는 검사이므로 항등식이 아니다.
→ "광선격자 수렴의 증거로 읽지 말 것 + 이 검사가 실제로 지키는 것은 **구현이 면 수에 의존하지
않는다**는 것" 으로 재서술(수렴 놉이 광선격자라는 정정은 유지).

**A-10. report03 '회전 정합 단계는 없다' 가 4대조 전부에 걸림**
`src/compare_phantom_scan.py:26-32` 는 실물 스캔에 PCA 주축 정렬을 건다.
→ "실물 스캔만 자기 주축으로 z-up 을 세우는 축 정렬을 거치고(부호·90° 모호성 잔존, 뒤집기 판정은
`pass` 라 무효), 그 밖의 세 대조에는 회전 단계가 없다" 로 정확화.

**A-11. report02 대각 3행이 같은 강도의 증거가 아님**
`fit_scale` 실측: s1000plus (0.9986, 0.9986)·phantom4 (1.0198, 1.0198) 는 **면내 등방**이라
"오차 = 배율 편차" 이고, 진짜 비등방은 matrice4e (0.851, 1.089) 하나다(그리고 그게 최악 −2.30 %).
→ "입력을 흔든 뒤에도 남는 일관성" 프레임에 이 한정을 각주로 붙였다(mini5pro 를 대수 항등식으로
제외한 논리를 같은 강도로 적용).

**A-12. 서지·표기 정정 (원문 확인)**
| 항목 | 전 | 후 | 근거 |
|---|---|---|---|
| report01 §4 소제목 | "독립 계산" | "손계산" | 같은 셀 ⚠ 와 충돌 |
| report01 인용 | `"SBR + image method"` | §1 축자 + §3.2 제목 | 2504.21719 원문에 그 문자열 0 hit |
| report01 일치 표기 | `0.000 ns / 0.00 dB` | 지수 표기 | 반올림이 완전일치처럼 읽힘 |
| report05 Di Seglio | IET RSN 18(1):108-124 | 18:107-124 + DOI 10.1049/rsn2.12506 | PDF 각주 "2024;18:107–124" |
| report10 학회명 | IEEE RadarConf 2024 | 2024 International Radar Conference (RADAR) | PDF 각인 |
| report04·12 Demissie | IET RSN 2025 "peer-reviewed" | 읽은 RADAR 2024 판으로 교체 + "IET 2025 는 페이월·미독" 병기 | pilot_findings.md L263 |
| report04 Jopanya | arXiv:2504.02641 | DOI 10.1109/SPAWC66079.2025.11143316 | PDF 표지(09·10·11 규약과 통일) |
| report12 Wypich | 무표기 | "표적은 차량·원문 미확보" | pilot_findings.md P11 |
| report02 106 mm | 하드코딩 | `lwh_full − lwh_frame` 에서 유도 | 규칙 1 |
| report03 ±1.79 dB | `:.2f` 내림 | `math.ceil` (±1.80) | 1.7931 은 1.79 를 넘어 자기 JSON 이 반증 |
| report06 spine.verify | "평판 −0.01·구 +0.39" 무조건 | 격자 라벨(@λ/12·@λ/10) + 우리 λ/16 값 병기 | report07 의 비단조 한정과 정합 |

**A-13. 리포트 간 나머지 잔존**
- report12 §6 "한 편도 / 전부" 절대주장 → "조사한 21편 안에서는" 으로 스코핑하고, 패시브 드론탐지
  밖에는 RCS 계산 선행이 있다는 사실(LAMBDA·Ziganshin·BVH SBR+PO)을 같은 칸에 명시.
- report12 §6 "명목 Pfa 를 그대로 신뢰" → report10 이 원문으로 반증(LTE450 은 참조셀 99 % 경험분위수)
  했으므로 "명목-vs-경험 **교정 곡선**을 잰 선행이 조사 범위에 없다" 로 좁힘.
- report11·12 "정답 거리빈에 **검출**" → "최강 봉우리의 거리빈 일치(발화 판정 아님)".
- report12 §2 에 **σ 적용범위 각주 신설** — report07 §5 가 "모노스태틱 등가값이며 report12 로 상속되지
  않는다" 고 선언했는데 받는 쪽이 침묵하고 있었다(방위평균·대역평균·el 15°·편파 미분리 명시).
- report12 SSB 무모호속도 "~1 m/s" 에 "모노 등가 표기, 거리합 좌표로는 ~2.1 m/s" 병기
  (report04 가 통일한 좌표 규약이 report12 에서 갈리고 있었다).
- report05 §3 그림 캡션에 ⚠ 신설 — PNG 안에 구워진 영문 문구가 μ 까지 CarrierConfig 출처라고 적는데
  본문은 로컬 대응표로 정정했다(report04 가 그림 (c) 속도축에 쓴 것과 같은 방식).
- report03 그림 제목 "matches to ~1 dB" 도 같은 방식으로 본문 범위(±1.80 dB)와 연결.
- report02 표 셀의 `|Γ|` 2곳이 파이프로 셀을 쪼개 표를 깨고 있었다 → 이스케이프
  (12편 전체 셀 단위 표 무결성 검사 통과: 파손 0).
- report08 죽은 변수 `_PEAK_ALL`·`_AZAVG_BAND` 제거, `prior_work.json` 의 낡은 `verdict` 를 인용하지
  말라는 경고를 상수 주석에 명시.

### B. 기각한 지적 — 근거

**B-1. "report06 이 재질 분해를 더 성긴 원장으로 되돌린 것은 과잉수정" → 기각.**
두 원장 모두 07-22 재생성본이고 조건도 fc 3.5 GHz·el 15°·mavic4pro 로 같다(차이는 `n_az` 36 vs 121).
report06 이 36점 원장(`report3_rt.json C_metal`)을 쓰는 이유는 **그림 [C] 제목이 그 값으로 구워져
있고**(`viz_report3.py:409`) 이번 라운드는 outputs 재생성이 금지이기 때문이다. 되돌리면 글과 그림이
어긋난다 — 더 나쁜 상태다. 현행은 조밀 원장(121점) 값과 `delta_db=+0.393` 을 캡션 각주에 병기하고
"금속만 > 전체(100 % 초과)는 아직 닫히지 않은 항목" 이라고 정직하게 남기고 있어 은폐가 아니다.
→ **미해결 항목(C-2)으로 이관**: 그림 재생성이 허용되는 라운드에 조밀 원장으로 통일.

**B-2. "report05 가 §2 표 3행을 전부 교체한 것은 범위 초과" → 부분 기각.**
지시서는 Jopanya 행 1개 삭제였으나, 남는 두 행도 검증을 통과하지 못한다(Demissie 인용판은 페이월·
미독이고 디스크의 RadarConf 판은 오히려 수신측 마스킹, Wypich 는 PDF 부재+표적 차량). Jopanya 만
지우면 **미검증 행 둘만 남는 더 나쁜 상태**가 된다. 교체 자체는 유지한다. 다만 지적된
"n=3 표본에서 나온 부정 일반화" 는 타당하므로 **미해결(C-4)** 로 남긴다.

**B-3. "report11 표가 깨졌다"·"report11 문단이 표에 붙었다" → 기각(검사 도구 오탐).**
셀 경계를 무시하고 노트북 전체를 이어붙여 검사한 결과다. 생성기는 표·문단을 **별도 셀**로 emit 하며
셀 단위 재검사에서 파손 0 이다.

**B-4. "report07/12 에 −28~−16 이 아직 남아 있다" → 기각(문자열만 일치).**
남은 것은 내가 이번에 넣은 **철회 경고문**("문헌이 보고한 값이 아니라 우리가 유도한 2차 산물이라
판정 근거로 쓰지 않는다") 안의 언급이다. 주장으로 쓰이지 않는다.

**B-5. 지시서 항목 중 "대상이 없다" 던 다수 → 재확인 결과 기각 유지.**
VaN3Twin(report01)·subdivision 인용(report02)·Semkin·Li&Ling(report03)·LTE_24 30.72 MHz·5G_25·
Jopanya 줄임표(report04)·Di Seglio 5 dB·Ai CP·OpenISAC EVM(report05)·Colone MC/CFAR-property·
exclusion neighborhood·8/13 dB·"21편 전부 명목 신뢰"(report10)·physical 블록·적분이득 등가(report11)
는 해당 생성기에 대상 문자열이 없다. 감사자가 `PRIOR_WORK_COMPARISON.md` 의 **아직 넣지 않은
제안(adopt/reject)** 을 정정 대상처럼 나열한 것으로 보인다.

### C. 미해결 (다음 라운드) — 2026-07-24 갱신

| # | 항목 | 상태 (2026-07-24) |
|---|---|---|
| C-1 | `numerology[*].cp_length_samples` = 0 (3행) | ✅ **해소(2026-07-27)** — viz_report2 재실행으로 `report2_waveform_rcs.json` 재생성 → cp_length_samples 0→**160/352/208** |
| C-2 | 재질 분해 원장 이원화(36 az ↔ 121 az) | ✅ **해소(2026-07-27)** — 재질기여도 그림 n_az=121 로 재생성, report02 에 36-az 모순 없음 |
| C-3 | `verify_pyapril.json` 의 `detected_at_truth`·`n_fired` | ✅ **해소(2026-07-24)** — `verify_pyapril.py` 재실행. 반전 결과: 3모드 전부 `detected_at_truth=True`, `n_fired=5`(옛 False 는 튜플언팩 버그 산물). report12 서술을 "CFAR 정답셀 발화" 로 정정·재빌드 |
| C-4 | report05 §2 의 "전샘플 대조 보고 관행은 드물다" (n=3 일반화) | ✅ **해소(2026-07-24)** — "이 세 예 모두 …대조해 보고하지는 않았다" 사실진술로 좁힘, 재빌드 |
| C-5 | `prior_work.json` `measured_rcs_anchor.verdict` 낡음 | ✅ **해소(2026-07-24)** — verdict 를 현재 σ(el0 평균 −16.6·중앙값 −19.7·봉우리 −8.4)로 갱신, `_verdict_source` 추가. make_notebook08 의 "낡음" 주석도 갱신. 두 리포트(08·11) 모두 이 필드를 본문 인용 안 함 확인 |
| C-6 | `src/waveforms.py:166` `v_unambiguous_ms` 모노 등가 | ✅ **해소(2026-07-24)** — 반환값 불변(호출부 보존), docstring 에 모노 등가·바이스태틱 기하인자 1/(cos(β/2)cosδ) 재배율 명시 |
| C-7 | 그림에 구워진 문구 3건 | ✅ **해소(2026-07-27)** — report03 "Floor-bounce amplitude agrees to.." 스코프 명시 · report04 v_max·report05 supxlabel 에 (mono-equiv) 표기 → 그림 재생성 |
| C-8 | report09 스냅샷(M=32) vs 궤적(M=48) | ✅ **전면 해소(2026-07-27)** — cd_ghost/b_clutter M=32→48 재실행 → _M_SNAP=48=_M_TRAJ. "적분시간 다른 별도 실행" caveat 을 _M_DIFFER 조건부로(동일설정→순수 위치효과) |

### D. 코드 버그 수정 (산출물 재생성 필요 — 이번 라운드에서는 재생성하지 않음)

**D-1. `src/waveforms_sionna.py` — CP 길이 초→int 절단**
`CarrierConfig.cyclic_prefix_length` 는 **초 단위 스칼라**(15 kHz → 5.208e-6 s)인데 `int()` 로 잘라
`cp_length_samples` 가 항상 0 이었다(필드명도 단위 오표기).
→ `cp_length_s`(초)와 `fs_ref_hz`(= FFT × SCS, FFT 는 부반송파 수를 2의 거듭제곱으로 올림)를 함께
기록하고 샘플 수는 둘의 곱으로 낸다. 검증: 15 kHz/106 RB → **160**, 30 kHz/273 RB → **352** 로
같은 파일의 독립 원장 `crosscheck.{lte,nr}.cp_head` 와 일치.
⚠ 재생성 대상: **`outputs/report2_waveform_rcs.json`** (`numerology` 블록). 생산자
`src/viz_report2.py`. 현재 report05 본문은 이 필드를 인용하지 않으므로 **틀린 문장은 없다**.
재생성 후에는 지시서의 "추가 ②"(CP 160/352 독립 근거표)를 하드코딩 없이 넣을 수 있다.

**D-2. `benchmark/verify_pyapril.py` — CFAR 튜플 언팩 누락**
`np.asarray(cfar(mag))` 가 `(hit_matrix, rd_snr)` 를 통째로 배열화해 shape 가 `(2,D,R)` 이 되고,
거리축 슬라이스가 빈 배열이 되어 발화 판정이 구조적으로 항상 False, `n_fired` 는 `rd_snr` 합이었다.
→ 튜플을 풀어 `hit` 만 bool 로 쓰고, 슬라이스가 조용히 비지 않도록 **축 모양 assert** 를 추가했다.
⚠ 재생성 대상: **`outputs/verify_pyapril.json`**. 실행: `cd benchmark && python verify_pyapril.py`.
재생성 전까지 report09·10·11·12 는 이 필드를 인용하지 않는다(위 A-2 에서 전부 제거).

### E. 재빌드

12/12 성공. 셀 수: 01(9) 02(21) 03(15) 04(11) 05(10) 06(14) 07(12) 08(12) 09(14) 10(21) 11(33) 12(25).
`nbformat.validate` 통과, 셀 단위 마크다운 표 파손 0, `outputs/*.json` mtime 불변(재생성 없음).

---

# §2026-07-23 논문반영 라운드 (DRONE_ISAC_PRIOR_READING.md 13편 정독 반영)

**범위**: 정독노트 **§4-P0(표기·규약)** + **§5(서술 정정)** 만 재실행 없이 반영. §4-P1/P2(새 계산·새 실험)
는 적용하지 않고 아래 "다음 단계"로 남겼다. 원시 σ 재생성에 걸린 항목은 판정 보류. 이후 적대검증 3렌즈
(과잉수정·날조·범위초과)로 재감사했고, 그 4건 지적을 아래 판정표로 마무리했다.

## 검증 지적 판정표

| 지적 | 렌즈 | 대상 | 판정 | 근거·조치 |
|---|---|---|---|---|
| **F1** — 큐브+확산을 엉뚱한 arXiv id 에 귀속 | 날조 | report06:370 | ✅ **수정** | `2509.21118`(Neural ISAC=점유맵) 은 큐브 아님. `metallic cube` 는 `2605.07623`(협력 FWA) — 원문 txt line 1072 *"The UAV is modeled as a metallic cube"*, 1085 *"specular and diffuse"*, RCS 카운트 0 직접 확인. `PRIOR_WORK_COMPARISON.md:764/765` 와 일치. **id 를 2605.07623 으로 교체 + 정확 서술**(금속 정육면체·경면·확산·RCS 0회) |
| **F2** — E²/(Rλ) 를 이번 라운드에 새로 계산(build_drone) | 날조·범위초과 | report02:161-206 | ✅ **수정(제거·보류)** | 노트 §7-2 item8 이 이 지표를 "**우리가 못 가진 것**"으로 분류, §8-4(새 계산 원장)에도 없음. per-mesh 표는 P0 표기가 아니라 **P1 신규계산**. `_e2rl_metric()`·`build_drone` 호출·per-mesh 표·"약 122배/병목 아님" 단정 삭제 → **report07 과 동일하게 보류**로 통일 |
| **F4** — 목표값에 맞춘 튜닝 상수 K=1.744 | 날조 | report02:155 | ✅ **수정(제거)** | F2 제거로 함께 소멸. K 는 icosphere↔uv_sphere 에 따라 1.744~2.46 이라 미명세였음. 대신 **R 이 정확히 아는 정준 구 앵커**(0.0071, 근사·상수 없음)만 인용 |
| **scope-creep** — report02↔report07 자기모순(계산 vs 보류) | 범위초과 | report02 | ✅ **수정** | F2 와 동일 조치로 두 노트북이 **둘 다 "보류·다음 단계"** 로 일치. `PRIOR_WORK_COMPARISON.md:151`("곡률 R 산출 필요·현재 없다")·:441(정준 구 0.0071 이미 산출) 로 정성 근거만 유지 |
| **F3** — "≲0.5" 미검증 임계값을 헤드라인 분모로 | 날조 | report02·report07 | ⚠ **부분 기각 + 프레이밍 정정** | ≲0.5 는 **날조 아님** — zig-journal 원문 §V-C 직접 확인: *"the improvement becomes progressively smaller once E²/(Rλ) approaches approximately 0.5"*. 단 원문 §III-C 는 최적 구간을 "경험적으로 정해야" 라 하므로 **보편 임계값이 아니라 수렴 무릎**으로 재프레이밍. report02 의 "122배" 분모 용도는 F2 로 삭제. report07 소스주석의 "노트가 검증"→"원문 §V-C 로 확인·노트는 0.4–0.6·E>1.5λ만 인용" 으로 정정 |
| **overcorrection** | 과잉수정 | 02·06·07·08 | — **결함 없음(동의)** | 삭제행 전수 대조에서 엄밀 물리 손실 0. report07 전방산란 σ≡0·상반성·모노 등가 서술 존속 확인 |

## 적용한 것 (P0 표기 · §5 서술) — 이전 워크플로 산출 + 이번 마무리

- **P0-2** (report02 §3): σ 표에 (재질·ε_r·σ·평가 f) 4열 강제. materials.py 단일 진리원 상수, 손기입 0. **유지**.
- **P0-7** (report07 §3): σ 를 네 각도 인수 `σ(θ_in,φ_in;θ_out,φ_out)` 로 표기 + 우리 구현이 후방산란(θ_out=θ_in) 단면만 채움을 명기. **유지**.
- **P0-8** (report07 §6): zig-journal `E_total = E_direct + ΣE_specular + ΣE_edge + ΣE_vertex` 원장 인용 + 우리가 채우는 항/비우는 항(에지·정점 회절) 대조표. **유지**.
- **§5-F** (report06): "스톡 Sionna 는 표적을 못 다룬다" → "**기본 경로 솔버는 형상·기종·자세별 절대 σ(dBsm)를 못 낸다**" 로 좁힘. clutter·md-rt 가 실제로 표적 반환을 낸 것을 정면 인정(둘 다 σ dBsm 미보고). **F1 수정으로 인용 정확도 완성**.
- **§5-G** (report07 §6): zig-journal 을 "가장 가까운 선행"→"**우리 SBR+PO 를 명시적으로 비판한 선행**"으로 재프레이밍, 원문 verbatim 인용 + 반론(그들=PEC 차량·UTD E>1.5λ, 우리=few-λ 유전체 드론). make_notebook07.py 의 자발 공개 한계(전방산란 σ≡0·상반성)와 명시적 연결. **F3 프레이밍 정정으로 완성**.

## 다음 단계로 남긴 것 (P1: 계산은 하되 새 실험 아님 / P2: 새 실험) — 이번 라운드 미적용

| # | 노트 | 항목 | 왜 지금 못 했나 (판정 보류 포함) |
|---|---|---|---|
| N-1 | §4-P1-11 | **원시 σ(az) 배열 `sigma_raw` 를 JSON 에 저장** | 현 `sigma_smooth` 는 밴드 5주파수 평균 + 3° 창(널을 ~18 dB 메움). 분포적합·분위점은 원시로만 유효 → **outputs 재생성 필요(규칙 3)로 판정 보류** |
| N-2 | §4-P1-12 | **μ(f)=af+b·ε(f)=cf+d 회귀(5기종)** | 1.8–6 GHz 0.2 GHz 스윕 ≥20점 새 산출 필요. 기준선 multiband a=0.21·mono3d a=0.315. report08 은 우리 회귀를 만들지 않고 **문헌 회귀계수를 우리 밴드에서 평가**하는 사과-대-사과 앵커까지만(비단정) |
| N-3 | §4-P1-13 | **분포적합(Rician/Gamma/LogNormal) + AD·CvM 거리** | 원시 방위표본 MLE 필요(N-1 선행). 기준 multiband Rician 0.436 |
| N-4 | §4-P1-9 | **금속구 절대교정 이식** | πa²=−7.07 dBsm(0.5 m) PEC 구를 SBR+PO 로 돌려 편차<2 dB 확인. 새 파이프라인 실행 |
| N-5 | §4-P1-10 | **문헌 대조 el=0° 컷 5기종×3밴드** | §2-3 이 phantom4·mavic4pro 만 재실행. report08 은 4곳 "다음 단계 산출" 로 명시 보류 |
| N-6 | §4-P1-14 | **정규화 σ 분위점 P10/P1(원시)** | 현재 `sigma_smooth` 기준값은 판정 보류(N-1 선행) |
| N-7 | §7-2 item8 | **E²/(Rλ) per-mesh 5기종** | facet 별 국소 곡률반경 R 산출 패스 필요(임의 메쉬 R 추정=열린 문제, `PRIOR_WORK_COMPARISON.md:151`). report02·report07 둘 다 보류로 통일. 산출 후 outputs 영속화 → 정성 결론(구 앵커 0.0071 ≈ 무릎 70× 아래)을 정량화 |
| N-8 | §5-C | **마이크로도플러 지표를 선피크 대 선피크로 재보고 + SBR 가림 재검증** | mavic4pro SBR −2.17 dB 이상치. npz 재계산·microdoppler.py 재검증 필요 |
| N-9 | §4-P2-19~26 | **클러터 도플러퍼짐 주입·hot clutter·σ 요동 Pd·X410 실측 프로토콜** | 전부 새 실험/재실행. VERIFY_CLUTTER.md 처방대로 다음 라운드 |

## 재빌드 (이번 라운드)

12/12 성공. 셀 수: 01(9) **02(22)** 03(15) 04(11) 05(10) **06(15)** **07(13)** 08(12) 09(14) 10(21) 11(33) 12(25).
`outputs/*.json` 재생성 0(읽기만). 검증: report02 구 헤드라인("약 …배(2자릿수)"/"기하 이산화는 병목이 아닙니다"/"build_drone 매 생성") grep=0, 신 보류문·정준 구 0.0071 존재; report06 `2605.07623`=1·`2509.21118`=0·`metallic cube`=1; report07 수렴 무릎 프레이밍 존재.


---

## 2026-07-24 P1 앵커 대조 완료 (B2)

`benchmark/rcs_anchor.py`(신규) 로 정독노트 §4-P1(9~16)의 선행 방법론을 **그대로 차용**해 우리 σ 를 문헌과
정량 대조. **원시 σ(az)**(평활 없음·단일주파수)로 회귀·분포적합·분위점 산출. 결과는
`outputs/rcs_anchor.json`(DJI 쿼드 4종), report08 §6.1 이 JSON 주입으로 대조표를 만든다.

**⭐핵심 발견 — 우리 σ 의 주파수 의존이 실측보다 3~6배 가파르다.**
μ(f) 기울기 a[dB/GHz]: 우리 mini5pro +1.04·mavic4pro +0.96·matrice4e +1.17·phantom4 +1.40
↔ multiband Phantom 3(실측) **0.21** · mono3d θ=90°(실측) **0.315**.
→ 우리 금속부가 광학영역 A²/λ²(∝f²)로 스케일해 기울기가 크다. 실측 드론은 공진·확산 혼합이라 완만.
   report08 이 이 격차를 정직한 한계로 서술(절대 σ 판정 보류 서사와 정합).
부수 산출: 분포적합(진폭 도메인 AD·CvM), 금속구 절대교정(합격선 2 dB), el0/el15 앙각편향, P10/P1 분위점.

⚠ **s1000plus 4종에서 빠짐** — 1045mm 옥토콥터(40k면)라 22개 신규주파수 회귀 씬빌드가 유독 느리고(40분+),
**대응 문헌앵커도 없다**(문헌 전부 ~350mm 쿼드) → 정지. report08 앵커함수는 결측에 graceful
(`AO=[d for d in ORDER if d in D]`). 보완은 조밀-주파수(0.4 GHz 11점) 재실행으로 씬빌드 절반.

⚠ **재발방지** — per-drone JSON 병합 시 `anchor_smoke.json`(impl 스모크, el0만)이 glob 에 걸려 mini5pro
정식항목을 덮어 KeyError('el15') → 병합은 **DRONES 명시목록**으로만, smoke/full 제외.

---

## 2026-07-24 E3 클러터 도플러퍼짐 완료 (N-9 처방 착수)

`benchmark/verify_clutter_doppler.py`(신규) 로 정독노트 §6-2 처방(Clutter-Aware ISAC 서베이 §V-A4: C=100·4 iso-Rb 링·반경속도 U[−1,1] m/s)을 챔버에 이식. **make_cpi 무수정**(기존 `ghosts=` 재사용, none-point==run_cell **Δ=0.00e+00 dB 3파형 비트일치 PASS** → report01~12 재현성 불변). 결과 `outputs/verify_clutter_doppler.json`, report09 §5.

**⭐핵심 — 'SCR 죽은 파라미터'는 항등식이었다.** 도플러퍼짐 arm 은 진폭 40 dB 스윕에서 SCR span 이 폭발(6.7 dB; 제로도플러 대조군은 여전히 4e-9 dB)하고 ref-region 이 정확히 amp²로 복귀(60.8→87.6→127.6 dB). **메커니즘 정정(적대감사 물리 결함1)**: 이 파일의 클러터는 tx_frame(파일럿+데이터) 반사라 ECA 가 ~1.4 dB 만 지운다(도플러 무관) — 죽인 건 **0-도플러 노치**(정지 잔류 99.99%가 zd±1)이지 ECA span(X) 이 아니다. verify_eca S5(파일럿-클러터)만 진짜 span(X) 항등식. **H2 (C4, 정직 정정)**: '노치 블라인드 존' 가설은 데이터로 반증됐다(에코 −86 dB 로 지워도 표적셀 Pd=1) — 진짜 결과는 **움직이는 클러터가 표적셀에 가짜 검출을 만든다**(target=1.00·notarget=1.00·clean=0.00; 링이 표적 거리빈 bracket) → 저속 드론은 움직이는 클러터와 분간 안 됨. M=96 은 SCR span 12.9 dB(더 취약).

**적대감사 3라운드 반영**: 경험적 앵커(라벨=실현 CNR, 물리 결함2), scnr_in 분모에 hot 포함+정의 명기(repro 결함1/F1), 노치 반폭 dfd/2 정정(F3), 불변증명 3파형(repro 지적3), C4 near-threshold 로 H2 "무너진다"를 데이터로 한정(F2), C2b/C3/C4 전부 JSON 영속화(F4).

⚠ 남은 한계: hot scnr_in 은 서베이 입력SCNR 과 정확한 apples-to-apples 아님(축 다름, 방향만 대조); 유한 ADC·양자화는 여전히 미포함; C2b/C3/C4 는 5G 헤드라인만(C2 는 3파형).
