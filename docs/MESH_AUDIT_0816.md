# 메쉬 적대적 감사 — 2026-08-16

**무엇을 했나**: 우리 드론 메쉬를 네 개의 렌즈로 따로 뜯어보고, 서로 반증시켰다.
**왜 지금**: 2026-08-16 재질 판정이 «움직이는 신호는 프로펠러가 사실상 전부 만든다» 를 실측했다.
그러면 프롭 형상의 메쉬 오차가 곧 결과 오차다 — 그게 이 라운드의 최우선 표적이었다.
**규칙**: GPU 미사용(전부 CPU). 코드 무변경(발견·권고까지). 대안 형상은 스크래치 프로세스 안에서만 지었다.
⭐**단, §⑦ 은 이후 라운드다** — 거기서는 코드에 **인자로만 켜지는 새 판**(`blade_law='per_airframe'`)과
`DroneSpec` 필드를 **추가**했다. 기본 동작은 안 바꿨고, 비트동일을 회귀로 증명해 둔다.

**산출 원장 4종** (수치의 출처는 전부 여기다):

| 파일 | 렌즈 |
|---|---|
| `outputs/mesh_audit_0816_prop_geometry.json` | 프롭 날 해부 — 시위·두께·비틀림·팁·단면을 DJI 공식 CAD·제품사진과 대조 |
| `outputs/mesh_audit_0816_topology_physics.json` | 위상·물리 — 구멍·관통·질량·검사기 자체의 결함 |
| `outputs/mesh_audit_0816_scale_anchor.json` | 치수 앵커 — 전개크기·투영면적·금속면, 낡은 기록 |
| `outputs/mesh_audit_0816_adversarial_refute.json` | 반증 전담 — 대안 형상을 실제로 지어 PO 커널로 σ 차이를 쟀다 |

**용어 한 줄 풀이** (첫 등장 기준)
- **시위(chord)**: 날 단면의 앞전에서 뒷전까지의 길이. 날의 «폭».
- **스팬(span)**: 회전 중심에서 팁까지의 방향. `r/R` 은 팁을 1 로 놓은 반경 위치.
- **평면형(planform)**: 날을 위에서 봤을 때의 윤곽. 시위가 반경에 따라 어떻게 변하는가.
- **AC / DC**: 시간에 따라 변하는 성분(AC, 프롭이 돌면서 만드는 것) / 안 변하는 성분(DC, 정지한 동체).
- **슬래브 두께**: 재질 계산에서 «이 판이 몇 mm 두께인가» 로 넣는 값. 메쉬 삼각형은 안 바뀐다.
- **수밀(watertight)**: 껍질에 구멍이 없어 «안/밖» 이 정의되는 상태.
- **PO / SBR**: 우리 산란 커널 두 종. PO 는 가림(그림자)을 안 보고, SBR 은 본다.

---

## ① 다섯 줄 요약

1. **우리 검사기는 자기가 방금 수리한 사본을 검사한다.** `trimesh.split()` 이 구멍을 조용히 메운 뒤 «수밀 통과» 를 돌려준다(합성 대조로 확증: 11면 → 12면). 그리고 회귀 게이트 `assert_ok()` 는 «상시 가동» 이라 적혀 있지만 **저장소에 호출부가 0 건**이다. ⇒ 기존의 «10/10 통과» 기록은 무결성의 증거가 아니다.
2. **블레이드 법칙의 근거 문장이 거짓이 됐다.** 코드와 원장이 «저장소에 DJI 프로펠러 실물 기하는 하나도 없다» 고 적고 그것을 근거로 3DR Solo 를 대리 기준으로 삼는데, `assets/meshes/reference/WM161_zhankai_1k.glb`(DJI Mini 2 공식) 안에 **프롭 날 8장이 들어 있다**. 대조해 보니 Solo 는 참조 4종 중 유일한 이상치였고, 우리 날은 실물보다 **외곽에서 27~39 % 좁다**.
   ⛔**[정정 2026-08-16 §⑥]** «Solo 는 유일한 이상치» 는 **거짓**이다 — 같은 원장의 Holybro 1345(0.490)가 Solo(0.576)보다, 우리(0.598)보다도 더 뿌리 편중형인데 감사의 정규화시위 표에서 그 행만 빠졌다. «27~39 %» 는 **≈30.5 %** 다(감사가 우리 쪽만 법칙 상수 0.25 로 나눴다 — 지어진 c_max/R 은 0.242). 그리고 «날 8장» 은 실은 **메쉬 2종 × 인스턴스 4개**다.
3. **그런데 그 형상 오차의 σ 대가는 생각보다 작다.** 실물 DJI 평면형으로 프롭을 다시 지어 PO 커널로 재보니 **+0.2 dB(모노스태틱) ~ +1.3 dB(바이스태틱 β=120°)**, 시위 오차의 그럴듯한 폭 전체로도 **±2.5 dB** 다. 면적비를 평판식에 그대로 넣어 얻은 «−3 dB» 는 **과대 번역이었다** — 비틀린 날의 반사는 면적이 아니라 정반사 띠가 크기를 정한다.
4. **진짜 큰 손잡이는 형상이 아니라 두께 규약이다(13~17 dB 축).** 프롭 슬래브 두께가 전 기종 스칼라 하나인데 메쉬 실측 두께는 기종마다 **4.3 배**(mini2 0.66 ~ m350rtk 2.88 mm) 벌어진다. 실측 표적이 Matrice 4E → **Mini 5 Pro** 로 옮겨가는데 mini5pro 는 0.833 mm 라, 정본 1.43 mm 를 그대로 쓰면 프롭 에코를 **3.8~4.6 dB 밝게** 본다.
5. **문서에 박힌 수치 4건이 사실과 다르다** — D2 잔차 «+1.96~+7.13 dB»(실제 +1.2~1.7 dB, 기체도 잣대도 다른 두 수를 뺐다), matrice4e 길이 «−0.36 %»(메쉬가 내는 값은 +7.96 %), typhoonh480 «+1.9 %»(현재 −1.1 %, 부호까지 반대), 정면 수직면 «106장 139 cm²»(뒷면까지 세어 2 배, 앞면만 69.67 cm² → 절대 σ −6.0~−9.4 dB).

> **한 줄로**: 형상 축은 «1~2 dB 축» 이고 두께·재질 규약은 «13~17 dB 축» 이다. 그리고 그 둘보다 먼저, **검사기가 검사를 안 하고 있었다.**

---

## ② 심각도별 발견표

dB 부호 규약: **(+)** = 우리가 실물보다 밝게 센다(과대), **(−)** = 어둡게 센다(과소).

### 치명 — 통과 기록의 신뢰도나 발표 수치를 직접 무너뜨리는 것

| # | 발견 | 근거 수치 | 영향 | 수리안 |
|---|---|---|---|---|
| **C1** | **`mesh_check` 가 출하 메쉬가 아니라 방금 수리된 사본을 검사한다** | `mesh_check.py:53,180` `tm.split(only_watertight=False)` — trimesh 5.0.0 의 `split` 은 `repair=True` 가 기본. 합성 대조 실증: 삼각형 1장 뺀 상자가 `repair` 기본에서 **11면→12면·수밀 True**, `repair=False` 에서 **11면·수밀 False**. 실사례 mini2 body 도 동일하게 갈린다 | dB 아님 — **검증 자체가 무효**. «10/10 통과» 는 구멍 없음을 증명하지 않는다 | `split(..., repair=False)` 명시 + 경계 모서리 수를 결과에 싣고 0 아니면 실패시킬 것 |
| **C2** | **회귀 게이트 `assert_ok()` 는 «상시 가동» 이라 적혀 있으나 호출부가 0 건** | 저장소 전수 grep: 정의(`src/mesh_check.py:237`)와 문자열 언급만. 거짓 문구 3곳 — `viz_report1.py:605` «runs inside the build pipeline as a hard gate», `report_mesh/src/make_mesh07.py:242` «빌드 자체가 실패한다», `mesh07` 노트북 «상시 가동». 정작 `mesh_check.py:22` 자신은 «부르면» 이라는 조건문으로 정직하다 | dB 아님 — 메쉬 회귀를 막는 장치가 실재하지 않는다. C1 과 겹치면 배선돼 있었어도 수리된 사본을 봤을 것 | 빌드 경로에서 실제 호출 + 리포트 3곳 문구 정정 |
| **C3** | **블레이드 법칙의 근거 문장이 거짓** — 저장소에 DJI 프롭 실물 기하가 **있다** | `WM161_zhankai_1k.glb`(2026-08-07 유입, 7.7 MB)에 **날 8장 + 허브 4개**. 회전축은 허브 관성주축 실측으로 월드 +y(편차 0.001). 디스크 지름 118.4 mm ↔ 공칭 4726F 119.4 mm(−0.9 %). 그런데 `drone_cad.py:67` · `benchmark/measure_reference_props.py:18,225` · `outputs/reference_props.json` meta.warning 이 아직 «하나도 없다» 고 적는다 | dB 아님 — 아래 C4·I1·I2 전부의 **상류 원인**. «DJI 참값을 알 수 없다» 는 전제 위에 법칙 전체가 서 있다 | 해당 문장 3곳 삭제, 1차 앵커를 Solo → DJI Mini 2 공식 CAD 로 교체 |
| | ⛔**[정정 2026-08-16 §⑥]** «회전축은 월드 +y(편차 0.001)» 는 **거짓**이다 — 실측 앞 로터 **2.969°** · 뒤 로터 **5.000°** 기울어져 있다(모터축 원통·프롭너트·허브 면적가중 공분산 세 추정기와 판결자 직접 측정이 일치). ⚠파급: `src/drones.py` mini2 note 의 «뒷 로터 코닝 틸트로 두 날이 2.00/3.00 in 로 갈린다» 는 **축 가정의 인공물**일 가능성이 크다. 또 이 GLB 는 «공식 3D CAD» 가 아니라 **Maya 저작 제품 뷰어용 자산**이다(비수밀·내부구조 없음). 정체 자체는 강하게 확증됐다 — DJI CDN 원본과 **바이트 동일**, 텍스처에 «MINI 2 / 249g» 인쇄 | |
| **C4** | **시위 분포가 실물과 다르다 — 외곽에서 27~39 % 좁다. Solo 는 참조 4종 중 유일한 이상치** | 정규화 시위 `c/c_max @ 0.70R`: 우리 **0.577** ↔ Mini2 공식CAD 0.866 · M4E 1157F 사진 0.785 · M4E 1154F 사진 0.952 · Yuneec 0.866. 정점 위치 우리 0.30R ↔ DJI 0.45R. 면적 무게중심 `r̄/R` 우리 0.4933 ↔ 실물 0.521~0.550. **정의 무관 교차검증 3종**(단면 안 씀): 투영면적비 0.798 · 표면적/2 비 0.755 · 단면적분비 0.710. ⭐`CHORD_FRAC` 은 Solo 를 소수 3자리까지 재현한다 — **유도엔 버그가 없고, Solo 를 고른 판단이 틀렸다** | σ 로 **+0.2~+1.3 dB**(아래 C5 가 실측). 스펙트럼 축에서는 별개 — 면적이 뿌리 쪽에 쏠려 **f_tip 부근을 깎는다** | `CHORD_FRAC` 를 DJI Mini 2 평면형으로 교체. **반드시 `CHORD_MAX_OVER_R` 와 묶어서** (I2) |
| | ⛔**[정정 2026-08-16 §⑥ — 4건]** ① **«날 면적 −29 %(−2.97 dB)» 는 죽었다.** 공통 창에서 세 잣대가 0.78~0.80 ⇒ **−20~−24 %(−2.0~−2.4 dB)**. 감사 **자신의 표**도 그렇게 적는다(`E_area_impact_dB['0.20-0.96'].area_ratio = 0.7893`, **−2.05 dB**). 0.710 은 **우리 날만 0.175R 에서 자르고 DJI 는 통째로 적분**해서 나온 창 불일치다. ② 그 잘라내기의 근거 «DJI 날 부품은 0.175R 부터» 도 **거짓** — 판결자 직접 측정으로 DJI 날 최소 반경 **r/R 0.033**(8장 전부), 0.175R 안쪽 면적은 오히려 DJI 가 더 많다(50.7 ↔ 46.9 mm²). ③ «Solo 는 유일한 이상치» **거짓**(1345 누락, 위 ① 요약 참조). ④ ⭐**«우리 날이 작다» 자체가 DJI 한정이다** — UIUC 실측 132종 대비 우리 날 면적은 **61 백분위**(중앙값보다 크다). 참인 명제는 «작다» 가 아니라 **«안쪽에 쏠렸다»**(면적중심 2 백분위·0.6R 바깥 몫 2.3 백분위). ⚠그런데 RF 층은 그 «쏠림» 이 σ 로 **전 고각 ≤0.14 dB** 라고 잰다. **외곽 밴드 −3.55 dB 는 살아남았다**(0.663 재현) — 단 그것은 면적비이지 σ 가 아니다 | |
| **C5** | **⭐ 그 형상 오차를 실제로 계산해 봤더니 «−3 dB» 가 아니라 «+1 dB» 였다 — 우리 자신의 dB 번역을 반증** | 실물 DJI 분포로 프롭을 다시 지어(matrice4e 크기, 표면적 +19.5 %) PO 커널로 측정한 **Δσ 평균**: 모노 el −30 **+0.24 dB** / β=81° **+0.87 dB** / β=120° **+1.29 dB**. 통제 민감도 시위×0.7 → −1.17(모노)~−2.50(β120), 시위×1.4 → −0.03~+2.21. **포화**: el −30 에서 시위를 1.4 배로 키워도 σ 는 −0.03 dB — 비틀린 날의 정반사는 면적이 아니라 정반사 띠(트위스트율)가 크기를 정한다. 수렴 확인: 점간격 λ/7→λ/24 에서 ≤0.06 dB, 로프트 3,476→23,468 면에서 ≤0.04 dB | 형상 축의 정직한 폭은 **±2.5 dB**. 면적비를 평판식 `20log10` 에 그대로 넣은 «−2.97 dB» 는 **과대 번역** | 형상 오차의 dB 를 인용할 때는 **면적비가 아니라 커널 측정치**를 쓸 것 |
| | ⛔**[정정 2026-08-16 §⑥ — 3건]** ① **«+0.2~+1.3 dB» 는 지금 얹힌 판이 아닌 프롭의 값이고 부호가 반대다.** 이 팔은 `c_max/R` 을 0.25 에 **고정**한 채 분포만 갈아 표면적을 +19.5 % 늘렸다. 실제 코드는 둘을 **한 묶음으로** 갈므로 matrice4e 날 면적이 **−0.503 dB(dji_mini2) · −0.375 dB(per_airframe)** 로 **줄어든다**. 출하 판으로 잰 Δσ 는 **−1.7 ~ +1.8 dB 이고 기종마다 부호가 다르다**(mini2 +1.4~+1.6 ↔ mavic4pro −1.3~−1.7). ⇒ 이 가격표를 교체 근거로 인용하지 말 것. ② **포화 기제(«정반사 띠 = 트위스트율»)가 죽었다** — 트위스트가 정하는 띠 반폭이 655~2559 mm 로 날 길이(127 mm)의 5~20 배라 날을 못 자르고, 정반사 고각 코히런스 비가 **0.998**(날 전체가 동위상)이다. 진짜 기제는 **시위 방향 위상 기울기**이고, el −30 에서 면적이 안 먹는 이유는 정반사(−72°)에서 **40~50° 벗어나 있어서**다. ③ «면적비 → 20log10» 이라는 자 자체가 커널의 성질이 아니다 — 실측 환산 지수 n = **0.12(el 0) · 0.88(el −30) · 1.69(el −90)**. 헤드라인 밴드에서는 20log10 이 아니라 **≈10log10** 이다. **자기반증(«과대 번역») 자체는 옳았고, 바이스태틱 민감도 증가도 살아남았다**(Kell 등가로 기전까지 붙음, β=81° 의 정직한 지수 1.39) | |
| **C6** | **`MATERIAL_CORRECTION` §2-2 D2 의 «실물 대비 +1.96~+7.13 dB» 잔차는 범주 오류** | 문서는 «우리 1.43 mm 대비 실물 0.53/0.80/1.08 mm» 로 뺀다. 그런데 **1.43 mm 는 matrice4e(지름 274 mm)의 시위평균**이고 **0.53~1.08 mm 는 Mini 2(지름 119 mm)의 최대두께 범위**다 — 기체도 잣대도 다르다. 같은 기체·같은 잣대로 다시 재면 Mini 2 우리 0.731 ↔ DJI 0.600 mm = **+21.9 %(+1.72 dB)**, 측정 스테이션만 적분하면 +14.8 %(+1.20 dB) | 문서에 박힌 **R50 +12~+51 %** 는 과대평가. 방향(우리가 밝게 센다)은 맞다. §6 재개 게이트·§7 R11 이 이 수치를 인용해 리포트로 흘러간다 | D2 잔차표를 **+1.2~+1.7 dB**(mini2 직접실측)로 재작성. matrice4e 칸은 «실측 없음, +0.3~+3.3 dB 로만 묶임» 으로 정직하게 |

### 중요 — dB 를 만들거나 무방비 구멍을 남기는 것

| # | 발견 | 근거 수치 | 영향 | 수리안 |
|---|---|---|---|---|
| **I1** | **⭐ 프롭 슬래브 두께가 스칼라 하나인데 메쉬 두께는 기종마다 4.3 배 벌어진다** | 시위평균 두께[mm]: mini2 0.664 · **mini5pro 0.833** · typhoonh480 1.255 · phantom3/4 1.300 · x500v2 1.370 · mavic4pro 1.447 · **matrice4e 1.484** · s1000plus 2.044 · m350rtk 2.879. 정본 «matrice4e 1.43 mm» 는 메쉬에서 재현됨(1.456~1.484) | **실측 표적이 Mini 5 Pro 로 옮겨간다.** 정본 1.43 mm 를 mini5pro 에 그대로 쓰면 슬래브 표 보간으로 **+3.8 dB(각도평균) ~ +4.6 dB(45°)** 밝게 본다. 이 감사에서 가장 큰 단일 실행가능 dB | 슬래브 두께를 **기종별로 메쉬에서 뽑을 것**. 계산법은 원장에 있다(원통단면 → 최대캘리퍼 시위선 → 상하면 차 → 시위평균 → 스팬평균) |
| **I2** | **`CHORD_MAX_OVER_R = 0.25` 단일 상수가 8기종 전부에 걸려 있다 — 실물은 0.177~0.273 이고 크기와 반비례** | 실측 `c_max/R`: Mini2 공식CAD 0.262(사진 0.272) · M4E 1157F 0.190 · M4E 1154F 0.212 · Mavic4Pro 1158F 0.181 · Solo 0.273 · 1345 0.225 · Yuneec 0.177. 우리는 전부 0.25 ⇒ mini2 −5 %, matrice4e **+31 %**, mavic4pro **+37 %** | 기종별로 **오차 부호가 뒤집힌다**. 큰 프롭은 총면적 +0.84~+1.38 dB 과대인데 외곽은 −0.34 dB — 기종 간 σ 비교와 스케일링 논증이 흔들린다 | `c_max/R` 을 `DroneSpec` 필드로 승격(위 실측표를 초기값). 저소음(1154F) vs 표준(1157F) 면적차가 **+25 %** 라 어느 프롭인지 spec note 에 명시 |
| | ✅**[유지·강화 2026-08-16 §⑥]** 단일 상수 지적은 **살아남았고 RF 층에서 더 강해졌다** — 면적→σ 환산 지수가 날의 **전기적 크기**에 달려 있다: el −30 에서 n = 1.80(mini2) · 1.65(mini5pro) · 0.88(matrice4e) · **−1.20(m350rtk — 면적을 키우면 σ 가 내려간다)**. ⭐우리 실측 표적 두 대가 이 경계의 **양쪽에** 있다. ⛔**단 «크기와 반비례» 는 죽었다** — 감사 자신의 표로 스피어만 **−0.05**, 모집단으로도 우리 함대가 사는 8~13 in 구간에서 ρ=−0.19(**p=0.082, 유의하지 않음**). «실물은 0.177~0.273» 도 표본 7개의 착시다 — 실측 132종의 범위는 **0.1529~0.3896**(중앙값 0.1875)이고 우리 0.25 는 **82 백분위**(넓은 편이나 이상치 아님). ⇒ 기종별 필드 승격은 **간다**. 빈칸을 **크기 스케일링으로 채우지 말 것** | |
| **I3** | **부품 간 관통 검사가 prop↔motor 한 쌍뿐 — 실제로는 표면적의 8~42 % 가 다른 부품 속에 있다** | 전 그룹쌍 내부판정(전체 면적 대비 매몰): mini5pro 41.8 · matrice4e 40.7 · phantom4 39.3 · mavic4pro 38.0 · m350rtk 29.3 · phantom3 27.5 · typhoonh480 22.5 · mini2 19.4 · x500v2 18.1 · s1000plus 8.0 % | **SBR 은 대부분 무해**(first-hit 가림이 처리, battery/pcb 투과는 설계 의도). **PO 는 가림이 없어**(`rcs_po.py:47` 자기선언) 매몰면이 이중계상 → mavic4pro **+38 %**·mini5pro **+41.8 %** 면적 과대 | `check_prop_bell_solid` 를 전 그룹쌍으로 일반화 + 기체별 예산표 |
| **I4** | **canopy 가 body 안에 69~100 % 묻혀 있다 — SBR 기여가 정확히 0 인 죽은 무게** | 광선 contains 와 부호거리 두 방법이 면 단위 100 % 일치: mavic4pro **100.00** · phantom3 100.00 · phantom4 92.86 · mini2 86.35 · mini5pro 85.13 · typhoonh480 74.66 · matrice4e 69.19 %. mavic4pro 는 canopy 상단 32.20 mm < body 상단 43.06 mm | SBR 에서 묻혀 first-hit 이 못 되고, 투과 패스에선 셸이라 제외(`rcs_sbr.py:231`) ⇒ **기여 정확히 0**. mavic4pro 2240 면·25659 mm² = 전체 면적의 **7.8 %** 가 계산비만 먹는다. PO 에선 그 7.8 % 가 이중계상 | 묻힌 캐노피는 빼거나 body 와 union. 최소한 phantom3 수준의 선언을 5기체에 남길 것 |
| **I5** | **mini2 body 가 삼각형 1장 빠진 채 출하 중** | 경계 모서리 3개, 구멍 삼각형 면적 4.96e-06 mm². 결정적 증거: mini2 body 면 수 **7757 은 홀수**, 같은 경로의 다른 기체는 전부 짝수(mini5pro 8508 · mavic4pro 7758 · matrice4e 7812). 출처는 `cadkit.Assembly.add()` 의 `m.update_faces(m.nondegenerate_faces())`. **10기체 중 이것 하나뿐**(내가 재확인) | 산란 자체는 0(표면적의 1.4e-8 %). 진짜 피해는 간접 — `is_watertight=False` 라 부피·`contains()` 가 무효가 되고 **내부판정을 쓰는 모든 검사가 이 부품을 조용히 건너뛴다** | 슬리버를 지우지 말고 정점 병합으로 붕괴시키거나, 지운 뒤 다시 닫고 로그를 남길 것 |
| **I6** | **검사를 통과하는 나쁜 메쉬 5종을 지어 시험했다 — 5/5 전부 통과** | D1 80 % 겹친 상자 2개(같은 그룹) 통과 · D2 슬리버 다발 통과 · D3 **단위를 1000 배 틀린 메쉬** 통과 · D4 **좌우 뒤집은 거울상 기체** 통과 · D5 삼각형 1장 구멍 통과. 별건: `cadkit.union_group()` 은 `except Exception: pass`(81-82행)라 불리언 union 이 실패해도 예외도 로그도 없다 | 겹침·단위/치수·좌우 대칭성·구멍 **네 축이 전부 무방비**. D4 는 프롭 회전방향과 피치 부호가 물리적으로 반대인 기체를 통과시킨다 | 치수 검사(bbox·대각·프롭 지름 대 스펙) + 그룹 내 겹침 + 손대칭성 추가, `union_group` 예외를 삼키지 말 것 |
| **I7** | **`PITCH_K` 의 공칭 피치 기준 반경이 0.5R — 실물 DJI 는 표준 규약대로 0.75R** | 국소 기하피치 `k(r)` — DJI: 0.50R 0.929 / **0.75R 1.015(최대)** / 0.90R 0.949. 우리(Solo 유래): 0.50R **1.000(기준)** / 0.70R 1.109(최대) / 0.90R 0.748. 외곽(0.6~0.9R) 피치각 폭 DJI **5.5°** ↔ 우리 **9.2°** = 1.7 배 | ⚠**추정이지 계산 아님**: 폭이 넓으면 반경마다 다른 회전각에서 번쩍여 플래시가 번지고 봉우리가 낮아진다(어림 −2.2 dB). 커널 민감도로는 피치×1.25 가 +0.15~+0.82 dB 라 **어림이 과했을 가능성이 크다** | `PITCH_K` 를 DJI 실측 `k` 로 교체(원장 G 절). «공칭 피치는 0.75R 에서 정의» 라는 프로펠러 공학 표준과도 그때 맞는다. **확정 전 플래시 폭을 커널로 직접 잴 것** |
| | ⛔**[정정 2026-08-16 §⑥ — 게이트 이행됨, 결과는 반증]** 요구한 대로 **커널로 쟀다**(두 라운드 독립). ① **«봉우리 −2.2 dB» 는 죽었다** — 판 교체의 봉우리 변화는 **≤0.10 dB**(앙각 0~−90 16점·모노/바이스태틱·3.5/10 GHz 전부). 감사 어림의 **1/22**. ② **부호까지 반대다** — 비틀림 기울기만 바꾼 통제 가족에서 3.5 GHz el −30 회귀가 **+0.060 dB/deg**(폭이 넓을수록 봉우리가 **올라간다**). 감사가 나란히 적은 «피치 ×1.25 → +0.49 dB» 는 외곽 폭을 9.27→11.27° **넓히는** 섭동이라, 두 수는 «5배 어긋난» 게 아니라 **서로 부호가 반대**였다. ③ 기전이 안 무는 이유: 플래시 폭이 비틀림이 아니라 **회절**로 정해진다(3.5 GHz matrice4e 폭 9.8~18.7° ↔ λ/2R = 17.9°, 날 반지름 1.60 λ). ⭐**틀린 물리가 아니라 틀린 영역**이다 — 30 GHz 에서는 비틀림 2배가 봉우리를 +9.98 dB 움직인다. ④ «0.75R 이 표준» 은 문헌으로 **살아남았지만** 모집단이 이 축을 죽인다 — 실물 132종의 P(0.5R)/P(0.75R) 중앙값 **0.996**(이름표 문제이지 형상 오차가 아니다), 우리 외곽 폭 7.73~9.41° 는 실물 밴드(중앙값 6.72·p95 9.46·최대 10.74°) **안**이고 DJI 의 5.5° 가 오히려 낮은 쪽이다. ⇒ 남는 정직한 근거는 **«공칭 피치(0.75R 표기 관례)를 0.5R 표에 먹여 날 전체가 +6.1 % 과피치»** 뿐이고 값은 **≈0.13 dB** 다. ⭐**판정: 멈춘다**(무해하지만 근거 없음) | |
| **I8** | **팁이 뾰족하다 — 실물은 뭉툭하고, f_tip 밴드 면적이 거기서 나온다** | `CHORD_FRAC` 이 0.95R 0.352 → 1.00R **0.10** 으로 급락. `c/R @0.98R` DJI 0.1028 ↔ 우리 0.0609, `@0.99R` 0.0924 ↔ 0.0476. 팁 밴드(0.90~0.96R) 면적비 **0.700**, 두께까지 넣으면 net −5.08 dB. matrice4e 처럼 총면적이 상쇄되는 기종도 0.80~0.96 밴드는 −0.35 dB | **σ 로는 소액**(C5 의 포화 논리가 여기도 적용된다). 그러나 **마이크로도플러 스펙트럼 축에서는 별개** — 회전 날의 스펙트럼 포락선은 대략 시위분포를 주파수축에 옮긴 것이라(f ∝ r) f_tip 근방 세기를 이 밴드가 정한다. ⚠**커널로 안 쟀다** | `CHORD_FRAC` 끝값 0.10 → 0.20 근처. 로프트 마지막 두 단면 간격(0.9557→1.000)도 좁힐 것 |
| | ⛔**[정정 2026-08-16 §⑥ — 4건]** ① **«−3.10 dB»·«net −5.08 dB» 는 죽었다** — 20·log10(0.700) = −3.098, 즉 **C5 가 «과대 번역» 이라 반증하고 «면적비 대신 커널 측정치를 쓰라» 고 수리안까지 적은 바로 그 자**다. I8 이 같은 표에서 그 규칙을 어겼다. ② **«f_tip 세기를 이 밴드가 정한다» 는 정의상 거짓** — `f_tip = 2·v_tip·cos(el)/λ` 는 **순수 운동학**이라 시위가 들어갈 자리가 문자 그대로 없고, 관측판 `fd_edge_hz` 도 안 움직인다. ③ **«스펙트럼 포락선 ≈ 시위분포를 주파수축에 옮긴 것(f ∝ r)» 도 거짓** — 실제로는 야코비–앵거의 **아벨형 적분변환**이다. 결정적 반증: 날의 **바깥 10 %(r>0.9R)만 남겨도** 그 조각 전력의 **37~40 %** 가 여전히 f_tip/2 **아래**에 떨어진다. ④ 실측 이득은 f_tip 근방 **+0.14~+0.39 dB** · 총 AC +0.01~+0.02 dB. **팁 밴드가 특별히 나쁘지도 않다**(0.90–0.96R 0.69 는 0.60–0.96R 0.66 보다 **덜** 부족). ✅**«우리 팁이 DJI 계열보다 좁다» 는 실물 4종(GLB + 제품사진 3 + FCC 실물사진)이 독립 지지 — 살아남았다.** ⛔단 **끝값 0.20 은 자에 끌려다니는 수**다(밴드 폭 ±0.06~0.50 mm 에서 0.114~0.341, 감사 자신의 측정은 1.00R 에서 **0.078** — 권고가 자기 측정과 2.5 배 어긋난다). 게다가 빈틈의 **8 %만** 메운다. ⭐**옳은 수리는 끝값이 아니라 마디**: 0.96/0.97/0.98/0.99 = 0.445/0.422/0.402/0.370(빈틈의 86 % 가 메워진다), 1.00R 은 «메쉬 마감 규약» 이라고 적을 것. 그리고 «실물은 뭉툭하다» 는 **DJI 계열 한정**이다 — 우리 c/R@0.98R 은 실측 132종 중 34 백분위(평범), DJI 의 0.1028 이 오히려 p80 | |
| **I9** | **메쉬 부피×밀도 질량이 공표 이륙중량의 1.1~5.2 배** | mavic4pro 1063→5503 g(+418 %) · mini5pro 250→1255 · typhoonh480 1950→7350 · matrice4e 1219→4000 · m350rtk 6470→20294 g. **6기체에서 body 하나가 기체 전체 무게를 넘는다.** 원인: 메쉬 body 가 속 꽉 찬 솔리드인데 `DENSITY['body']=1150` 은 ABS 벌크 밀도 — 주석은 «속이 빈 껍데기라 실효 밀도는 낮다» 고 적고 정작 벌크 값을 쓴다 | **RF 영향 없음**(PO/SBR 은 표면만 본다 — 확인됨). 그러나 `gazebo_export` 는 총질량만 TOW 로 정규화하므로 **질량 배분과 관성텐서는 틀린 채 남는다**(body 가 40~54 %). ⭐관성이 틀리면 자세 응답이 틀리고, 그것이 **이번 라운드의 다른 축인 로터 회전수 요동 모델로 내려온다**. typhoonh480 은 질량중심이 z −103.8 mm 로 암 평면보다 10 cm 아래 | body/canopy 를 벽두께 등가 실효 밀도로 낮추거나 셸을 껍데기로 모델링. 최소한 arm 처럼 선언은 남길 것 |
| **I10** | **문서·주석 4건이 사실과 다르다 (낡음 또는 계산 오류)** | ① `drones.py` matrice4e note «−0.36 %» 는 **모터 벨 앞끝까지만** 잰 값 — 실제 프레임 길이는 331.43 mm = 공표 307 대비 **+7.96 %**(사진검사로도 +4.9 %) ② typhoonh480 note «+1.9 %» — 현재 bbox 는 452.17×517.00 = **−1.06 %/−0.58 %**, **부호까지 반대** ③ ~~mini5pro 가 버린 `255×181×91` 을 복원하라~~ **[고침 2026-08-16 적대검증 — 이 항목은 틀렸다]** 저장소 자신의 `docs/drone_specs_2026.json` 이 이미 이 수를 조사해 기각해 놓았다: DJI 는 Mini 5 Pro 의 **언폴드(프롭 제외)** 치수를 공개하지 않고(스펙 페이지 전문 검색으로 확인, `unfolded_*` = null), `255×181×91` 은 Heliguy·drdrone 등 **2차 매체**가 «unfolded **with** propellers» 로 실은 값이며, 폭 181 mm 는 152.4 mm 프롭 4장이 물리적으로 못 들어가서(좌우 모터 간격 28.6 mm 가 된다) **오류로 판정**됐다 — 같은 파일의 별도 «무고 판정» 이 그 기각을 재확인한다. 저장소에 DJI Mini 5 Pro PDF 는 **없다**. 게다가 «유일한 비순환 외부 검사» 도 아니다: 우리 프레임 bbox 는 **180.26 × 256.19 mm**(적대검증 실측)라 255×181 에 0.5 % 로 붙지만, 그것은 공식 «펼침(프롭 포함) 304×380» 에서 프롭 지름 152.4 를 빼고 모터 포드 **28.6 mm** 를 더하면 그대로 나오는 수다(두 축에서 28.66·28.59 로 자기일관). 즉 프레임이 **이미 맞춰져 있는 공식 봉투의 함수**이지 독립 정보가 아니다. 원래 note 의 «DJI 미공개» 는 **대각(250 mm)** 에 붙은 말이고, note 는 정작 «OFFICIAL envelope (255×181×91)… the envelope now rules» 라고 적어 이 수를 **쓰고 있다** — 감사가 note 를 오독했다 ④ `az_falsify_ours.py` «시선에 수직인 삼각형 106장·139 cm²» — 선택 조건이 `\|n·u\| ≥ cos(tol)` 라 **뒷면까지 센다**. 앞면만 69.67 cm²(정확히 1/2), 가림 반영 47.15, 전부 반영 35.23 | ④는 절대 σ 를 **−6.02 ~ −9.41 dB** 움직인다(`σ_max −4.78 → −10.8~−14.2 dBsm`). az45 대비 낙차 −57 dB 는 비율이라 살아남는다. ①②④ 는 인용하면 결론이 뒤집히는 문장들 — **①②④ 는 적대검증에서 소수점까지 재현됐다**(331.43 / 452.17×517.00 / 106장·139.35 cm²→앞면 90장·69.67 cm²). **③ 은 반증됐다**(위 [고침]) | ①②④ 를 현재 상태로 정정. ④는 앞면·가림 반영판으로 절대치 재작성. **③ 은 정정하지 말 것 — 기각 상태가 옳다** |
| **I11** | **날이 강체가 아니다 — 플래시가 가장 센 고각이 5~9° 밀린다** | matrice4e 호버 3800 rpm, 날 1장 5.8 g·`r_cg` 55.9 mm. 경첩 코닝 β 2.29°(팁 5.46 mm), 외팔보 처짐 E=5~15 GPa 에서 3.17~9.51 mm → 채택 브래킷 **δ_tip 3~5.5 mm(λ/25~λ/14)**, 면 기울기 2.5~4.6°, **정반사 방향 이동 5.0~9.2°**. 원심 신장은 λ/1300~λ/3900 로 무시 가능 | 고정 고각의 σ 는 ~0.5~1.5 dB 지만 **가장 센 플래시 고각이 밀린다.** 추력에 따라 변하므로 호버↔기동에서 달라진다 — 강체 메쉬로는 못 낸다. `rotor_dynamics.py:57` 의 «선례 0 건» 은 «크기 작음» 의 근거가 아니다 | 지금 고치지 말 것(별도 축). **선언**으로 남기고, 고각 스윕 결론에 «강체 가정» 단서를 붙일 것 |

### 사소 — 방향은 맞고 크기가 작거나, 기록만의 문제

| # | 발견 | 근거 수치 | 영향 | 수리안 |
|---|---|---|---|---|
| **m1** | 정본 1.43 mm 는 **메쉬를 잰 값이 아니라 상수에서 유도한 값** | `benchmark/material_sources.py :: blade_thickness_stats()` 가 메쉬를 안 열고 계산 — matrice4e 1.4302 mm 정확 재현. 메쉬를 직접 재면 **1.4559 mm(+1.8 %)**. 편향 둘: 스윕디스크 정규화 −0.679 % 누락 + NACA-4 시위평균/최대 비 0.684879 는 **대칭 익형** 값인데 캠버 얹은 실측 비는 0.695 | \|Γ\| 로 **+0.13 dB** — 무해. 다만 «우리 메쉬 자신의 두께» 라는 문면이 실제로는 «법칙의 해석 근사» | 메쉬 실측판으로 교체(권장) 또는 두 편향을 주석·원장에 명시. 값은 그대로 둬도 된다 |
| | ⛔**[정정 2026-08-16 §⑥]** «상수 유도값» 이라는 **전반부는 살아남았다**(소스 확인 — 함수가 상수만 import 하고 메쉬를 한 번도 안 연다). ⛔**후반부 «메쉬 실측 1.456 mm(+1.8 %)» 는 확정값이 아니다** — 단면을 안 쓰는 **부피 항등식** t = V/∫c dr 로 재면 **1.3961 mm(−2.4 %)** 로 **부호가 뒤집힌다**. 저장소 안에 세 번째 값도 이미 있다(`material_sources.py` 주석 **1.488, +4.1 %**) ⇒ «우리 메쉬 두께» 가 **1.396~1.488 로 6.6 % 벌어져 있다**. 원인은 정의다 — 캠버 단면에서 «캘리퍼 시위선 기준 상하면 차» 는 캠버 부풂을 두께로 세고, 시위 양끝을 자르면(x/c 0.03~0.97) +4.62 % 높아진다. ⇒ **«메쉬 실측판으로 교체(권장)» 는 멈춘다** — 그대로 따르면 값을 **어느 쪽으로 옮길지조차 정해지지 않는다**. 교체하려면 **두께의 정의를 먼저 못박을 것**(슬래브 반사에 쓰려면 **단면적÷시위**가 맞는 잣대다). 영향은 어느 쪽이든 ≤0.15 dB 라 급하지 않다 | |
| **m2** | 퇴화면 잣대가 **절대** 면적 1e-14 m² 라 슬리버를 하나도 못 잡는다 | 1e-14 에 걸리는 면 전 기체 0개. 상대 잣대(최소 내각 <0.1°)로는 mini2 49 ~ s1000plus 142개. 최소 내각 0.0002~0.0032°, 종횡비 100 초과 면이 기체당 225~702개 | 면적 비중 0.0001~0.03 % 라 σ 에는 무해(확인됨). 법선이 수치적으로 불안정한데 조명판정이 `n̂·û>0` 인 점이 걸린다 — ⚠**부호가 실제로 흔들리는지는 안 쟀다(추측)**. 그리고 이 슬리버들이 I5 의 구멍을 만든 그 부류다 | 잣대를 상대값으로(최소 내각 <0.5° 또는 종횡비 >100). 절대 면적은 기체 크기에 따라 뜻이 달라진다 |
| **m3** | 설정 파라미터와 실현 형상이 다르다 (t/c +19~21 %, 캠버 +29~46 %) | 실현 t/c 6.3~11.5 %(설정 `TC_TIP/TC_ROOT` 6.5~9.5 %), 캠버 6.4~7.3 %(설정 `CAMBER_M` 5.0 %). 원인은 캠버 — 캠버 익형은 시위좌표계 상하면 차가 NACA `t` 파라미터보다 커진다. 실현 최대 t/c 11.3~11.5 % 는 소비자 참조 둘(1345 9.3 · Solo 8.2 %)보다 여전히 두껍다(Typhoon 밴드 8.6~12.8 % 안에는 든다) | 버그는 아니다. **파라미터와 실현값이 다르다는 사실이 어디에도 안 적혀 있는 것**이 문제 | 두 값을 모두 문서에 박고, 실현값을 참조 밴드와 비교하는 회귀 검사를 둘 것 |
| **m4** | x500v2 accent 와 arm 이 정점 96개를 비트 동일하게 공유하고 표면이 겹친다 | 웰딩 후 비다양체 모서리 172개(z=4.0 mm 부근). accent 면적의 10.7 %(9451 mm²)가 arm 솔리드 안, 6.1 %(5409 mm²)가 arm 표면 위 1 µm 이내. **다른 9기체는 비다양체 모서리 0** | 동일평면 5409 mm² 는 z-fighting 후보 — 광선이 어느 면을 먼저 맞을지가 반올림으로 갈린다. 두 그룹 재질이 다르므로(accent plastic \|Γ\|0.28, arm carbon 0.90) **같은 자리에서 재질이 뒤집힐 수 있다.** ⚠σ 로 얼마인지는 안 쟀다(추측: 면적 비중 1.2 % 라 작을 것) | accent 를 arm 과 union 하거나 두 표면을 0.1 mm 이상 띄울 것 |
| **m5** | 프롭 삼각형 크기가 λ 로 묶여 있지 않다 | 3.5 GHz(λ=85.7 mm) 최장 모서리: mini2 λ/16.9 ~ **m350rtk λ/3.8**. 프롭 면 수가 기체 크기와 무관하게 고정돼 큰 기체일수록 성기다. **[고침 2026-08-16] «전 기체 12752» 는 부정확** — 적대검증 실측: 4로터 7기종 12752 · mini2 12736 · typhoonh480(6로터) 19152 · s1000plus(8로터) 25600. 고정된 것은 총량이 아니라 **로터 1개당 3184~3200 장**이고, 그동안 프롭 지름은 119→533 mm 로 4.5 배 변한다 — 결함의 실체는 그대로다 | PO 면적분은 삼각형 안 위상을 상수로 본다 — 최악 λ/3.8. SBR 은 광선 간격 λ/12 라 자유롭다. ⚠**얼마나 틀리는지는 안 쟀다.** 4 GHz 수렴시험은 «≤0.06 dB» 였으나 그건 matrice4e 급 | 면 수가 아니라 **모서리 길이**를 λ 로 묶을 것(`n_sec/n_pts` 를 R/λ 에 비례) |
| **m6** | `uv_sphere` 극점 수정은 유지됐으나 중복 정점 178~358개가 남고 인덱스 기준 비수밀 | 면적 0 삼각형 **0개**(2026-07-01 수정 유효). 다만 극점이 seg 개씩 겹쳐 중복 정점 seg180 358 · seg120 238 · seg90 178 · 기본 34개, `process=False` 로는 전부 수밀 False. 구 부피 해석값 대비 −0.051 %(seg180) ~ −4.416 %(기본) | RF 영향 없음(PO 는 면 중심·법선·면적만, Mitsuba 는 삼각형 수프). 다만 내보낸 OBJ 가 유효한 솔리드가 아니고, `mesh_check` 는 웰딩해 보므로 **이 사실을 영원히 보고하지 않는다** | 극점을 정점 1개로 공유. 별건: `src/geom.py:328` 자체점검이 존재하지 않는 `blade()` 를 불러 `python src/geom.py` 가 NameError 로 죽는다 — 지울 것 |
| **m7** | 재질 판정 헤드라인이 **0.9 mm** 에서 나왔는데 정본은 1.43 mm | 슬래브 각도평균 0.9 mm −19.85 ↔ 1.43 mm −16.59 = **3.26 dB**. 문서 스스로 «정본 1.43 mm 프롭 점은 미착지»(`MATERIAL_CORRECTION.md:858`) | ④절에서 별도로 다룬다 — **단순한 과대평가가 아니다** | ④절 참조 |
| **m8** | `outputs/reference_props.json` meta.method 가 실제 코드와 다르다 | meta 는 «스팬축 수직 평면 절단» 이라 적었는데 `measure_reference_props.py::_cyl_sections` 는 **원통 단면**이고 그 docstring 이 평면절단을 못 쓰는 이유를 명시한다. **값은 옳다**(독립 재측정으로 확인) — 틀린 건 기록 | 없음 | meta.method 문면 정정 |

### 판정 보류 — 모른다

| # | 항목 | 왜 보류인가 |
|---|---|---|
| **?1** | 익형 단면 **최대두께 위치** | 0.70R 에서 `t_peak x/c` 가 DJI 0.735 · 우리 0.289 로 나왔으나, 최대캘리퍼 시위선은 **방향 부호가 임의**라 `x/c` 와 `1−x/c` 가 구별되지 않는다 — 0.735 ≡ 0.265 이므로 우리 0.289 와 사실상 같다. **«차이 있다» 고 보고하면 틀린 보고다.** 앞전을 곡률 최대점으로 따로 찾는 판별이 필요. 3.5 GHz 에서 λ/200 급이라 RF 영향은 없다 |
| **?2** | **matrice4e 실물 프롭 두께** | 실측이 저장소에 **없다**. 두 추정이 0.99 mm(1157F 사진 시위 × Mini2 t/c)와 1.40 mm(Mini2 반경 환산)로 **1.41 배** 벌어지고 그 배수는 정확히 시위비다. `matrice4-M4T_v2.step` 에는 **프로펠러가 없다**(bbox 387.51 ↔ DJI «프롭 제외» 387.5, +0.003 %) ⇒ 실증 표적의 날 형상은 **1차 출처가 0** |
| **?3** | matrice4e·mavic4pro 가 **표준 프롭인가 저소음 프롭인가** | 실측 면적이 2411 vs 3014 mm² = **+25 %** 차이인데 저장소에 기록이 없다 |
| **?4** | 큰 프롭이 작은 프롭과 **기하학적으로 닮았는가** | 이 가정이 ?2 의 두 갈래를 가른다. `c_max/R` 실측이 크기와 반비례(0.262@4.7in → 0.177@9in)하므로 **닮지 않았을 가능성이 크지만**, 두께 축은 확인 못 했다 |

---

## ③ 확인됨 — 검사했고 문제 없었다

무엇을 **안 고쳐도 되는지**가 무엇을 고쳐야 하는지만큼 중요하다. 아래는 전부 이번에 실제로 재본 것이다.

**메쉬 무결성**
- **자기교차 전 기체 0 건.** AABB 광역탐색 → 정점 공유 쌍 제외 → 모서리-삼각형 관통(Möller–Trumbore) 양방향. **양성 대조 4종**(교차 삼각형 1 · 관통 삼각형 1 · 회전 상자쌍 18 hits · 구쌍 84 hits) 통과, 음성 대조 5종 전부 0. ⇒ cadkit 정리 파이프라인과 manifold 불리언 union 이 실제로 잘 돈다. 한계: 완전 동일평면 겹침은 이 방식으로 못 본다(그 부류는 m4 에서 따로 잡았다).
- **프롭 어셈블리 10기종 전부 수밀 · 중복면 0 · 퇴화면 0.** 3472~3488 삼각형.
- **안쪽 법선 수정(2026-07-01)은 살아 있다.** trimesh 수리를 **전혀 거치지 않고** 출하 인덱스에서 손으로 부호부피를 계산 — 전 10기체 프롭 부품의 최소 부호부피가 전부 양수(mini2 984 ~ m350rtk 88431 mm³), winding 일관. 나아가 **전 기체·전 그룹·전 연결요소에서 부호부피 ≤0 인 부품이 하나도 없다.** ⇒ 조명판정 `n̂·û>0` 이 오염되지 않는다.
- **`uv_sphere` 극점 수정 유효** — 실제 쓰이는 전 테셀레이션에서 면적 0 삼각형 0개.

**치수·좌표**
- **z-up · 단위 m.** 로터 대각 오차: mavic4pro·typhoonh480·x500v2·phantom3·m350rtk **+0.00 %** · matrice4e +0.01 · mini2 +0.02 · s1000plus −0.14 · phantom4 +1.98 %. mini5pro −9.72 % 는 spec note 가 «diagonal_mm 275 는 로터 위치를 정하지 않는다» 고 이미 선언한 값이라 결함이 아니다.
- **프롭 스윕 지름 전 10기체 −0.000 %** — `build_propeller_cad` 의 스윕디스크 정규화(배율 0.99321, 8기종 동일)가 정확히 작동한다.
- **날개 수 2장 = 실물 2장**(mini2·mavic4pro·matrice4e·phantom4·s1000plus).
- **CW/CCW 거울상**: `drones.build_propeller` 가 `mirror=True` 로 반대회전 프롭을 만든다(2026-07-28 이름 뒤바뀜도 이미 수정됨).

**형상 축 중 괜찮은 것**
- **로프트 이산화**: 설계평면 절단 대조로 시위 오차 평균 −1.22 % · 두께 오차 +0.47 %. 최악 −6.5 % 는 0.94~0.95R 한 곳뿐.
- **캠버 피크 위치** `x/c 0.50` ↔ 참조 0.49/0.52/0.51. **트위스트 총량** 13.3~24.2° ↔ 참조 밴드 17.2/20.2/29.6° 안. **캠버 크기** 우리 4.4~4.6 % ↔ DJI 3.2~4.4 % — 약간 과하나 3.5 GHz 에서 곡률반경이 λ 급이라 방위평균 1 dB 미만.
- **곡면 매끈함**: sagitta(현과 호의 최대 간격) 모터 벨 0.046 · 프롭 허브 0.073 · 암 튜브 0.068 · 짐벌 볼 0.062 mm ↔ 기준 λ/16 = 4.69 mm. 패싯 폭 2.09~3.14 mm = λ/24~λ/36 ⇒ **가짜 글린트 안 생김**.
- **굵은 상자(battery·pcb, 최장 157 mm = 2.1 λ)**: `rcs_po.mesh_to_points` 가 λ/11 로 재분할하고(`rcs_po.py:127`) Sionna 는 광선추적이라 평면은 평면 ⇒ 위상 표본 문제 아님. 수렴시험이 확증(≤0.06 dB).
- **인공물 팁 캡**(로프트 마감면): 면적 ~7 mm² → 판 정반사 상한 −70 dBsm, 프롭 플래시(−33 dBsm)보다 **37 dB 아래**.
- **실물 표면거칠기**: 사출 플라스틱 Ra ~1 µm ≪ 레일리 기준 λ/(8cosθ)=9.4 mm ⇒ 실물도 전파적으로는 거울면. 우리가 매끈하게 지은 것이 맞다.

**감사 파이프라인 자체의 검증** (이게 없으면 위 수치를 믿을 이유가 없다)
- 새로 짠 측정 파이프라인이 **기존 `outputs/reference_props.json` 3종을 소수 4자리까지 재현**한다: 1345 dia 346.66 · `c_max/R` 0.2253@0.300 / Solo 253.82 · 0.2726@0.275 / Yuneec 230.10 · 0.1769@0.450. **자를 검증한 뒤에야** 새 대상에 댔다.
- 슬래브 계산은 ITU-R P.2040 식 43·44 독립 구현이 `outputs/slab_thickness_check.json` 과 수직 \|R\| 0.10 dB · 1↔3 mm 폭 0.000 dB 이내로 일치.
- 감사 대상이 실제 출하 경로인지 확인: `drones.build_propeller → drone_cad.build_propeller_cad(n_sec=20)` 가 `*__prop.obj`(4로터 12192 삼각형)를 낸다. 측정한 `n_sec=22` 판은 같은 법칙·같은 형상.

**메쉬 미세정밀화는 권하지 않는다** — 로프트 이산화가 이미 −1.2 % 이고 수렴시험이 3,476→23,468 면에서 ≤0.04 dB 다. **병목은 삼각형 수가 아니라 법칙(시위·피치·팁)과 두께 규약이다.**

---

## ④ ⭐ 프롭 형상이 재질 판정에 미치는 영향

재질 판정(`outputs/material_verdict_0816.json`)의 헤드라인은 «진짜 손잡이는 프롭이다 — 0.9 mm 에서 AC −16.99 dB · 확산바닥 −12.56 dB · 빗살 −6.98 dB, 셸은 AC ±0.00» 이었다. 이 절은 그 판정이 **프롭 메쉬 오차에 얼마나 흔들리는가**를 따진다.

### 4-1. 먼저 판정의 프레임을 좁혀야 한다 — «프롭이 지배» 는 AC 채널 한정이다

우리 PO 커널로 matrice4e 를 부품별로 분해했다(가림 없음, 방위평균 σ):

| | 전체 | 프롭 | body | camera | motor | battery | canopy |
|---|---|---|---|---|---|---|---|
| el −30 | −21.1 | **−37.3** | −31.1 | −26.9 | −28.1 | −28.4 | −33.8 |
| el 0 | −14.9 | **−43.0** | −26.2 | −22.1 | — | −18.4 | — |

프롭 비중은 **el −30 에서 2.4 %(16.2 dB 아래), el 0 에서 0.2 %(28.1 dB 아래)** 다.
⇒ **총 σ 로는 프롭이 동체보다 한참 아래다.** «프롭이 표적 축을 혼자 진다» 는 **DC 를 걷어낸 AC(변조) 채널에서만** 참이다.

그리고 그것은 우연이 아니라 **구조적 필연**이다: 헤드라인 팔은 `max_depth=1`·굴절/회절 끔이고(`elevation_sweep_md.py:342-344`), `FastPoser` 는 몸체 자세를 생성 시점에 고정하고 로터 위상만 돌린다(`articulated_fast.py:88-90`). 정지 부품의 기여는 순수 DC 이므로 DC 제거 후 AC 에는 **정의상** 프롭만 남는다. 셸 재질을 어떻게 바꿔도 AC 는 못 움직인다(관측치가 정확히 그렇다: 셸 0.5/0.75/1.5 mm 에서 `d_ac` 0.003/0.002/0.010 dB). **깨지는 조건은 `max_depth≥2` 또는 몸체가 움직이는 자세열이다.**

> 리포트 문면 권고: «프롭이 지배한다» 대신 **«움직이는 성분(AC)은 정의상 프롭만 남는다 — 총 반사로는 동체가 16~28 dB 더 세다»** 로 쓸 것.

### 4-2. 형상 오차의 대가는 «1~2 dB 축» 이다 (우리 자신의 −3 dB 번역을 반증)

실물 DJI 평면형으로 프롭을 다시 지어(matrice4e 크기, 표면적 +19.5 %) PO 커널로 직접 쟀다:

| 바꾼 것 | 모노 β=0 | 바이 β=81° | 바이 β=120° |
|---|---|---|---|
| **DJI 실물 법칙으로 교체** | **+0.24 dB** | **+0.87** | **+1.29** |
| 시위 ×0.7 (통제) | −1.17 | −1.98 | −2.50 |
| 시위 ×1.4 (통제) | −0.03 | +1.34 | +2.21 |
| 피치 ×1.25 (통제) | +0.82 | +0.52 | +0.15 |

세 가지가 나온다.
1. **형상 축 전체의 정직한 폭은 ±2.5 dB**, 우리 법칙을 실물로 고치면 **+0.2~+1.3 dB** 다.
2. **포화가 있다.** el −30 에서 시위를 1.4 배로 키워도 σ 는 −0.03 dB — 비틀린 날의 정반사 크기는 **면적이 아니라 정반사 띠(트위스트율)** 가 정한다. 면적비를 평판식 `σ=4πA²/λ²` 에 그대로 넣어 얻은 «−2.97 dB» 는 **이 포화를 무시한 과대 번역이었다.** (프롭 형상 렌즈의 자기 반증.)
3. ⚠**바이스태틱에서 형상 민감도가 커진다.** 헤드라인 팔은 바이스태틱이다(원장 `f_tip` 1102.4 Hz ÷ 모노 예상 1453.8 Hz = 0.758 = cos(β/2) → β≈81°). «형상은 거의 안 먹는다» 는 **모노 한정 결론**이었고, β=120° 에서는 시위 오차가 ±2.5 dB 까지 벌어진다.

이산화 인공물이 아님을 확인했다: 점간격 λ/7→λ/24 에서 σ 변화 ≤0.06 dB, 로프트 3,476→23,468 면에서 ≤0.04 dB.

### 4-3. 두께 축은 13~17 dB 축이다 — 우선순위가 여기서 갈린다

재질 판정 자체가 슬래브 두께 손잡이를 100 mm → 0.9 mm 로 내리면서 **−11.4~−16.7 dB** 를 움직였다. 형상은 1~2 dB, 두께 규약은 13~17 dB. **같은 라운드에 둘 다 고칠 수 없다면 두께가 먼저다.**

그런데 두께 축에서 두 렌즈가 정반대 방향의 사실을 하나씩 들고 왔고, 그걸 합치면 **판정의 프레임이 바뀐다**:

| 잣대 | 값 |
|---|---|
| 재질 판정이 쓴 프롭 손잡이 | **0.9 mm** |
| 문서가 «정본» 이라 부르는 값 (matrice4e, 해석식) | 1.43 mm |
| 그 정본을 메쉬에서 직접 재면 | 1.456 mm |
| **DJI Mini 2 실물 프롭의 면적가중 평균 두께** | **0.876 mm** |
| matrice4e 실물 프롭 | **실측 없음** — 0.99~1.40 mm 로만 묶임 (?2) |

⭐**여기가 핵심이다.** 프롭 형상 렌즈는 «0.9 mm 는 정본 1.43 mm 보다 3.26 dB 낮으니 억제를 과대평가한 것» 이라고 읽었다. 그러나 치수 앵커 렌즈는 **0.9 mm 가 DJI 실물의 면적가중 평균 0.876 mm 와 2.7 % 안에서 일치**한다는 것을 독립적으로 찾아냈다.

두 사실이 모순이 아니다. **«정본» 1.43 mm 는 실물 측정이 아니라 우리 자신의 법칙(뿌리가 너무 두꺼운 Solo 유래)에서 해석적으로 유도한 값**이기 때문이다. 즉 —

> **1.43 mm 를 «참값», 0.9 mm 를 «민감도 점» 이라 부른 것은 실물 충실도 기준으로는 순서가 거꾸로일 수 있다.**
> Mini 2 급 프롭에 대해서는 **0.9 mm 가 오히려 잘 앵커된 값**이고, 1.43 mm 는 우리 메쉬의 내부값이다.

⛔**[정정 2026-08-16 §⑥ — 이 재프레이밍은 무너졌다]**
«0.9 mm 가 DJI 실물 0.876 mm 와 2.7 % 안에서 일치» 는 **잣대를 섞은 비교**다. 0.876 은 **최대두께 계열**이고(독립 재측정의 최대캘리퍼 0.918 의 95 %), 같은 날을 슬래브가 요구하는
**부피보존 두께**(단면적÷시위)로 재면 실물은 **0.468 mm** 다 — 시위평균 계열 두 자(0.468 · 0.495)는 0.876 근처에도 없다. 얇은 판의 산란장은 `E ∝ (εr−1)·k0²·∫d dA` 라 등가 두께는
**부피÷평면형면적**이지 최대두께가 아니다. 그 자로 견주면 **0.9 mm 는 Mini 급 실물보다 +3.5~+5.7 dB 밝다.**
⇒ 감사가 C6 에서 스스로 적발한 «시위평균 ↔ 최대두께» 범주 오류를 §4-3 이 되풀이했다. **0.9 mm 도 1.43 mm 도 Mini 급 실물보다 두껍고, 1.43 이 더 두꺼울 뿐이다** — 순서는 거꾸로가 아니다.
⚠덧붙여 **0.876 mm 자체가 판정 불가**다: GLB 날은 스팬 전체(r 5.7→59.7 mm)를 따라 **열린 경계**가 있어(경계 루프 4 · 경계 모서리 245) `fill_holes` 로도 8장 전부 안 닫히고,
«메운 부피» 가 같은 부품인 날 8장에서 **23 % 벌어진다**. 부록 C 의 «GLB 두께는 ±10 % 로 읽어라» 가 옳았고, 그 폭이 이 절의 결론을 좌우할 만큼 크다.
⇒ **§7 요약표의 «Mini 2 급에서는 0.9 mm 가 실물에 더 가깝다» 와 §4-4 의 해당 판정도 같이 고칠 것.**

다만 **크기 급이 다르다는 함정**이 있다 — 0.876 mm 는 Mini 2(지름 119 mm)의 실물이고, 헤드라인 팔은 matrice4e(지름 274 mm)였다. matrice4e 실물은 안 재봤다. 슬래브 표를 로그보간해 정직하게 적으면:

| 두께 | 각도평균 \|R\| | 0.9 mm 팔 대비 |
|---|---|---|
| 0.876 mm (Mini2 **실물**) | −20.05 dB | −0.20 |
| **0.9 mm (헤드라인 팔)** | −19.85 | 0.00 |
| 0.99 mm (M4E 실물 하한 추정) | −19.18 | +0.67 |
| 1.40 mm (M4E 실물 상한 추정) | −16.74 | +3.11 |
| 1.43 mm (문서 «정본») | −16.59 | +3.26 |
| 1.456 mm (우리 메쉬 실측) | −16.47 | +3.38 |

⇒ **matrice4e 에서는 0.9 mm 팔이 실물 추정 구간(0.99~1.40 mm)보다 0.7~3.1 dB 아래**에 있다. 즉 그 기체에 대해서는 억제를 **과대**평가한 것이 맞다. **Mini 2·Mini 5 Pro 급에서는 반대로 0.9 mm 가 거의 정확하다.** 하나의 스칼라로 함대 전체를 덮으려 한 것이 문제의 뿌리다(I1).

**팁 구간은 따로 나쁘다**: r/R 0.8~1.0 에서 실물 0.604 · 우리 메쉬 0.443 mm 이므로, 0.9 mm 슬래브는 그 구간을 **실물 대비 +3.5 dB · 우리 메쉬 대비 +6.1 dB 과대반사**시킨다. 그리고 그 구간이 바로 `f_tip` 을 만드는 구간이다.

### 4-4. 그래서 재질 판정 중 무엇이 흔들리고 무엇이 안 흔들리나

| 판정 항목 | 상태 | 이유 |
|---|---|---|
| 셸 두께는 표적 축에 영향 0 (AC ±0.00) | **안 흔들림 — 구조적 필연** | `max_depth=1` + 몸체 자세 고정 ⇒ 정지 부품은 순수 DC. 메쉬 형상과 무관 |
| 프롭이 AC 채널을 사실상 전부 만든다 | **안 흔들림** | 위와 같은 이유. 단 **«총 σ 지배» 로 확대 해석 금지**(4-1) |
| AC −16.99 dB 가 45° 슬래브 예측(−16.65)과 0.34 dB 차 | **안 흔들림** | 두께 축의 내적 정합성이라 형상 오차와 독립 |
| **«프롭 손잡이는 −16.99 dB 짜리다»** 를 matrice4e 서사에 인용 | **⚠흔들림** | 0.9 mm 는 그 기체 실물 추정보다 0.7~3.1 dB 얇다. **«0.9 mm 민감도 점 기준» 을 반드시 붙일 것** |
| 절대 판독거리 R50 · D2 잔차 +1.96~+7.13 dB | **⚠틀림** | C6 — 범주 오류. 실제 +1.2~+1.7 dB |
| f_tip 밴드 세기 · 빗살 대비 | **⚠미확인** | 팁이 좁고(I8) 얇으며(4-3) 면적이 뿌리로 쏠려 있다(C4). σ 로는 소액이나 **스펙트럼 축은 안 쟀다** |
| 기종 간 σ 비교 · 스케일링 논증 | **⚠흔들림** | I2 — `c_max/R` 오차 부호가 기종별로 뒤집힌다 |

---

## ⑤ 수리 우선순위 — 상류부터

층: **검사기 → 기록 → 메쉬 결함 → 법칙·규약 → 재계산 → 서술**. 아래층을 안 고치고 위층을 고치면 다시 해야 한다.

### 0층 — 검사기 (다른 모든 것을 가로막는다)
검사기가 고쳐지기 전에는 어떤 메쉬 수정도 «되었는지» 확인할 방법이 없다.
1. **C1** `mesh_check.py:53,180` 의 `split(..., repair=False)` 명시 + 경계 모서리 수를 결과에 싣고 0 아니면 실패. *(작다 — 두 줄)*
2. **C2** `assert_ok()` 를 빌드 경로에 실제로 배선. 배선 전까지 리포트 3곳의 «상시 가동/빌드 실패» 문구를 현재 상태로 정정.
3. **I6** 검사 4축 추가 — 치수(bbox·대각·프롭 지름 대 스펙) · 그룹 내 부품 겹침 · 손대칭성 · 상대 잣대 퇴화면(m2). `union_group` 의 `except: pass` 도 로그를 남기게.
4. 회귀 게이트에 **«수리 없는 손계산 부호부피»** 를 넣을 것 — 이 축은 지금 깨끗하고(③) C1 과 무관하게 지킬 수 있다.

### 1층 — 기록 (거짓 문장이 법칙의 근거가 되어 있다)
코드를 고치기 전에 «왜 그 값인가» 를 먼저 바로잡는다. 안 그러면 잘못된 근거 위에 새 값을 얹는다.

5. **C3** «저장소에 DJI 프로펠러 실물 기하는 하나도 없다» 3곳 삭제(`drone_cad.py:67` · `measure_reference_props.py:18,225` · `reference_props.json` meta.warning). 1차 앵커를 DJI Mini 2 공식 CAD 로 선언.
6. **I10** 낡거나 틀린 주석 **3건** 정정 — matrice4e «−0.36 %»→실제 +7.96 %, typhoonh480 «+1.9 %»→−1.1 %, `az_falsify` 뒷면 이중계상. ⛔**[고침 2026-08-16] mini5pro `255×181×91` «복원» 은 취소한다** — 그 수는 2차 매체의 오류이고 저장소가 이미 물리 논거로 기각했다(I10③ 참조). 복원하면 **알려진 오류를 레지스트리에 다시 넣는 것**이 된다.
7. **m8** `reference_props.json` meta.method 문면(평면절단 → 원통단면).
8. **I11 · m3 · m1** 을 **선언**으로 남길 것 — 강체 가정, 파라미터 대 실현값 차이, 정본 1.43 mm 가 해석 근사라는 것.

### 2층 — 메쉬 결함 (형상 법칙을 바꾸기 전에 껍질을 닫는다)
9. **I5** mini2 body 구멍 — 슬리버를 지우지 말고 정점 병합으로 붕괴시킬 것. *(10기체 중 이것 하나뿐)*
    - ✅**[고침 2026-08-16] `MESH_FIX=i5`**(기본 꺼짐). 경계 모서리 3 → 0 · 수밀 0/1 → 1/1 ·
      면 7757 → 7756. 결함이 태어나는 자리는 `Assembly.add()` 가 아니라 **`union_group('body')`** 다
      (불리언 출력의 needle 삼각형을 다음 줄이 지우면서 껍질이 열린다).
      **σ 는 안 바뀐다 — Δ = 1.6e−09 ~ 5.7e−09 dB**(⑨-2). 값어치는 σ 가 아니라 «껍질이 닫혀야
      내부판정이 성립한다» 다: 안 닫으면 매몰면 44.45 % 를 **29.71 %** 로 낮춰 본다.
      ⚠**i4 의 선행 조건**이다 — mini2 에서 i5 없이 i4 를 켜면 `RuntimeError` 로 죽는다.
      원장 `outputs/mesh_layer2_holes_poles_0816.json`.
10. **I4** 묻힌 캐노피 — mavic4pro(100 %, 주력 표적) 포함 5기체. 빼거나 body 와 union.
    - ✅**[고침 2026-08-16] `MESH_FIX=i4`**(기본 꺼짐). 대상은 **5기체가 아니라 셸형 7기체 전부**다
      (phantom4 92.8 · mini2 86.4 · mini5pro 85.1 · typhoonh480 74.7 · matrice4e 69.3 %).
      **물리 판정: 「body 와 합치기」가 옳다** — DJI 매뉴얼 그림과 공식 사진이 몰드 셸 **한 장**을
      보여 주고 배터리는 셸 윤곽에 맞춰 뒤에서 꽂힌다. 셸 안의 두 번째 껍질은 실물에 없다.
      완전 매몰(mavic4pro·phantom3)이면 합집합 결과가 곧 body 라 **캐노피만 빼고 body 는 안 건드린다**.
      σ 대가(방위평균, PO): +0.01~+0.74 dB — 전 기체 **보임 밴드 안**, 1 dB 를 안 넘는다.
      ⭐예측(뺄 면만 마스크)↔실제(다시 지음)가 **≤0.03 dB** 로 맞는다 = 수리가 의도한 것만 했다.
      ⚠mini2 는 I5 가 선행이다(body 가 비수밀이면 불리언이 안 돈다 — 예외로 막아 뒀다).
      원장 `outputs/mesh_layer2_buried_canopy_0816.json`.
11. **m4** x500v2 accent/arm 동일평면 — union 하거나 0.1 mm 띄움.
    - ⚠**[부분 고침 2026-08-16] 두 제안은 둘 다 재 보고 기각했다.** 동일평면의 정체는 z=4 mm 에서
      **나일론 시트 윗면(5,408.5 mm²) ↔ 카본판 아랫면(5,783.9 mm²)** 이 맞닿은 것이고, 제조사 STEP
      (`y_step` 20.0/22.0)이 **틈이 없다**고 말한다. 0.1 mm 띄우면 실물에서 멀어지면서 PO 이중계상은
      **그대로**다(실측 Δσ = 0.0000/0.0016/0.0017 dB). union 하면 재질 경계(10.14 dB)가 사라져
      **없애려는 오차보다 큰 오차**가 생긴다(carbon 으로 −1.20/−0.64/+0.03 · plastic 으로
      +0.68/+1.51/+1.18 dB). ⇒ 접촉면 자체는 실물이고, 결함은 **PO 가 가림을 안 본다**는 커널 축(I3)이다.
    - ✅대신 같은 항목의 **다른 절반**을 고쳤다 — `MESH_FIX=m4`: 나일론 클램프를 **관통**하는 카본
      튜브 14,274 mm² 를 잘라낸다(간섭 제거, 치수 변경 0). 열린 프레임은 `shell_groups=()` 라 SBR 이
      그 면을 영영 못 맞히므로, 수리는 PO 기하를 기본 엔진과 같게 만든다.
      σ 대가 +0.14 / **+0.98** / +0.53 dB.
12. **m6** `uv_sphere` 극점 정점 공유. `geom.py:328` 죽은 자체점검 삭제.
    - ✅**[고침 2026-08-16] `MESH_FIX=m6`**(기본 꺼짐). 경계 모서리 4·seg(최대 720) → 0 ·
      수밀 False → True(전 테셀레이션). 죽은 자체점검은 이미 지워져 있었고 그 자리를
      «주장을 스스로 검사하는» 블록으로 바꿨다.
      ⭐**드론 σ 에는 아무 영향이 없다 — Δ = 정확히 0.000000, 10기체 전부 지문 불변**(⑨-2).
      `uv_sphere` 는 `drone_cad` 어디에서도 안 쓰이고 호출부가 전부 **기준구**(rcs_po·rcs_sbr·
      검증 스크립트)이기 때문이다. 기준구 자체의 σ 차는 ≤ 1.0e−14 dB. **위생 수리로 분류할 것.**
      감사 문면 2건 정정 — ①북극은 정확히 축 위지만 **남극은 sin(π)=1.22e−16** 만큼 벌어져 있어
      weld 는 «스냅» 도 한다 ②중복정점 수 34·178·238·358 은 구성상의 수이고 **좌표가 정확히 같은
      것만 세면 그 절반**(17·89·119·179).
      ⭐**battery**(팩·구조판 자기겹침)와 **i3**(매몰면 PO 제외)도 같은 스위치로 들어갔다 —
      크기·σ·판정은 ⑨-2, 원장은 `outputs/mesh_layer2_battery_overlap_0816.json` ·
      `outputs/mesh_layer2_buried_faces_0816.json`.

### 3층 — 법칙·규약 (여기가 dB 를 만든다)
⭐**이 순서를 지킬 것.** 두께가 13~17 dB, 형상이 1~2 dB 다.

13. **I1 (최우선)** 프롭 슬래브 두께를 **기종별로 메쉬에서** 뽑을 것. 실측 표적이 Mini 5 Pro 로 옮겨가는데 정본 1.43 mm 를 그대로 쓰면 **+3.8~+4.6 dB** 밝게 본다. 계산법은 원장에 있다.
14. **I3** 매몰면 검사 일반화 + 기체별 예산표. **PO 경로에서는 매몰면이 곧 이중계상**이라 mavic4pro +38 %·mini5pro +42 % 면적 과대다.
15. **C4 + I2 를 한 묶음으로** — `CHORD_FRAC` 를 DJI 평면형으로 교체하면서 `c_max/R` 을 `DroneSpec` 필드로 승격. **따로 하면 큰 프롭 총면적이 튄다.** 권고값은 원장 `L.priority_1` 에 있다.
16. **I8** 팁 끝값 0.10 → 0.20 근처 + 마지막 두 로프트 단면 간격.
17. **I7** `PITCH_K` 를 DJI 실측 `k` 로. ⚠단 «플래시 봉우리 −2.2 dB» 는 어림이고 커널 민감도는 +0.15~+0.82 dB 라 **어림이 과했을 가능성이 크다** — 바꾸기 전에 커널로 플래시 폭을 직접 잴 것.
18. **m5** 프롭 삼각형을 면 수가 아니라 **모서리 길이**로 λ 에 묶을 것(m350rtk λ/3.8).
19. **I9** body/canopy 실효 밀도. RF 에는 영향 없지만 **관성텐서 → 자세 응답 → 로터 회전수 요동 모델**로 내려간다.

### 4층 — 재계산
20. 3층 반영 후 프롭 지배 밴드(el −30·−60) 재계산. ⭐**바이스태틱으로** 돌릴 것 — 형상 민감도가 모노보다 크다(4-2).
21. **m7** 정본 두께 점(기종별)을 큐에 넣고 헤드라인 재인용.

### 5층 — 서술 (리포트·덱)
22. **C6** D2 잔차표를 같은 기체·같은 잣대로 재작성(+1.2~+1.7 dB). matrice4e 칸은 «실측 없음, +0.3~+3.3 dB 로만 묶임».
23. **4-1** «프롭이 지배» 를 «AC 는 정의상 프롭만 남는다 — 총 반사로는 동체가 16~28 dB 세다» 로.
24. **4-4** 그 전까지 모든 프롭 손잡이 인용에 «0.9 mm 민감도 점 기준» 을 붙일 것.
25. **I10 ④** 절대 σ_max 를 앞면·가림 반영판으로(−4.78 → −10.8~−14.2 dBsm).

### 지금 하지 말 것
- **메쉬 미세정밀화(삼각형 증가)** — 로프트 이산화 −1.2 %, 수렴시험 ≤0.04 dB. 병목이 아니다.
- **이산화·매끈함 방향 추가 공격** — 반증 렌즈가 이미 실패를 기록했다(sagitta·패싯폭·팁캡·표면거칠기 전부 통과).
- **?1 익형 최대두께 위치** — 3.5 GHz 에서 λ/200 급. 측정법부터 고쳐야 하고 RF 영향은 없다.

---

## 부록 A — 이번에 반증된 이전 결론 (우리 것 포함)

정직 규율상 남긴다. **반증된 쪽이 이겼다.**

| 이전 결론 | 반증 |
|---|---|
| «저장소에 DJI 프로펠러 실물 기하는 하나도 없다» (코드·원장) | `WM161_zhankai_1k.glb` 에 날 8장. 2026-07-29 원장 작성 시점엔 참이었으나 2026-08-07 GLB 유입으로 거짓이 됐다 |
| «3DR Solo 가 소비자용 쿼드 프롭이라 DJI 급에 가장 가깝다» | 실물 DJI 평면형은 **Yuneec 형**(늦게 정점 0.45R, 천천히 가늘어짐). Solo 는 참조 4종 중 유일한 이상치 |
| **«외곽 시위 부족 = −2.97 dB»** (프롭 형상 렌즈 자신의 번역) | 커널로 실제 재보니 **+0.2~+1.3 dB**. 비틀린 날은 면적이 아니라 정반사 띠가 크기를 정해 **포화**한다 |
| **«형상은 el −30 에서 거의 안 먹는다»** (반증 렌즈 자신의 중간 결론) | **모노 한정**이었다. 바이스태틱 β=120° 에서 시위 오차가 ±2.5 dB 로 커진다 |
| «mini2 캐노피는 body 밖(매몰 0.00 %)이라 코드 주석과 모순» | 검사가 수밀 부품만 골랐는데 mini2 body 는 구멍(I5) 때문에 탈락했다. 구멍 메우고 재니 **86.35 %** — 코드 주석이 맞고 검사가 틀렸다 |
| «전 기체에 자기교차 1504~2747쌍» | Möller 구간겹침 구현이 **양성 대조를 통과하지 못했다**. 고쳐서 양성·음성 다 통과시킨 뒤 재니 전 기체 **0** — 앞 수치는 거짓양성 |
| «묻힌 캐노피 때문에 투과 τ 가 두 번 곱해진다» | `rcs_sbr` 는 first-hit 셸 하나로 τ 를 정하고 내부 패스에서 셸을 전부 제외 ⇒ τ 는 한 번만 |
| «1.43 mm 가 정본, 0.9 mm 는 민감도 점» | 1.43 mm 는 **우리 법칙의 해석 근사**이고 실물 측정이 아니다. Mini 2 급에서는 0.9 mm 가 실물(0.876)에 더 가깝다 (4-3) |

⛔**[정정 2026-08-16 §⑥ — 이 표의 «반증» 중 셋이 다시 반증됐다]** 정직 규율은 여기에도 걸린다.

| 이 표가 적은 «반증» | 다시 반증된 내용 |
|---|---|
| «Solo 는 참조 4종 중 유일한 이상치» | **거짓.** 같은 원장의 Holybro 1345(0.490)가 Solo(0.576)보다, 우리(0.598)보다도 더 뿌리 편중형이다 — 감사의 정규화시위 표에서 그 행만 빠졌다. 정확한 진술은 «참조 CAD 3종 중 2종이 뿌리 편중, DJI 계열은 늦은 정점» 이고, 앵커를 옮길 **진짜 근거는 «실증 표적이 DJI 기체다»** 이다 |
| «−2.97 dB 는 과대 번역, 커널로는 +0.2~+1.3 dB» | **양쪽 다 틀렸다.** −2.97 은 창 불일치 산물(정직한 값 −2.0~−2.4 dB)이고, +0.2~+1.3 은 `c_max/R` 을 0.25 로 **고정한** 프롭의 값이라 실제 얹힌 판과 **부호가 반대**다(matrice4e 날 면적 −0.375~−0.503 dB). 자기반증의 **방향**(면적비 20log10 은 과대)만 옳았다 |
| «Mini 2 급에서는 0.9 mm 가 실물(0.876)에 더 가깝다» | **거짓.** 잣대를 섞었다 — 0.876 은 최대두께 계열이고 슬래브가 요구하는 부피보존 두께로는 실물이 **0.468 mm** 라 0.9 mm 는 +3.5~+5.7 dB 밝다(§4-3 [정정] 참조) |

⭐**세 번째 교훈**: 이 감사는 자기반증을 잘했지만, **자기반증의 반대편 수치는 검증하지 않았다.** «−2.97 은 과대» 는 맞혔으면서 그 자리에 넣은 «+0.2~+1.3» 이 다른 프롭의 값인 것은 못 봤다. **반증할 때 새로 넣는 수도 똑같이 재야 한다.**

**교훈 두 개.**
- **수밀 필터를 건 검사는 «깨끗함» 이 아니라 «못 봄» 을 0 으로 보고한다.** (mini2 캐노피 사례)
- **검사기를 만들면 양성 대조부터 통과시켜야 한다.** 음성 대조만으로는 «0 이 나오는 검사기» 와 «0 이 맞는 대상» 을 구별할 수 없다.

## 부록 B — 이 감사가 모르는 것

- matrice4e 실물 프롭 **두께** (?2) — 1차 출처 0. `matrice4-M4T_v2.step` 에 프로펠러가 없다.
- matrice4e·mavic4pro 가 **표준(1157F) 프롭인가 저소음(1154F) 인가** (?3) — 면적 +25 % 차이.
- 익형 **최대두께 위치** (?1) — 측정법이 앞전/뒷전을 구별 못 한다.
- **큰 프롭이 작은 프롭과 닮았는가** (?4) — `c_max/R` 은 안 닮았다고 말하지만 두께 축은 확인 못 했다.
- 슬리버의 **법선 부호가 실제로 흔들리는지** (m2) — 안 쟀다.
- x500v2 동일평면 겹침의 **σ 값** (m4) — 안 쟀다.
- **팁 밴드 면적·두께 오차가 f_tip 스펙트럼 세기에 주는 dB** (I8) — σ 로는 소액이나 스펙트럼 축은 안 쟀다.
- 프롭 **패싯 크기(λ/3.8)가 PO 에 주는 오차** (m5) — 4 GHz 수렴시험은 matrice4e 급이었고 m350rtk 는 안 쟀다.

## 부록 C — GLB 를 얼마나 믿을 것인가

`WM161_zhankai_1k.glb` 는 **제품 뷰어용 1k 간략화판**이지 계측 스캔이 아니다. 정직한 읽기:

- **실루엣·평면형은 믿을 만하다** — 디스크 지름 118.4~119.7 mm ↔ 공칭 119.4(−0.9~+0.3 %), 4로터가 서로 ±0.003 이내로 일치, 그리고 **Mini 2 실물 사진과 +4 % 안에서 일치**(사진 `c_max/R` 0.272 ↔ CAD 0.262).
- **두께는 시위만큼 검증되지 않았다** — 날 1장이 1635~1691 삼각형뿐. **±10 % 로 읽어라.** 그래서 두께 결론을 «+15~+22 %» 범위로 적었다.
- **일관성 논거**: 레지스트리는 이미 이 GLB 를 `prop_dia`·`prop_pitch` 의 출처로 채택했다. 같은 파일을 시위·두께에만 안 쓰는 것은 앞뒤가 안 맞는다.
- 제품사진 측정은 스팬축 수직 폭이라 스윕·비틀림 때문에 **참시위를 약간 크게** 읽는다 — 절대값은 상한으로.

---

## 부록 D — 적대적 재검증 기록 (2026-08-16, 별도 검증자)

이 감사를 **다른 세션이 원장·원시파일로 되짚었다**. GPU 미사용·코드 무변경·git 미접촉.
목적은 «맞장구» 가 아니라 **반증 시도**다. 아래는 그 결과다.

### D-1. 독립 재계산으로 **재현된** 것 (표본 14건)

전부 이 감사의 산출 원장이 아니라 **소스·메쉬·GLB 를 새로 열어** 계산했다.

| # | 항목 | 감사가 적은 값 | 재검증 값 | 판정 |
|---|---|---|---|---|
| 1 | **C1** `trimesh.split` 이 구멍을 메움 | 11면 → 12면·수밀 True | 11 → **12**, 수밀 **False→True**, 부피 0.75→1.00 | ✅ 재현 |
| 2 | **C1** 실사례 mini2 body | 갈린다 | `repair=False` 비수밀 ↔ `repair=True` 수밀 | ✅ 재현 |
| 3 | **C2** `assert_ok()` 호출부 | 0 건 | 전수 grep 결과 **정의 1 · 문자열 언급만** | ✅ 재현 |
| 4 | **C3** GLB 안의 실물 DJI 날 | 날 8장 + 허브 4개 | 면수 히스토그램에서 **1635×4 + 1691×4 = 날 8장**, **1704×4 = 허브 4개** | ✅ 재현 |
| 5 | **C3** 디스크 지름 | 118.4 mm (−0.9 %) | 최적중심 최대반경으로 **118.13~119.07, 평균 118.60 (−0.67 %)** | ✅ 재현 |
| 6 | **I1** 프롭 두께 기종별 폭 | 메쉬 실측 4.3 배 | **해석 법칙으로 독립 유도해도 4.48 배**(0.622~2.784 mm), 메쉬값과 1.03~1.07 배 안 | ✅ 다른 경로로 재현 |
| 7 | **I1** mini5pro 에 1.43 mm 를 쓰면 | +3.8 dB(각도평균)·+4.6 dB(45°) | ITU-R P.2040 독립 구현: **+3.82 / +4.60 dB** | ✅ 정확히 재현 |
| 8 | **I5** mini2 body 구멍 | 7757 면(홀수)·경계모서리 3 | **7757 · 경계모서리 3 · 10기체 중 유일** | ✅ 재현 |
| 9 | **I9** 메쉬 질량 | 5503·1255·7350·4000·20294 g | **전부 g 단위까지 일치** | ✅ 재현 |
| 10 | **I10①** matrice4e 프레임 길이 | 331.43 mm (+7.96 %) | **331.43 mm** | ✅ 재현 |
| 11 | **I10②** typhoonh480 bbox | 452.17 × 517.00 | **452.17 × 517.00** (−1.06 / −0.58 %) | ✅ 재현 |
| 12 | **I10④** 뒷면 이중계상 | 106장 139 cm² → 앞면 69.67 | 출하 코드 `\|n·u\|` 로 **106장 139.35 cm²**, 앞면 **90장 69.67 cm²**(면적 정확히 1/2), **−6.02 / −9.41 dB**, σ_max **−10.80~−14.19 dBsm** | ✅ 정확히 재현 |
| 13 | **m1** 정본 1.43 mm 는 상수 유도 | 1.4302 재현·메쉬 안 엶 | 함수가 **상수만 import**함을 소스로 확인, **1.4302** · NACA 비 **0.684879** | ✅ 재현 |
| 14 | **m7 · §4-3** 슬래브 표 | 0.876→−20.05 … 1.456→−16.47 | ITU-R P.2040 독립 구현이 **6점 전부 0.01 dB 안**, 0.9↔1.43 차 **3.26 dB** | ✅ 정확히 재현 |

**③ «확인됨» 절의 핵심 축도 독립 재현됐다**: 2026-07-01 안쪽법선 수정이 살아 있다는 주장을
**trimesh 수리를 전혀 거치지 않고** 출하 인덱스에서 손으로 부호부피를 계산해 확인 —
10기체 전 프롭 부품 **전부 양수**, mini2 **984.5 mm³** · m350rtk **88,430.9 mm³** 로 감사값과 일치.
`uv_sphere` 도 전 테셀레이션에서 **면적 0 삼각형 0개**, 중복 정점 seg90/120/180 = **178/238/358** 로 일치.

⇒ **이 감사의 수치는 원장 장식이 아니라 실재한다.** 특히 «두께 축이 13~17 dB» 라는 우선순위 판단의
근거(슬래브 표)는 독립 구현으로 소수점까지 맞았다.

### D-2. 재검증이 **반증한** 것 — 위 본문에 [고침] 으로 반영

1. ⛔**I10③ (mini5pro `255×181×91`) 은 틀렸다.** 저장소가 이미 물리 논거로 기각한 2차 매체 오류이고,
   우리 프레임이 그 값에 붙는 것은 **공식 봉투에서 유도되는 결과**라 «비순환 검사» 가 아니다.
   §5 1층의 «복원» 권고는 **취소**했다 — 그대로 두면 알려진 오류를 레지스트리에 다시 넣게 된다.
   ⭐**교훈**: 이 감사는 «낡은 기록» 을 잡는 데는 강했지만, **이미 기각된 기록을 되살릴 때
   기각 사유를 안 읽었다.** 정정 대상 4건 중 3건은 맞았고 1건은 정반대였다.
2. **m5 «프롭 면 수 전 기체 12752»** 는 부정확(mini2 12736 · 6로터 19152 · 8로터 25600).
   고정된 것은 **로터당 3184~3200 장**이다 — 결함 자체는 유효.

### D-3. 판정이 갈려 **[남김]** 으로 두는 것

- **I9 body 비중 «40~54 %»** — mavic4pro 는 재측정에서 **36.8 %** 로 밴드 아래끝을 살짝 벗어난다.
  나머지 4기체(40.1·40.5·42.3·53.8 %)는 밴드 안. 결론(관성 배분이 틀렸다)에는 영향 없어 고치지 않았다.
- **③ «프롭 어셈블리 3472~3488 삼각형»** 과 «출하 OBJ 4로터 12192» 가 `build_drone` 의 프롭 그룹
  실측(4로터 12752 = 로터당 3188)과 안 맞는다. **경로가 다른 것**(n_sec 판·export 판)으로 보이나
  이번에 확정 못 했다 — 인용 전 경로를 명시할 것.
- **부록 C «4로터가 서로 ±0.003 이내»** — 디스크 **지름**으로는 앞뒤 로터가 118.13 vs 119.07 mm
  (0.8 %) 로 갈린다. ±0.003 은 `c_max/R` 비율 축의 값으로 읽어야 한다.

### D-4. 규약 확인

**GPU 미사용**(전부 numpy·trimesh·pyulog CPU) · **`src/rotor_dynamics.py` PRESETS 무변경**
(mtime 2026-08-11, 값 `outdoor` 2.35/2.45/0.227364 그대로) · **메쉬 생성 코드 무변경**
(`drone_cad.py` 2026-08-10 · `drones.py` 2026-08-07 · `mesh_check.py` 2026-08-10 전부 원본) ·
**git 명령 0회**. 이 라운드에서 바뀐 파일은 **이 문서와 짝 문서 두 개뿐**이다.

---

## ⑥ ⭐⭐적대 검증 판결 (2026-08-16) — «우리 편 감사를 믿지 마라» 라운드

**왜 이 절이 있나.** 이 감사의 결론으로 **날 법칙을 통째로 갈아끼우려는 참**이었다. 법칙 교체는
지금까지의 모든 절대 수치를 다시 계산하게 만드는 **최상류 변경**이라, 틀린 근거로 갈아끼우면
피해가 크다. 그래서 이 라운드의 목적은 «확인» 이 아니라 **«반증 시도»** 였다 — 감사가 틀렸을
가능성을 적극적으로 찾았고, **살아남은 주장만** 반영 대상으로 삼는다.

**누가 쟀나**: 재측정 3갈래(다른 방법으로 같은 양을 다시 잼 · 참조물 정체 검증 · 모집단 대조) ·
반증 4갈래(시위 · 피치/팁 2라운드 · 두께 · 결과) · RF 층(우리 커널로 dB 를 직접 잼) · 판결자 직접 측정.
**전부 CPU**(GPU 큐 미접촉) · **저장소 코드 무변경** · **git 미접촉**.
판결 원장은 `outputs/mesh_adversarial_verdict_0816.json`, 재현 스크립트는 `benchmark/adv_*_0816.py`.

### 6-0. 판결자가 직접 잰 것 (남의 수치를 옮기지 않은 자리)

결론을 가르는 자리만 골라 직접 쟀다. 방법: 모터축 부품(200면)의 면적가중 공분산 **최대분산**
고유벡터 = 원통 축(고유값비 `[0.0616, 0.0616, 1.0]` 로 완전 원통임을 먼저 확인).

| 잰 것 | 값 | 무엇을 가르나 |
|---|---|---|
| 로터축 기울기 | 앞 **2.969°** · 뒤 **5.000°** (월드 +y 아님) | C3 의 «편차 0.001» 이 **거짓** |
| 날 8장 디스크 지름 | 118.17~119.42 mm (공칭 4726F 119.4) | C3 의 정체 주장은 **참** |
| **DJI 날 최소 반경** | **r/R = 0.0327~0.0343** (8장 전부) | «DJI 날은 0.175R 부터» 가 **거짓** ⇒ −29 % 헤드라인이 무너진다 |
| 부품 서명 | 허브 1704×4 · 날 1691×4 + 1635×4 · 모터축 200×4 | «날 8장» 은 실은 **메쉬 2종 × 인스턴스 4개** |
| 감사 자신의 표 | `E_area_impact_dB['0.20-0.96'].area_ratio = 0.7893` = **−2.05 dB** | 감사가 **자기 표와 모순되는 값**을 헤드라인으로 올렸다 |
| 코드 상태 | 기본값 `blade_law='legacy'` 유지 · `prop_law_t_mm` 의 `drone_cad.py` 내 등장 **0회** | 교체는 아직 **안 일어났다**(되돌리기 비용 0) · **두께 축은 배선이 안 됐다** |

### 6-1. 주장별 판결

부호 규약: ✅ 살아남음(반영 대상) · ⛔ 무너짐(반영 금지) · ✏️ 크기·범위가 바뀜.

| # | 감사 주장 | 판결 | 근거 한 줄 |
|---|---|---|---|
| ① | GLB 가 DJI Mini 2 실물이고 날·허브가 실재 | ✅ | DJI CDN 원본과 **바이트 동일**, 텍스처에 «MINI 2 / 249g» 인쇄. 단 회전축·«8장»·«공식 CAD» 문면은 ✏️ |
| ②a | 우리 날이 외곽에서 좁다(정규화시위 0.60 ↔ 0.86, 정점 0.31R ↔ 0.45R) | ✅ | 세 잣대·두 렌즈 독립 재현. **네 갈래 공격이 전부 실패** |
| ②b | **날 면적 −29 %(−2.97 dB)** | ⛔ | 실제 **−20~−24 %**. 창 불일치 산물이고 감사 자신의 표(−2.05)와 모순 |
| ②c | 외곽 밴드 −3.55 dB | ✅ | 0.663 소수 셋째 자리까지 재현. ⚠단 **면적비이지 σ 가 아니다** |
| ②d | «DJI 날은 0.175R 부터» (−29 % 의 뿌리 논거) | ⛔ | 실측 **0.033R**. 0.175R 안쪽 면적은 오히려 DJI 가 더 많다 |
| ②e | (파생) 우리 날이 실물보다 «작다» | ⛔ | UIUC 132종 대비 **61 백분위** — 중앙값보다 크다. 참인 명제는 «안쪽에 쏠렸다»(면적중심 2 백분위) |
| ③ | Solo 는 참조 넷 중 유일한 이상치 | ⛔ | 저장소 **자신의 원장**에 있는 1345(0.490)가 더 극단. 감사 표에서 그 행만 빠졌다 |
| ④ | `c_max/R` 단일 상수 / 크기와 반비례 | ✏️ | **단일 상수 지적은 살아남고 RF 가 강화**. ⛔«크기 반비례» 는 죽음(ρ_s −0.05, 우리 크기급 p=0.082) |
| ⑤a | `PITCH_K` 기준 반경 0.5R vs 표준 0.75R | ✏️ | 문헌은 ✅, 그러나 증거가 0.75R 을 못 가려내고 모집단은 «이름표 문제»(P비 중앙값 0.996)라고 말한다 |
| ⑤b | 외곽 피치폭 ⇒ **플래시 봉우리 −2.2 dB** | ⛔ | 커널 실측 **≤0.10 dB**(1/22), **부호도 반대**(+0.060 dB/deg). 폭을 정하는 것은 비틀림이 아니라 **회절** |
| ⑥ | 팁이 뾰족 / 팁 밴드 −3.10 dB / f_tip 을 정한다 | ✏️ | «좁다» 는 실물 4종이 ✅. ⛔dB 는 C5 가 폐기한 자, ⛔f_tip 은 **순수 운동학**이라 시위가 못 들어간다 |
| ⑦ | 정본 1.43 은 상수 유도값 / 메쉬는 1.456 | ✏️ | 전반부 ✅(소스 확인). ⛔후반부는 자에 따라 1.396~1.488 로 **부호가 갈린다** |
| ⑧ | 두께 13~17 dB vs 형상 1~2 dB | ✏️ | **순서는 살아남고 오히려 강해진다**. ⛔단 통화가 다르다(남은 오차끼리는 3~5 대 1.3~2.5) · ⛔«가장 큰 실행가능 dB» 도 거짓 |
| ⑨ | 무해 확인 목록 | ✅ | 직접 다시 지어 재현. ✏️단 «지름 −0.000 %» 는 **새로 지은 메쉬** 한정 — 정본 자산 OBJ 는 낡았다 |

### 6-2. ⭐공격했고 살아남았다 (반증 실패도 성과다)

무너뜨리려고 실제로 시도했고 **실패한** 것들이다. 이 축들은 더 파지 않아도 된다.

- **«시위 비교는 스윕(정의) 인공물이다»** — 감사가 포기한 스윕각 Λ 를 실제로 쟀다. cosΛ 로 보정해도
  0.70R 비가 0.6514→0.6499 로 안 움직인다. 반대 렌즈에서는 **우리 날이 더 휘어** 있어 보정하면
  비가 오히려 내려간다(0.7944→0.7917).
- **«반경 정의(팁 끝 vs 스윕 반경)가 결손을 부풀렸다»** — 공칭·레지스트리 R 어느 것으로 정규화해도
  결손 29.6~29.8 %. 회전축을 월드 +y 로 바꿔도 면적비 0.7944→0.7958.
- **«GLB 가 저폴리라 팁이 뭉개졌다»** — 제품사진 3종 + **FCC 실물사진**이 GLB 와 같은 방향을 준다.
- **«Solo 측정이 틀려서 유도는 무죄다»** — `solo_prop_cw.stl` 독립 재측정이 원장을 **소수 3자리까지**
  재현(0.576 / 0.275R / 0.268). 탈출구가 없다: 법칙이 실물과 다른 이유는 계산 실수가 아니라
  **대리 기준의 선택**이다.
- **«13~17 dB 의 기준점 100 mm 가 파브리-페로 물결 위라 임의롭다»** — 감사가 실제로 쓴 두 규약에서는
  1.1~1.4 dB 밖에 안 흔들린다.
- **자 검정** — 새 파이프라인이 `outputs/reference_props.json` 3종을 0.5 % 안에서 재현했다.
  **자를 먼저 검증하고 나서** 새 대상에 댔다.

### 6-3. ⭐RF 층 — 기하가 참이어도 dB 는 따로 재야 한다

감사의 dB 는 거의 전부 «면적비를 `20log10` 한다» 는 규칙 위에 서 있었다. 커널로 그 규칙을 쟀다.

- **환산 지수는 커널의 성질이 아니라 형상의 성질이다.** 커널이 읽는 것은 넓이 조각·기울기·위상
  셋뿐이다. 우리 실제 날에서 잰 지수 `n = dlnσ/dlnA` 는 **0.12(el 0) · 0.88(el −30) · 1.69(el −90)**.
  ⇒ 헤드라인 밴드에서 `20log10` 은 **정확히 두 배 과대**다(≈`10log10` 이 맞다).
- ⭐**살아남은 기하 사실이 하필 σ 를 안 만드는 축이다.** 총면적을 같게 맞춘 «면적중립» DJI 평면형으로
  재면 **전 고각 |Δσ| ≤ 0.14 dB**. σ 축에서 움직이는 것은 **총면적** 하나이고, 그 면적을 반경 방향으로
  어떻게 나눠 놓았는지는 사실상 0 이다.
- **실제 얹힌 판의 σ 이동은 −1.7 ~ +1.8 dB 이고 기종마다 부호가 다르다**(mini2 +1.4~+1.6 ↔
  mavic4pro −1.3~−1.7). 감사의 «+0.2~+1.3 dB» 는 **부호가 반대**다.
- **바이스태틱 민감도 증가는 살아남고 기전까지 붙었다** — Kell 등가(유효 파장 λ/cos(β/2)).
  헤드라인 팔 β≈81° 의 정직한 환산 지수는 **1.39**.

### 6-4. 그래서 우리 발표 수치가 흔들리나 — 안 흔들린다

| 축 | 판정 | 수치 |
|---|---|---|
| 엔진 비교(PO/SBR/Sionna) | **안 흔들림** | 원장 자신의 잣대로 최대 이동 **0.148 dB** ↔ 엔진 간격 7.54~79.23 dB = 가장 얇은 칸의 **2.0 %** |
| 분류 | **살아남음** | 빗살 정합 **0 flip**(최대 점수이동 0.052~0.085), 6클래스 간 거리 안 좁아짐. ⚠최종 정확도는 데이터셋 재생성 필요 |
| 절대 판독거리 R50 | 한 자릿수 % | +1.0~+8.0 %(방위 4개에서 부호 유지). 추정기 잡음(±0.8~1.5 %)보다 크고 **엔진 격차 13.9 % 보다 작다** |
| σ 앵커 | ⭐**검증 불가** | 총 σ 이동 **+0.001~+0.003 dB**(프롭이 총 σ 보다 24.9 dB 아래). 지금 앵커 오차는 −5.16 dB · NOT_VALIDATED |

> ⭐**불편한 결론**: 새 법칙은 σ 앵커를 **좋게도 나쁘게도 못 만든다.** 따라서 «고치면 실물에
> 가까워진다» 는 이 라운드가 가진 **어떤 외부 잣대로도 검증할 수 없다**. 남는 정당한 명분은
> σ 정확도가 아니라 **기종 구분(분류)** 이다 — 그런데 그 명분에는 6-5 의 구멍이 나 있다.

### 6-5. 이 라운드가 새로 찾은 결함

| # | 무엇 | 어디 |
|---|---|---|
| **N-A** | **무너진 근거가 이미 코드 주석에 박혀 있다** — «Solo 유일 이상치» · «−29 %(−2.97 dB)» · «날개끝 −3.1~−3.6 dB» | `src/drone_cad.py:153-154` ⚠줄번호는 15:10 KST 재확인값(다른 세션이 편집 중이라 이동한다) |
| **N-B** | **두께 축이 배선 안 됨** — `prop_law_t_mm`·`prop_law_tc_max` 를 `_blade` 가 **한 번도 안 읽는다**(등장 0회). 두께는 여전히 전 기종 공통 `TC_ROOT/TC_TIP` 이고 실측은 그 밖이다(mini2 t/c 0.058) | `drones.py:1314-1315` ↔ `drone_cad.py` |
| **N-C** | `resolve_chord_max_over_r` 이 판 **이름 문자열**로 분기 — 다른 이름의 판은 조용히 기종별 실측값으로 떨어진다(실제로 한 반증 실험이 1.1~1.4 dB 오염됐다) | `src/drone_cad.py:343` (분기 355행) |
| **N-D** | **정본 자산 프롭 OBJ 5개가 낡았다**(mtime 2026-07-20, 스윕디스크 정규화 이전 — 지름 +0.84 %). RCS 는 매번 새로 지어 dB 무영향이나 자산을 읽는 쪽은 옛 법칙을 본다 | `assets/meshes/drones/*/*__prop.obj` |
| **N-E** | **함대가 «한 자» 로 안 재졌다** — 사진 유래 6기종 중 4기종이 하드 문턱값이고 독립 재계측과 **+7.9 %** 어긋난다. R3 에 따르면 이 법칙의 유일한 값어치가 기종 구분인데, **그 구멍이 곧 명분에 난 구멍**이다 | §7-7 7 |
| **N-F** | «DJI 평면형» 이라는 **단일 곡선은 없다** — 같은 기체의 두 정품 프롭이 0.15 벌어진다(1157F 0.80 ↔ 1154F 0.95) | ?3 미해결 |

### 6-6. ⭐⭐판정 — 날 법칙을 갈아끼워도 되는가

> # 부분만 간다
>
> **기종별 `c_max/R` 승격은 간다. 평면형 곡선 교체는 근거 문장을 고쳐 쓰고 [A]/[B] 등급 기종만
> 간다. 피치 법칙·팁 끝값·두께 정본 교체는 멈춘다. 그리고 어느 것도 기본값으로 승격하지 않는다.**

**왜 전면 통과가 아닌가**
1. 감사가 내세운 **헤드라인 근거 셋이 죽었다**(«−29 %(−2.97 dB)» · «Solo 유일 이상치» · «DJI 날은
   0.175R 부터»). 그중 둘은 **이미 코드 주석에 박혀** 법칙의 명분 문장이 돼 있다(N-A).
2. 감사의 **dB 가격표는 부호가 반대다** — 실제 판은 matrice4e 날 면적을 −0.375~−0.503 dB 로 **줄인다**.
3. ⭐**살아남은 기하 사실이 σ 를 안 만든다** — 면적중립 평면형 교체는 전 고각 **≤0.14 dB**.
4. ⭐**외부 잣대로 검증할 수 없다** — σ 앵커가 프롭을 못 본다(0.001 dB ↔ 앵커 오차 5.16 dB).
5. **법칙의 절반이 배선 안 됐다** — 두께가 13~17 dB 축이라 해 놓고 켜려는 판은 형상 쪽만이다(N-B).
6. **한 자로 안 쟀다** — 4기종이 다른 자이고, 하필 그것이 유일한 명분(분류)의 근거다(N-E).

**왜 전면 중단도 아닌가**
1. **방향 자체는 네 갈래 공격을 전부 견뎠다**(6-2). 우리 날이 DJI 실물보다 외곽에서 좁고 정점이
   안쪽인 것은 세 잣대·두 렌즈가 독립으로 재현한다.
2. **기본값이 안 바뀐 상태다**(판결자 확인) — 지금 되돌리기 비용은 **0** 이다.
3. **결과 렌즈가 «아무 발표 주장도 안 뒤집힌다» 를 실측했다**(6-4).
4. `c_max/R` 기종별 승격은 **RF 층에서 오히려 강해졌다** — 면적→σ 지수가 전기적 크기에 달려 있고
   (mini2 1.80 ↔ m350rtk **−1.20**), **우리 실측 표적 두 대가 그 경계의 양쪽에 있다**.

**항목별 판정**

| 항목 | 판정 | 조건 |
|---|---|---|
| `CHORD_MAX_OVER_R` → `DroneSpec` 기종별 필드 | ✅ **간다** | ⛔빈칸을 **크기 스케일링으로 채우지 말 것**. 실측 없는 기종은 «모른다» 로 |
| 평면형 곡선 교체 — **[A]/[A-]/[B] 만**(mini2·typhoonh480·matrice4e·phantom3) | ✅ **조건부로 간다** | 명분을 **σ 정확도 → 기종 구분**으로 바꿔 쓸 것. 나머지 6기종은 N-E 를 먼저 닫을 것 |
| `PITCH_K` 교체 | ⛔ **멈춘다** | 무해하지만(≤0.10 dB) **근거가 없다**. 켜려면 «+6.1 % 과피치»(≈0.13 dB)로 근거를 바꿔 쓸 것 |
| 팁 끝값 0.10 → 0.20 | ⛔ **멈춘다** | 대신 **마디**를 넣어라(0.96~0.99 = 0.445/0.422/0.402/0.370). 빈틈의 8 % ↔ 86 % |
| 정본 두께 «메쉬 실측판» 교체 (m1) | ⛔ **멈춘다** | **두께의 정의를 먼저 못박을 것**(슬래브에는 단면적÷시위) |
| **기본값 승격** | ⛔⛔ **멈춘다** | N-A·N-B·N-C·N-E 가 전부 열려 있다. σ 디스크캐시 세대가 섞인다(이미 7기체 중 6 불일치) |

**갈아끼우기 전에 반드시 고칠 문장** (판정만 — 이 라운드는 코드를 안 건드렸다)

1. `src/drone_cad.py:153-154` — «Solo 유일 이상치» → «참조 CAD 3종 중 2종이 뿌리 편중, DJI 계열은
   늦은 정점. 앵커를 DJI 로 옮기는 근거는 **실증 표적이 DJI 기체**라는 것» / «−29 %(−2.97 dB)» →
   «공통 창 면적비 0.78~0.80 = −20~−24 %. ⚠면적비는 σ 가 아니다(el −30 지수 0.88)»
2. `src/drone_cad.py:154` (같은 주석 블록) — «날개끝 밴드 −3.1~−3.6 dB» → 커널 실측치로
3. `outputs/mesh_audit_0816_prop_geometry.json` — `E_area_impact_dB` 의 −29 %,
   `M_robustness_definition_free` 의 뿌리 보정, C3 의 회전축 «월드 +y»
4. `outputs/reference_props.json` meta.warning · `benchmark/measure_reference_props.py`
5. `src/drones.py` mini2 note — «뒷 로터 2.00/3.00 in» 은 축 가정의 인공물일 가능성. `prop_pitch_in`
   재검사 전까지 단서를 달 것

**판정을 정한 수치**

| | 감사 | 이 라운드 |
|---|---|---|
| 면적 결손 | −29 %(−2.97 dB) | **−20~−24 %(−2.0~−2.4 dB)** — 감사 자신의 표도 −2.05 |
| 그 결손의 σ 환산 지수 | 2.0 (`20log10`) | **0.88 @ el −30** · 0.12 @ el 0 |
| 평면형(반경 분포) 교체의 σ | (안 쟀음) | **\|Δσ\| ≤ 0.14 dB, 전 고각** |
| 실제 얹힌 판의 σ 이동 | +0.2~+1.3 dB | **−1.7 ~ +1.8 dB, 기종마다 부호가 다름** |
| 피치 교체 | −2.2 dB | **≤0.10 dB** (부호도 반대) |
| 팁 끝값 교체 | −3.10 dB | **+0.14~+0.39 dB** (빈틈의 8 %) |
| σ 앵커가 느끼는 변화 | (안 쟀음) | **+0.001~+0.003 dB** ↔ 앵커 오차 −5.16 dB |

---

## ⑦ ⭐⭐기체별 프로펠러 — «그 기체의 진짜 프롭» (2026-08-16)

**한 줄**: 10기종이 **같은 프로펠러**를 달고 있던 상태를 끝내고, 기체마다 그 기체의 순정 프롭
평면형과 통통한 정도를 세웠다. 새 법칙은 **인자로 골라야 켜지고**, 안 고르면 예전 그대로다.

**왜 이게 최우선이었나**: 같은 날 재질 판정이 «움직이는 성분은 사실상 전부 프로펠러» 를 실측했고
(프롭 두께를 바꾸면 요동 −16.99 dB, 셸 두께는 ±0.00 dB), 우리 메인 태스크는 **기종 분류**다.
분류는 «기종끼리 무엇이 다른가» 위에 서 있는데, 프롭이 전 기종 공통이면 그 차이를 우리가
지워 놓고 시작하는 셈이었다.

### 7-1. 무엇이 달라졌나 — 산포 0 % → 53 %

| | 지금(legacy) | 새 법칙(per_airframe) |
|---|---|---|
| 통통한 정도 c_max/R | **10기종 전부 0.25** | 0.176(s1000plus) ~ 0.269(phantom3) |
| 평면형(시위 분포) | 3DR Solo 프롭 **하나**를 복사 | 기체별 곡선 **8종**(대리 2기종 제외) |
| 봉우리 위치 r/R | 전부 0.30 | 0.25(2110s) ~ 0.55(1158F·6028F) |
| 기종 사이 산포 | **0 %** | **53 %** |

⭐**봉우리 위치는 배율로 흉내 낼 수 없다.** c_max/R 한 수만 기종별로 바꾸고 곡선을 공용으로 두면
«통통한 정도만 다른 같은 프로펠러» 가 된다 — 그래서 곡선과 c_max/R 을 **한 묶음으로** 갈았다.

### 7-2. 기체별 법칙 (정본 후보)

| 기체 | 순정 프롭 | 등급 | c_max/R | 봉우리 r/R | 날 두께 | 불확실도 | 근거 |
|---|---|---|---|---|---|---|---|
| **matrice4e** | DJI 1157F (표준·순정 동봉) | **[B]** | 0.200 | 0.30 | **빈칸** | ±3 % | 1157F 제품 렌더 정밀 계측 |
| **mavic4pro** | DJI 1158F | **[B-]** | 0.179 ⚠하한 | 0.55 | **빈칸** | ±6 %(비대칭 −6/+12 %) | 1158F 제품 사진(3/4 투영) |
| **mini5pro** | DJI 6028F | **[C]** | 0.209 | 0.55 | **빈칸** | ±14 % | 기체 사진(기종 동일성 미확정) |
| mini2 | DJI 4726F | **[A]** | 0.257 | 0.45 | 0.478 mm (시위가중) | ±0.4 % | DJI 공식 3D `WM161_zhankai_1k.glb` |
| typhoonh480 | Yuneec Propeller A/B | **[A-]** | 0.177 | 0.45 | t/c 0.086~0.128 | ±1 % | 그 프롭의 3D(출처 문서 없음) |
| phantom3 | DJI 9450 (자동조임) | **[B]** | 0.269 | 0.35 | **빈칸** | ±3 % | DJI 공식 상면 렌더 |
| s1000plus | DJI 1552 / 1552R | **[B-]** | 0.176 | 0.30 | **빈칸** | ±6 % | 판매점 상면 사진(문턱값 지배) |
| m350rtk | DJI 2110s | **[B-]** | 0.181 | 0.25 | **빈칸** | ±10 % | 기체 사진 |
| phantom4 | DJI 9450S (유력·미해결) | **[C]** | 0.269 | 0.35 | **빈칸** | ±5 % | ⚠**대리** — phantom3 의 9450 |
| x500v2 | 1045 (범용 규격) | **[D]** | 0.269 | 0.35 | **빈칸** | ±30 % | ⛔**대리** — 근거 없음 |

등급: **[A]** 그 프롭 자체의 제조사 공식 3D · **[A-]** 그 프롭의 3D 인데 출처 문서가 없다 ·
**[B]** 그 프롭 자체의 사진을 정밀 계측 · **[B-]** 시점·출처·문턱값 중 하나가 약하다 ·
**[C]** 계열 유추 또는 기체 동일성 미확정 · **[D]** ⛔다른 회사·다른 계열을 빌린 **대리**.

- **matrice4e 순정은 1157F 다**(저소음 1154F 는 별매 액세서리). 1154F 를 달면 c_max/R 0.221 ·
  날면적 +25 % 다 — 그 판도 원장에 `alternative` 로 같이 실었다. 순정 판정의 근거는 [DERIVED] 3개
  (이륙중량 1219 g · 별매 여부 · 광고 호버 49분)이지 DJI 문서의 «동봉품» 문장이 아니다.
- ⛔**공식 CAD `matrice4-M4T_v2.step` 에는 프로펠러가 없다.** 그래서 주력 1순위가 [A] 가 아니라 [B] 다.
- ⚠**[고침 2026-08-16 적대검증] mavic4pro 0.179 는 «값» 이 아니라 «하한» 이다.** 근거 원장
  `outputs/prop_measure_mavic4pro_mini5pro_0816.json` 의 `E_verdict.mavic4pro_1158F` 가 스스로
  «0.182 ~ 0.201, 한 수만 적어야 하면 **0.19**» 라고 적어 두었고, 같은 원장의
  `projection_bias_estimate` 는 «c10 은 비스듬한 컷이라 c/R 이 아래로 치우친다 ⇒ 이 값을 **하한**으로
  다룬다» 고 못박는다. 위 표의 0.179 는 그 하한(0.1824)에 격자 스냅 −1.8 % 가 더 걸린 수다.
  독립 재계측(문턱값 없는 덮개율 시위, 같은 사진)은 투영 호폭 **0.191**(→ c_cal ≈ 0.197)을 준다.
  ⇒ **주력 2순위를 인용할 때는 «0.179(하한) ~ 0.20» 으로 쓸 것.** 위로만 움직인다.
- ⚠**[고침 2026-08-16 적대검증] mini2 의 ±0.4 % 는 날 8장 사이의 산포이지 방법 오차가 아니다.**
  같은 GLB 를 저장소가 두 번 쟀는데 **0.2575** (채택) 와 **0.2625** (`mesh_audit_0816_prop_geometry.json`
  §B)로 **1.9 % 갈린다**. 독립 재계측은 날 8장 0.2577~0.2632, 평균 **0.2606** 이다 — 즉 채택값이
  재현 범위의 **아래 끝**이다. 기준 곡선이자 다른 6기종의 «빈 구간 메우개» 인 값이니 **±2 %** 로 읽을 것.

### 7-3. 두 «시위» 를 절대 안 섞는다 — 이게 3 % 를 만든다

- **투영 호폭 c_arc** = 회전축에서 본 폭(`r·Δθ`). **사진이 줄 수 있는 유일한 값**이다.
- **설계 시위 c_cal** = 반경 r 원통 단면의 최대 캘리퍼. `drone_cad` 의 로프트가 익형을 세울 때
  쓰는 길이이자 `CHORD_MAX_OVER_R` 이 뜻하는 값이다. ⇒ **코드 자리에는 c_cal 을 넣어야 한다.**
- 다리(arc → cal) = **×1.031**. 3D 가 있는 프롭 둘에서 **직접 재서** 정했다(Mini 2 완전전개 1.020 ·
  Yuneec 1.045). ⛔«1/cos θ 로 되돌리기» 는 그 둘 다에서 **+3.8 %** 높게 나온다 — 두께와 스윕이
  각폭을 넓히는 몫을 무시하기 때문이다. 그래서 안 쓴다. ~~다리 불확실도 ±1.2 %~~
- ⚠**[고침 2026-08-16 적대검증] 다리 불확실도 ±1.2 % 는 반증됐다 — ±3 % 로 읽고, 중심도 1.031 보다
  ~2 % 높을 것이다.** 3D 가 있는 프롭 **넷**을 독립 재계측하니(같은 규약: 원통 껍질을 펼쳐 최대 캘리퍼
  ÷ 각폭) Mini 2 8장 @r/R 0.461 **1.055**(완전전개만 1.054) · Yuneec **1.059** · 3DR Solo **1.043** ·
  Holybro 1345 **1.051** 로, 넷 다 1.031 보다 위다. 저장소 안에도 이미 어긋난 값이 있다 —
  같은 GLB 에 대해 `A_ruler_validation.step1_cad_truth` 는 1.031 을 적고
  `step1_cross_check_vs_audit_ledger` 는 **1.0595** 를 적는다(둘이 화해된 적 없다).
  대가: 사진 유래 6기종의 c_max/R 이 **한 방향으로 ~2 % 낮다**(matrice4e 0.200→0.205 꼴).
  기종 사이 순서·비율은 공통 인자라 안 흔들리므로 **분류 이야기는 무사**하고, 절대값 인용만 조심할 것.
- ⇒ 7-2 표의 c_max/R 은 **전부 c_cal 축**이다(사진 유래 기종은 ×1.031 이 이미 들어갔다).

### 7-4. 지금 값 → 새 값 → 몇 % 바뀌나

| 기체 | c_max [mm] | 날 1장 면적 [mm²] | 기체 총 날면적 [mm²] | 면적 dB |
|---|---|---|---|---|
| **matrice4e** | 33.9 → **27.4** (−19.0 %) | 2896 → **2657** (−8.3 %) | 23171 → **21256** | −0.75 |
| **mavic4pro** | 33.0 → **23.9** (−27.6 %) | 2750 → **2198** (−20.1 %) | 22002 → **17580** | −1.95 |
| **mini5pro** | 18.8 → **15.9** (−15.4 %) | 896 → **814** (−9.2 %) | 7168 → **6510** | −0.84 |
| mini2 | 14.7 → **15.3** (+4.0 %) | 547 → **656** (+19.8 %) | 4378 → **5245** | +1.57 |
| typhoonh480 | 28.5 → **20.3** (−28.6 %) | 2044 → **1684** (−17.6 %) | 24533 → **20209** | −1.68 |
| phantom3 | 29.7 → **32.2** (+8.7 %) | 2222 → **2346** (+5.6 %) | 17778 → **18771** | +0.47 |
| s1000plus | 47.1 → **33.5** (−29.0 %) | 5600 → **3965** (−29.2 %) | 89604 → **63439** | −3.00 |
| m350rtk | 65.9 → **48.2** (−26.8 %) | 10976 → **9334** (−15.0 %) | 87812 → **74668** | −1.41 |
| phantom4 | 29.7 → **32.2** (+8.7 %) | 2222 → **2346** (+5.6 %) | 17778 → **18771** | +0.47 |
| x500v2 | 31.4 → **34.1** (+8.7 %) | 2489 → **2628** (+5.6 %) | 19912 → **21025** | +0.47 |

⚠**면적 dB 는 참고용**이다(20·log₁₀ 면적비). σ 가 면적² 에 걸리는 구간이 있어 적어 둘 뿐,
이 값을 σ 변화라고 인용하면 안 된다 — 진짜 크기는 커널로 재야 한다(§⑤ 4층).
⭐방향이 갈린다: 큰 프롭 기체는 날이 **좁아지고**(s1000plus −29 %), 작은 프롭 기체는 **넓어진다**
(mini2 +20 %). 전 기종 공통 0.25 가 큰 프롭을 과대·작은 프롭을 과소로 만들고 있었다는 뜻이다.

### 7-5. ⭐지어진 메쉬로 되쟀다 — 표에 적는 것과 실제로 들어가는 것은 다른 주장이다

`benchmark/verify_prop_law_per_airframe_0816.py` (원장 `outputs/prop_law_verify_0816.json`,
17 s · CPU). 설계표를 한 번도 안 읽고, `build_propeller` 가 뱉은 **삼각형만** 잘라서 잰다
(x = r 평면 단면의 최대 캘리퍼 — 회전에 불변이라 비틀림이 값을 안 건드린다).

| 판정 | 결과 |
|---|---|
| **V1 기본값 불변** | 인자 없는 호출 == `legacy` — 10기종 **꼭짓점 바이트 동일** |
| **V2 실현 충실도** | 0.20~0.96R 에서 레지스트리 곡선과 **≤2.7 %**. m350rtk 만 3.8 % 인데 단면 수를 4배로 올리면 **1.1 %** 로 줄어든다 ⇒ 법칙이 아니라 **로프트 해상도** |
| **V3 기체 구분** | 45쌍 중 **42쌍이 1 % 넘게 다르다**. 가장 크게 갈리는 쌍 matrice4e ↔ mini5pro **66.5 %**. 같은 3쌍은 **선언된 대리**(phantom3·phantom4·x500v2)뿐 |
| **V4 변화표 재현** | 지어진 메쉬로 잰 날면적 변화율이 7-4 표와 **≤0.7 %p** |
| **V5 실현 적자** | 지은 날의 c_max 는 법칙보다 **~2 % 좁다** — 스팬 0.991~0.993 × 로프트 외곽선 재샘플 0.991~0.993 × 잔여(단면 수) 0.991~0.997 |

⭐**V5 를 적어 두는 이유**: 파라미터와 실현값이 다른데 같은 이름으로 인용하는 것이 이 저장소가
반복해서 걸린 함정이다(§②·m3). 적자의 원인 둘은 **의도된 보정**(스윕 때문에 팁이 공칭 지름을
넘지 않게 스팬을 줄인다)과 **로프트가 단면 외곽선을 36점 등간격으로 다시 찍으면서 앞·뒷전
꼭짓점을 스치는 것**(`cadkit._resample`, 288점으로 올리면 회복)이다. 둘 다 `legacy` 에도 **똑같이**
걸리므로 7-4 의 «옛→새» 비율에는 안 들어간다 — V4 가 그것을 확인한다.
⇒ 인용 규칙: «우리 날의 c_max/R» 을 말할 때 **법칙값과 실현값 중 어느 쪽인지 밝힐 것**
(실현값 = 법칙값 × 0.964~0.981, 기종별로 원장에 있다).

### 7-6. 켜는 법 — 기본은 안 바뀐다

```python
drone_cad.build_propeller_cad(spec, blade_law="per_airframe")   # 그 기체의 프롭
drones.build_propeller(spec,        blade_law="per_airframe")   # 같은 것(래퍼)
drones.build_propeller(spec)                                    # 인자 없음 = 예전 그대로
```
- 값은 `drones.PROP_LAW_0816` 레지스트리(→ `DroneSpec.prop_law_*` 필드)에 실려 있고, 없는 기체는
  `dji_mini2` 공용 곡선으로 되돌아간다(그때는 «대리를 쓰는 중» 이라는 뜻이라 출처 문자열에 남는다).
- 회귀 증명 2개: `benchmark/regress_blade_law_bitidentical.py`(비트동일) ·
  `benchmark/verify_prop_law_per_airframe_0816.py`(실현 충실도).

### 7-7. ⛔빈칸은 빈칸이다 — 이 법칙이 **모르는** 것

1. **두께는 10기종 중 2기종만 근거가 있다**(mini2 [A] 절대 mm · typhoonh480 [A-] t/c). 나머지 8기종은
   **빈칸**이다. 사진으로는 원리적으로 못 잰다 — 옆모습의 겉보기 높이는 `c·sinβ + t·cosβ` 이고
   앞항이 두께의 5배라, 피치 1° 오차가 두께의 절반을 움직인다. **두께는 13~17 dB 축**(§4-3)이므로
   이 빈칸이 이 라운드에서 가장 비싼 구멍이다.
   ⚠**[고침 2026-08-16 적대검증] 게다가 근거가 있는 그 2기종도 메쉬에 안 들어간다.**
   `prop_law_t_mm` · `prop_law_tc_max` 는 레지스트리에 적히기만 하고 `drone_cad._blade` 가
   **한 번도 읽지 않는다** — 두께는 여전히 전 기종 공통 상수 `TC_ROOT/TC_TIP = 0.095/0.065` 다.
   그런데 실측은 그 구간 **밖**이다(mini2 t/c 0.058 · typhoonh480 0.086~0.128). 예: 지어진
   mini2 날은 c_max 자리에서 t/c ≈ 0.083 이라 실측 0.058 보다 **~40 % 두껍다**. ⇒ 표의
   «날 두께» 칸은 **기록일 뿐 실현값이 아니다** — 인용할 때 반드시 구별할 것.
2. **x500v2 [D]** — 순정 1045 의 기하 근거가 저장소에 하나도 없다. 9450 평면형을 **대리**로 쓰고
   ±30 % 를 붙였다. 1045 는 범용 사출품이라 DJI 몰드 프롭과 계열이 다르다.
3. **phantom4 [C]** — 자체 측정 없음(사진 폴더는 **다른 기체**인 Phantom 4 Pro+ V2.0 이다).
   phantom3 의 9450 을 그대로 쓰고 9450 ↔ 9450S 갈래로 ±3 % 를 더 본다.
4. **mini5pro [C]** — 사진 속 기체 동일성 미확정(그 배치에 다른 기종이 섞여 있는 것이 확인됐다).
   밴드 0.176~0.239. 주력 3순위인데 등급이 [C] 라는 사실을 발표물에 그대로 달 것.
5. **다리** — ~~±1.2 %~~ → **±3 %, 중심도 ~2 % 낮다**(7-3 의 [고침] 을 보라. 3D 프롭 넷 재계측이
   1.043~1.059 를 준다). 사진 유래 6기종이 한 방향으로 같이 낮으므로 **비율은 무사, 절대값만 조심**.
6. **뿌리 쪽은 어느 기종도 계측이 아니다.** 0.20R 안쪽은 전 기종이고, 실제로는 **10기종 중 7기종이
   0.25R 안쪽**이 계측 밖이다(허브·힌지 하드웨어에 가린다). 그 구간은 mini2 곡선을 이어붙인 것이라
   «그 기체의 프롭» 이 아니다 — 시위적분 면적의 **4.8~17.8 %**(뿌리) + **0.8~3.2 %**(팁)가 여기서 온다.
   `segment_grade` 에 `root_filled_D` / `tip_filled_C` 로 표시돼 있다 — 실측이라고 인용하지 말 것.
7. ⚠**[고침 2026-08-16 적대검증] 함대가 «한 자» 로 재지지 않았다.** matrice4e 만 2차 패스에서
   **문턱값 없는 덮개율 시위**로 다시 쟀고(그래서 [B] 가 단단하다), phantom3 는 진짜 알파 채널을 썼다.
   나머지 사진 유래 4기종(mavic4pro·mini5pro·s1000plus·m350rtk)은 **하드 문턱값**이다. 문턱값 램프는
   기종마다 원장에 있으나(mavic 0.168→0.181 · s1000plus 0.164→0.184) 그것은 «그 기체 안의 폭» 이지
   «기종끼리 견줄 수 있다» 는 증거가 아니다. 독립 재계측(덮개율 자)은 덮개율로 잰 matrice4e 를
   **+1.2 %** 로 재현하고 문턱값으로 잰 mavic4pro 와는 **+7.9 %** 어긋난다.
   ⇒ 분류(기종 대 기종) 서사를 이 표 위에 세우기 전에 **나머지 4기종을 덮개율 자로 다시 잴 것.**

### 7-8. 다음 상류 작업 (등급을 올리는 가장 싼 길)

1. **1158F·6028F 정면 단품 사진 각 1장**(자 포함) → mavic4pro [B-]→[B], mini5pro [C]→[B].
2. **1045 정면 단품 사진 1장** → x500v2 [D]→[B]. 지금은 대리다.
3. **두께**: 사진으로는 못 잰다. 실물 프롭 한 장을 캘리퍼로 재거나(실측 기체 2종 보유),
   프롭 CAD 를 구하는 것 말고 길이 없다. 커널 우선순위에서는 이것이 형상보다 위다.

**산출물**: `outputs/prop_law_by_airframe_0816.json`(정본 후보) ·
`outputs/prop_law_verify_0816.json`(검증) · 생산자 `benchmark/prop_law_by_airframe_0816.py` ·
계측 원장 `outputs/prop_measure_{mini2_reference,mini2_xcheck,matrice4e,mavic4pro_mini5pro,others}_0816.json`
· 프롭 모델명 확정 `outputs/prop_identity_0816.json`.

---

## ⑧ ⭐공식 CAD 를 쓸 때의 상시 규칙 — **그 CAD 는 M4T 판이다** (2026-08-16 사용자 지시)

`assets/meshes/reference/matrice4-M4T_v2.step` 는 **Matrice 4T**(열화상 판)의 CAD 다.
우리 표적은 **Matrice 4E** 다. DJI 는 M4E CAD 를 공개하지 않는다(403).

**갈라 쓰는 규칙** — 이 CAD 에서 무엇을 가져올 수 있는지:

| 부품군 | 4T ↔ 4E | CAD 를 써도 되나 |
|---|---|---|
| 셸(동체)·팔·다리·모터 | **공용** | ✅ 그대로 |
| 어안·비전 센서·비콘 위치 | **공용**(기체 등판·벨리) | ✅ 그대로 |
| RTK 안테나 | 공용 | ✅ 그대로 |
| ⚠**짐벌·카메라 블록** | **다르다**(탑재체가 갈리는 지점) | ⛔**치수는 쓰지 마라**. **매다는 자리(크래들·댐핑플레이트)만** 공용이라 위치는 써도 된다 |
| 내부 기판·배터리 | CAD 는 외장 모델이라 **애초에 없다** | — |

**지금 상태 점검(2026-08-16)**: 정정 14건 중 CAD 출처 13건을 훑었고, **짝이 어긋난 항목은 없다**.
짐벌 항목(F10)은 규칙을 이미 지키고 있다 — *«CAD 짐벌은 4T 다 → 치수(59×47×52)는 2026-07-31
정면사진 실측을 그대로 두고 **매다는 자리만** 쓴다»*. 나머지 12건은 전부 공용 부품군이다.

⭐**앞으로도 이 CAD 를 쓸 때마다 위 표를 먼저 볼 것.** 특히 «정면 139 cm² 금속 거울»(0° 익사
서사의 주역)에는 **카메라 블록이 들어간다** — 그 면적을 다시 잴 일이 생기면 **4E 사진 치수**로
재야 하고 CAD 치수를 쓰면 안 된다.

⚠**남은 불확실**: 두 판이 «기체는 공용» 이라는 것은 DJI 제원·매뉴얼 대조에서 나온 판단이고,
부품 단위로 전수 대조한 것은 아니다. 미세한 차이(방열구·라벨·내부 배치)는 확인되지 않았다.

---

## ⑨ ⭐⭐2층 수리 결과 — 수리 하나하나가 σ 를 얼마나 바꿨나 (2026-08-16, 영향 측정 라운드)

§⑤ **2층**(메쉬 결함)에 네 수리자가 값을 넣었다 — `MESH_FIX` 스위치로 켜는 여섯 손잡이
**i5 · m6 · battery · i4 · m4 · i3**. 이 절은 그 여섯이 **무엇을 얼마나 바꿨는지** 별도
프로세스로 다시 재서 적은 것이다. 값을 넣지 않았고, 재기만 했다.

정본 수치: `outputs/mesh_impact_layer2_0816.json` ·
재현: `benchmark/mesh_impact_l2_probe_0816.py`(조합 하나를 재는 자) +
`benchmark/mesh_impact_l2_md_0816.py`(마이크로도플러 축) +
`benchmark/mesh_impact_l2_ledger_0816.py`(원장 조립).

### 9-0. 어떻게 쟀나

* **σ 규약** — 앞선 2층 수리 라운드 넷과 **똑같이** 맞췄다(그래야 표가 이어진다):
  우리 커널 PO(`src/rcs_po.py`) · **CPU 전용** · 가림 없음 · 재질 가중 |Γ| ·
  fc 3.5 GHz · 5G 100 MHz **9 주파 비코히런트 대역평균** · 점 간격 λ/7 ·
  방위 **2° 격자 180 점** · 배치 3종 = **모노 el 0° / 모노 el −30° / 바이스태틱 β 120° el −30°**.
  방위평균 = 10log₁₀(mean σ). Δ dB = **수리 후 − 수리 전**(음수면 어두워진 것).
* **판정 밴드** — |Δ| < 0.1 «무해» · 0.1~1.0 «보임» · **> 1.0 «결론을 바꿈» = 판정 밴드 밖**.
* **한 번에 하나씩** — 표의 각 칸은 «그 수리 **하나만** 켠 세계» 와 «아무 것도 안 켠 세계» 의 차다.
  ⚠예외 하나: **mini2 의 i4 는 i5 가 선행**이라(구멍 난 body 에는 불리언이 안 돈다 — 켜면
  `RuntimeError` 로 죽는다) «i5+i4» − «i5» 로 쟀다.
* ⭐**동시 편집 사고 회피** — 이 라운드가 도는 동안 다른 라운드가 같은 파일을 고치고 있다.
  그래서 `src/` 를 통째로 얼려 두고 **그 스냅샷 위에서만** 쟀다. 원장의 모든 수치가 스냅샷
  하나에서 나온다(파일별 sha256 을 원장 `_meta` 에 박았다).
* **대조군** — 지문(정점·면·그룹의 sha256)이 안 바뀐 기체에서는 σ 가 **한 비트도** 움직이면
  안 된다. 여섯 수리 전부 대조군 위반 **0 건**이다.

### 9-1. 한 줄 판정

**메쉬를 고치는 다섯 수리는 σ 를 거의 안 움직인다. dB 를 만드는 것은 메쉬가 아니라 «PO 가
어느 면을 더하는가»(i3) 다.** 메쉬 5종을 다 켜도 방위평균은 −0.0~−1.3 dB 인데 i3 하나가
−0.15~**−5.34** dB 를 만든다. 그리고 여섯을 다 켜면 개별 합보다 **+0.0~+2.6 dB 덜** 내려간다 —
**두 수리가 같은 결함을 겹쳐 세고 있기 때문**이다(9-3).

### 9-2. 전수표 — 기체 × 결함 × (수리 전 σ · 수리 후 σ · Δ dB · 밴드 밖인가)

| 수리 | 기체 | 결함 크기 | σ 전 el0 | σ 후 el0 | **Δ el0** | Δ el−30 | Δ bi β120 | 밴드 밖? |
|---|---|---|---|---:|---:|---:|---:|---:|---|
| I5 mini2 구멍 | mini2 | 면 -1 · 면적 -0.00% | -24.59 | -24.59 | **+0.000** | +0.000 | -0.000 | 아니오 |
| battery 자기겹침 | mini5pro | 면 +20 · 면적 -5.60% | -19.48 | -19.97 | **-0.490** | -0.721 | -2.833 | ⭐**예** |
| battery 자기겹침 | mavic4pro | 면 +20 · 면적 -5.32% | -15.26 | -15.74 | **-0.481** | -0.333 | -1.349 | ⭐**예** |
| battery 자기겹침 | matrice4e | 면 +0 · 면적 +0.00% | -16.04 | -16.04 | **+0.000** | +0.000 | -0.000 | 아니오 |
| battery 자기겹침 | mini2 | 면 +20 · 면적 -7.40% | -24.59 | -25.11 | **-0.527** | -0.450 | -2.568 | ⭐**예** |
| battery 자기겹침 | phantom3 | 면 +0 · 면적 -0.00% | -19.49 | -19.49 | **-0.000** | -0.000 | -0.000 | 아니오 |
| battery 자기겹침 | phantom4 | 면 +20 · 면적 -5.80% | -18.56 | -19.15 | **-0.589** | -0.808 | -0.316 | 아니오 |
| battery 자기겹침 | m350rtk | 면 +0 · 면적 -0.00% | -5.62 | -5.62 | **-0.000** | +0.000 | -0.000 | 아니오 |
| I4 묻힌 캐노피 | mini5pro | 면 -1394 · 면적 -8.36% | -19.48 | -19.68 | **-0.194** | -0.185 | -0.561 | 아니오 |
| I4 묻힌 캐노피 | mavic4pro | 면 -2240 · 면적 -8.02% | -15.26 | -15.38 | **-0.112** | -0.282 | -0.364 | 아니오 |
| I4 묻힌 캐노피 | matrice4e | 면 -1532 · 면적 -9.55% | -16.04 | -16.52 | **-0.478** | -0.244 | -0.121 | 아니오 |
| I4 묻힌 캐노피 | phantom3 | 면 -2240 · 면적 -3.24% | -19.49 | -19.50 | **-0.009** | +0.149 | +0.666 | 아니오 |
| I4 묻힌 캐노피 | phantom4 | 면 -2240 · 면적 -9.41% | -18.56 | -18.52 | **+0.039** | -0.076 | -0.399 | 아니오 |
| I4 묻힌 캐노피 | typhoonh480 | 면 -384 · 면적 -4.21% | -14.16 | -14.20 | **-0.037** | +0.067 | +0.335 | 아니오 |
| I4 묻힌 캐노피 | mini2 | 면 -1642 · 면적 -10.30% | -24.59 | -25.33 | **-0.739** | -0.382 | -0.689 | 아니오 |
| m4 튜브 관통 | x500v2 | 면 +438 · 면적 -2.28% | -16.92 | -17.06 | **-0.139** | -0.977 | -0.528 | 아니오 |
| I3 매몰면(PO) | mini5pro | 면 5107장 · 43603 mm² (32.9%) | -19.48 | -23.47 | **-3.988** | -2.384 | -3.710 | ⭐**예** |
| I3 매몰면(PO) | mavic4pro | 면 5793장 · 94862 mm² (28.5%) | -15.26 | -17.72 | **-2.454** | -1.594 | -2.076 | ⭐**예** |
| I3 매몰면(PO) | matrice4e | 면 4658장 · 58289 mm² (19.1%) | -16.04 | -17.24 | **-1.201** | -0.843 | -0.875 | ⭐**예** |
| I3 매몰면(PO) | mini2 | 면 3795장 · 31400 mm² (34.3%) | -24.59 | -27.92 | **-3.329** | -3.166 | -5.339 | ⭐**예** |
| I3 매몰면(PO) | phantom3 | 면 3771장 · 22994 mm² (8.3%) | -19.49 | -20.02 | **-0.530** | -0.542 | -0.413 | 아니오 |
| I3 매몰면(PO) | phantom4 | 면 4913장 · 102949 mm² (30.7%) | -18.56 | -19.51 | **-0.952** | -1.909 | -0.255 | ⭐**예** |
| I3 매몰면(PO) | m350rtk | 면 2793장 · 91805 mm² (8.8%) | -5.62 | -5.96 | **-0.347** | -0.145 | +0.151 | 아니오 |
| I3 매몰면(PO) | x500v2 | 면 1130장 · 79104 mm² (18.1%) | -16.92 | -18.33 | **-1.403** | -2.820 | -3.277 | ⭐**예** |
| I3 매몰면(PO) | s1000plus | 면 642장 · 82313 mm² (6.9%) | -10.90 | -11.27 | **-0.363** | -0.214 | -1.145 | ⭐**예** |
| I3 매몰면(PO) | typhoonh480 | 면 4268장 · 63107 mm² (13.9%) | -14.16 | -14.96 | **-0.802** | -0.800 | -1.280 | ⭐**예** |

* **행이 없는 기체 = 그 수리가 안 건드린 기체**다. 지문이 바이트 단위로 같아서 σ 도 비트동일이다.
* ⭐**m6 은 행이 하나도 없다** — `uv_sphere` 는 `drone_cad` 어디에서도 안 쓰인다(호출부는 전부
  `rcs_po`·`rcs_sbr`·검증 스크립트의 **기준구**다). 드론 10기체 전부 지문이 그대로고 Δσ 는
  **정확히 0.000000** 이다. m6 은 σ 수리가 아니라 **기준구 위생** 수리다.
* **I5 도 σ 수리가 아니다** — mini2 삼각형 1장(4.65e−06 mm²)이라 Δ = **1.6e−09 ~ 5.7e−09 dB**.
  값어치는 다른 데 있다: 껍질이 닫혀야 내부판정(`contains`)이 성립하고, 그래야 매몰면 검사가
  참값을 본다(수리자 원장: 안 닫으면 44.45 % 를 29.71 % 로 **낮춰** 본다).
* **battery 는 기체를 갈라 본다** — 팩·구조판이 실제로 겹친 4기체(mini5pro·mavic4pro·mini2·
  phantom4)에서는 면 +20 · 표면적 −5.3~−7.4 % 가 사라지고 Δ 가 −0.32~**−2.83** dB 다.
  겹침이 없던 3기체(matrice4e·phantom3·m350rtk)는 **지문은 바뀌는데**(합집합이 좌표를 나노미터
  급으로 다시 찍는다) **σ 는 소수 넷째 자리까지 0.000** 이다 — 결함이 없는 곳에서 아무 일도
  안 한다는 것이 수로 확인된다.
* ⭐**바이스태틱이 모노보다 크게 흔들린다** — battery 는 모노 −0.5 dB 인데 바이 β120 에서
  −2.8 dB(mini5pro), i3 는 mini2 에서 모노 −3.3 / 바이 **−5.3** dB. 형상 오차가 바이스태틱에
  더 아프다는 §4-2 의 결론과 같은 방향이다.
* ⚠**최악방위 Δ 는 결론에 쓰지 마라.** 널 안에서 일어나는 일이라 부호가 쉽게 뒤집힌다(원장에는
  칸마다 `worst_az_delta_db` 로 실어 두었다). 판정은 방위평균으로만 한다.

### 9-3. 누적 — 전부 켜면 얼마나 바뀌나 · 상쇄하나 더하나

| 기체 | 메쉬 5종만 Δ el0 / el−30 / bi | 전부(6종) Δ el0 / el−30 / bi | 개별합 el0 | 상호작용 el0 |
|---|---|---|---:|---:|
| mini5pro | -0.69 / -0.91 / -3.29 | **-2.79 / -1.28 / -2.96** | -4.67 | **+1.88** |
| mavic4pro | -0.59 / -0.51 / -1.23 | **-1.92 / -1.99 / -1.49** | -3.05 | **+1.13** |
| matrice4e | -0.48 / -0.24 / -0.12 | **-1.19 / -0.88 / -0.86** | -1.68 | **+0.48** |
| mini2 | -1.29 / -0.77 / -3.22 | **-1.98 / -2.13 / -4.54** | -4.60 | **+2.62** |
| phantom3 | -0.01 / +0.15 / +0.67 | **-0.20 / -0.30 / -0.21** | -0.54 | **+0.34** |
| phantom4 | -0.55 / -0.86 / -0.72 | **-0.17 / -1.55 / +0.43** | -1.50 | **+1.33** |
| m350rtk | -0.00 / +0.00 / -0.00 | **-0.35 / -0.15 / +0.15** | -0.35 | **+0.00** |
| x500v2 | -0.14 / -0.98 / -0.53 | **-1.28 / -2.92 / -3.42** | -1.54 | **+0.26** |
| s1000plus | +0.00 / +0.00 / +0.00 | **-0.36 / -0.21 / -1.15** | -0.36 | **+0.00** |
| typhoonh480 | -0.04 / +0.07 / +0.34 | **-0.80 / -0.80 / -1.27** | -0.84 | **+0.04** |

**상쇄한다.** 상호작용(= 전부 켬 − 개별합)이 표에 적은 **el 0° 기준으로 10기체 전부 양수**
(최대 **+2.6 dB**, mini2)이고 배치까지 넓히면 최대 **+4.1 dB**(mini5pro 바이스태틱)다.
⚠다만 배치를 넓히면 음수 칸도 둘 있다 — phantom3 −0.46 · typhoonh480 −0.33 dB(둘 다 바이).
뜻은 이렇다 — battery·i4 는 겹치거나 묻힌 면을 **메쉬에서** 없애고, i3 는 묻힌 면을 **PO
적분에서** 뺀다. 앞의 둘이 이미 없앤 면을 i3 가 다시 뺄 수는 없으므로, 따로 재면 같은 결함을
**두 번 세게 된다**. ⇒ ⭐**여섯을 켜기로 하면 σ 를 다시 재야 하고, 개별 Δ 를 더해서 쓰면 안 된다.**

* 상호작용이 **정확히 0** 인 기체가 둘 있다 — m350rtk · s1000plus. 둘은 i3 말고는 아무 수리도
  안 닿는 기체라(메쉬 지문 불변) 겹칠 것이 애초에 없다. 이것이 상호작용 계산이 옳다는 대조다.
* phantom4 만 부호가 갈린다(모노 el0 −0.17 인데 바이 **+0.43**). 널 근처가 아니라 방위평균에서
  갈리는 것이라 **방위 패턴의 모양이 바뀐** 것이다 — 왜인지는 안 팠다.

### 9-4. ⭐⭐마이크로도플러 축 — 프레임 수리는 0 이다. **i3 는 0 이 아니다.**

규약은 «프로펠러는 아무도 안 건드렸으니 움직이는 성분의 변화는 0 이어야 한다» 였다.
움직이는 성분만 떼어 보려고 세 가지를 따로 쟀다(로터 한 바퀴 12 위상 · az 0/45/90/180° · el 0/−30°):

* `프롭만 σ` — 프롭 점만의 σ(위상).
* ⭐`AC` — 전체 필드에서 **위상평균을 뺀 뒤**의 세기. 프레임은 위상에 안 변하므로 평균을 뺄 때
  **대수적으로 소거된다** — 즉 이 양은 프레임과 원리상 무관하다. 프레임 수리가 여기 나타나면
  그 수리가 프롭을 건드린 것이다. (메모리의 ⚠AC/DC 잣대 아티팩트를 피하려고 레벨과 AC 를
  **따로** 적고 비율은 안 쓴다.)
* `전체 σ` — 관측되는 신호. 프레임이 바뀌면 **당연히** 바뀐다.

| 판 | 기체 | 프롭 메쉬 | 프롭만 σ 최대Δ | AC 최대Δ(**프롭만** — 9-7 참조) | 전체 σ 최대Δ | 변조깊이 전→후 |
|---|---|---|---:|---:|---:|---|
| 메쉬 5종 | mavic4pro | 동일 | **0** | **0** | 3.20 | 3.61→3.37 dB |
| 메쉬 5종 | mini5pro | 동일 | **0** | **0** | 4.57 | 2.42→3.26 dB |
| 메쉬 5종 | mini2 | 동일 | **0** | **0** | 3.48 | 3.01→3.54 dB |
| 메쉬 5종 | matrice4e | 동일 | **0** | **0** | 1.87 | 4.82→5.73 dB |
| 메쉬 5종 | m350rtk | 동일 | **0** | **0** | 0.00 | 2.46→2.46 dB |
| i3 | mavic4pro | 동일 | 14.65 dB | 0.097 dB | 13.35 | 3.61→3.76 dB |
| i3 | mini5pro | 동일 | **0** | **0** | 8.16 | 2.42→2.35 dB |
| i3 | mini2 | 동일 | 8.24 dB | 0.210 dB | 13.41 | 3.01→3.69 dB |
| i3 | matrice4e | 동일 | 0.19 dB | 0.002 dB | 9.22 | 4.82→6.14 dB |
| i3 | m350rtk | 동일 | 20.28 dB | 0.059 dB | 3.02 | 2.46→3.11 dB |


**메쉬 수리 5종: 0 이다 — 어림이 아니라 비트 단위로 0.** 프로펠러 메쉬 지문이 그대로고,
프롭만의 복소 필드가 **바이트 단위로 동일**하며(36 개 필드값의 sha256 일치), AC 성분의 차는
반올림 찌꺼기뿐이다(≤ 1.4e−13 dB). 로터 중심 좌표와 외형보정 배율도 전 기체 불변이다 —
즉 «날도 안 바뀌고 날이 앉는 자리도 안 바뀌었다».

⛔**i3 는 다르다 — 여기서 멈추고 보고한다.** i3 는 프로펠러 **면을 PO 적분에서 뺀다**:
mavic4pro 156 장(306.7 mm²) · m350rtk 232 장(8,301 mm²) · phantom4 3,263 mm² ·
typhoonh480 2,788 mm² · mini2 289 mm². 그 결과 프롭만의 σ 가 방위·위상에 따라 최대
**14.7 dB(mavic4pro) · 20.3 dB(m350rtk)** 움직이고, 프레임과 무관해야 할 **AC 성분까지
0.002~0.210 dB** 움직인다.

* **왜 그런가**(진단): 빠지는 면은 대부분 **프롭 허브가 모터 벨 안에 든 부분**이다 —
  매몰면 인구조사가 그 쌍을 `prop_in_motor` · `motor_in_prop` 로 집어낸다. 물리적으로는
  «진짜 안 보이는 면» 이 맞으므로 **i3 가 틀렸다는 뜻은 아니다.**
* ⚠**그런데 마스크가 날과 함께 돈다.** mini2 는 빠지는 프롭 면이 위상에 따라
  **878 장(0°) → 360 장(30°) → 88 장(90°)**, 면적으로 **288.8 → 207.6 → 177.2 mm²** 로 변한다.
  = 프로펠러가 **다른 부품 안으로 들락거린다**(로터 디스크가 겹치는 기체다). 마스크가 위상마다
  달라지면 i3 는 **없던 위상 변조를 넣는다.**
* ⇒ **판정**: i3 를 **마이크로도플러 계산에 그대로 쓰면 안 된다.** σ(정적 RCS)에는 켜도 되지만,
  움직이는 성분을 보는 계산에서는 (a) 프롭 그룹을 마스크에서 빼거나 (b) 위상마다 마스크를 다시
  계산하되 그 위상 의존이 **모델인지 아티팩트인지** 먼저 가려야 한다. 지금은 어느 쪽도 안 되어 있다.
* 참고로 **전체 σ 의 변조 깊이**(위상에 따른 최대−최소)는 메쉬 수리만 켜도 2.4→3.3 dB 처럼
  움직인다. 이건 날이 바뀌어서가 아니라 **날이 부딪는 정지 성분(DC)이 움직여서**다 —
  같은 이유로 이 수를 «마이크로도플러가 바뀌었다» 로 읽으면 안 된다.

### 9-5. 이 라운드가 스스로 확인한 것 (게이트 · 교차검증 · 귀속)

1. **검사기** — 얼린 스냅샷에서 `src/mesh_check.py` **10/10 통과**(수리 끔), 수리를 켠 판
   (`--mesh-fix i5,m6,battery,i4,m4`)도 **10/10 통과**. 적대 시험
   `benchmark/adv_mesh_check_faults.py` 는 끔·켬 둘 다 **16/16 통과**.
   (i3 는 메쉬를 안 바꾸는 PO 쪽 수리라 검사기 인자에 안 넣었다.)
2. ⭐**앞 라운드 수치 재현 75 칸, 최대차 0.0000 dB.** 네 수리자가 **자기 스냅샷**에서 낸
   방위평균 Δ 를 내 스냅샷·내 프로브로 다시 재서 칸마다 맞췄다(battery 21 · i4 21 · m4 3 ·
   i3 30 칸). 부호 규약만 다르다 — 그쪽은 «결함이 부풀린 양»(전 − 후)을 양수로 적었고
   이 표는 «후 − 전» 이다. **네 라운드의 σ 표는 독립 재측정으로 살아남았다.**
3. ⭐**«기본 경로 지문이 6기체에서 어긋난다» 는 경보를 닫았다 — 범인은 2층이 아니다.**
   i5·m6 수리자가 라운드 끝에 이 사실을 보고하고 원인 규명을 넘겼다. 확인 결과 바뀐 6기체는
   mini5pro·mavic4pro·matrice4e·phantom4·phantom3·mini2 = **끝단 캡(`smooth_iters=0`)이 닿는
   셸형 6기체와 정확히 같다.** 내 스냅샷 사본에서 **3층 값 5묶음만** 문자열로 되돌리자
   (표식 개수까지 검사: caps 6 · env 1 · gimbal 1 · b4 1 · b7 2) 지문이 **14/14 전부**
   23:46 KST 기준선과 일치했다(드론 10 + uv_sphere 4, 어긋난 항목 0).
   ⇒ **기본 경로 변화는 100 % 3층 «값 적용» 몫이고 2층 스위치 몫은 0 이다.** 2층은 안 켜면
   여전히 비트동일이다. 다만 «기본 경로가 안 바뀐다» 는 문장은 이제 **2층 스위치에 대해서만**
   참이고 저장소 전체로는 3층이 이미 바꿔 놓았다.

### 9-6. 안 잰 것 (빈칸을 빈칸으로 남긴다)

* ⛔**SBR 교차확인 — 못 했다(GPU 금지).** 예측은 «SBR 은 first-hit 이라 매몰면을 애초에 안
  더하므로 **i3 는 SBR 에서 0**, 메쉬 수리는 바깥 껍질이 바뀐 만큼만 움직인다» 인데
  **확인 못 했다. 미해결.**
* **σ 를 안 잰 칸이 있다** — i5·m4·m6 은 대상 기체와 대조 1~2 기체만 쟀다(이 기계가 메모리
  압박으로 느려져 σ 예산을 아꼈다: 한때 40000 점짜리 행렬곱 하나가 25 초 걸렸다).
  안 잰 칸은 원장에서 «안 잼» 이고 **0 으로 적지 않았다.** 그 기체들의 메쉬 지문이 안 바뀐다는
  것은 따로 확인했다.
* **PTD(모서리 회절)** — 합집합·절단이 만든 새 모서리의 기여는 안 쟀다(우리 PO 커널에 없다).
* **스펙트로그램** — 위상 스윕 σ(φ) 와 그 AC 성분까지만 쟀다. STFT 로 플래시 폭을 재지는 않았다.
* **i3 의 위상 의존** — mini2 에서 3 위상(0·30·90°)만 자세히 봤고, 나머지는 12 위상의 면 수
  변화만 세었다. «그 위상 의존이 물리인가 마스크 알갱이 문제인가» 는 **모른다.**

### 9-7. ⭐적대 검증 — 다른 사람이 다시 재고 빠진 축을 찾았다 (2026-08-16, 검증자 라운드)

정본: `outputs/mesh_impact_layer2_adversarial_0816.json` ·
재현: `benchmark/adv_mesh_impact_l2_{sigma,i3rule,i3sens,health,attrib,inertia,bitident,ledger}_0816.py`.
CPU 전용(GPU 금지 준수) · git 무접촉 · 소스 값 변경 없음.

**재현된 것(전부 통과)**

* **게이트** — 지금 저장소에서 다시 돌렸다: `mesh_check` 끔 10/10 · 켬 10/10,
  `adv_mesh_check_faults` 끔 16/16 · 켬 16/16.
* **인자를 안 주면 비트동일** — 수리 함수 세 개(`collapse_degenerate_faces` · `_fix_i4_canopy` ·
  `_fix_m4_arm_clamp`)를 감시하며 10기체를 지었더니 **호출 0회**. 빈 문자열 · `mesh_fix=False` ·
  켰다 끈 뒤 모두 지문 동일. 3층 값 5묶음만 되돌린 사본에서 **14/14**(드론 10 + uv_sphere 4)가
  «수리전코드» 지문으로 복귀 ⇒ **2층 스위치 몫 0** 이라는 9-5 의 귀속이 독립으로 재현됐다.
* **σ 독립 재계산** — 샘플러(4분할 무게중심)·방위격자(1°부터 3°, 120점)·점간격(λ/6)·주파수(5점)를
  전부 바꿔 12칸을 다시 쟀다. **최대차 0.15 dB**(i3/mini5pro), 중앙 0.02 dB — 부호·크기·밴드 판정 동일.
* **대조군·누적** — 지문 불변 칸의 σ 바이트 동일, 겹침 없던 3기체의 battery 수리 Δ ≤ 1e−5 dB,
  상호작용 30칸 재계산 전부 일치.
* ⚠**«앞 라운드 75칸 재현» 의 성격** — 같은 커널·격자·소스 스냅샷을 다시 돌린 것이라 **재실행**이지
  독립 방법이 아니다(그래서 최대차가 0.0000 이다). 독립성은 위 σ 재계산이 담당한다.

**[고침] 9-4 의 «AC» 칸은 정의문과 다른 값을 실었다**

표는 원장의 `max_dac_prop_db`(**프롭 점만**의 AC)를 실었는데, 같은 절의 정의문은 «**전체 필드**에서
위상평균을 뺀 뒤» 라고 적는다 — 그 값은 `max_dac_full_db` 이고 원장에 이미 들어 있다.

| 기체 | AC 프롭만(표에 실린 값) | ⭐AC 전체필드(정의문이 가리키는 값) |
|---|---:|---:|
| mini2 | 0.210 | **1.803** |
| mavic4pro | 0.097 | **0.140** |
| m350rtk · matrice4e · mini5pro | 0.059 · 0.002 · 0 | 0.059 · 0.002 · 0 |

왜 갈리나: i3 는 위상마다 마스크가 달라져 **프레임 면까지** 위상 의존이 생긴다(mini2 는 전체 제거
면이 3795→2997 로 움직인다). 큰 쪽이 실제로 관측되는 «없던 변조» 다. ⇒ 판정
(«i3 를 마이크로도플러에 그대로 쓰지 마라»)은 **더 강해진다**. 메쉬 수리 5종은 두 값 다 0 이다.

**[남김] «거의 안 움직인다» 는 방위평균에서만 참이다**

같은 원시 배열을 방위별로 보면 패턴이 재배열된다(원장의 최악방위 경고는 **최댓값**만 물리쳤다 —
중앙값이 1~3 dB 라는 것은 널 문제가 아니다).

| 판 | 배치 | 방위평균 Δ | 방위별 \|Δ\| 중앙값 | 1 dB 넘는 방위 |
|---|---|---:|---:|---:|
| 메쉬5종 mavic4pro | 바이 β120 | −1.23 | **3.30** | 79 % |
| 메쉬5종 mini5pro | 바이 β120 | −3.29 | 3.56 | 94 % |
| m4 x500v2 | 모노 el0 | −0.14 «보임» | **1.30** | 59 % |
| i4 mavic4pro | 바이 β120 | −0.36 | 1.09 | 57 % |
| battery mavic4pro | 모노 el−30 | −0.33 | 0.84 | 42 % |

방위평균을 쓰는 결론(탐지거리 사다리·기체 간 서열)은 안 흔들린다. 흔들리는 것은 **자세별 σ 를
그대로 쓰는 계산**이다. 전 칸 표는 적대 원장 `남김_1_...표` 에 있다.

**[남김] i3 의 dB 는 «어느 그룹을 내부로 인정하나» 라는 규칙에 1~2 dB 매달린다**

`mesh_buried.INTERNAL_GROUPS` 는 «반투명 셸 뒤라 보인다» 를 battery·pcb·fc **셋에만** 인정한다.
그런데 셸 **안에만** 든 camera·motor·gear·accent 도 물리적으로 같은 상황인데 «결함» 으로 지워진다 —
지우는 면적 중 그런 면이 mavic4pro **40.3 %**(38,233/94,862 mm², camera 만 18,296) ·
mini5pro 35.0 % · mini2 31.9 %. 그것들을 도로 살린 판과 비교하면
**mavic4pro −2.46 → −1.16 dB(차 1.30) · mini5pro −4.10 → −1.86(차 2.24) · mini2 −3.48 → −2.99(차 0.49)**.
어느 쪽이 옳은지는 이 검증으로 못 정한다. 다만 **이 라운드의 가장 큰 dB 가 한 번도 재 본 적 없는
규칙 선택에 1~2 dB 매달려 있다**는 사실은 9-6 «안 잰 것» 에 들어가야 한다.

**[남김] 게이트 통과는 예산 두 칸을 넓힌 뒤의 통과다**

i4 의 불리언 합집합은 씨접합 슬리버를 새로 만든다 — 내가 센 값 mini2 185→215 · typhoonh480 461→508 ·
mini5pro 235→285 · matrice4e 239→261 · x500v2 381→389(m4), 수리자 선언과 일치.
그래서 `mesh_check.SLIVER_BUDGET_MESH_FIX` 가 두 칸을 넓혔다(mini2 198→240 · typhoonh480 508→560).
면적은 mini2 기준 3.63→4.36 mm²(표면적의 0.005 %)라 σ 는 안 움직이지만, 9-5 의 «10/10 통과» 문장은
**예산을 같이 조정했다**는 사실과 함께 읽어야 한다. (외형은 안 바뀐다 — 10기체 전부 bbox Δ = 0.)

**[남김] 질량·관성 축이 영향 원장에 안 실렸다(측정 누락이 아니라 인계 누락)**

battery 합집합은 겹친 부피를 한 번만 세게 만든다 ⇒ battery 부피 **−12.27 %**(4기체 공통) ·
battery 질량 mavic4pro 214.2→192.7 g · phantom4 319.9→288.8 g · CoM 0.18~0.45 mm ·
관성 대각 **+1.9~+2.8 %**(겹침 0 인 matrice4e 는 정확히 0). 수리자 원장
(`outputs/mesh_layer2_battery_overlap_0816.json :: 질량축_감사I9`)과 소수점까지 같다.

---

## ⑩ ⭐⭐값 적용 결과 — 수리 하나하나가 무엇을 얼마나 바꿨나 (2026-08-16, 영향 측정 라운드)

§⑤ 가 «수리 자리만 뚫고 값은 안 바꿨다» 로 끝난 뒤, 세 라운드가 **값을 넣었다**.
이 절은 그 값들이 **형상을 얼마나 움직였고 σ 를 얼마나 바꿨는지**를 별도 프로세스로
다시 재서 적은 것이다. 값을 넣지 않았고, 재기만 했다.

정본 수치: `outputs/mesh_impact_0816.json` · 재현: `benchmark/mesh_impact_0816.py`(드라이버) +
`benchmark/mesh_impact_probe_0816.py`(자) · 표 렌더러 `benchmark/mesh_impact_report_0816.py`.

### 10-0. 어떻게 쟀나 (잣대와 사고 회피)

* **σ 규약** — 우리 커널 PO(`src/rcs_po.py`) · **CPU 전용** · 가림(self-shadowing) 없음 ·
  재질 가중 |Γ| 켬 · fc 3.5 GHz · 대역 100 MHz **5 주파 비코히런트 평균** ·
  점 간격 λ/7(중심주파수) · 방위 **1° 격자 360 점** · el 0°·15°.
  방위평균 = 10log₁₀(mean σ), 최악방위 = 3° 각도평활 뒤 최저 σ.
* ⭐**동시 편집 사고 회피** — 이 라운드가 도는 동안 다른 라운드가 같은 파일을 고치고 있다.
  그래서 저장소를 직접 재지 않고, `src/` 를 얼린 사본에서 **수리를 하나씩 되돌린 소스 트리**를
  짓고 트리마다 **별도 프로세스**로 쟀다. 되돌리기 규칙이 옳다는 증거: 전부 되돌렸다가
  전부 되넣은 트리(T5)의 소스가 스냅샷과 **바이트 단위로 같고**, 그 트리가 짓는 10 기종 메쉬
  지문이 **저장소 현재 소스가 짓는 것과 10/10 일치**한다.
* **사슬 vs 단독** — 단계 사슬(T0→T1→…→T5)의 단계별 Δ 를 더하면 누적 Δ 가 되는 것은
  **망원경 항등식**이라 상호작용의 증거가 못 된다. 그래서 «그 수리 **하나만** 넣은 세계»를
  따로 지어 단독 Δ 를 재고, 그 합을 누적과 비교했다(10-3).

### 10-1. 한 줄 판정

**형상은 크게 고쳤는데 σ 는 거의 안 움직였다. 이번 라운드에서 dB 를 만든 것은 부품 형상이
아니라 «기체 전체 크기 배율» 두 건뿐이다.**
mavic4pro **+0.92 dB**(짐벌 수리가 드러낸 세로 배율 1.352→1.598) ·
phantom4 **−0.29 dB**(면내 강제 배율 1.0198 해제) · 나머지 8 기종은 |Δ| ≤ 0.05 dB.

### 10-2. 수리별 표 — 기체 × 수리 × (형상 Δ · Δσ 방위평균 · Δσ 최악방위 · 검사기)

| 기체 | 수리 | 형상이 실제로 움직인 양 | Δσ 방위평균 el0 / el15 [dB] | Δσ 최악방위 el0 / el15 [dB] | 검사기 |
|---|---|---|---|---|---|
| mini5pro | ③ 끝단 캡 | 셸(`body`)만 · 캡 테두리 최대 1.10 mm · 삼각형 −480 | +0.001 / +0.005 | +0.019 / +0.042 | ✅ |
| mavic4pro | ③ 끝단 캡 | 셸만 · 캡 테두리 최대 2.31 mm | −0.009 / −0.002 | −0.033 / +0.029 | ✅ |
| matrice4e | ③ 끝단 캡 | 셸 + 짐벌이 기수에 매달려 0.01 mm 따라옴 · 캡 2.73 mm | −0.008 / −0.014 | +0.608 / −0.158 | ✅ |
| mini2 | ③ 끝단 캡 | 셸만 · 캡 1.03 mm | −0.001 / −0.001 | −0.007 / −0.001 | ✅ |
| phantom3 | ③ 끝단 캡 | 셸 1.82 mm + 높이 배율 sz 0.998109→0.998073 파급(전 그룹 0.007 mm 급) | −0.005 / −0.001 | −0.051 / −0.067 | ✅ |
| phantom4 | ③ 끝단 캡 | 셸만 · 캡 2.42 mm | −0.002 / −0.003 | +0.059 / +0.172 | ✅ |
| phantom4 | ⑥ L/W 강제 해제 | **축간거리 −6.92 mm**(356.92→350.00) · 로터 중심 각 축 −2.45 mm · L/W −5.61 mm · 높이 강제 유지 | **−0.286 / −0.177** | +1.821 / +2.122 | ✅ |
| mavic4pro | ① 짐벌 축소 | 짐벌 볼 95→60 mm · **최저점이 카메라에서 발로 바뀜** · 그 결과 sz **1.3524→1.5977**, 기체 전체가 세로 +18 % | **+0.928 / +0.464** | −1.885 / +5.774 | ✅ |
| matrice4e | ④ 뒷다리 inboard | `gear` 만 · 뒷다리 뿌리 반경 +5.05 mm(xy 만, 발 z 불변) · 삼각형 +4 | −0.002 / +0.004 | −0.442 / −0.522 | ✅ |
| matrice4e | ⑦ 모터 벨 z 사슬 | **모터 −22.47(뒤)/−14.65(앞) mm** · 프롭 장착면 같은 양 · 다리 꼭대기 −6.29 mm · 발 z 불변 · sz 7.2e−6 | −0.034 / **+0.137** | −0.781 / −0.067 | ✅ |

* 출처 구분: 「캡 테두리 최대 N mm」는 **캡 라운드가 정점 1:1 대응으로 잰 값**을 옮긴 것이고
  (`outputs/mesh_apply_caps_envelope_0816.json`), 나머지 형상 숫자(그룹별 z 이동·축간거리·
  로터 중심·배율·삼각형 수)와 **모든 σ 값은 이 라운드가 다시 쟀다**.
* 「검사기」 = `src/mesh_check.py`. 전 수리가 들어간 **현재 저장소 상태에서 10/10 통과**를
  다시 확인했다(기체별 ✅, 매몰면 «진짜결함» 은 전부 예산 안: matrice4e 19.12/20.6 ·
  mavic4pro 28.50/32.7 · phantom4 30.74/33.6 · mini5pro 32.94/36.2 · phantom3 8.27/9.2 ·
  mini2 34.27/37.7 %).
* ⚠**최악방위 Δ 는 결론에 쓰지 마라.** 널(null) 안에서 일어나는 일이라 기준선이 조금만
  흔들려도 부호가 바뀐다 — 캡 라운드가 자기검사로 이미 선언했고, 이 라운드도 같은 자리에서
  같은 불안정을 봤다(예: matrice4e ③ 의 el0 +0.608 dB 는 방위평균 −0.008 dB 와 같은 수리다).

### 10-3. 누적 — 전부 넣으면 기체마다 σ 가 얼마나 바뀌나 · 상쇄하나 더하나

| 기체 | σ 전 el0 [dBsm] | σ 후 el0 | **누적 Δ el0** | **누적 Δ el15** | 방위패턴 RMS el0 [dB] |
|---|---|---|---|---|---|
| mavic4pro | −16.185 | −15.266 | **+0.919** | **+0.462** | 6.61 |
| phantom4 | −18.267 | −18.555 | **−0.288** | **−0.180** | 1.80 |
| matrice4e | −15.991 | −16.035 | **−0.044** | **+0.127** | 0.44 |
| phantom3 | −19.481 | −19.486 | −0.005 | −0.001 | 0.10 |
| mini5pro | −19.483 | −19.482 | +0.001 | +0.005 | 0.05 |
| mini2 | −24.586 | −24.587 | −0.001 | −0.001 | 0.01 |
| m350rtk · x500v2 · s1000plus · typhoonh480 | — | — | **0 (메쉬 비트동일)** | 0 | 0 |

**상쇄하나 더하나** — «그 수리 하나만» 넣은 세계와 비교(el 0°, 방위평균):

| 기체 | 단독 Δ [dB] | 단독 합 | 누적(전부) | **상호작용** |
|---|---|---|---|---|
| mavic4pro | ③ −0.009 · ① +0.916 | +0.907 | +0.919 | **+0.012** |
| phantom4 | ③ −0.002 · ⑥ −0.288 | −0.290 | −0.288 | **+0.002** |
| matrice4e | ③ −0.008 · ④ +0.000 · ⑦ −0.032 | −0.040 | −0.044 | **−0.004** |
| mini5pro · mini2 · phantom3 | ③ 만 | = 단독 | = 단독 | 0.000 |

⇒ **수리들은 서로 거의 간섭하지 않는다**(최대 0.012 dB). 더하지도 상쇄하지도 않고
**따로 논다**. 이유는 각 수리가 서로 다른 부품·서로 다른 축을 만지기 때문이다.
⚠ 단 **방위별 패턴은 더하고 상쇄한다** — mavic4pro 는 전후 패턴 RMS 가 6.6 dB 라
«같은 기체의 다른 실현»에 가깝다(세로로 18 % 늘며 모든 위상중심이 이동). 방위평균만 비교할 것.

### 10-4. ⭐마이크로도플러 축 — 움직이는 성분은 정말 0 인가

이번 수리는 프로펠러를 안 건드렸으니 **움직이는 성분이 0 이어야 한다**. 로터 위상을 한 바퀴
(36 단계) 돌려 σ(위상)을 재고, 위상축 평균을 뺀 **변동분**(= 마이크로도플러를 만드는 바로 그
성분)을 비교했다. el 0°·30° × az 0/45/90/180°.

| 무엇 | 결과 |
|---|---|
| 프로펠러 메쉬 지문(CCW·CW) | **10 기종 전부 불변** ✅ |
| 로터 1개 회전에코(중심을 원점으로 = 평행이동 불변 축) | **10 기종 전부 비트동일 (Δ = 0.0000 dB)** ✅ |
| 프롭 4개 합성에코 — mini5pro · m350rtk 등 | 비트동일 ✅ |
| 프롭 4개 합성에코 — mavic4pro | 수치적으로 0 (최대 |Δ| < 0.0001 dB) — 네 로터가 **같은 양**만큼 올라가 공통 위상이 되기 때문 |
| 프롭 4개 합성에코 — **matrice4e** | ⚠ **변동분 RMS 최대 3.58 dB** — 전부 ⑦ 탓, 전부 **el 30° 에서만**(el 0° 은 정확히 0) |
| 프롭 4개 합성에코 — **phantom4** | ⚠ 변동분 RMS 최대 2.22 dB — 전부 ⑥ 탓 |

⭐**판정: 날(blade) 축은 0 이다.** 0 이 아닌 것은 **날이 앉은 자리**뿐이고, 그것은 두 수리가
의도적으로 옮긴 것이다 — ⑦ 이 matrice4e 로터면을 앞뒤 **다른 양**(−14.65 / −22.47 mm)으로
내렸고, ⑥ 이 phantom4 로터 중심을 xy 로 −2.45 mm 씩 당겼다. 네 로터의 **상대** 위상이 바뀌면
합성 에코의 간섭 무늬가 옮겨간다. 의도 밖의 것을 건드린 흔적은 없다.

⚠**el 0° 만 보면 이 변화가 안 보인다** — 시선의 z 성분이 0 이라 위상식에 z 가 아예 안 들어간다.
1차 측정에서 실제로 «matrice4e 프롭 에코 비트동일» 이라는 **거짓 안심**이 나왔다. 앞으로 z 를
건드리는 수리를 검사할 때는 **el ≠ 0 을 반드시 넣을 것.**

⚠**파급**: matrice4e 의 저장된 마이크로도플러·σ 원장(report13/15/16 · `das_fleet_*`)은 이 라운드로
낡았다. phantom4 도 같다.

### 10-5. 비트동일 — 안 건드린 기체는 그대로인가

| 기체 | 지문(수리 0건) | 지문(전부 적용) | 판정 |
|---|---|---|---|
| m350rtk | `aca348407b60f5c7` | `aca348407b60f5c7` | ✅ 완전 동일 |
| x500v2 | `8699b9adc578dca9` | `8699b9adc578dca9` | ✅ 완전 동일 |
| s1000plus | `bb7a86d302fe43cb` | `bb7a86d302fe43cb` | ✅ 완전 동일 |
| typhoonh480 | `5296bbe633fac64e` | `5296bbe633fac64e` | ✅ 완전 동일 |
| mini5pro · mini2 · phantom3 | 바뀜 — ③ 에서만 | | 의도 |
| phantom4 | 바뀜 — ③·⑥ | | 의도 |
| mavic4pro | 바뀜 — ③·① | | 의도 |
| matrice4e | 바뀜 — ③·④·⑦ | | 의도 |

⭐부품 단위로도 **깨끗하게 귀속된다**: ④ 는 `gear` 하나만, ③ 은 `body` 하나만(matrice4e 는
짐벌이 기수에 매달려 0.01 mm 따라옴). ⑦ 은 실제로 움직인 것이 `motor`·`prop`(−14.65/−22.47 mm)
와 `gear`(꼭대기 −6.29 mm)·`body`(암) 넷뿐이고, 나머지 그룹은 높이 배율 sz 가 7.2e−6 만큼
다시 풀리며 **마지막 자리(0.0007 mm)** 가 흔들린 것이다 — 형상 변화가 아니다.
① 도 같은 구조다: 실제로 고친 것은 `camera`(짐벌) 하나인데 sz 1.3524→1.5977 이 전 그룹을
세로로 늘린다. 그래서 ① 의 σ 는 «짐벌 수리의 σ» 가 아니라 «배율의 σ» 다(10-1 · 10-7 참조).
⚠ 1차 측정에서는 그룹 지문을 **면 인덱스로** 냈다가 «앞 그룹의 정점 수가 바뀌면 뒤 그룹이
전부 바뀐 것처럼 보이는» 유령을 봤다. 그룹 지문은 **좌표만**으로 내야 한다(고쳐서 다시 쟀다).

### 10-6. 세 라운드가 적은 σ 를 독립으로 다시 재 봤다

| 수리 | 그 라운드가 적은 Δσ 방위평균 el0 / el15 | 이 라운드가 다시 잰 값 | 차이 |
|---|---|---|---|
| ① mavic4pro 짐벌 | +0.93 / +0.47 | +0.928 / +0.464 | 0.002 / 0.006 |
| ③ 끝단 캡(6종 최대차) | mavic4pro +0.003 / matrice4e −0.009 | −0.009 / −0.014 | **0.012** / 0.005 |
| ④ matrice4e 뒷다리 | −0.001 / +0.004 | −0.002 / +0.004 | 0.001 / 0.000 |
| ⑥ phantom4 외형 해제 | −0.286 / −0.177 | −0.286 / −0.177 | 0.000 / 0.000 |
| ⑦ matrice4e 벨 z | −0.035 / +0.140 | −0.034 / +0.137 | 0.001 / 0.003 |

⇒ **전부 재현된다**(최대 어긋남 0.012 dB, 그것도 방위평균이 ±0.01 dB 인 «없는 것과 같은» 수리에서).
잣대가 다르다(그쪽 72 방위·9 주파 ↔ 이쪽 360 방위·5 주파)는 점을 감안하면 사실상 일치다.

### 10-7. 안 들어간 값 — 빈칸과 그 이유

| 감사 항목 | 상태 | 왜 빈칸인가 |
|---|---|---|
| ② mini5pro·mavic4pro **세로 배분** | ⛔**안 넣음** | matrice4e 를 고친 것은 «방법» 이 아니라 **공식 STEP CAD 한 벌**이었다. DJI 는 이 두 기종 CAD 를 공개하지 않고, 두 기체 셸 높이는 **이미 사진 실측**이라 늘리면 실측을 지운다. ⚠ 게다가 ① 을 넣으면서 mavic4pro sz 가 1.3524 → **1.5977** 로 **더 나빠졌다**(짐벌이 가리고 있던 구멍이 드러난 것 = 의도된 폭로). **지금 mavic4pro 는 세로로 +59.8 % 늘어난 채 PO 에 들어간다 — 다음 라운드 1순위.** |
| ⑤-a phantom4 착륙아치 | 결함 실재 · **지시된 손잡이로 못 품** | 붙는 자리의 세로 간극이 33.7 mm 다. `GEAR_ARCH` 를 가로로 당기면 정면사진 실측 스팬 141 mm 가 0 이 되고, 세로로 올리면 다리 높이가 실측 113 → 147 mm 가 된다. 진짜 자리는 셸 밑면 프로파일(B3·B8). |
| ⑤-b phantom3 착륙아치 | 같음 | y_top·x_top 이 넷 다 매뉴얼 정투영 실측이라 «닿게 하려고» 옮길 수 없다. |
| ⑤-c m350rtk 프롭허브 | ⛔**철회** | 프레임만(프롭 없는 메쉬) 보고 잰 거짓양성. 전체 드론에서는 프롭 허브와 표면거리 **0.0 mm** 로 접촉한다. 지시대로 내리면 겹침이 6 mm 깊어져 PO 이중계상을 **만든다**. |
| ⑤-d x500v2 레일 | ⛔**철회** | 정점만 쓰는 잣대의 산물. 표면 대 표면은 0.000 mm(관통). s1000plus·typhoonh480 에서 이미 철회한 실패의 3·4 번째 사례. |
| matrice4e 벌림각 로터별 분리 | 미착수 | F07 단면은 앞 −14.8°·뒤 −9.8° 인데 우리는 공통 −13.4°. 발 반경 잔차(앞 +3.7 mm)의 대부분이 여기서 나온다. |
| phantom4 프레임 L/W 잔차 −1.94 % | 의도적으로 남김 | 배율로 늘리면 ⑥ 을 도로 무르는 것이다. 셸 표(fl·fw)와 다리 아치로 풀 문제. |

### 10-8. 이 라운드가 모르는 것 (선언)

* **가림(self-shadowing)·다중반사가 없는 PO 로만 쟀다.** SBR 로 다시 재면 특히 el 15°/30° 의
  값은 달라질 수 있다(가림이 로터면 높이에 민감하다). ⛔GPU 금지 제약으로 이번엔 못 했다.
* **절대 σ 는 이 시각 기준선의 것이다.** 병행 «2층 수리» 라운드가 같은 파일을 고치고 있어서,
  Δ 는 안전하지만(같은 스냅샷 안에서만 대조) 절대값은 그 라운드가 합류하면 다시 재야 한다.
* **σ 는 정지 자세 한 점**이다. 자세(roll/pitch)·거리·편파는 안 봤다.

---

## ⑪ 적대 검증 — «값 적용» 라운드 재검 (2026-08-16, 검증자 라운드)

⑩ 을 **믿지 않고 다시 지어 다시 쟀다**. 되돌리기 패처를 새로 짜서 수리를 하나씩 뺀 소스 트리를
따로 만들고(현재 저장소와 md5 일치 확인), σ 는 **다른 자**로 쟀다 — 방위 2°(180점) · 3주파 ·
점간격 λ/6 (⑩ 은 1°·5주파·λ/7). ⛔GPU 미사용 · git 미접촉 · 저장소 소스 무편집.

### 11-1. 재현된 것 (전부 통과)

| 무엇 | ⑩ 이 적은 값 | 검증자가 다시 잰 값 |
|---|---|---|
| ① 짐벌 Δσ 방위평균 el0/el15 | +0.928 / +0.464 | **+0.919 / +0.462** |
| ⑥ 외형해제 Δσ el0/el15 | −0.286 / −0.177 | **−0.276 / −0.176** |
| ⑦ 벨 z Δσ el0/el15 | −0.034 / +0.137 | **−0.037 / +0.135** |
| 수리 사이 상호작용 | ≤ 0.012 dB | **≤ 0.012 dB** (짐벌 +0.0122 · 외형 +0.0028 · 벨z −0.0021) |
| 절대 σ (현재 저장소, el0) | mavic4pro −15.266 · matrice4e −16.035 · mini2 −24.587 | **−15.276 · −16.048 · −24.591** |
| 최악방위 Δ | ⚠ 결론에 쓰지 말 것 | **동의** — 같은 수리가 자를 바꾸면 el0 에서 1.3 dB 까지 움직인다(짐벌 −1.885 ↔ −3.147) |

형상도 부품 단위로 다시 쟀다: ⑦ 은 벨 밑동이 공식 STEP 값 **−8.510 / −16.330 mm 에 정확히**
앉고 네 발은 여전히 한 평면(−59.820)이다 · ④ 는 `gear` 만 움직여 뒷발이 CAD 208.54 에 대해
203.22 → **208.27 mm** 로 붙었다 · ③ 은 기수 캡이 점 하나에서 **41.020 × 36.870 mm** 테두리로
돌아왔다(실측표 41.02 × 36.88) · ⑥ 은 축간거리가 **350.000 mm** 다.
비트동일도 확인 — 안 건드린 4종 지문 동일, 프롭 지문 10/10 동일, 그룹 귀속(③ = body 하나,
matrice4e 만 body+camera, phantom3 만 sz 파급)까지 ⑩ 과 일치. 검사기 10/10 · 적대 시험 16/16
직접 재실행 통과. ① 이 새 유령 부품을 만들지도 않았다(늘어난 연결요소 2개는 큰 볼이 삼키고
있던 **전방 어안 2개**이고, 둘 다 셸 안에 162 정점 중 86 개가 박혀 있다). 매몰면도 오히려
줄었다(29.74 → 28.50 %).

### 11-2. ⭐마감 규칙 위반 — «다음 라운드» 통은 없다

⑩ 은 mavic4pro 세로 배분을 «**다음 라운드 1순위**» 로 적었다. 그런데 `docs/RESUME.md` 의
**메쉬 마감 규칙**(2026-08-16 15:18, 사용자 지시)은 그 통을 없앴다 — 발견은 **A(지금 고침)**
아니면 **B(영구 한계 선언 → 인증서 `not_guaranteed`)** 둘뿐이다. 따라서 아래 셋은 **인증서로
가야 하고, 이 감사에서 다시 열지 않는다**:

* mavic4pro·mini5pro **세로 배분** — 지금 상태를 정확히 적으면: mavic4pro 는 sz **1.5977**
  (세로 +59.8 %)로 PO 에 들어간다. 셸 높이가 설계값 62 mm 인데 **출하 메쉬에서는 103.6 mm**
  다(수리 전 87.7 mm). ⑩ 의 헤드라인 **+0.92 dB 는 짐벌이 아니라 이 늘어남이 만든 값**이다.
  ⚠ ⑩ 이 적은 이유(«공식 CAD 가 없다»)는 사실이지만 **반쪽**이다 — 같은 라운드가 ① 을 풀 때
  쓴 FCC 정면컷과 **같은 세트에 측면 정투영 자 사진**(`p04`·`p06`)이 있고 그쪽은 시도하지
  않았다. A 로 갈지 B 로 갈지는 그 사진으로 세로 예산을 **잴 수 있느냐**로 갈린다.
* phantom4·phantom3 **착륙아치**(⑤-a/b) — 손잡이로 못 푼다는 판정에 동의한다 → **B**.
* matrice4e **벌림각 로터별 분리** — 근거(F07 앞 −14.8°·뒤 −9.8°)는 있으나 미착수 →
  A 로 넣거나 B 로 선언해야 한다. 발 반경 잔차(앞 +3.6 mm)가 여기서 나온다.

### 11-3. ⑩ 이 안 적은 부작용 — ⑥ 이 프롭↔벨 겹침을 6 배로 키웠다

phantom4 에서 외형 강제를 풀면 프레임이 면내로 1.98 % 줄고, **프롭은 안 줄어서** 프롭이
벨 솔리드 안으로 더 들어간다. 실측(같은 검사기, 같은 스냅샷):

| 잣대 | ⑥ 전 | ⑥ 후 |
|---|---|---|
| 프롭↔벨 **솔리드** 겹침 | 0.616 % · 242 mm² | **3.742 % · 1473 mm²** (예산 4.2 %) |
| 프롭↔벨 원통 근사 | 1.818 % · 깊이 1.755 mm | 1.588 % · 깊이 1.539 mm |

두 잣대가 **부호가 반대**라 «검사기 ✅» 만 보면 안 보인다(솔리드 쪽이 2026-08-10 에 원통
근사를 대체하려고 만든 정본 잣대다). 예산 4.2 % 는 08-10 에 **3.74 % 를 재서** 적어 둔 값이라
지금 값이 그 안에 들어갈 뿐이고, «안 늘었다» 는 뜻이 아니다. 성질은 ① 과 같다 —
**가짜 부풀림을 걷어내니 밑에 있던 오차가 드러난 것**이고, 지금 출하 메쉬는 프롭 표면의
**3.74 %**(≈1.2 × 10³ mm²)를 PO 에서 **두 번 센다**. 인증서 `not_guaranteed` 에 실을 것.
⚠같은 문단이 두 줄 위에서 같은 수를 «3.74 %» 로 적는다 — 반올림 표기를 3.74 로 맞췄다
(2026-09-04). ⚠이 몫은 **한 기체·한 판**에서 잰 것이고 기종마다 다르다.

### 11-4. 작은 것 둘 (수치는 안 건드림)

* ① 의 «발보다 2.16 mm 위» 는 **배율 걸기 전 좌표**에 넣었는데 z 는 뒤에 sz 로 곱해진다 —
  출하 메쉬의 실제 간극은 **3.41 mm** 다. 짐벌 볼도 60 mm 로 지었지만 출하되는 세로는
  **95.9 mm**(사진 실루엣 51 × 48 mm). 즉 ① 은 짐벌 세로 오차를 **없앤 게 아니라 줄였다**.
  (s 를 바꾸면 sz 가 바뀌고 sz 가 다시 간극을 바꾸는 순환이라, 11-2 의 세로 배분과 **같이**
  풀어야 한다.)
* ⑦ 의 암 **뿌리** z 층차 ±3.91 mm 는 CAD 실측이 아니라 datum 정의에서 나온 **주장**이다
  (CAD 근거는 암 **끝** r=100~200 구간뿐). 몸통 속이라 σ 영향은 없다.

> 이 절이 쓴 자·산출: 검증자 자체 패처로 지은 트리 12 벌 · `src/mesh_check.py`(직접 재실행) ·
> FCC 사진 `mavic4pro_p05` 픽셀 재판독(⑩ 과 2~3 px 안에서 일치). 저장소 소스는 한 줄도 안 고쳤다.
