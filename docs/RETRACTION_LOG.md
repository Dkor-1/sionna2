# 철회 기록 — 정본

> 이 프로젝트가 **주장했다가 스스로 거둬들인 것**의 단일 정본이다.
> 새 철회가 생기면 여기에 먼저 적고, 그다음 다른 문서를 고친다.
> ⭐ **정정 전 표현이 아직 살아 있는 파일을 찾을 때 이 문서를 grep 한다.**

## 왜 이 문서가 있나

2026-08-03 사용자 지적:
> *"지금 선행 연구나 문헌을 제대로 안 읽어서 작업 자체가 잘못 진행되고 자꾸 빙빙 돌아가는 느낌이 좀 있는거같아"*
> *"너의 주장 여러개가 앞으로도 더 틀릴 수 있으니 진짜 사전 조사는 더 힘줘서 해"*

정확한 지적이었다. **패턴이 하나다 — 원문을 열기 전에 단정하고, 그 위에 쌓고, 나중에 되물린다.**
아래 여덟 건 전부 같은 원인이다.

---

## R1 ⭐ 무모호 속도식의 우선권 — **두 번 틀렸다**

| 시점 | 주장 | 실제 |
|---|---|---|
| 처음 | `v_max = λ·PRF/4` 는 **우리 발견** | ❌ |
| 1차 정정 | **Chen 2024** 가 같은 기호로 먼저 | ❌ **또 틀림** |
| **확정** | **Abratkiewicz 외, IEEE JSTARS 16:3469–3484 (2023), 게재, 식 (16) p.3476** | ✅ |

원문(직접 확인):
> *"The Doppler range is limited by T^SSB_dist so that **Vb ∈ [−λ/(4·T^SSB_dist), λ/(4·T^SSB_dist)]**"*

**반구간 규약까지 동일**하다. Chen 2024(Appl. Sci. 14(10):4282, 게재)는 **닫힌형 식이 없다** —
`PRF` 기호 0회, 바이스태틱 도플러 식 (4)와 수치 예시뿐이고 그마저 Samczynski TGRS 2021 인용이다.

⭐ **보상 발견 — 이게 R1 을 유리하게 만든다.** Abratkiewicz 결론 p.3482:
> *"The subsequent problem worth considering is **small target detection, for instance, drones whose
> reflectivity is significantly lower than the car** used in the experiment."*

그들 표적은 **Volvo XC90**(`car` 31회, `drone` 3회는 전부 향후과제 맥락, `UAV` 0회).
Chen 표적은 **스테퍼모터 회전 모형**. **두 논문 다 드론을 안 썼다.**

→ **우리 위치**: *"우리가 처음"* 이 아니라 **"식의 저자가 직접 열어둔 자리"**. 반박이 안 된다.

## R2 모노스태틱의 PRF 자유도

`"모노는 PRF 를 자유롭게 고른다"` → **거짓**. 3GPP sub-6 CSI-RS **500 Hz 천장**(13단 슬롯 사다리).
n78 에서 **10.71 m/s**, 공개 기체 최고속도를 **0/5** 로 못 덮는다(최저 typhoonh480 13.5 m/s).
⭐ 정정이 논증을 **강화**했다 — *"모노도 10.71 m/s 에서 멈춘다"* 가 더 방어 가능하다.

## R3 "5G 커버리지 = 0"

**단일 CPI 아티팩트.** CPI=100 ms **와** 선언 2.5빈 가드가 동시에 성립할 때만 참이다.
검출기의 실제 1.5빈 하드가드에서는 같은 CPI 에서 **0.636**, 200 ms 면 0.303.
⚠ **LTE 도 짧은 CPI 면 1.000 이 된다** — 전 헤딩 블라인드는 5G 의 성질이 아니라 짧은 CPI 의 성질이다.
⚠ 슬라이드에 쓸 때 **어느 가드 규약인지 반드시 명시**할 것(선언 2.5빈이면 LTE T_max=0.005 s,
하드 1.5빈이면 0.0393 s).

## R4 σ 앵커가 레벨을 옮긴다

`slope_only`(생산 기본)는 평균을 **4.74e-15 dB** 움직인다 = 안 움직인다.
**절대 레벨은 우리 PO 출력 그대로**이고 앵커는 **기울기만** 옮긴다.
⭐ 대가까지 적을 것: `level_and_slope_L2` 면 +7.42 dB, `L4` 면 +16.92 dB 이고
**크기전이법칙 선택만으로 s1000plus 에서 9.50 dB** 가 갈린다.
우리 자체 크기지수는 **1.24~1.32** (r = 0.64~0.70) 로 L² 도 L⁴ 도 아니다.

## R5 상반성 위반 13.7 dB

한 셀(matrice4e·5G·β=45)의 **이등분선 근사 p95** 였다. 상반성 위반이 아니다.
실제: 이등분선 근사 p95 최대 20.04 dB / rms 중앙 7.47 dB, **상반성 rms 최대 5.80 dB**(β=90 에서 8.24).

## R6 ⭐ "스톡 Sionna 에 회절이 없다"

**우리 오류다.** 기술보고서 p.47 에 **1차 UTD 쐐기회절이 구현**돼 있다
(Kouyoumjian–Pathak 을 Luebbers 가 유한도전율로 휴리스틱 확장한 판).
우리 설치본 2.0.1 이 `diffraction=False` / `edge_diffraction=False` 로 **기본값이 꺼져 있을 뿐**이다.
⚠ 반대 방향 오류도 금지: *"Sionna UTD 가 우리 PTD 결손을 메운다"* 도 **거짓**이다 —
Sionna 의 D 는 **전파경로의 필드 전달**용이지 표적 산란단면 산출이 아니다. **같은 물리의 다른 사용처**다.

## R7 SBR 44회 (48 아님)

기술보고서에서 대소문자 구분 **44회**. 대소문자 무시 45회 중 1건은 참고문헌 [28] 의 독일어
`Ausbreitungsbedingungen` 오탐.

## R8 "19편이 표적을 씬에 세웠다"

`reflib_sweep_sionna.json` 실제값: `role_TARGET_IN_SCENE_any = 7`, 고유 저작 **6편**, 전파전용 118편.
19 는 우리 주장 C1 의 **문구**였을 뿐이다.

---

## 추가 정정 (철회는 아니나 반드시 반영)

| # | 잘못 | 옳음 |
|---|---|---|
| A1 | 이면각 수리 전 오차 **−26.46 dB** | **출처 없음.** a=0.30 m 에선 1회반사 −118.33 dBsm vs 정확해 14.43 → 오차 **−132.77 dB**. −27.19 는 a=0.40 케이스 |
| A2 | 측정 밴드기울기 **0.175~0.315** dB/GHz | **0.07~0.315**. ⭐ **Das WCL 2026 은 Yuan EuCAP 2025 원자료의 재분석**이다(사사에 명시) — **"독립 앵커 2건" 이 아니라 측정 1건** |
| A3 | plastic εr 2.7 **출처 없음** | **출처 있다** — Zechmeister & Lacik, COMITE 2019, 1–10 GHz, ABS 2.55–2.95. **진짜 출처 없는 건 셸 두께** |
| A4 | εr 1.4 / σ 1.2 = **배터리** | **챔버 흡수체**다. 배터리는 ITU metal(표피깊이 계산으로 정당화) |
| A5 | Ziganshin 차량 dBsm 이 **MLFMM 검증** | **FEKO 의 PO 솔버** 대조다 — *"MLFMM is complicated for a car … so we use the PO solver"*. 구만 전파해석과 맞췄다 |
| A6 | v_max 표를 **c 표기 없이** 인용 | 우리 표는 **c = 3e8** 로 계산됐다. Abratkiewicz 는 1.0901, 정확한 c 면 1.0894 — **어느 c 인지 밝혀야** 모순으로 안 읽힌다 |
| A7 | 기술보고서 판본 | 파일명은 `v1` 이나 내용은 **arXiv:2504.21719v2, Version 1.2 (2025-11-24)**, 59쪽 |

---

## ⭐⭐ A8~A11 — 2026-08-03 정정 전파 라운드 (`outputs/correction_sweep.json`)

> ⚠ **이 네 건의 공통점: 정정 지시 자체가 틀렸거나 범위를 잃었다.**
> R1~R8 은 "원문 안 읽고 단정" 이 원인이었다. A8~A11 은 **"정정하면서 범위를 떼어냈다"** 가 원인이다.
> 새 실패 유형이므로 따로 묶는다.

### A8 ⭐ "+2.5068 dB 앵커 오프셋은 근거 없으니 끄라" — **그 지시가 틀렸다**

`src/sigma_anchor.py : LOG_TO_LIN_EXPONENTIAL_DB = 10/ln10 · γ = 2.5068 dB` 는
dB영역평균 `E[10log10 σ]` → 선형평균 `10log10 E[σ]` 변환 상수다(σ 가 지수분포 = Swerling I/II 일 때 **정확**).

**증거 — 직접 재실행**(`reconcile_das_yuan`, Das Table III Phantom 3 vs Yuan 고도풀링 선형평균):

| 분기 | 오프셋 | 우리 3밴드(1.843/3.5/5.21 GHz) 잔차 | 전대역(1.8~18.2) 잔차 |
|---|---|---|---|
| **`exponential`** | **+2.5068 dB** | **0.1161 ~ 0.2502 dB** ✅ | 0.4894 dB |
| `lognormal` | +3.1921 dB | 0.8013 ~ 0.9355 dB | 1.1773 dB |
| `none` (= 끄기) | 0.0000 dB | **2.2566 ~ 2.3908 dB** ❌ | 2.9963 dB |

두 논문은 **같은 원자료**이므로(A2) 잔차는 0 이어야 한다. 끄면 2.26~2.39 dB 어긋나고, 켜면 0.25 dB 안에 든다.

⭐ **물리도 같은 쪽을 가리킨다.** Das Table II 는 Phantom 3 최적적합을 **Rician** 으로 놓고(d_AD 0.436),
Yuan Fig.6(a) 축자: *"The K factors vary around −10 dB at some frequencies, while the others are
significantly lower, falling below −30 dB."* K ≤ −10 dB 인 Rician 은 레일리 진폭 = **지수 전력**이고,
그 자리가 바로 +2.5068 이 **근사가 아니라 정확**한 자리다.

**규칙(이게 A8 의 본체다):** 이 상수는 **분포 가정**이지 측정값이 아니다.
→ **두 분기를 언제나 함께 들고 다닌다**(`exponential` +2.5068 / `lognormal` +3.19~3.43).
→ **조용한 단일값 금지.** 끄는 것도 단일값이다(`none` 을 고른 것이고, 그게 제일 나쁘다).
→ ⭐ 그리고 **기울기로 이끈다** — 오프셋은 레벨 상수라 `dμ/df` 에서 **완전히 상쇄**된다.
   규약 논쟁이 안 닿는 자리가 기울기다.

### A9 ⚠ Phantom 2 / Phantom 3 뒤집힘 — **브리프가 틀렸고 코드가 맞았다**

과제 브리프는 Phantom 2 를 광대역 원거리장 플랫폼으로 적었다. Das Table I 실제값:

| | 대역 | 조건 | 앵커 자격 |
|---|---|---|---|
| Phantom 2 | **11–26 GHz** | 근거리장 2.6 m · **비**무향 실내 | ❌ 이중 부적격 (우리 대역과 겹치지도 않음) |
| **Phantom 3** | **1.8–18.2 GHz** | 원거리장 · 무향실 CATR | ✅ 유일한 앵커 |

`src/sigma_anchor.py` 는 **원래부터 맞게 돼 있었다**(`DEFAULT_ANCHOR = "das_phantom3_mono"`,
그리고 :490 이 *"Das 의 Phantom 2 만 근접장 2.6 m 비무향이고, 우리는 그 행을 앵커로 쓰지 않는다"*).
전수 스윕 결과 저장소 안에 **뒤집힌 채 살아 있는 인스턴스는 0건**이다.
→ 교훈: **브리프를 코드보다 위에 두지 않는다.** 충돌하면 코드와 원문을 먼저 연다.

### A10 ⭐ Ziganshin 런타임 — **판본을 섞었고, 초 단위를 per-pose 로 읽었다**

**(a) 0.7 s / 19.0 s 는 per-pose 가 아니다.** 저널판 p.8 축자:
*"two vehicle meshes with **360 angular points**"* + *"all observation locations are processed
**simultaneously (in parallel) within a single RT simulation**."*
→ 각도점당 **1.94 ms (RT) → 52.8 ms (+EV/VE)**. 우리 per-pose 값 옆에 0.7 s 를 놓으면 **360배 틀린다.**
⚠ 단 **비율 15~27배는 그대로 유효하다** — 분자·분모가 같은 스윕이라 척도가 상쇄된다. 고칠 것은 **절대 초** 뿐이다.

**(b) 회의판과 저널판은 다른 실험이다. 절대 합치지 않는다.**

| | EuCAP 회의판 (5쪽, IEEE Xplore) | 저널 프리프린트 arXiv:2604.05991v2 (11쪽) |
|---|---|---|
| 차량 대조 상대 | **FEKO PO 솔버** (Fig.6 *"RCS of the simplified car"*, `EM (PO)` vs `RT+UTD+VD`) | **실측**(백스캐터, 3 GHz HH) + **MLFMM**(그림자, 2 GHz VV) |
| 인쇄 단위 | **dBsm** | `\|E_sc\|` · `\|E_tot\|` **dB** — dBsm 곡선 자체가 없다 |
| 런타임 문장 | PO 솔버 **약 하루** vs RT 약 2초 / MLFMM **약 1시간** vs RT <1초 (구, 720점) | MLFMM *"several hours"* · Table II 사다리 |

→ **A5 는 회의판에 한정해서 참이다.** 저널판은 차량을 MLFMM 과도 대조한다(그림자 영역).
   판본을 안 밝히고 A5 를 적용하면 **맞는 기록을 틀리게 "고치게" 된다.**

**(c) ⚠ "EuCAP 회의판은 우리 코퍼스에 없다" 는 틀렸다.** arXiv 이름이 아니라 제목으로 저장돼 있다:
`/data/public/sionna_jeong/papers_isac_sionna/paper_sionna_Ray_0723/Ray-Based_Simulation_of_Multistatic_Scattering_from_Target_Objects_in_ISAC.pdf`
→ 회의판 인용 4건(하루 · 1시간 · Fig.6 dBsm · *"MLFMM is complicated for a car"*) 전부 **축자 재확인 완료**.
⭐ 파일명 검색으로 "없다" 를 선언하지 않는다. **내용으로 검색한다.**

### A11 ⚠ A2 의 대체 구간 `0.07~0.315` 도 **대역을 섞는다**

A2 는 `0.175~0.315` 를 버리고 `0.07~0.315` 를 세웠는데, `src/sigma_anchor.py : SLOPE_CENSUS` 를 열면
그 코드가 **이미 반례를 들고 있다**(`overlaps_our_band` 필드):

```
겹침 O  das_phantom3 0.21 · yuan_az 0.315 · yuan_top 0.231 · yuan_bot 0.175   (1.8~18.2 GHz)
겹침 X  das_phantom2 0.21 (11~26)   das_mini2 0.07 (21~27)   das_m350rtk 0.17 (21~27)
```

**0.07 은 21~27 GHz 값이다.** 우리 밴드는 1.843/3.5/5.21 GHz 다.
→ 우리 대역 비교에 쓸 구간은 **0.175~0.315**(겹침 4건), 전 기체 총람은 **0.07~0.315**(7건).
   **둘 다 옳고, 범위를 안 밝히면 둘 다 틀린다.**
⚠ 그리고 겹치는 4건마저 **한 측정의 네 통계**다(A2 본문). "4개 독립값" 이 아니다.
⚠ 더 넓게: Das Table III 28칸 전체는 **−0.13 ~ +0.50 dB/GHz** 이고 한 칸은 음수다(`HOW_OTHERS_SOLVED_IT.md`).

---

## ⭐⭐ R9~R11 — 2026-08-03 적대검증 라운드 3 (`outputs/r3_claim_attack.json`)

### R9 "우리 커널은 **해석해** 대비 검증됐다"

**한 낱말이 세 가지 다른 과녁을 뭉갠다.**

| 과녁 | 정체 | 무엇을 잡는가 |
|---|---|---|
| **정확 Mie** | 참 해석해 | ⭐ **진짜 외부 검증. 이것 하나뿐이다** |
| 해석 PO 구 | **커널과 같은 물리** | 수치 수렴만. PO 자체 오차는 원리적으로 검출 불가 |
| 평판 4πA²/λ² · 이면각 8πa²b²/λ² | **PO/GO 폐형해** | 역시 PO 로 PO 채점 |

`src/rcs_sbr.py:800` 이 스스로 적어놨다 — *"금속 평판 … **정확 PO** σ=4πA²/λ²"*. **정확한 것은 답이 아니라 PO 의 답이다.**

**살아남는 문장:** 정확 Mie 대비 **0.375~0.851 dB** (r=0.178 m, ka 6.88~19.44 세 점).
세 밴드 모두 **같은 부호의 결손**이고 ka 가 커질수록 줄어든다 = 저 ka 에서 PO 가 지는 신호이지 스케일 오차가 아니다.

### R10 이면각 **+0.076 dB**

**4개 크기 중 최량 1개를 인용해왔다.** `outputs/sbr_defect_fixes.json : d3_multibounce_phase.rows`

```
a=0.15  +0.556      a=0.20  +0.466      a=0.30  +0.076      a=0.40  −0.108   →  max|err| = 0.556 dB
```

⚠ **더 나쁘다** — 0.076 이 나온 a=0.30 행은 1-bounce σ 가 **−118.33 dBsm** 으로 붕괴한 행이다
(이웃 a=0.40 은 −7.76 dBsm, **110 dB 도약**). 잔차가 작은 이유가 **수렴이 아니라 그 격자에서의 우연한 상쇄**다.

**정직한 문장:** *"a = 0.15~0.40 m 에서 ≤ **0.56 dB**"*. 0.076 을 단독 인용하지 않는다.

⚠ 추가: **kr 스윕 0.2006 dB 는 div=16 정밀격자** 값이다. **생산 기본 div=12 는 0.2544 dB**.
그리고 이 자 역시 과녁이 해석 PO 이므로 수치수렴 자다 — 같은 스윕의 **정확 Mie 대비 최악은 6.729 dB**(kr=1).

### R11 순위불변성 `dR_dB/dσ_dB = 0.2462`

- 상수가 아니라 **15개 전역 적합의 평균**. 셀별 0.2227~0.2561, 폭이 평균의 **13.6 %** → 유효숫자 4자리는 거짓 정밀
- **국소** 기울기는 0.0493~0.3566 로 **7.2배** 벌어진다
- ⚠ 국소 최대 0.3566 이 σ/R⁴ 사슬의 **이론 상한 0.25 를 넘는다** → 물리가 아니라 **σ 격자 고도각 조회의 계단 아티팩트**
- ⭐ **그리고 순위불변성은 발견이 아니라 항등식이다** — 세 모드가 같은 σ/R⁴ 사슬을 쓰므로 균일 dB 오프셋은 모든 곡선을 같은 양만큼 평행이동시킨다. 스윕은 산술을 확인할 뿐 새 정보를 만들지 않는다

### R12 "Sionna 내부 드론 산란적분은 **공개문헌상 공백**"

**코퍼스 안의 미발견을 문헌의 부재로 말했다.** 근거 센서스는 sionna_used 137편 중 **110편이 표적표현 미확인**(기계집계만)이고,
우리가 **직접 정독한 반례 md-rt**(IEEE ICCT 2025, Sionna RT + Blender 블레이드 메시 + 발사기 개조)가 **센서스에 아예 없다.**

**살아남는 문장:** *"보유 218편 아카이브 안에서, 드론 메시를 PO 표면전류 적분에 통과시켜 절대 σ(dBsm) 를 낸 저작을 찾지 못했다."*
⚠ 그리고 그 공백은 **벤더가 공표한 범위결정**이지 아무도 못 한 일이 아니다.

⭐ **대신 기계로 셀 수 있는 사실이 나왔다**(`outputs/evasion_catalogue.json`, 18편):
표적 산란을 주장한 **17편 중 계산된 절대 dBsm 을 인쇄한 것은 3편**, **드론은 0편**.
가장 흔한 회피는 **기하 대리표적** — 사람을 1.8×0.5×0.25 m 직육면체로, UAV 를 *"metallic cube"* 로 놓는다.

---

## ✅ 공개 반론 — **2026-08-03 답변됨** (`outputs/runtime_benchmark.json`)

**Ziganshin 저널판 `arXiv:2604.05991v2` pp.1–2** — 우리 아키텍처를 정면으로 겨냥한다:

> *"This **SBR+PO approach**, however, is **limited to the illuminated region** and is not suitable to
> predict the scattered field in the **shadow region** of the obstacle. Furthermore, **the need to
> cascade PO after RT negates the computational advantages of RT**."*

| 절반 | 우리 답 |
|---|---|
| 그림자 영역 | 우리 관심은 후방산란(모노)·β≤45°. 전방산란은 애초에 주장 밖. **답 가능** |
| **런타임** | ✅ **잼. 9.2~356 ms/pose (중앙값 38.1, 기하평균 36.3)**, 공유 RTX 4090 1장 |

**비율은 인정하고, 결론은 반박한다.**
- 인정 — 우리 빌드에서 **PO/RT = 16배**이고 RT 는 벽시계의 **3.1 %** 뿐이다. 호스트 numpy PO 가 48.5 %,
  히트레코드 D2H 핸드오프가 26.2 %. 즉 **호스트 측이 96.9 %**다.
- 반박 1 — 그건 **캐스케이드의 성질이 아니라 우리 구현의 성질**이다. SagittaSBR(arXiv:2604.09243)
  Table 1 p.11 은 GPU 상주 SBR+PO 의 커널 분해를 인쇄한다:
  **A100 FP32 Ray Launch 98.96 ms vs PO Integral 6.43 ms (6.5 %)** · MI250X 1GCD **249.81 vs 4.83 (1.9 %)**.
  PO 적분이 **디바이스 커널**이면 캐스케이드 비용은 한 자릿수 %다.
- 반박 2 — **같은 카드 대조**로 하드웨어 교차 반론까지 닫았다: 스톡 `sionna.rt.PathSolver` 를
  **우리 4090 에서** 돌리면 **72.8~128.0 ms**. 우리 중앙값 38 ms 가 그 **아래**다.
- 위치 — 우리 38 ms 는 Ziganshin 자신의 각도점당 **RT 1.94 ms** 와 **전 사다리 52.8 ms** **사이**에 있다.

⚠ 인용 규율은 **A10** 을 따른다 — 0.7 s / 19.0 s 는 **360 각도점 전체 스윕**이고, 회의판·저널판을 섞지 않는다.

---

## ⚠⚠ R13 — "TW-ILDC 를 구현했다" (2026-08-03)

**우리가 만든 것의 이름이 틀렸다.** `outputs/ptd_attack_lit.json` 판정:

> **정직한 이름**: *"untruncated Michaeli PTD-EEC (first order)"*
> — Michaeli 1986(IEEE TAP 34(7):912–918)의 **비절단** 등가모서리전류를 직선 모서리 조각을 따라
> 해석 적분한 **1차 PTD 프린지파 보정**.
> ⛔ **NOT "TW-ILDC" · NOT "ILDC" · NOT "Gao's method"**

⭐ **"TW"(truncated wedge)가 Gao 의 기여 전부인데 우리는 그걸 안 만들었다.**
Johansen 1996 식 (3)(9)(10)(11)(21)(22) 가 Gao 의 TW 이고, 우리가 구현한 것은
식 (4)(5) = Michaeli 1986 (4)–(7) = **비절단 절반**뿐이다.

### 그리고 이 라운드의 결론 자체가 잠정적이다 — C-P1 **REFUTED AS STATED**

| | |
|---|---|
| (a) **생산 커널에 없다** | `src/ptd_edges.py` 는 `benchmark/*.py` 에서만 import 되고 `attach_to_sbr_field` 는 **호출처 0건** |
| (b) ⚠ **전역 부호 뒤집힘** | `A_code / [+(j/k)·L·Σf¹·e^{j2k r·û}]` = **1.0000000000 ∠180°**(θ=10/25/40/65/80°, 양 편파). 같은 전개의 PO 항 `+(j/k)Σf⁰` 은 정확 PO 폐형해를 9자리로 재현하므로 **위상 규약 탓이 아니다** |
| (c) ⭐ **평판 "개선" 이 정확성의 증거가 아니다** | **위상맹목**이다 — 같은 사각 마스크에서 PO 11.000 → PO+PTD 5.425 dB 인데, 모서리 전력을 **비코히런트로 더해도** 비슷한 개선이 나온다 |

⭐ **따라서 "PTD 가 대역기울기 격차를 못 메운다"(4.54→4.08배) 는 결론도 잠정적이다** —
부호가 뒤집힌 채로, 생산 엔진에 붙이지도 않고 잰 값이다. **부호 수리 + SBR 배선 후 재측정해야 한다.**

### ⭐ 살아남은 것

```
회귀 무결      regression_identical = true. 레거시 커널(rcs_po·rcs_sbr·geom·mie_pec_sphere)에
               'ptd' 문자열 0회 — 호출되지 않는 순수 추가분이다
카드 간 결정성  기준 GPU2 / 재실행 GPU0 에서 동일. rcs_anchor.py(PID 1364837)는 무사
참조 솔버      TM/TE 오차 1e-4 dB (n_seg 240)
비용           PO 16.7 → PO+PTD 35.2 ms/pose (+111%, 모서리 추출 포함)
               ⚠ Gao 의 +4.6~17.2% 와 안 맞는다 — 다른 방법을 다른 규모 표적에 쓴 값이라 당연하다
```

⚠ **추가 근사 (슬라이드·논문에 반드시 병기)**: PO 면적분 항은 **스칼라**(|Γ| 가중, 편파 없음)이고
프린지 항만 편파를 갖는다. 같은 스칼라 PO 장을 V·H 양쪽에 더한다. **동일편파만**, 교차편파 없음.

### ⭐ 후속 (2026-08-03, 같은 날 수리) — `outputs/ptd_defect_fixes.json`

| | |
|---|---|
| (b) 전역 부호 | ✅ **수리됨.** 근원은 `docs/PTD_ILDC_FORMULATION.md` §9.1 이 상수를 `λk/4π = ½` 로 **크기만** 잡아 (A) 우리 A 규약의 원거리장 상수 `−jk/2π` 와 (B) 문헌 EEC 복사적분 `+jk/4π` 사이 **상대부호**를 잃은 것. (A) 의 음부호는 `d̂ = −û` 에서 온다. 옳은 상수 = `−½`. Michaeli 계수·법선·적분방향은 전부 무죄(각각 6e−12 일치 / 면순서 불변 / 곱에서 상쇄로 확인) |
| 합격 기준 | `arg(A_code/A_analytic) = 0°` 로 **승격**(크기비는 180° 오류를 통과시킨다). 측정 `|arg| ≤ 5.8e−13°`, `|ratio|−1 = 3.8e−15`. `benchmark/smoke_ptd.py` (f) 와 `ptd_plate_validation.py` 에 상설, 실패하면 예외 |
| Rung 2 재실행 | 사각 oblique pooled RMS: PO **12.25** → PO+PTD **2.16 dB** (해석 1 차 PTD 2.156 dB 와 동일). soft 편파는 0.05 dB. **부호반전 대조군 5.21 dB 도 PO 보다 좋다** — RMS 지표가 위상맹목이라는 증거를 계속 같이 싣는다 |
| (c) 드론 결과 | ⛔ **철회 유지.** 오목 모서리(N<1)가 s1000plus 금속 모서리 길이의 6.5 % 로 모서리장의 99.99 % 를 냈다 → 기본적으로 제외하고 버린 길이를 보고한다. "효과가 금속 모서리 길이에 비례한다" 문장은 **철회**. 대역기울기 주장도 **철회**(부호·문턱·유의성 셋 다 실패) |
| (a) 생산 커널 | ⛔ **여전히 유효한 지적.** `attach_to_sbr_field` 는 원점을 메쉬에서 유도·검증하도록 고쳤지만 **아직 한 번도 실행되지 않았다**(Mitsuba/GPU 필요) |

---

## ⚠⚠ R14 — "φ 를 쓸면 기하 차가 최대 23.17 dB" (2026-08-03, `outputs/phi_sweep.json`)

**φ 를 실제로 쓸어보니(0~355°, 5° 간격, 72점) 그 숫자가 φ 의 성질이 아니었다.**

> *"23.17 dB 는 φ 의 성질이 아니라 **스윕하지 않은 Δz = alt − h_TX = 35 m 의 성질**이며
> (alt 30 m → 40.0 dB, 120 m → 14.8 dB), 표적이 조명원 위 35.2 m 를 지나는 **단일 d칸의 값**이고,
> **R90 동작점에서는 ≤1.20 dB · d 중앙값 ≤3.10 dB** 에 그친다."*

⭐ **즉 우리가 "φ=90° 한 컷이라 위험하다" 고 걱정한 근거 자체가 다른 축(고도차)의 값이었다.**

### 그리고 방향도 반대였다

`DECK_FACTS` G5 는 *"링크버짓 축만 φ 에 매달린다"* 고 적었는데 **반대다:**
```
상수-σ 에서 R90 span  0.48 %  (≤0.083 dB)
자세평균                0.83 %
⭐ φ=90° 가 세 팔 모두 **최소**
```
**φ 의존은 사실상 없다.** 발표된 검출 결과가 φ=90° 라서 위험한 게 아니었다.

⛔ **무효화 대상**: `docs/DECK_FACTS.md` F27 · G5, `outputs/deck_facts.json`,
`outputs/capability_matrix.json:3598`, `docs/GEOMETRY_BENCHMARK.md` 의 23.17 dB 인용 전부.

### ⚠⚠ 대신 **진짜 결함이 두 개 드러났다**

**(a) σ 격자의 고도 범위가 조회 고도를 못 덮는다 — 13.7% 클램프**
```
SNR 천장이 잡히는 d ≈ 248 m 에서 look el = −82° ~ −88°
그런데 σ 격자는 el 0° ~ −20° 만 있다
→ 조회의 13.7% 가 범위 밖에서 **클램프**된다
```
⛔ `report13_freespace` 의 `snr_ceiling_db` (66.813 / 67.357 / 63.561, mini5pro 53.217 / 62.752 / 61.017)
와 `REPORT13_SPEC` 의 *"+24.7 dB @ d452"* 는 **컷 특이(+17.13 dB)이자 수치 불신**이다.

**(b) `published_at_phi90` 로 적힌 값(52.169 / 61.195 / 55.625)이 발표값이 아니다** —
σ 격자 조회가 실패해 **fallback σ = 0.01** 로 계산된 값이다.

⭐ 교훈: **한 축을 의심할 때 그 축만 쓸면 안 된다.** 23.17 dB 는 φ 를 쓸어서 나온 값이었지만
실제 원인은 함께 움직인 **고도차**였다. 축을 하나씩 고정해 분리해야 한다.

---

## ⚠ R15 — "σ 앵커가 **오늘 메쉬**로 갱신됐다" (2026-08-03, 사용자 지적)

사용자 질문: *"mesh 갱신을 지금 실시간으로 하고 있는 거 아니니?"* — 맞는 지적이다.

```
meta.generated  2026-08-03 05:49:19 UTC   ← **착수** 시각
파일 mtime      2026-08-03 12:12:36 UTC   ← 종료 시각
                ─────────────────────────
                6시간 23분이 벌어진다
```
파이썬은 import 시점에 모듈을 한 번 읽고 끝이다. 그래서 **담긴 메쉬는 05:49 의 것**이지
"오늘 최신"이 아니다. 그 사이 `src/drones.py` 는 실제로 **08:00 UTC 에 바뀌었다**.

### 그래도 이번 산출은 유효하다 — 다만 그 근거를 적어둔다

```
git diff src/drones.py  →  +97 / −2
  삭제 2줄 = docstring "현재 7종" → "8종"
  추가 97줄 = 신규 phantom3 엔트리 (line 555)
```
✅ **순수 추가**였고, 이 파일이 계산하는 5기체(`DRONE_KEYS`)의 정의는 손대지 않았다.
`src/rcs_sbr.py` 도 09:44 에 바뀌었으나(PTD 배선) 프로세스는 이미 이전 판을 import 한 뒤였고,
어차피 `ptd=False` 에서 비트 동일이 증명돼 있다(`outputs/ptd_wiring.json`).
→ 원래 문제(07-30 앵커 vs 07-31 메쉬 개편)는 **진짜로 해소**됐다. 05:49 은 07-31 보다 뒤다.

### ⚠⚠ 근본 결함 — 산출물이 **자기가 쓴 메쉬를 안 적었다**

`meta` 는 GPU·div·밴드·caveats·runtime 을 다 적으면서 **메쉬만 안 적었다.**
07-30 판이 낡은 걸 아무도 못 알아챈 이유가 이것이다(그 사이 σ 는 1.66~2.69 dB 움직였다).

**수리**: `benchmark/rcs_anchor.py` 에 `_mesh_provenance()` 신설 → `meta.mesh_provenance` 에
두 층으로 지문을 남긴다.
- `source_sha256_16` — 형상 소스 파일 해시(사람이 git 과 대조하기 좋다)
- `built_mesh` — **실제로 만들어진 꼭짓점·삼각형의 sha256**. 소스가 리팩터링만 됐는지
  형상이 바뀌었는지를 이것만이 구별한다

기존 `outputs/rcs_anchor.json` 에는 **소급**으로 붙였고, 소급이라는 사실과 그 정당화를
항목 안에 함께 적었다(추정을 기록으로 위장하지 않는다). 05:49 시점 `built_mesh` 는 위 diff
논증으로 현재와 같지만, `source_sha256_16` 은 **현재** 파일 해시라 키 이름에 그렇게 박았다.

### ⚠ 앞으로의 위험은 살아 있다

`mavic4pro` · `matrice4e` 사진 정밀화가 **진행 중**이고 둘 다 앵커 5기체 안에 있다.
착지하면 앵커는 **07-30 판이 낡았던 것과 똑같은 메커니즘으로** 다시 낡는다.
⛔ 그때 재생성 전까지 앵커 기반 비교표·그림을 새로 그리지 않는다.

---

## 재발 방지

1. **주장 전 원문** — 에이전트 보고를 그대로 옮기지 않는다. 파일명까지 틀린 적이 있다(R1 확인 중).
2. **인용 등급 3단** — `verbatim` / `reconstructed`(표·그림에서 옮김) / `unverified`.
   기계 검사기 `benchmark/verify_quotes_tool.py` 가 판정한다.
3. **숫자에 등급을 붙여 보고** — "직접 확인" / "인용문 있음" / "기계집계" / "방금 센 것".
   마지막 것은 **발견이 아니라 가설**로만 말한다.
4. ⚠ **기계집계는 인용을 계산과 구별하지 못한다.** wave 1 에서 12편 중 6편이 정정됐다 —
   **미독 논문에도 같은 오류율을 가정**한다.
