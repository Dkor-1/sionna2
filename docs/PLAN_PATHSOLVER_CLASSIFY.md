# PathSolver 로 분류 데이터셋을 만들 수 있나 — 할 일

> 사용자(2026-08-11):
> *"40 m 나 떨어진 드론 마이크로도플러 관측에 1.8억 발 ray 로도 충분히 잘 그려지네?
>  PO 방식 안 쓰고 pathsolver 로 마이크로도플러 그릴 수 있는 거 아니야?
>  이런 식이면 드론 분류나 detection 같은 태스크도 충분히 pathsolver 로 가능한 거 아니니?"*

**정당한 질문이고, 지금 자료로는 «아마 된다» 쪽이다.** 아래는 그것을 정면으로 재는 계획이다.
⚠ 이 문서는 **아직 착수 안 한 것**이다. 시작하면 이 줄을 지우고 진행을 적는다.

---

## 0. 먼저 인정할 것 — 내 예상 두 개가 틀렸다

| 내가 말하려던 것 | 실측 |
|---|---|
| «우리 커널이 싸다» | ⚠ **예산에 따라 다르다** — 아래 표. 한 줄로 못 말한다 |
| «메모리가 광선 수 상한» | ❌ **2³² 정수 한계**다(4,294,967,296). 40억 발이 1.1 s·메모리 증가 0 |

### ⭐ 2026-08-11 재정정 — 비용은 «싸다/비싸다» 로 못 자른다

사용자 지적: *"비용은 40억발 쏘면 훨씬 비싼 거 아니니?"* — **맞다.** 내가 인용하던
«0.21 s/자세» 는 **3 m·100만 발**, 즉 PathSolver 가 가장 유리한 구석의 값이었다.

| | 자세당 | 우리 대비 | 출처 |
|---|---|---|---|
| 우리 커널 3 / 15 / 40 m | 0.601 / 0.512 / **0.635 s** | — | `outputs/deck_ours_by_range.json` |
| PathSolver 3 m (1 M 발) | 0.21 s | **3 배 싸다** | `report07_three_engines.json` |
| PathSolver 40 m (178 M 발) | 0.49 s | 대등 | `report07_range40_raybudget.json` |
| **PathSolver 40 m (4,000 M 발)** | **4.13 s** | **6.5 배 비싸다** | 같은 원장 |

⭐**구조가 다르다.** 우리 커널은 **거리에 비용이 안 걸린다**(0.51~0.64 s, 이 폭은 잡음).
PathSolver 는 광선 규칙 `(R/3)²×1 M` 때문에 **거리 제곱으로 는다.**

**정본 서술**: «가까운 거리에서 최소 예산이면 PathSolver 가 싸고, 멀어지거나 예산을 올리면
우리 커널이 싸다.» ⛔«PathSolver 가 더 싸다» 를 무조건으로 쓰지 마라.

### ⭐ 2026-08-11 철회 — «시드 잡음이 CNN 에겐 자산» 은 틀렸다

내가 «PathSolver 의 몬테카를로 변동이 공짜 데이터 증강» 이라고 주장했다.
사용자 반박: *"환경에 잡음 모델링을 하면 결국 장점은 아니지 않나?"* — **맞다. 철회한다.**

- 우리가 **세기를 지정할 수 없는** 잡음이다. 손잡이가 아니면 축이 못 된다.
- **분포가 다르다** — 수신기 열잡음이 아니라 유한 표본의 추정 분산이다.
- ⭐**거리에 따라 안 커진다.** 진짜 잡음은 멀수록 신호를 묻는데 이건 그 법칙을 안 따른다.
- 수신기 잡음과 겹치면 **성능 하락을 어느 쪽에 붙일지 못 가른다.**

이건 이미 `NOISE_AND_ROTOR_PLAN.md` §T4 에 경고로 적혀 있었다 —
*"PathSolver 는 이미 «잡음이 든» 팔이다. 수신기 잡음을 더 얹으면 이중이 된다."*
논쟁하다 내가 그 문장을 잊었다. **변동성은 우리가 의도적으로 넣는다**(로터 흔들림·수신기
잡음·자세각). 솔버 부작용은 자산이 아니다.

---

그러므로 «PathSolver 로는 못 한다» 는 주장을 **비용 하나로 세울 수 없다** — 단거리·최소
예산에서는 실제로 더 싸기 때문이다. 반대로 «PathSolver 가 싸서 좋다» 도 못 세운다.

---

## 1. 지금까지 측정된 것 — 무엇이 실제 약점인가

`outputs/report07_range40.json` · `deck0811_range_figs.json` · `probe_ray_ceiling.json`

**PathSolver 가 잘하는 것**
- 맵을 그린다. 블레이드 플래시가 보인다.
- 박자가 예측에 꽂힌다 — 3 m −0.32 % · 15 m +0.06 %. 우리와 대등하다.

**측정된 약점 넷 (전부 40 m · 178M 발)**

| | Sionna | 우리 |
|---|---|---|
| 시드 3판의 박자 | 253.3 / 126.5 / 128.1 Hz | 난수 없음 |
| 시드 간 레벨 폭 | **8 dB** | 0 |
| 1× 가 2× 를 이기는 여유 | **+1.3 dB** | **+7.4 dB** |
| 52 ms 창 중 2× 로 뒤집힘 | **4 / 7** | 0 / 7 |

⭐**넷 다 «자세당 경로가 6 개뿐» 에서 온다.** 그리고 경로 수는 광선 수에 정비례한다
(178M→6 · 356M→14 · 712M→25 · 1424M→55 · 2848M→114 · 4000M→**199**).
⇒ ~~광선을 22배 올리면 사라질 가능성이 크다.~~

### ⛔ 2026-08-11 — 위 예상은 **실측으로 반증됐다**

40 m 를 **40억 발**(2³² 바로 아래)로 4,096 자세 × 시드 2개 돌렸다
(`outputs/report07_range40_raybudget.json` · 그림 `outputs/figures/raybudget_40m.png`).
경로 수 중앙값은 **9 → 193**(시드 2 는 165)으로 예상대로 늘었다. 그런데:

| | seed 1 박자 | seed 2 박자 | 시드 간 폭 |
|---|---|---|---|
| 1.78억 발 | 253.27 Hz | 126.51 Hz | **126.76 Hz** |
| **40억 발** | **253.03 Hz** | **126.22 Hz** | **126.81 Hz** |

**시드 의존이 조금도 안 줄었다.** 광선을 22.5 배 부어도 시드 1 은 여전히 **2 배 조화**에 앉는다.

| 지표 | 1.78억 발 | 40억 발 | |
|---|---|---|---|
| 시드 간 박자 폭 | 126.76 Hz | 126.81 Hz | **그대로** |
| 시드 간 레벨 폭 | 2.61 dB | **1.22 dB** | 좋아짐 |
| 1× 대 2× 여유의 시드 간 폭 | 2.30 dB | **9.42 dB** | **나빠짐** |

⭐**광선이 고친 것은 «배경» 이고, 못 고친 것은 «어느 조화가 이기나» 다.**
맵은 눈에 띄게 깨끗해졌지만(잡티가 확 줄었다) 대역 에너지의 최강선이 어디 앉는지는
여전히 난수가 정한다. 이것은 §1 끝의 «광선으로 못 고치는 것»(산란적분 부재)과 **같은 뿌리**로
보이며, 경로 수를 더 늘려서 해결될 성질이 아니라는 첫 실측 증거다.

⚠ **시드가 판마다 2개뿐이다.** «안 줄었다» 는 두 추첨의 관측이지 통계가 아니다.

### ⭐⭐ 2026-08-11 시드 사다리 — 8 시드로 다시 재니 위 두 줄이 **둘 다 부정확했다**

`benchmark/raybudget_seed_ladder.py` · `outputs/raybudget_seed_ladder.json`
40 m · 예산 2 종 × **시드 8 개** · 자세 768~1024. p-p 대신 **표준편차**로 잰다
(p-p 는 표본 수에 따라 커지므로 2 판과 8 판을 비교할 수 없다).

| 예산 | 경로 중앙 | **sd(레벨)** | p-p | **sd(1×/2×)** | ⭐**관측 / i.i.d. 예측** |
|---|---|---|---|---|---|
| 178 M | 8 | **4.156 dB** | 13.18 | 10.44 dB | **23.4 ×** |
| 4,000 M | 165 | **1.833 dB** | 4.86 | 5.33 dB | **31.2 ×** |

**정정 ①** — «레벨 산포 2.61 → 1.22 dB» 는 **시드 2 개만 쓴 부분집합**이었다.
8 시드로는 **4.156 → 1.833 dB**(sd). 방향은 맞았고 크기가 틀렸다.
(`outputs/report07_range40.json` 의 시드 3 판만 봐도 p-p 가 8.008 dB 인데, raybudget 원장은
그 중 둘만 써서 2.61 을 냈다.)

**정정 ②** — «40 m 시드 3 판 중 둘은 예측을 맞춘다» 도 표본이 적어서 나온 인상이다.
178 M · 8 시드의 박자는 `[127.58, 254.33, 126.27, 254.19, 253.78, 254.93, 253.63, 169.63]` Hz
— 예측 126.667 을 맞춘 것은 **8 개 중 2 개**다. 다섯은 2 배 조화, 하나는 169.6 Hz.

**⭐⭐ 그리고 이것이 «그냥 몬테카를로 잡음이려니» 를 정면으로 반증한다.**
시드가 자세마다 다시 뽑는 i.i.d. 잡음이라면 판별 평균 레벨의 산포는
`자세 안 산포 ÷ √N` 이어야 한다. 실측은 그 예측의 **23~31 배**다.
그리고 광선을 22.5 배 올려도 sd 가 4.74 배(=√22.5)가 아니라 **2.27 배**만 줄었다.

이유는 구조에 있다 — `PathSolver(..., seed=a.seed)` 가 **모든 자세에 같은 시드**를 넘긴다.
Fibonacci lattice 는 결정론적이므로 한 시드 = **광선 방향 집합 하나 + 히트 추첨 난수열 하나**이고,
그 집합을 4,096 자세 내내 쓴다. ⇒ **자세 평균으로 안 지워지는 계통 편향**이다.

**⇒ 판정**: 광선을 늘리면 **흔들림은 절반으로 줄지만 종류가 바뀌지 않는다.**
경로 수를 더 늘려 해결될 성질이 아니다.

**검산**: 다시 잰 1.78억 발 수치가 덱 v19 가 인용한 253.27 / 126.51 Hz 와 소수점까지 일치한다
(같은 `flash_spec` 규약·같은 대역 430.05~1228.72 Hz).

**⇒ T1(가산성) 이 이제 1단계다.** 광선으로 약점을 지울 수 없다면, PathSolver 로 분류 데이터셋을
만들 수 있는지는 **가산성이 성립하느냐**에만 달렸다.

**광선으로 못 고치는 것 하나**
PathSolver 에는 산란적분이 없다(경로마다 프레넬 계수를 곱할 뿐). 표적 전체가 만드는
**코히어런트 간섭 무늬(RCS 로브 구조)** 는 광선을 아무리 늘려도 안 나온다.
⇒ **박자·타이밍은 되고, 레벨과 각도별 로브는 안 된다.** 분류가 어느 쪽에 기대는지가 관건.

**거리 벽**
광선 상한 2³² 과 규칙 `(R/3)²×1M` 을 합치면 `R_max = 3√4294 ≈ **196 m**`(송신점 하나).
우리 커널은 거리가 안 들어가 이 벽이 없다.

---

## 2. 비용 — 표 방식이면 반나절도 안 걸린다

우리 분류는 **위상표**를 쓴다: 표 하나에 `E_ref`(정적 기준) + `dE[로터, 위상100]`.
표 108개(기체 6 × 자세 18) = **SBR 호출 54,108 회**로 끝나고,
그 표로 **864 시행 × 4096 자세**를 조회·보간으로 공짜 합성한다.

| 방식 | 호출 수 | 12 병렬 |
|---|---|---|
| 표 방식 · 3 m(1M 발) | 54,108 | **16 분** |
| 표 방식 · 40 m(178M 발) | 54,108 | **37 분** |
| 직접 풀기(표 없이) | 3,538,944 | 17~40 **시간** |

⭐ 표 방식이 **130 배 싸다.** 다만 전제가 하나 있고, 그것이 첫 관문이다.

---

## 3. 할 일 — 순서대로

### T1. ⭐**가산성이 PathSolver 에서도 성립하나** (관문) — 반나절
표 방식은 `E(φ₁..φ₄) = E₀ + Σ ΔE_j(φ_j)` 에 기댄다. 우리 커널은 얼린 격자에서
**잔차 1e−15**(기계정밀도)로 성립한다(`outputs/md_classify_verify.json`).
PathSolver 는 **안 재봤다.**
- 같은 잣대로 잰다: 로터를 하나씩만 돌린 표를 만들고, 네 로터를 동시에 돌린 참값과 비교.
- ⚠**몬테카를로 잡음이 각 항에 실리면 차분이 더 시끄러워진다.** 잔차가 신호 대비 얼마인지가 답.
- 광선 수를 사다리로(1M · 178M · 4000M) 태워 **잔차가 광선으로 줄어드는지** 본다.
- 산출: `benchmark/verify_pathsolver_additivity.py` → `outputs/pathsolver_additivity.json`
- **판정**: 잔차가 작으면 T2 로. 크면 T2 를 건너뛰고 T3(직접 풀기, 축소판)으로 간다.

### T2. **PathSolver 위상표 생성** — 반나절 (T1 통과 시)
- `benchmark/md_classify_dataset.py` 의 표 생성부를 엔진 교체 가능하게 열고
  (`--engine sbr|pathsolver`), 같은 자세 격자·같은 100 위상으로 뽑는다.
- ⛔기존 표(`outputs/md_classify_tables/`)를 덮지 말고 `_pathsolver/` 로 따로.
- ⭐**시드를 표마다 기록**한다. 재현이 안 되면 비교가 성립하지 않는다.

### T3. **분류 정확도 비교** — 반나절
- `md_classify_run.py` 를 그대로 쓰되 입력 표만 바꾼다(같은 특징 27개·같은 분할).
- ⭐**같은 leave-one-aspect-out**, 같은 시행 수, 같은 rpm 산포. 하나만 바꾼다.
- 낼 것: 전체 / f_flash 제외 / geometry_only / 널대조 **네 칸을 나란히**.
- ⚠기대: 박자 기반 특징은 살고 **레벨·로브 기반 특징이 죽을 것**이다. 어느 특징이
  얼마나 죽는지가 이 실험의 진짜 산출물이다(특징별 중요도 차이표).

### T4. **잡음을 넣고 다시** — T3 뒤, 잡음 구현이 끝난 뒤
- PathSolver 의 몬테카를로 잡음과 수신기 잡음이 **겹칠 때** 어느 쪽이 지배하나.
- ⚠PathSolver 는 이미 «잡음이 든» 팔이다. 수신기 잡음을 더 얹으면 이중이 된다 —
  그 층위를 반드시 갈라 적을 것.

### T5. **거리 벽 확인** — 반나절
- `R_max ≈ 196 m` 이 실제로 벽인지 확인(100 m · 150 m · 196 m 근처).
- 송신점을 여러 개 두면 늘어나는지도(멀티 소스로 2³² 을 우회할 수 있나).

---

## 4. 판정 기준 — 미리 못 박는다

**PathSolver 로 충분하다** 고 말하려면 셋을 다 넘어야 한다.
1. 가산성 잔차가 신호의 **1 % 아래**(T1)
2. 분류 정확도가 우리 팔의 **5 포인트 이내**(T3)
3. 시드를 바꿔도 정확도가 **2 포인트 이내**로 재현(T3)

하나라도 못 넘으면 «되는 범위» 를 좁혀 적는다 — 예: «박자 특징만 쓰면 된다» 처럼.

⛔ **결과가 어느 쪽이든 그대로 적는다.** PathSolver 가 충분하다는 결론이 나오면
우리 커널의 역할은 «RCS 로브가 필요한 자리» 로 좁아지고, 그것도 정직한 결과다.

---

## 5. 광선 예산 선행 — ✅ 끝났고, 결과가 계획을 바꿨다

40 m · 40억 발 · 시드 2개 · 4,096 자세. **결과는 §1 의 정정 절**에 있다.
한 줄로: **광선으로는 안 고쳐졌다.** 그래서 T1(가산성)이 1단계가 된다.

⚠ **자료 취급 주의** — 1.78억 발 판은 **8 샤드** 실행이었는데 40억 발 실행(6 샤드)이
`outputs/range40_shards/` 의 00~05 를 **같은 경로에 덮어썼다**. 온전한 4,096 자세를 얻으려면
`outputs/range40_178M/`(00~05, git HEAD 에서 대피) 과 `outputs/range40_shards/`(06~07) 을
**meta 의 spp 로 걸러 합쳐야** 한다. `benchmark/build_raybudget_40m_fig.py` 의 `merge()` 가
그렇게 한다 — 다른 분석기를 새로 쓸 때도 같은 규약을 지켜라.
⛔ `outputs/report07_range40.json` 은 **덱 v19/v20 이 인용하는 1.78억 발 원장**이다. 덮지 마라.
새 예산 결과는 `outputs/report07_range40_raybudget.json` 으로 나간다.

---

## 6. ⭐⭐2026-08-11 탐지 축 판정 — «PathSolver 는 σ 를 안 잰다»

`w50rvnyrg` 12 에이전트. 축마다 두 렌즈로 «우리가 유리하다» 를 반증 시도했고, **둘 다 살아남았다**.

### 6-1. PathSolver 는 σ 를 **크게 틀리는 게 아니라 아예 안 낸다** (증거 셋)

`outputs/rt_no_rcs_verify.json`

| 실험 | 무엇을 바꿨나 | RT 응답 |
|---|---|---|
| `A_plate` | 금속 평판 변 0.2 → 4.0 m (**σ +52.04 dBsm**) | 진폭비 변화 **1.8e−06 dB** |
| `B_sphere_S` | 같은 금속구, 산란계수 S 0.2 → 1.0 | 진폭 **15.76 dB** 이동 |
| `C_pec_sphere` | PEC 구 r = 0.3/1.0/3.0 m × spp 1e6/1.6e7 | **6/6 조합 표적 경로 0 개** |

읽는 법: 정반사 채널은 무한거울 image-source 라 **표적 크기가 답에 안 들어간다**(A).
확산 채널의 진폭은 표적이 아니라 **우리가 손으로 넣은 S** 의 함수다(B) — 이론에 맞추려면
S ≈ 0.85 가 필요한데 그건 우리가 고른 값을 되읽는 순환이다. 그리고 물리적으로 옳은 PEC
곡면에서는 경로가 아예 안 잡힌다(C).

⇒ **탐지 축에서 PathSolver 는 약한 경쟁자가 아니라 경쟁자가 아니다.**
(`outputs/report15_verdict.json` 의 caveats 가 스스로 «절대 σ 는 여전히 우리 PO 커널이
담당한다» 고 적는다.)

### 6-2. ⚠ 그런데 **우리 σ 도 검증 실패 상태다** — 이기는 이유는 정확도가 아니다

| 원장 | 무엇 | 값 |
|---|---|---|
| `das_fleet_validation.json` `prereg_judgement` | 다기체 실측 대조 | **NOT_VALIDATED** · 산포 **16.27 dB**(문턱 6.0) · 최악 m350rtk **+9.08 dB** |
| 같은 파일 `box_control` | 부피등가 PEC 구와 비교 | **4 기체 중 2 기체에서 구가 우리를 이긴다** |
| `sbr_kr_sweep.json` | 정확 Mie 대비 | 저-kr 에서 **max 6.73 dB** |
| `md_range_sweep_mf.json` `meta` | 탐지 사다리가 실제로 쓰는 σ | «가림 없는 순수 PO 점구름», 스스로 **«NOT an absolute RCS anchor»** |
| `benchmark/md_snr_vs_range.py` caveats[0] | 그 σ 가 Das 앵커 대비 | **+3.78 dB**(2.39 ×) → 탐지거리 **× 0.79** |

⇒ 정본 서술: **«우리만 σ 를 낸다»** 이지 **«우리 σ 가 맞다»** 가 아니다.
⛔ 발표·논문에서 이 둘을 섞지 마라.

### 6-3. 얼린 팔의 절대 레벨은 ⛔**인용 금지** (확정)

`src/rcs_sbr.py::sbr_field_bistatic` docstring — *"여기서는 jitter 를 두지 않는다. 단일 격자다."*
위상을 쓰려면 디더 평균을 못 쓴다. 대가(`outputs/freeze_plate_sensitivity.json`):

- 절대 레벨 판 선택에 **3.452 dB p-p**
- 가림 축 **4.163 dB p-p** 이고 `occlusion_level_survives_plate_choice` = **false** — 부호까지 뒤집힌다

⇒ **절대 σ 는 정적(jitter) 경로에서, 얼린 복소장은 «모양» 에만.** 원장이 이미 그렇게 규정했고 옳다.

### 6-4. ⚠ 패시브 호버 사슬은 **아직 안 이어져 있다**

호버 표적은 0-도플러 노치가 동체를 지우므로 탐지가 **블레이드선의 절대 레벨**에 걸린다. 그런데

- 블레이드선을 낼 수 있는 팔은 **얼린 팔뿐**인데 그 팔이 절대 레벨을 못 낸다(§6-3)
- 절대 σ 를 내는 정적 jitter 팔은 **위상이 없어 블레이드선을 못 낸다**

⇒ 지금 탐지거리를 지탱하는 것은 «가림 없는 PO 점구름의 방위평균 σ_eq» 하나다.
그리고 **ECA 노치가 DC 를 지우면 AC 행이 최대 37 dB 움직인다** — `md_snr_vs_range` 자신의
**미측정** caveat 다. **이 사슬을 잇는 것이 8/18 «잡음» 축의 핵심 과제다.**

### 6-5. ⚠ 인용할 때 조심할 것 (감사자가 못 박은 함정)

- ⛔ `outputs/report07_three_engines.json` 의 `levels_db`(sbr −51.5 / po −52.2 / sionna −117.1)를
  «PathSolver 가 65 dB 낮다» 로 읽지 마라. sbr·po 열은 **산란 면적분 E [m²]**, sionna 열은
  **채널 h [무차원]** 이다. **다른 양이다.**
- 우리 최악 오차 9.08 dB 와 PathSolver 시드 산포 8.01 dB 는 크기가 비슷하다. 판정을 가른 것은
  크기가 아니라 **종류**다 — 우리는 σ 라는 양의 **오차**, PathSolver 는 σ 가 아닌 양의 **분산**.
  이 문장을 빼고 숫자만 옮기면 결론이 뒤집힌 것처럼 보인다.
