# 2026-08-10 (일) — 5갈래 스프린트 완주 + 거리스윕·5G 실검증 + 적대적 검증 78편

## 오늘 한 일

### ⭐5갈래 병렬 스프린트 (워크플로 5/5 완료, 커밋 676e448)
- **A 웹 실측 로터 로그**: NeuroBEM(실내 0.54%/0.74%@0.7~2.2Hz)·CODEV(야외 2.4%/2.5%@0.74Hz)·
  DJI P3 DAT(PWM 환산 2~6%) 확보 → `outputs/rotor_rpm_web_anchor.json`.
  판정: SITL ±0.22%는 대칭 이상화 과소치. **야외 프리셋(2%/2.5%@1Hz)·실내 프리셋(0.5%/0.8%) 이원화 제안**.
- **B report07 재작성**: §0 시나리오(측정모형 CW 단일톤·TX/RX 좌표·평면파 A군 vs 근거리장 B군 구분),
  그림별 데이터 이력표, 설정 부록(계산·표시 분리), 조건형 서술(원장 바뀌면 문장 따라감).
- **C 순수 PO Γ(θ) 배선**: opt-in, 끔이면 18/18 비트동일. ⭐정직한 음성 — Sionna 일치 0.696→0.676
  (Sionna 드론 에코는 확산 지배라 벌크 프레넬 전제가 안 맞는 축). sbr↔po 내부 +0.016.
- **D mini5pro 메쉬**: 블레이드 루트가 벨캡에 0.860mm 관통 → 프롭 z +0.165mm로 참 무관통(솔리드 자 0%).
  9기 비트동일. `mesh_check`에 `check_prop_bell_solid` 솔리드 자 + 기체별 예산표(10/10 통과).
- **E 덱 v2**: 14슬라이드, 수치 JSON 주입, PRF 진실표 36/36 assert 기계검증.

### ⭐거리 스윕 (사용자 질문 실측 답)
진짜 모노스태틱(기선 0) · spp∝(R/3)² 1M→32M: 경로 중앙 **5/7/3/0** @ 3/10/20/40m,
40m는 빈 자세 78.3% — PathSolver 구조적 붕괴((D/R)² 입체각). SBR+PO는 표적앵커 λ/16 격자라
원거리장(8.3m) 밖 거리 불변. → `outputs/report07_sionna_ranges.{json,npz}`

### ⭐5G 파형 실검증
같은 채널에 OFDM(SCS 30k·1024SC·QPSK·기지심볼 나눗셈): **풀캡처≡CW 2.4e-14**.
차이는 반복률 — CRS 1kHz 팁 접힘, SSB 50Hz 플래시 접힘. 한계 정직 기록: 가드 2.06e-3, ICI 1차 −17.8dB.
→ `outputs/report07_5g_waveform.{json,npz}`, `benchmark/report07_5g_waveform.py`

### 적대적 검증 78편 (7에이전트)
태그 1,277건 깨짐 0 · 발견 49건(HIGH 6/MED 20/LOW 23) → `docs/REPORTS_ADVERSARIAL_0810.md`.
HIGH: 57편 자기원장 반증(DPI 잔차 주장), 37편 ±2% 낡음(→재계산 완료로 원장은 해소),
15편 ⚠ 역방향 오표시, 18·19·60편. 처방 사다리(커널→원장→빌더 문자열 14항→재빌드).

### 기타
- report15b 재계산 1차(±0.22%, 04:29) 완료 — 단 mini5pro 칸은 메쉬수정 전이라 재계산 재투입(아래).
- HQ 렌더 f0a/f0b/f0c(2304×1440·spp1024, 13.6GB) — 2560×1600×2048은 32GiB OOM 실측.

## 하는 중 (이 파일 작성 시점)
- GPU2: report15b 재계산(메쉬수정 반영, 2기) → 종료 후 σ격자 --force 자동 체인(m4e→mini5)
  (σ프리필이 GPU2 3작업 동시로 BrokenProcessPool 1회 — 순차 체인으로 전환)
- GPU2: 같은구간 RCS 스윕 2~18GHz(matrice4e 진행 중, 이후 mini5pro·phantom4)
- 에이전트①: f9(거리스윕)·f10(5G 4팔) 그림 빌더 + report07 두 절 추가
- 에이전트②: 적대적 처방 적용(빌더 문자열) + 해당 편 재빌드 + 링크검사

## 할 것
- 재계산 완료 → f1~f8 재생성 → report07 재빌드 → 덱 v2 재빌드 → 일괄 커밋
- 덱 v2 낡은 줄 수정(슬라이드14 "mini5pro 9.12% 먼저" → 수정 완료) + 5G·거리스윕 슬라이드 검토
- 로터 산포 프리셋 결정 반영(야외/실내 이원화) — report07·report15b 서술 연동
- 같은구간 스윕 완료 → Das 사과-대-사과 기울기 보고 + 덱 1부 그림

## 미해결 선언
- σ격자 --force가 SLOPE_LEDGER_GAP assert(1.40, 여유 0.0045)를 설계대로 멈출 수 있음 — 멈추면 의식적 정합 작업.
- v1 덱 표지가 2026.08.04로 찍혀 있음(빌더가 _cover_base 재사용) — v2는 패치됨, v1은 방치 중.
- freespace_link.py:207 "버그로 기록한 패턴" — 에이전트② 판정 대기.
- 확인 불가 2건(적대적 검증) 미판정 유지.
