# 2026-08-10 (일) — 마이크로도플러 대개편 · RCS 사과-대-사과 · 리포트 구조 전환

## 오늘의 큰 줄기 넷

### ⭐1. 마이크로도플러 그림을 실측으로 다시 세웠다
- **시간 분해능 상향**: PRF 를 4×f_tip → **16×(19.7 kHz)** 로 열고 자세를 4,096 으로.
  환경변수 `SIONNA2_MD_PRF_MULT` 로 네 스크립트에 배선.
- ⭐**조각 길이를 실측으로 결정**(사용자: «STFT 로 뚜렷하게»). 플래시 대비(날개끝 띠의 시간축
  변동 폭)를 재니 0.15주기 2.6 dB · 0.20 7.4 · 0.30 16.1 · **0.45 19.1(최대)** · 0.60 17.6.
  짧으면 도플러 번짐이 띠를 뭉개고 길면 시간평균된다 → `auto_periods` 를 0.45 로.
  ⚠내 예상이 뒤집혔다 — «시간분해능 우선» 을 «조각을 최대한 짧게» 로 옮긴 것이 틀렸다.
- **STFT 전용** 확정(사용자 지시) — 재할당 스펙트로그램 모듈을 만들다 삭제.
- **애니메이션 재제작**: 274배 → **46배 슬로모션**, 96프레임, **TX/RX 포함**, 두 칸 구성.
- 그림 신설: f9(거리스윕) · f10(5G 4팔) · f11(실내/야외 산포) · f12(세 엔진 × 3거리) · f13(실측 로그)
- 그림 3 = f12 로 통합(사용자 제안 — f12 가 f5 의 상위집합이고 기하도 정확)
- 그림 6 = 능선판 폐기하고 **세 엔진 60 ms 플래시 비교**로 재작성

### ⭐2. RCS 사과-대-사과를 끝냈다
- 같은구간 스윕(2~18 GHz, az 360, 4기체) 완료. ⚠**대리 기체 오류 발견** — Das 가 잰 것은
  Phantom **3** 인데 phantom4 를 썼다. phantom3 추가해 다시 재니:
  | 기체 | 2~18 GHz | Das(0.21) 대비 |
  |---|---|---|
  | **Phantom 3(진짜 대응)** | **0.503** | **2.39배** |
  | Phantom 4 | 0.388 | 1.85 |
  | Matrice 4E | 0.243 | 1.16 |
  | Mini 5 Pro | 0.312 | 1.49 |
- ⭐결론: **구간을 맞추니 수는 움직였지만 차이가 닫히지 않았다.** 기종까지 같으므로
  «구간이 달라서» 라는 설명은 이제 못 쓴다. 남은 후보는 메쉬↔실물·편파 규약·측정 조건.
- z=9.6σ, 레벨 1.5~3.3 dB 낮음 — negative result 로 정직하게 적었다.

### ⭐3. 적대적 검증 두 라운드 (에이전트 239명)
- 5갈래 라운드(155 에이전트) — 내 서술의 **사실 오류 6건** 적발·정정:
  «(R/3)² 로 보상» 실제 0.18배 · «λ/16» 실제 λ/12 · «풀캡처≡CW 2.4e-14» 는 항등식 검사 ·
  도플러축 ±2334 실제 ±1300 · 원거리장 8.0/8.3 이중 · 그림3 «진짜 모노» ↔ 렌더는 준-모노
- RCS 축 감사(84 에이전트) — **거짓 해제 게이트** 발견(원장 1개 시각만 봐서 5기가 낡아도
  경고가 사라짐) → 기체별 판정으로 수정. **σ 격자에 기체별 세대 도장** 신설.
- ⚠PDF 폭증(gouraud 스펙트로그램 114 MB → GitHub 100 MB 한도 초과) → `rasterized=True`

### ⭐4. 리포트 구조 전환 + 3GPP ISAC 조사
- 사용자: «78개는 너무 과해, 20개 이내로» → 78편을 `reports/_parts/` 로 옮기고
  **셀 수 상한 규약 폐지**(그 규칙이 «넘치면 쪼개라» 를 강제해 과잉 분할을 낳았다).
  `src/build_volumes.py` 로 15권 매핑 작성(실행 대기).
- 3GPP ISAC 표준 시나리오 **원문 조사**(1차자료 10건) — Rel-20 은 UAV 를 **gNB 모노스태틱**으로
  규격화, 표준 시나리오는 **UMa-AV**. 우리 패시브 바이스태틱은 **기하는 안, 제도는 밖**.
  ⭐논문 논거 둘 확보: 3GPP 스스로 모노가 성능목표 3/11 만 만족한다고 적었고, 모노는
  자기간섭이 Tx 전력 상한인데 패시브엔 그 항이 없다.
- 바이스태틱 복소장 커널 `sbr_field_bistatic` 추가 — 모노 회귀 144케이스 **최대오차 0.0**

## 커밋 사슬
676e448 → 7a6984f → bfd510e → f23f771 → 24b9539 → 30048dc → ab8dff3 → 37828a6 → 45708a1 →
b7ab903 → 1be5f68 → 7cafee4  (+ team_meeting: teammeeting_0811_v3.pptx)

## 미해결 선언
- 40 m 붕괴의 «구조 vs 광선 예산» 미분리 — 결판 시험(178M) 미완
- Γ(θ) 가 `rcs_sbr_multistatic`·다중반사에 미배선 → 바이스태틱 σ 는 서술과 다른 물리
- σ 격자 mini5pro 재계산 실패(watcher cwd 오류) — 다시 돌릴 것
- 우리 σ ↔ Das 2.39배 열린 문제
- `docs/DRONE_ISAC_PRIOR_READING.md:73` 의 −20 dBsm 표기 오류(실제 −12.81/−5.85)
- 패시브 2채널(기준·감시) 처리 현실화는 열린 과제
