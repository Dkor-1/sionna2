# 솔버 판 갈이 — 2.0.1 → 2.1.0 (2026-09-14)

사용자 지시(2026-09-14): 「시오나 업데이트해서 작업하고 싶어. 기존 결과는 다 유의미하게 쓸 수
있다고 생각되는데 나는 굳이 가상환경을 따로 만들기보다는」 · 시점은 「큐가 빠진 뒤」.

⇒ **제자리 업그레이드**한다. 가상환경을 새로 만들지 않는다.

## 무엇이 바뀌나 — 예행 실측

```
drjit 1.3.1 → 1.5.0 · mitsuba 3.8.0 → 3.9.1 · sionna 2.0.1 → 2.1.0 · sionna-rt 2.0.1 → 2.1.0
```

`uv pip install --dry-run` 이 **네 꾸러미만** 바꾼다고 냈다. 나머지 66 개는 그대로다
(numpy 2.5.2 · torch 2.11.0+cu128 포함). 되돌림 기록은 `runners/env_freeze_20260914_sionna201.txt`.

⚠**우리 커널도 영향을 받는다** — `src/rcs_sbr.py:80-82` 가 mitsuba·drjit·sionna.rt 를 직접
임포트한다. 「PathSolver 팔만 바뀐다」가 아니다.

## ⭐기존 결과는 왜 그대로 쓸 수 있나 — ⛔근거를 한 번 내렸다

⛔⛔**2026-09-14 정정.** 처음에 「창고 샤드 7,730 개가 **전부** 2.0.1 설치(08-13 00:24)
이후다 — `find -newermt` 전수」라고 적었다. **그 근거는 못 쓴다.** 적대 검증이 짚었고 다시 세어
확인했다: 창고 7,736 개 중 **4,438 개(57 %)가 나노초 0 인 통짜 시각**을 달고 있고 그중 대부분이
**2026-08-24 하루**에 몰려 있다 — 컨테이너 이관이 찍은 도장이지 굽기 시각이 아니다.

샤드가 스스로 적는 굽기 시각(`t_start`)으로 다시 재 봤더니 그것도 부족하다 —
**7,736 중 210 개**만 적혀 있고, 그 210 개는 09-12 이후다(그날 도장을 넣었다).

⇒ 지금 댈 수 있는 것은 이것뿐이다:

| 있는 것 | 무엇 |
|---|---|
| 저장소 문서가 **2026-08-03** 에 「우리 스택은 2.0.1」이라고 적었다 | `docs/SIONNA_RT_REFERENCE.md:828` · 커밋 `5ad359b0` |
| 저장소 어디에도 **다른 판을 쓴 기록이 없다** | 로그·원장·문서 훑기에서 sionna-rt 는 2.0.1 뿐(2.1.0 은 오늘 글) |
| 굽기 시각이 적힌 샤드 210 개 중 그보다 이른 것 | **0 개** |
| 설치 기록(dist-info) | 네 꾸러미 모두 한 시각 — ⚠이것도 이관 도장일 수 있다 |

⇒ 바르게 적으면 **「2.0.1 이 아닌 판으로 구웠다는 기록이 없다」** 이지
  **「전부 2.0.1 이후다」가 아니다.** 쓰기에는 충분하지만, 근거의 세기를 올려 적지 않는다.
⛔원장에 「2.0.1」이라고 **적어 넣지 않는다** — 안 적힌 것을 적힌 것처럼 만들지 않는다.
⭐오늘부터 굽는 것은 판을 **스스로 적는다**(`solver_build`). 이 물음은 오늘 이후로는 안 생긴다.

## ⛔이름이 같으면 — 덮어쓰는 게 아니라 **조용히 건너뛴다**

사용자 물음(2026-09-14): 「안 덮어쓰게 실행을 하면 되는 거 아니니?」
⇒ **코드를 읽고 고쳤다.** 기본 동작은 덮어쓰기가 아니다:

```python
# elevation_sweep_md.py:651 · :774
if shard_done(f) and not a.overwrite:      # ← 있으면 건너뛴다
```

같은 이름으로 2.1.0 을 돌리면 갈래가 **둘뿐**이고 **둘 다 못 쓴다**:

| 어떻게 돌리나 | 무슨 일이 나나 |
|---|---|
| 그냥 (기본) | 샤드가 이미 있으니 **몇 초 만에 «건너뜀»으로 끝난다.** 2.1.0 자료는 **0 개**인데 잡이 전부 rc=0 이라 성공처럼 보인다 |
| `--overwrite` | 2.0.1 샤드를 **지우고 덮는다.** 비교하려던 상대가 사라진다 |

⭐첫 줄이 2026-08-17 사고 그 자체다 — 메쉬 수리를 기본으로 켰는데 이름이 그대로라 러너가
**옛 메쉬로 낸 샤드를 재사용**했고, 작업이 3~4 초 「건너뜀」으로 끝나 재계산이 통째로 무효였다.
(`elevation_sweep_md.py` 의 `tagmf` 주석이 그 사고를 적어 둔 자리다.)

⇒ **한 이름에는 한 판밖에 못 담는다.** 「안 덮어쓰게」로는 두 판을 같이 가질 수가 없다.
  그래서 이름을 가르는 것 말고 길이 없다 — 그것이 판 꼬리표다.

| 어디 | 무엇 |
|---|---|
| `benchmark/elevation_sweep_md.py` | `BUILD_BASELINE` · `build_tag()` — 기준 판이면 **빈 글자**, 2.1.0 이면 `_rt210` |
| `src/arm_grammar.py` | `_F("solver_build", "rt", …)` — 되읽기·되짓기 |
| `runners/SOLVER_BUILDS.json` | **판 장부** — 꼬리표 ↔ 판 묶음. 처음 보는 판이면 빌더가 선다 |
| `benchmark/check_solver_build.py` | 상시 검사 ①~⑤ |

⛔⛔**2026-09-14 적대 검증이 문지기 셋을 더 세웠다** — 앞 판은 아래가 전부 뚫려 있었다:

| 뚫려 있던 곳 | 무슨 일이 났을까 | 지금 |
|---|---|---|
| 문지기가 **기준 판일 때만** 섰다 | 2.1.0 으로 올린 뒤 mitsuba·drjit 만 움직이면 이름이 그대로 `_rt210` → 다시 「건너뜀」 | 판 장부와 맞대 본다. 어긋나거나 장부에 없으면 **선다** |
| 지은 이름을 **되읽어 보지 않았다** | 정식 판이 아닌 것(`2.1.0rc1`)이면 `_rt210rc1` — 문법이 못 읽고 감사기가 그 샤드를 조용히 건너뛴다. ⚠`_rt210` 을 품고 있어 눈으로도 통과한다 | `build_tag()` 가 지은 이름을 문법에 되먹여 본다 |
| 판을 **import 때 디스크에서** 읽고 솔버는 한참 뒤에 올라온다 | 제자리 업그레이드 중이면 **새 판으로 굽고 옛 이름·도장으로 저장** | 굽기 도장 직전에 이미 올라온 모듈의 `__version__` 과 맞대 본다 |

값은 싸다 — 함수 하나와 문법 한 줄이고, 2.0.1 에서는 **이름이 한 글자도 안 바뀐다**(예행 확인).

⛔**이름이 못 담는 차이는 빌더가 멈춘다** — sionna-rt 는 기준 그대로인데 mitsuba·drjit 만
움직이면 꼬리표가 안 붙어 다시 「건너뜀」이 된다. 그 경우 `build_tag()` 가 예외로 선다.
「조용히 건너뛰기」보다 「서서 사람을 부르기」가 낫다.

⭐꼬리표가 생기면 2.0.1 팔과 2.1.0 팔이 **한 축만 다른 대조군**이 된다 —
`check_arm_names.py --groups solver_build` 가 그대로 묶어 준다.

## 절차

1. **워커가 0 이 될 때까지 기다린다.** 큐가 빠지기 전에 올리면 도는 워커가 옛 바이너리와
   새 파이썬 코드를 섞어 읽을 수 있다. ⚠판 도장은 임포트 때 찍히므로 **라벨은 안 틀린다**.
2. `taskset -c 8-11 uv pip install --python /workspace/.venvs/py312/bin/python \
   "sionna==2.1.0" "sionna-rt==2.1.0"`
3. ⭐**판 장부에 한 줄 적는다** — 처음 구울 때 빌더가 서서 적을 줄을 그대로 알려 준다
   (`runners/SOLVER_BUILDS.json` 의 `"builds"` 에 `"_rt210": "sionna=2.1.0 …"`).
   ⛔먼저 적어 두지 않는다 — **설치된 뒤의 실제 판 문자열**을 그대로 넣어야 한다.
4. `benchmark/check_solver_build.py` — ①~⑤ 전부. ⭐④ 가 `deterministic` 을 찾아낼 자리다
   (2.0.1 에는 `__init__`·`__call__` 어디에도 **없다**. 2026-09-14 실측).
5. 연기 시험 한 칸 — 작은 자세 수로 굽고, **이름 끝이 정확히 `_rt210` 인지**(⛔`_rt210rc1` 은 `_rt210` 을 품는다 — 눈으로 보면 속는다. 문법으로 되읽어 확인한다)·도장이 2.1.0 인지 본다.
6. `runners/jobs_0931_bridge.txt` 를 `filter_jobs.sh` 로 거른 뒤 발주한다.
   ⚠**업그레이드 뒤에** 걸러야 한다 — 2.0.1 에서 걸면 꼬리표가 없어 전부 DONE 으로 나온다.

## 다리 묶음 — 판 차이를 **짐작하지 않고 잰다**

`runners/jobs_0931_bridge.txt` (24 줄 · 2.0.1 짝의 **실측** 소요 합 27.17 일꾼시간 ⇒ 경과 약 3.5 시간).
이미 2.0.1 로 있는 칸을 2.1.0 으로 다시 구워 **한 축(solver_build)만 다른 짝**을 만든다.
⭐꼬리표가 생기므로 `check_arm_names.py --groups solver_build` 가 그 짝을 그대로 묶어 준다.

⭐⭐**고른 기준은 「짝이 현 세대인가」다.** 처음 짠 판(16 줄)은 `filter_jobs.sh` 로 걸어 보니
**10 줄의 2.0.1 짝이 옛 세대(STALE·n_dup 없음)** 였다. 그대로 샀으면 판 차이와 세대 차이가
겹쳐 「한 축만 다르다」는 전제가 깨진다.

⛔⛔**2026-09-14 정정 둘.** 앞 판의 「빠진 것」 표가 사실과 달랐다:

1. 「굴절 팔(R1)의 깊이 2 는 **지면만 장면**에서 본다 — 그쪽 짝이 현 세대다」는 **틀렸다.**
   건물만 장면(`outdoor01_bldg`)의 R1D0E0F1 깊이 2 도 앙각 −30·−60 **네 장 모두 현 세대**다.
2. 「그래서 빠진 것」이 **두 줄뿐**인 것처럼 적었다. 전수로 세니 현 세대인데 다리에 안 넣은 칸이
   **121 개**다(깊이 3 · 협곡 장면 · 건물 장면 · 다른 앙각 …).

⇒ 그러니 다리는 **전수가 아니라 표본**이다. 12 칸을 골랐고 133 칸이 있었다.
  고른 까닭은 「축마다 한 점씩 — 환경 넷 · 팔 둘 · 깊이 둘 · 앙각 둘」이고, 값은 27.17 일꾼시간이다.
  ⛔「이것이 남은 전부」로 읽지 않는다. 판 차이가 크게 나오면 표본을 넓히면 된다.

| 다리에 넣은 것 | 팔 | 깊이 | 앙각 |
|---|---|---|---|
| 빈 하늘 | R0D0E0F1 | 1 | −30 · −60 |
| 빈 하늘 | R0D0E0F1 | 2 | −60 |
| 빈 하늘 | R1D0E0F1 | 1 | −30 · −60 |
| outdoor01 | R0D0E0F1 | 1 | −30 · −60 |
| outdoor01_ground | R0D0E0F1 | 2 | −30 · −60 |
| outdoor01_ground | R1D0E0F1 | 2 | −30 · −60 |
| outdoor01_bldg | R0D0E0F1 | 2 | −60 |

⛔**못 사는 것**(고른 것이 아니라 자료가 없다): 빈 하늘 R0D0E0F1 깊이 2 앙각 −30 ·
빈 하늘과 outdoor01 의 R1D0E0F1 깊이 2 — 2.0.1 짝이 옛 세대다.
⛔el 0 은 안 산다(GATES_0902). ⛔뮌헨은 결과가 아니다.

### ⭐회절을 다리에 안 넣는 것은 구멍이 아니라 **계획이다**

사용자 결정(2026-09-14): 「회절은 어차피 한동안 안 쓰고 **신버전에서만** 사용하기로 했어」.

⇒ 회절 축에는 **이을 옛 판 자료가 필요 없다.** 앞으로 회절을 켜는 팔은 전부 2.1.0 에서 난다.
  2.1.0 이 회절 모형을 다시 썼다는 것도 이 결정과 맞는다 — 새 모형만 쓰게 된다.

⚠**이미 2.0.1 로 구운 회절 자료는 그대로 있다**: 팔 184 개 · 원장 569 행
(`R0D1E1F1` 109 · `R1D1E1F1` 54 · 나머지 21). 지우지 않는다.
⛔다만 그것은 **옛 판의 회절 모형**으로 낸 것이다. 새로 굽는 2.1.0 회절과
**한 표에 섞어 놓지 않는다** — 판 꼬리표가 그 둘을 이름에서 가른다.

⛔재질이 ITU-R P.2040-4 로 바뀌었다고 릴리스 노트가 적는다 — 절대 레벨이 움직일 수 있다.
  **두 판의 절대 레벨을 나란히 놓지 않는다.** 다리로 재는 것은 「얼마나 움직였나」이지
  「어느 쪽이 옳나」가 아니다. ⛔실기 계측 대조는 여전히 **0 건**이다.

## ⭐받아들임 시험 — 숫자를 **미리** 박아 둔다

판을 올린 뒤 무엇이 움직여도 되고 무엇이 움직이면 안 되는지를 **먼저** 적는다.
뒤에 가서 「이 정도는 괜찮다」고 말하지 않기 위해서다.

### 움직여도 되는 것 (다리 24 줄 = 7 팔 × 12 칸을 구운 결과)

| 자리 | 지금 | 뒤 | 왜 |
|---|---|---|---|
| 창고 샤드 | — | **+24** | 24 줄이 샤드 하나씩 |
| 원장 행 | — | **+12** | 24 줄 ÷ 조각 2 = 12 칸 |
| 원장 팔 | — | **+7** | 앙각은 팔 이름에 안 들어간다 |

⚠지금 워커 2 개가 아직 돌고 있어 그 몫이 따로 더해진다 — 위 수는 **다리 몫의 증가분**이다.

### ⛔움직이면 안 되는 것

`benchmark/freeze_0912.py --check` 에서 **다음 묶음이 한 자리도 움직이면 안 된다**:
`factorial_verdict` · `factorial_burial` · `ground_collapse` · `mixed_generations` · `checks`.

움직였다면 뜻은 하나다 — **2.1.0 팔이 2.0.1 판독에 섞여 들어갔다.**
판독기들은 팔 이름을 `src/arm_grammar.parse` 로 되읽으므로 `solver_build` 가 열쇠에 들어가
묶음이 갈려야 한다(⛔이건 설계상 그래야 한다는 것이지, 아직 **실측한 것이 아니다** —
이 시험이 그것을 실측으로 바꾼다).

### 미리 확인한 것 (2026-09-14, 2.1.0 을 «인 척» 하고 돌려서)

| 무엇 | 결과 |
|---|---|
| 다리 24 줄이 2.1.0 에서 **다른 이름**을 내고, 옛 짝은 있고, 새 이름은 없다 | **24/24** |
| 우리 커널(ours · ours_free · +div)·시오나 팔 모두 `_rt210` 이 붙고 왕복한다 | **4/4** |
| 7 팔이 전부 «없음(2.0.1) ↔ 210» 한 축만 다른 짝으로 묶인다 | **7/7** |
| 병합의 이름 긁기 정규식(`_r`·`_p`·`_sw`·`_az`·`_div`·`_shift`·`_shell`·`_prop`)이 `_rt210` 과 부딪치나 | 전부 접두 뒤에 **숫자**를 요구한다 — 부딪치지 않는다 |
| 기존 팔 860 개 이름·뜻 대조 | **860/860 · 8,886 쌍 어긋남 0** |
