# ⭐⭐최종 판정 — 물리 스위치 (2026-08-12, 서버 이전 직전)

## 사용자 가설의 답 — **반증됐다**

사용자 가설: *"PathSolver 방식에서는 굴절 회절 등이 우리 PO 방식보다 잘 반영되어 있고,
평면파 가정이 들어가지 않으면서 90 도에서도 도플러가 약간 남는 현상이 있는 것 같은데"*

### 판정표 (el −15°, 8/11 덱과 **기하가 정확히 같은 자리**)

| 팔 | 경로 | 플래시 1× [dB] | 2× [dB] | **덱과 상관** |
|---|---|---|---|---|
| **ours** (SBR+PO) | — | **38.4** | 34.1 | (기준) |
| 덱 15 m | — | 38.2 | 34.1 | **0.9877** |
| sionna 물리 **끔** 11.1M | 12 | 35.4 | 34.9 | 0.6961 |
| sionna 물리 **켬** 11.1M | **2** | 13.1 | 10.0 | 0.6903 |
| sionna 물리 **켬** 250M(공정) | **48** | **11.2** | 12.6 | **0.6291** |

⭐**광선을 22 배 늘려 경로를 2 → 48 개로 만들어도 상관이 떨어진다**(0.6903 → 0.6291).
빗살은 1× 가 35.4 → 11.2 dB 로 **24 dB 지워진다.** 예산 부족이 아니다.

⇒ **「굴절·회절을 켜면 더 현실적인 마이크로도플러가 나온다」는 참이 아니다.**
켜면 블레이드 빗살이 지워진다.

### 왜 그런가 — 단일축 분해가 이미 답했다 (`outputs/diag_physics_paths_el*.json`)

| 스위치 | 경로 중앙값 | 레벨 | AC/DC |
|---|---|---|---|
| 기준(전부 끔) | 11 | −130.78 | +0.89 |
| **굴절만** | **2** | −132.48 | +4.60 |
| **회절만** | 14 | **−64.23** | **−68.13** |
| 모서리회절만 | 11 | −130.78 | +0.89 (기준과 동일) |
| 다중반사만 | 12 | −130.76 | +0.88 (기준과 동일) |

· **경로 붕괴(11 → 2)는 굴절**이다 — 껍데기가 플라스틱이라 반사 대신 통과한다.
· **레벨 66 dB 상승은 회절**이고, AC/DC 가 −68 dB 로 보이는 것은 **분모(정적 성분)가
  커진 것**이지 변조가 준 것이 아니다. 나딧에서 정반사보다 66 dB 큰 회절 항은 물리적으로
  말이 안 된다.
· **모서리회절·다중반사는 영향 0.**

### `max_depth` — 사다리 1·2·3·4 로 확정 (`outputs/diag_depth_ladder_el*.json`)

| el | 깊이 1 | 2 | 3 | 4 | 폭 |
|---|---|---|---|---|---|
| 0° | −59.65 | −59.65 | −59.65 | −59.65 | **0.000 dB** |
| −45° | −126.33 | −126.04 | −126.03 | −126.03 | **0.302 dB** |
| −90° | −129.02 | −129.03 | −129.04 | −129.04 | **0.013 dB** |

**2 에서 포화하고 비용은 오히려 0.83~0.85 배**다. 깊이는 올릴 이유가 없다.
⭐우리 `sbr_field` 도 단일 반사라, 대칭을 지키려면 **1** 이 맞다.

### ⭐사용자가 맞았던 부분

우리가 PathSolver 를 **굴절 끄고·회절 끄고·단일반사로** 돌리고 있었다는 지적은 옳았고,
그것이 이 실험을 낳았다. Sionna 기본값은 `max_depth=3 · refraction=True · diffraction=False`
이므로 **우리는 기본값에서 둘 벗어나 있었다**(`diffraction=False` 는 Sionna 자신의 기본값).

---

# ⭐⭐재개 지점 — 2026-08-11 (앙각 실험 라운드)

> 이 파일 하나만 읽으면 이어서 할 수 있게 쓴다. 끝낸 것 · 도는 것 · 할 것 · **미해결 선언**.

---

## 0. 세션이 끊겨도 계산은 계속된다 — 확인된 사실

계산 프로세스는 전부 **`setsid nohup` 으로 떼어 놓았고 부모가 `init`(pid 1)** 이다.
터미널·세션이 죽어도 안 죽는다. 재개할 때는 아래 셋만 확인하면 된다.

```bash
# ① 드라이버가 살아 있나 (부모가 1 이면 세션과 무관하게 산다)
ps -eo pid,ppid,etimes,cmd | grep '[s]cratchpad/run_.*\.sh'
# ② 워커가 도나
ps -eo pid,etimes,times,cmd | grep '[e]levation_sweep_md.py'
#    ⭐가동 판정은 «CPU 누적 ≈ 경과» 로 한다. 로그가 0 바이트여도 버퍼링일 뿐이다.
# ③ 산출이 늘고 있나
ls outputs/elev_sweep_shards/ | sed 's/_[0-9][0-9]\.npz//' | sort | uniq -c
```

⭐**계산을 다시 띄워도 안 버린다** — 모든 드라이버에 «이미 난 샤드는 건너뛴다» 가 들어 있다.
드라이버가 죽었으면 같은 스크립트를 그냥 다시 띄우면 된다.

```bash
SP=/tmp/claude-1015/-workspace/a78e7d06-306f-4e2d-b124-5fe972bc4462/scratchpad
setsid nohup bash $SP/run_physics.sh      > $SP/physics_driver.log 2>&1 < /dev/null &
setsid nohup bash $SP/run_nadir_budget.sh > $SP/nadir_driver.log   2>&1 < /dev/null &
```

⚠**스크래치패드는 세션 폴더 안**이라 세션이 완전히 사라지면 드라이버 스크립트도 사라진다.
그때는 이 문서의 §3 을 보고 같은 명령을 다시 만들면 된다(인자는 전부 적어 두었다).

⚠**살아남지 못하는 것**: 워크플로(서브에이전트)는 세션과 함께 죽는다. 결과 파일
(`outputs/*.json`)은 남으므로 읽어서 이어가면 된다.

---

## 1. 끝낸 것

| # | 무엇 | 근거 원장 |
|---|---|---|
| 1 | ⭐⭐**구면파 도플러 부호 결함 정정(R28)** — `sbr_field(range_m=)` 이 **거리**에 `+j` 를 걸어 평면파 갈래(**투영**에 `+j`)와 −1 배였다 | `verify_phase_sign.json` 게이트 3/3 · 커밋 `91a8101` |
| 2 | ⭐⭐**동시실행 OBJ 파일 경합 정정** — 임시 폴더를 PID 별로 갈랐다(`src/proc_scratch.py`). 15 개 호출부가 한 번에 해결 | 경합 시험 4/4 · 커밋 `f6f2fc9` |
| 3 | ⭐⭐**앙각 스윕 본편 완결** — `ours` · `sionna`(11.1M) · `sionna_p250000000` 각 7 앙각 | `elevation_sweep_md.{json,npz}` |
| 4 | ⭐⭐**el 0 예산 사다리 완결** — 11.1M · 250M · 1,000M · 4,000M 네 계단 | 같은 원장 |
| 5 | **PO 커널 운동학 검증** — 해석 점산란체와 ρ = 0.9999~1.0000, λ/12 사면 | `verify_po_elev_unit.json` · `verify_po_elev_rebuttal.json` |
| 6 | **`md_range_sweep` 37 dB 반영 재생성** (`g_mf_db` = 36.99, 규약 `v2_2026-08`) | `md_range_sweep.json` |
| 7 | **Chapter 1 그림 5 장 + 판정 294 줄** | `docs/CH1_ELEVATION_FINDINGS.md` · `outputs/figures/ch1_f1~f5.png` |
| 8 | ⭐**8/11 덱 STFT 재현 확인** — 우리 팔이 덱 15 m 와 **상관 0.9877** | `outputs/physics_vs_deck.json` |

### ⭐ el 0 결론 — 예산 문제가 아니다

| 광선 | 경로 수 | 박자 [Hz] | 참값(126.67) 대비 | 레벨 [dB] |
|---|---|---|---|---|
| 11.1M | 9 | 376.73 | +250.06 | −59.65 |
| 250M | 127 | 50.27 | −76.40 | −59.66 |
| 1,000M | 471 | 122.12 | −4.55 | −59.67 |
| 4,000M | 2,008 | 58.15 | −68.52 | −59.68 |
| ours | — | 126.40 | −0.27 | — |

**레벨은 360 배 예산에 0.03 dB 안에서 같은데(수렴) 박자는 헤맨다 — «수렴했는데 틀렸다».**
반면 el −30 은 11.1M 252.32 → 250M 126.61 로 **고쳐졌다**. 그건 예산 문제였다.

---

## 2. ⛔ 오늘 반증된 것 — 인용하기 전에 반드시 읽는다

| # | 내가 했던 말 | 정정 |
|---|---|---|
| 1 | 「나딧 잔여는 1/r⁴ 로 죽으니 실전 거리에서 사라진다」 | ⛔**부분 반증.** 1/r⁴ 는 **재질·가림·격자 없는 대리모형**의 성질이다. 생산 격자 λ/12 로 커널을 재면 지수 **2.589**, 총 AC/DC 는 10 m~1 km 에서 **−48.5 ~ −50.3 dB 로 거의 평평**하다. 격자를 조이면 2.24→2.59→3.35→3.74 로 4 에 수렴 |
| 2 | 「나딧에 남는 건 진짜 신호」 | ⛔**대부분 수치 잡음이다** — 광선 격자 표본화 **64 %** · 근접장 곡률 31 % · 가림 5 % |
| 3 | 「나딧 도플러 27.94 Hz, 위상 25.29°」 | ⛔**조각 하나의 값**이다. 2 엽이라 **1 차항이 정확히 상쇄**되고(−319 dB) 남는 것은 **진폭 리플 1.3 %** 뿐. 상쇄는 2 엽 고유가 아니라 **N 겹 대칭이면 항상** 일어나고 일반형은 **AC 전력 ∝ 1/R^{2N}** |
| 4 | 「우리 커널만 −75° 에서 박자를 맞춘다」 | ⛔**좁혀야 한다.** PathSolver 도 126.45, 250M 도 126.41 로 맞춘다. `f_flash` 는 **입력값**이라 이 잣대는 산란 커널을 재지 않는다. 「오차 0.00 Hz」도 못 쓴다 — 로터 4 개가 126.39~126.95 Hz 로 흩어져 있다 |
| 5 | 「1/r⁴ 이면 2.5 배에 −7.96 dB」 | ⛔ `20log10` 을 쓴 산수 오류. 옳은 값은 `40log10(2.5)` = **15.92 dB** |
| 6 | Chapter 1 의 G3 판정 | ⚠ 채점된 앙각은 7 점이 아니라 **5 점**이고 −15° 는 기준점 자신(항등식). −75·−90 은 «예측 적중» 이 아니라 **채점에서 빠진 점**이다 |

---

## 3. 지금 도는 것 (2026-08-11 기준)

| 드라이버 | 무엇 | 진행 |
|---|---|---|
| `run_physics.sh` | ⭐**물리 켜고 끄기 단일축 대조** — `sionna_phys`(굴절·회절·모서리회절 ON, `max_depth=3`) · `ours_ptd`(PTD ON) | `sionna_phys` el 0·−90 완결, −15 5/8, −45 2/8 · `ours_ptd` 0/56 |
| `run_nadir_budget.sh` | 직하방 예산 사다리 — el −90 을 1,000M·4,000M 으로 | 0/8 (샤드당 ~18 분) |
| `run_el0_resume.sh` | el 0 사다리 | ✅ 완결 |

### 명령 원본 (스크래치패드가 사라졌을 때 다시 만드는 법)

```bash
export CUDA_DEVICE_ORDER=PCI_BUS_ID CUDA_VISIBLE_DEVICES=3 SIONNA2_GPU=3
PY=~/.venvs/py312/bin/python;  cd /workspace/sionna
# 물리 켠 PathSolver (11.1M 광선, 앙각 7 점 × 샤드 8)
PYTHONPATH=src:benchmark $PY -W ignore benchmark/elevation_sweep_md.py \
  --engine sionna --physics --els <EL> --shard <SH> --nshards 8
# 우리 팔 + 모서리 회절
PYTHONPATH=src:benchmark $PY -W ignore benchmark/elevation_sweep_md.py \
  --engine ours --ptd --els <EL> --shard <SH> --nshards 8
# 직하방 예산 사다리
PYTHONPATH=src:benchmark $PY -W ignore benchmark/elevation_sweep_md.py \
  --engine sionna --els -90 --spp 1000000000 --shard <SH> --nshards 8
# 병합 (전부 끝난 뒤)
PYTHONPATH=src:benchmark $PY -W ignore benchmark/elevation_sweep_md.py --merge
```

### ⚠ 동시 개수 상한 (실측)

| 예산 | 프로세스당 GPU | 자세당 | 안전한 동시 개수 |
|---|---|---|---|
| 11.1M 광선 | 1,378 MiB | 0.44 초(물리 끔) / **1.55 초(물리 켬)** | 8~14 |
| 1,000M·4,000M 광선 | **몇 GB 씩 튄다** | ~2.1 초 | **2** |
| `ours --ptd` | 거의 안 씀(CPU) | ⚠**10 배 비싸다** — 45 분에 512 자세 미완 | — |

⛔**«지금 GPU 메모리» 로 자리를 판단하지 마라.** 이 작업은 띄운 뒤 **몇 분 지나서** 메모리를
크게 잡는다. 그렇게 판단했다가 42 개를 던져 **35 개가 out of memory 로 터졌다**(2026-08-11).
**개수로만** 조절한다.

---

## 4. ⚠ 미해결 선언

1. ⛔⛔**GPU 3 에 프로세스를 못 올린다.** `CUDA_VISIBLE_DEVICES=3` 을 줬는데 워커가
   **`/dev/nvidia2` 를 연다**(프로세스가 실제로 연 장치 파일로 확인).
   배제한 것 — `gpu.pick()` 덮어쓰기(런타임에 «3» 유지 확인) · `SIONNA2_GPU`(설정 안 됨) ·
   CUDA 번호 순서(**격리 시험에서는 `CVD=3` → `/dev/nvidia3` 로 정상**).
   ⇒ 격리는 맞고 실제 스크립트만 어긋난다. 원인 미확인.
   지금은 세 겹으로 못 박아 두었다(`CUDA_DEVICE_ORDER=PCI_BUS_ID` + `CUDA_VISIBLE_DEVICES` +
   `SIONNA2_GPU`). **판정은 GPU 3 의 `memory.used` 가 오르는지로 한다.**
   ⚠ 덧붙여 — 이 작업은 **CPU 병목**이라(부하 90/64 코어) 애초에 GPU 메모리를 못 채운다.
   GPU 를 메모리로 채우려면 **광선 많은 작업**(1,000M 이상)이어야 한다.
2. ⚠**인프라** — 서브에이전트가 «`Your organization has disabled Claude subscription access`»
   로 대량 실패했다(한 워크플로에서 40 중 36). 두 워크플로의 **종합 단계가 이 이유로 죽어**
   최종 보고서가 없다. 조사·반증 결과 파일은 남아 있다.
3. ⚠**Sionna el 0 자료 결함** — σ 2,931 m² 인데 물리 상한 5.8 m²(27 dB 초과). 기전 미확인.
4. ⚠**mavic4pro `dc_ac` −30 dB 이동** — 원장 두 판이 37.18 ↔ 5.33 으로 갈린다. 미설명.
5. ⚠**우리 σ 는 여전히 `NOT_VALIDATED`** — 산포 16.27 dB(문턱 6.0). 이 라운드는 **운동학만** 판정했다.
6. ⚠`build_part07_microdoppler.py:740·799·837` 이 얼린 팔 절대 레벨을 리포트에 **다시 써넣는다**.
   리포트 08_3 자기모순(:1119 ↔ :1100·:1228·:1304)도 남아 있다.
7. ⚠**구면파로 낸 그림들의 도플러 축이 뒤집혀 있었다.** 수치는 안 바뀌지만 그림은 재생성해야 한다
   (8/11 덱 거리 그림 포함 — 덱이 인용한 수치는 크기 대칭이라 **주장은 그대로 선다**).

---

## 5. 할 것 (상류부터)

1. **물리 대조 수확** — `sionna_phys` 가 8/11 덱과의 상관을 0.6961 → 얼마로 올리는가.
   `benchmark/build_physics_vs_deck_fig.py` 를 다시 돌리면 자동으로 들어간다.
   ⭐이것이 사용자 가설(「PathSolver 가 굴절·회절을 더 잘 담는다」)의 **직접 시험**이다.
2. **직하방 예산 사다리 수확** — el −90 을 네 예산으로. 물리면 안 변하고 잡음이면 준다.
   지금 두 점(11.1M −0.11 dB → 250M −11.35 dB)이라 **1/N 잡음**과 **잡음+물리 바닥**이 안 갈린다.
3. **단일축 분해** — 지금 `--physics` 는 굴절·회절·모서리회절·다중반사를 **한꺼번에** 켜서
   무엇이 효과를 냈는지 귀속이 불가능하다. (a) 회절만 (b) 굴절만 (c) `max_depth=3` 만 세 판.
4. ⭐**나딧 대칭 정리 직접 확인** — `ours --els -90` 을 `range_m=None`(평면파)으로 한 판.
   예측: AC/DC 가 −38.31 dB 에서 수치 0 으로 떨어진다. **근접장 가설과 격자 churn 가설을 가른다.**
   el 한 점뿐이라 싸다.
5. **구면파 그림 재생성**(도플러 축) · `md_range_sweep` 수확 · 로터 맵 수확.
6. 3 층 — 딥러닝 분류 착수(결정트리 버리고 CNN).
7. 5 층 — 8/18 덱. ⭐사용자: «재료가 쌓인 뒤에 만들자, 급할 것 없다».

⛔**투과 관련 작업은 사용자의 명시적 지시 전까지 하지 않는다.**
