# 재개 지점 — 2026-08-03 21:00 KST

> **새 세션은 이 문서부터 읽는다.** 날짜별 재개 문서는 과거 기록이다.
> ⛔ **GPU 작업은 유지한다** — `benchmark/rcs_anchor.py` (PID 1364837, **6시간 8분** 경과)는 죽이지 않는다.
> ⚠ **서버는 UTC 다. 한국 시간은 +9.** 이 문서의 시각은 전부 **KST**.

---

## 0. ⭐⭐⭐ 오늘의 결론 — **세 결과가 한 방향을 가리킨다**

```
형상 (실루엣 IoU)    상한 대비 77.9 % · free_iou_gain 0.0 · 8기체 중 3위   ⭐ 형상은 맞다
고주파 σ (6~12 GHz)  격차 +0.080 ± 0.136 → z = 0.6                        ⭐ 6 GHz 위는 맞다
저주파 σ (1.8~6)     격차 +1.042 ± 0.29 → z ≈ 3.6 (2.0~4.6) · 3.54배      ⚠ 우리 대역이 여기다
```

⭐⭐ **형상도 맞고 고주파도 맞는데 저주파만 틀린다** → 원인이 **메쉬가 아니라 PO 근사의 저 ka 한계**로 좁혀졌다.

### ⭐ 지금 쓸 수 있는 문장 (정본)

> **우리 SBR+PO 는 6 GHz 위에서 실측과 구별되지 않고(z ≈ 0.6), 그 아래에서 3.5배 가파르다(z ≈ 3.6).**
> **신뢰 하한 ≈ 6 GHz.** ⚠ 그리고 **우리 실험 3대역(1.843 / 3.5 / 5.21 GHz)이 전부 그 아래에 있다.**

⛔ **"저 ka 물리다" 라고 쓰지 마라** (적대검증 지적):
*"앵커가 실측인 이상 표적의 진짜 저 ka 물리는 앵커 곡선 안에 이미 있으므로 격차를 '물리' 로 부를 수 없다."*
→ **"저 ka 에서 PO 근사가 무너지는 구간"** 으로 쓴다.

### ⭐⭐⭐⭐ 그리고 이게 실측의 자리를 정한다

```
우리 도구가 무너지는 대역  =  1.8 ~ 6 GHz
우리가 실측할 대역        =  1.8 ~ 6 GHz     ⭐ 정확히 같다
```
**실측은 "있으면 좋은 것" 이 아니라 시뮬이 가장 약한 자리를 정확히 메운다.**

---

## 1. ⭐⭐ Phantom 3 눈감기 검증 — 결과 전부

### 1-1. σ 대조 (`outputs/p3_validation.json`)

**고도·규약·추정량이 모두 정합인 유일한 대조 = 우리 el=0 ↔ Yuan θ90 실측곡선**

```
레벨   −4.91 dB (밴드 전체 평균)          ⭐ 지배 오차
기울기 우리 0.420 ± 0.066 vs 측정 0.315 → 1.6σ, 95%에서 유의하지 않다
       16.4 GHz 폭에서 기울기가 만드는 차 1.7 dB vs 레벨 4.7 dB → 레벨이 3배 크다
```
⛔ **"기울기 2.00배" 는 쓰지 마라** — 그건 **고도풀링 Das(0.21)** 와 맞댈 때만 나오고 고도가 정합이 아니다.
⚠ 우리 기울기는 끝점이 끈다 — 양 끝 2점 빼면 0.420 → 0.334.
⚠ **Das 고도풀링은 우리 추론**이다(본문에 `elevation` 0회).

**우리 대역(1.8~6 GHz)** — `outputs/anchor_subband_compare.json`, 우리 쪽 6점 → **91점** 조밀화:
```
우리 +1.452 ± 0.101   앵커 +0.411 ± 0.210   격차 +1.042 ± 0.29 → 3.54배
국소 기울기  LTE 1.941 · 5G 2.578 · WiFi 2.245 dB/GHz
레벨 결손    1.8 GHz −8.0 dB → 5.9 GHz −3.4 dB
```
⭐ **우리 Pd 는 보수적(비관적)이다** — 최악 대역 LTE 에서 σ 가 가장 낮으므로 마진을 과소평가한다.

**고주파 진단** — 고대역 15점 → **52점**(37점 신규):
```
6~12 GHz 격차 +0.080 ± 0.136 → z = 0.6.  저대역 격차의 1/13
계통 후보 전부 크기 불일치: 통계규약 ≤0.005 · 재질 Γ ≤0.0001 · 축교정 0.0 · 광선격자 ≤0.4(6%) · 메쉬 부호 반대
```

### 1-2. ⭐ 실루엣 IoU — **held-out 시험** (`outputs/p3_photo_match.json`)

```
IoU 0.7072 · 상한 0.9079 · ⭐ 상한 대비 77.9 % · 8기체 중 3위
⭐⭐ free_iou_gain = 0.0   자유카메라가 더 나은 각도를 못 찾았다 = 형상이 진짜로 맞는다
경계오차 8.91 mm (p95 32.5)
```
⭐⭐⭐ **메쉬 완성 08:02 UTC · 사진 도착 10:40 UTC** — 사진은 메쉬를 만들 때 존재하지 않았다. **튜닝 불가.**
⚠ 유보: 헤드라인은 알파배경 정면 뷰. 4뷰 중앙값 0.627, 최악 뷰(흰배경 JPEG)는 임계값에 0.31 흔들림.

### 1-3. ⭐⭐ 큐브 대조군 (`outputs/p3_control.json`, `p3_attack.json`)

```
우리 메쉬 |−4.85| dB   vs   상자(PEC 0.350×0.200×0.185 m) |+6.94| dB
better_than_cube: true   ⭐ 메쉬가 약 2 dB 샀다
```
⭐ **이 비교는 봉인 논쟁과 무관하다** — 같은 커널·같은 조건이라 제작자가 답을 봤든 아니든 상자에는 영향이 없다.

### 1-4. ⚠ 봉인 파손 + 자유도

```
blind_intact: FALSE · verdict: WEAK · n_free_choices: 205 (자유/고정 = 15.8)
```
⚠ 로그가 적은 `grep 'phantom 3' docs/*.md` 가 **봉인 리터럴 4종을 전부 출력**한다
(`docs/DRONE_ISAC_PRIOR_READING.md:71-72`, 07-31 커밋본에 이미 있었다).
⭐ **다만 방향 시험이 무죄 정황이다** — 맞추려 했다면 실측 쪽(위)으로 갔어야 하는데 **−4.85 dB 아래**로 나왔다.
⭐ 봉인 대상은 **논문의 σ 값 4개뿐**이고, **사진·치수·스펙·재질은 처음부터 허용된 정당한 입력**이다(사용자 확정).

⭐ **자유도 205 의 내역** (`p3_attack.json : Q3`):
```
관측 고정 13 · P3 전용 판단 32 · ⚠ 물려받은 형상 상수 160 · 엔진 손잡이 13
이유: "저장소에 P3 사진이 한 장도 없으므로 형상 수는 거의 전부 여기 들어간다"
→ phantom4 형상표를 그대로 옮겼다
손잡이 하나가 σ 를 1~3.6 dB 움직인다 → 잔차 4.85 dB 는 손잡이 2~3개 폭
```
⭐ 그런데 §1-2 가 **형상은 맞다**고 했으므로, 이 160개가 잔차의 주원인이 아닐 가능성이 커졌다.

---

## 2. ⚠ 철회·정정 — `docs/RETRACTION_LOG.md` R9~R14

| | 잘못 | 옳음 |
|---|---|---|
| R9 | "**해석해** 대비 검증" | 과녁 셋 중 둘이 **PO 로 PO 채점**. 진짜 외부검증은 **정확 Mie 하나**(0.375~0.851 dB) |
| R10 | 이면각 **0.076 dB** | 4개 중 최량. **max 0.556 dB**. kr 0.2006 은 div=16, 생산 div=12 는 0.2544 |
| R11 | 순위불변 0.2462 | 15개 적합 평균. 국소 0.049~0.357(상한 0.25 초과). **항등식이지 발견 아님** |
| R12 | "공개문헌상 공백" | "보유 218편에서 미발견". ⭐ 대체: **17편 중 절대 dBsm 3편, 드론 0편** |
| R13 | "**TW-ILDC** 를 구현했다" | **`untruncated Michaeli PTD-EEC (first order)`**. Gao 의 truncated wedge 는 미구현 |
| R14 | "**φ 최대 23.17 dB**" | φ 가 아니라 **고도차 Δz=35 m 의 성질**. 실제 φ 의존은 R90 span **0.48 %**, φ=90° 가 최소 |
| — | 기울기 격차 6.4~9.5배 | 구간 명시: 전대역 대 Das 2.00배 · 우리 대역 대 Yuan곡선 **3.54배** |
| — | 실측 "X410 4채널" | **기준+감시 2채널, 같은 클럭** |

⚠ **R14 무효화 대상**: `docs/DECK_FACTS.md` F27·G5, `outputs/deck_facts.json`,
`capability_matrix.json`, `GEOMETRY_BENCHMARK.md` 의 23.17 dB 인용 **전부**. 아직 안 고쳤다.

---

## 3. ⭐⭐ 급소 실험 — REAL 이나 MISATTRIBUTED (`outputs/tm_result.json`, `tm_attack.json`)

```
차이는 실재한다 (재실행 편차 0.00 dB, 1,080 자세 × 5 환경)
순서 M1(통계) < M3(우리) < M2(정육면체) 가 21/21 셀 불변
⚠ 낙차 소유자는 정육면체 — 105개 셀 전부에서 max=M2, min=M1. M3 는 낙차의 20~33%
⚠ 재는 것은 '형상' 이 아니라 '**σ 분포의 폭**'
⚠ 크기가 통계에 매달린다: 기하평균 14.65 / 중앙값 16.01 / 선형평균 29.30 / p95 33.71 / 최대 43.97
   검출기가 읽는 **p10 에서 2.30 dB 로 붕괴하고 순서가 뒤집힌다**(젠슨 격차 이중계수)
⚠⚠ 내 가설 반증: 환경의 각다양성이 차이를 **지운다**(N_eff 1.0→3.8 에서 29.3→10.1 dB)
    가장 민감한 환경은 자유공간
```
⭐ **Sionna 의 자리**: 표적 산란이 아니라 **환경 축(N_eff·각퍼짐·β)** 만 진다.
⚠ **선행 충돌 1순위 RadarTwin**(arXiv 2606.28396v1): *"물체단독 −0.24 ↔ 환경포함 +0.42"* 를 이미 인쇄.
남은 것: 77 GHz FMCW 모노스태틱이고 **PO 면적분·절대 dBsm·Pd 곡선이 없다.**

---

## 4. ⭐ 그 밖의 오늘 확정

**주입 아키텍처는 탑베뉴 게재** (`outputs/injection_archive.json`, 아카이브 327편 전수)
```
IEEE JSAC 44(2026) p.702 · IET Radar Sonar Nav. 2020 · IEEE J-STEAP 2025 · IEEE WCL 15(2026)
⭐ Deep(IET) 은 자체 in-house SBR 솔버 — 우리와 같은 동작, 동료심사 통과
⭐⭐ 게재된 것은 전부 자기 σ 를 검증했다. 검증 안 한 LAMBDA 만 미게재
→ 입장료는 아키텍처가 아니라 **주입한 σ 의 검증**
```

**런타임 반론 답변** (`outputs/runtime_benchmark.json`, 97설정)
```
우리 per-pose 중앙값 38.1 ms  <  같은 카드 스톡 PathSolver 72.8~128.0 ms
단계분해: GPU ray_intersect 3.1% · PO(host numpy) 48.5% → 호스트 96.9%
⚠ Ziganshin 0.7/19.0 s 는 360 각도점 전체 스윕(각도점당 1.94→52.8 ms)
```

**스톡 Sionna 표적 에코 기전** (`outputs/facet_count.json`)
```
정반사는 조건 만족 삼각형당 1경로, 진폭이 λ/(4π(R1+R2)) = 무한거울 값 (면적·곡률 무관)
mavic4pro 36자세: 경로 213.8개/자세인데 정반사는 통틀어 2개(자세 1개)
정반사 −33.87 vs 확산지배 −78.29 dB = 44 dB 차이
평판 세분 2→512 삼각형에서 +9.54 dB (동일 경로 중복, 20log10 N)
⚠ 벤더 결함이 아니라 image-source 설계다. 범위 진술로만 쓴다
```

**PTD** — 부호 근원수리(`A_FW_CONST = −0.5`, 원인 `d̂ = −û`, |arg| ≤ 5.8e-13°).
⭐ **09:44 UTC 에 SBR 에 배선됨**(`rcs_sbr.py` ptd 51회, 호출 L265-269). 기본 `ptd=False`, ptd=False 는 비트 동일.
⚠ **Phantom 3 시험은 배선 이전(08:34)에 돌아 모서리항이 없다.** 켜면 비용 +47.2%.
⚠ 기울기 효과는 4.54 → 4.08배(10%)뿐 — **PTD 로는 저대역 격차를 못 메운다.**

**σ 격자 재생성 + 고도확장 완료** (`outputs/sigma_grid_regen.json`, 5.57 CPU-시간)
```
새 고도 14점: −24 −28 −33 −38 −44 −50 −57 −64 −71 −78 −81 −84 −87 −90
  근거: 조회밀도 실측(alt60 에서 −20° 밖 11.90%, 최저 −88.06°) + σ(el) 곡률
        (−20~−60 은 2.5°당 0.2~1 dB, −78 아래는 2.5°당 4~10.7 dB)
  후보 8종 채점: 이 14점 rms 0.75 / max 4.8 dB
⚠⚠ 구↔신 차이가 크다: mavic4pro max 26.22 · rms 7.14 · **패턴상관 0.089**(사실상 무관)
    phantom4 상관 0.646 · matrice4e 0.395
SNR 천장: 메쉬 효과 +1.6 dB, 클램프 해소는 s1000plus 에서 0.0 dB
백업: outputs/archive/report13_sigma_grid_pre_merge_0803.json
```

**팀미팅 선행연구 감사** (`docs/TEAMMEETING_PRIORWORK_AUDIT.md`) — 197주장 중 **25건 오류** · 27건 대조불가.
LaSen(에코는 자체 USRP PDSCH+DM-RS) · CISSIR(세 칸 전부 거짓 + 저자 오귀속) ·
LIPASE(GT 는 sub-meter DGPS) · OpenISAC(붕괴는 **200 MHz 단독**, 50·100 MHz 는 안정).

**sim-to-real 방향** (`docs/SIM2REAL_PLAN.md`) — 33편 중 조합 보유 **0편**.
⭐ 태스크는 **분류가 아니라 유무 검출**(큐브·통계에 로터가 없어 분류로 가면 ablation 이 '로터를 넣었느냐' 를 잰다).
⚠⚠ 판정 **RISKY**: 자세·로터위상축이 σ 생산자에 미배선이라 우리 σ 가 **(σ̄, τ_decorr, 분포형) 3개 수**로
환원되고 **40줄 확률모형 S+** 가 재현한다. 실측 없이 sim-to-sim 으로 검사 가능.

---

## 5. ⛔ 2026-08-03 21:00 KST — 정지 상태

**살아 있는 것: `benchmark/rcs_anchor.py` (PID 1364837, 6시간 8분) 하나뿐.** 죽이지 않는다.
고아 드라이버 0개 · 워커 0개 확인함.

### 🔄 아직 도는 것 (내일 오전 발표라 이것만 남겼다)

| | 상태 |
|---|---|
| `wty5a7yyj` **팀미팅 덱 최종** | Fix 완료(원고 21:54 갱신, 새 그림 `p3_photo_vs_mesh.png`·`p3_result.png` 생성됨). Build·Score 남음 |

### ⏸ 멈춘 것과 재개 명령

| 워크플로 | 상태 | 재개 |
|---|---|---|
| **Phantom 3 사진 재제작** | 착수 | `Workflow({scriptPath:'.../phantom3-remesh-fast-wf_dc093fe9-1db.js'})` |
| **Das 전 4기체 × 바이 7각도** | 착수 | `Workflow({scriptPath:'.../das-fleet-validation-wf_9cf873b7-24a.js'})` |
| **실측 2기체 사진 상향**(mavic4pro·matrice4e) | 착수 | `Workflow({scriptPath:'.../measure-targets-photo-upgrade-wf_1498d811-aee.js'})` |
| **리포트 6편 반영** | 4/6 완료 | `Workflow({scriptPath:'.../reports-absorb-0803-wf_9c274ecb-24c.js', resumeFromRunId:'wf_9c274ecb-24c'})` |
| 저주파 격자 진단 | 착수 | `.../lowfreq-grid-vs-po-wf_26f2f411-33e.js` |
| PTD SBR 배선 검증 | 부호·배선 완료 | `.../ptd-signfix-wire-wf_0d067296-85c.js` |

⭐ **σ 재생성 중간본 보존**: `outputs/partial/sigma_regen_0803/` (20/21 셀 + 워커·드라이버·병합 스크립트).

⚠ **고아 프로세스 주의** — 에이전트가 `nohup ... &` 로 드라이버를 띄우면 워크플로 종료 후에도 산다(오늘 실제로 겪었다).
정지할 때 `ps -eo pid,ppid,etime,cmd | grep -E "driver|worker"` 를 반드시 확인하고 **PID 로** 죽인다(⛔ `pkill -f` 금지).

---

## 6. ⭐ 다음 할 일 (우선순위)

1. ⭐⭐ **Phantom 3 사진 재제작** — 사진 6→25~40장(⭐ **분해 사진**이 내부 18개 상수를 푼다) → 메쉬 재제작 → σ 재계산(**el=0 만 → 약 25분**)
   ⚠ 기대치 조정: §1-2 가 형상은 맞다고 했으므로 σ 가 크게 안 움직일 수 있다. **그것 자체가 진단 확정이다.**
2. ⭐⭐⭐ **Das 전 4기체 × 바이스태틱 7각도 = 28쌍** — N=1 을 N=4 로.
   ⭐ **Phantom 2 의 θb 요동**(0.21/0.50/0.22/0.21/0.12/−0.13/0.08, 부호까지 바뀜)을 우리가 재현하는가?
   재현하면 물리, 못 하면 **근거리장 2.6 m·비무향 측정 아티팩트라는 증거**. 어느 쪽이든 결과다.
3. ⭐ **실측 2기체 사진 상향** — mavic4pro(4장·IoU 57%) · matrice4e(5장·63%). **실측 대조 상대가 하위권이다.**
4. **리포트 6편 반영 마무리** (4/6 완료)
5. ⚠ **R14 무효화 전파** — DECK_FACTS F27·G5 등의 23.17 dB
6. ⚠ 감사 오류 25건 전파
7. ⭐ **저 ka 대책** — (d) 하이브리드: f<6 GHz 만 전파해석(openEMS 등) 몇 점으로 보정계수 · (e) 실측
8. ⚠ 자세·로터위상 축을 σ 생산자에 배선 (S+ 40줄 반례 대응)

---

## 7. git

**푸시됨**: `00fbbe4`(결과 62파일) · `4265045`(덱 v3 12장).
미커밋 다수 — 워크플로 산출물. ⛔ `git add -A` 금지, `team_meeting/` 제외, `groupmeeting_*` 제외.
⭐ **상시 규칙**: 덱이 완성되면 묻지 말고 `sionna2/decks/` 복사 → 커밋 → 푸시(완성본만).
