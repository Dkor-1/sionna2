# 다시 못 굽는 자리 — 메쉬 갤러리 원장이 낡았다 (2026-09-14 진단)

`src/make_report02_target.py` 가 **선다**:

```
AssertionError: 외접반경이 갤러리 원장과 어긋난다: 최대 2.13%
```

## 무엇이 어긋나나 — 기체 하나다

| 기체 | 갤러리 원장 `r_encl_m` | 지금 메쉬로 계산 | 어긋남 |
|---|---|---|---|
| mini5pro | 0.2218 | 0.2216 | 0.09 % |
| **phantom4** | **0.3110** | **0.3045** | **2.13 %** ⛔ |
| mavic4pro | 0.3592 | 0.3582 | 0.30 % |
| matrice4e | 0.3784 | 0.3782 | 0.05 % |
| typhoonh480 | 0.3890 | 0.3892 | 0.06 % |
| x500v2 | 0.3900 | 0.3902 | 0.04 % |
| s1000plus | 0.7302 | 0.7295 | 0.10 % |

문턱은 `worst < 5e-3`(0.5 %)다. 나머지 여섯은 0.04~0.30 % 로 통과하고 **phantom4 하나**가 넘는다.

## 왜 — 원장이 메쉬보다 오래됐다

| 파일 | 마지막 변경 |
|---|---|
| `outputs/mesh_gallery.json` (원장) | **2026-08-07** |
| `src/drones.py` (메쉬 정의) | **2026-09-06** |

메쉬 정의가 원장보다 **한 달 뒤**에 바뀌었다. 같은 정의(외접구 반경)를 두 코드가 따로 계산하고
어긋나면 멈추게 해 둔 그 검사가 제 일을 한 것이다.

## ⛔오늘 일과 무관하다

2026-09-14 의 솔버 판 갈이 준비(원장 병합 · 아틀라스 재생성 · 판 꼬리표)와 **관계가 없다.**
세 입력(`make_report02_target.py` · `drones.py` · `mesh_gallery.json`)이 전부 HEAD 그대로다
(`git status` 깨끗). 병합 전에도 같은 자리에서 섰을 것이다.

## 고치는 법과 **왜 지금 안 고쳤나**

고치는 법은 `src/viz_mesh_gallery.measure()` 로 원장을 다시 굽는 것이다.
⛔다만 그러면 **발간물의 수가 바뀐다** — phantom4 반경이 0.3110 → 0.3045 m (−2.1 %) 이고,
그 값을 쓰는 곳이 `src/build_part03_target_mesh.py` · `src/build_volumes.py` ·
`src/viz_mesh_material.py` · `src/viz_mesh_photo.py` · `reports/02_3_target-mesh.ipynb` 다.

⇒ **솔버 판 갈이와 같은 날 섞지 않는다.** 섞으면 뒤에 수가 움직였을 때 그것이 엔진 판 때문인지
메쉬 때문인지 못 가른다. 판 갈이가 가라앉은 뒤 **따로**, 무엇이 움직이는지 먼저 세어 놓고 고친다.

⚠그때 함께 볼 것: phantom4 메쉬가 **왜** 바뀌었나(2026-09-06 커밋 셋 중 어느 것인가), 그리고
그 변경이 `kr` 값을 타는 레포트 문장을 바꾸나.
