# 엔진 검증 — 측정된 기체와 대조한다

> 판정 원본: `outputs/engine_validation_verdict.json`  ·  실행 원본: `outputs/validate_measured_airframe.json` · `outputs/validation_feasibility.json` · `outputs/material_sources.json`

```
한 일
  실제로 RCS 가 측정된 기체(DJI Phantom 3)를 우리 방식대로 짓고, 생산 커널에 앵커를 끄고
  통과시켜 발표된 측정과 대조했다.

결과
  절대 레벨 -23.23 / -19.65 / -16.86 dBsm — 발표된 봉투 대비 -4.43 / -1.20 dB 와 봉투 안.
  밴드 기울기는 6 GHz 위에서 0.264 dB/GHz 로 측정 구간 0.175~0.315(**우리 대역과 겹치는 4행**) 안에 들고,
  1.8~6 GHz 에서 1.540 로 벗어난다.
  교정구는 정확 Mie 대비 0.851 dB 안 — 절대 스케일은 맞는다.

방법
  Das(IEEE WCL 2026) 와 Yuan 두 논문이 준 치수(모터 대각 350 mm, 높이 200 mm)로
  메쉬를 짓고 rcs_sbr_batch div=16 · n_az=720 로 돌렸다.
  sigma_anchor.relevel() 은 한 번도 부르지 않는다 — 레벨은 커널 출력 그대로다.

재현
  PYTHONPATH=src:benchmark python benchmark/validate_measured_airframe.py
  PYTHONPATH=src:benchmark python benchmark/engine_validation_verdict.py
  → outputs/validate_measured_airframe.json · outputs/engine_validation_verdict.json (1.03 GPU-h)

앞 편에서
  02편 — 메쉬 7종과 산란 커널, 그리고 해석해 게이트 세 가지.
```

---

## §1. 판정 — 한 표

커널이 재현하는 것과 재현하지 않는 것을, 재현한다면 어느 정확도로인지까지 한 표에 적는다.

| 양 | 재현 | 정확도 | 출처 |
|---|---|---|---|
| 교정 표적의 절대 σ (PEC 구 r = 17.8 cm, Yuan 자신의 교정 표준) | 예 | 정확 Mie 대비 0.375~0.851 dB, ka = 6.88~19.44 | validate_measured_airframe.json : 2_calibration_sphere |
| 해석 PO 구 (커널 자체 수렴) | 예 | kr 1~100 · 입사 48방향에서 최대 0.2006 dB | sbr_kr_sweep.json : summary_div16.max_abs_db_vs_po |
| 측정 기체의 밴드 기울기 dμ/df, 6 GHz 위 | 예 | 우리 0.264 dB/GHz vs 측정 0.175~0.315 — 측정 구간 **안** | validate_measured_airframe.json : 8_comparison._headline |
| 방위 산포 ε (Das Table III 와 같은 양) | 부분 | 밴드별 차 -1.30~+1.16 dB. 측정은 세 밴드에서 5.22~5.32 dB 로 거의 평평하고 우리는 밴드마다 움직인다 | validate_measured_airframe.json : 8_comparison.C_azimuth_pattern_shape |
| 절대 σ 레벨, 5.21 GHz | 예 | -16.86 dBsm — 발표된 봉투 [-18.10, -14.51] **안** | validate_measured_airframe.json : 8_comparison.A_absolute_level |
| 절대 σ 레벨, 3.5 GHz | 아니오 | -19.65 dBsm — 봉투 최근접변 대비 -1.20 dB, 기하정합 행 대비 -4.60 dB | validate_measured_airframe.json : 8_comparison.A_absolute_level |
| 절대 σ 레벨, 1.843 GHz | 아니오 | -23.23 dBsm — 봉투 최근접변 대비 -4.43 dB, 기하정합 행 대비 -7.66 dB | validate_measured_airframe.json : 8_comparison.A_absolute_level |
| 측정 기체의 밴드 기울기 dμ/df, 1.8~6 GHz (우리 운용 대역) | 아니오 | 우리 1.540 dB/GHz vs 측정 0.210(Das) / 0.315(Yuan) | validate_measured_airframe.json : 8_comparison.B_band_slope |
| 바이스태틱 각 의존성 | 대조 불가 | Das 의 Phantom 3 행은 0~90° 전 구간 기울기·ε 가 동일하고 절편만 0.63 dB 움직인다 — 우리 div=12 격자 산포보다 작다 | validate_measured_airframe.json : 8_comparison.D_bistatic |
| 편파 | 대조 불가 | 측정은 VV 단독, 커널은 스칼라(편파 자유도 없음) | validation_feasibility.json : 3_convention_audit.3c_polarisation |

### §1.1 프로젝트가 자기 σ 에 대해 쓸 수 있는 문장

> 우리 σ 의 절대값은 5.21 GHz 에서 발표된 측정 봉투 안에 있고, 3.5 GHz 에서 1.20 dB, 1.843 GHz 에서 4.43 dB 낮다. 기체 1종 · 측정 체인 1개에 대한 대조이고, 커널의 절대 스케일 자체는 같은 측정의 교정구에서 0.851 dB 안에 든다.

이전까지 이 자리에 들어갈 문장은 **"검증되지 않았다"** 였다. 이제는 숫자다 — 밴드 하나는 통과, 둘은 미달, 결손의 크기와 부호가 밴드마다 적혀 있다.

### §1.2 실패한 자리

불일치는 **1.8~6 GHz** 에 몰려 있고, 우리 세 밴드가 전부 그 안에 있다. 6 GHz 위에서 우리 기울기 0.264 dB/GHz 는 측정 구간 0.175~0.315 **안**에 든다.

> ⚠ **구간 표기 규율(RETRACTION_LOG A11).** `0.175~0.315` 는 `sigma_anchor.SLOPE_CENSUS` 중
> **우리 대역과 겹치는 4행**(전부 Phantom 3, 한 측정의 네 통계)이다. **전 기체 7행 총람은 0.07~0.315**
> 이고 하한 `0.07` 은 das_mini2 의 **21~27 GHz** 값이라 우리 세 밴드 비교에 넣으면 외삽이다.
> 두 구간 다 옳고, **범위를 안 밝히면 둘 다 틀린 인용**이 된다.

| 적합 구간 | 우리 [dB/GHz] | Das [dB/GHz] | Yuan(방위면) [dB/GHz] | 배수(vs Das) |
|---|---|---|---|---|
| 1.8~18.2 GHz (논문 구간과 동일) | 0.435 | 0.210 | 0.315 | 2.07× |
| 1.8~6 GHz | 1.540 | — | — | — |
| 6~18.2 GHz | 0.264 | — | — | 1.26× |
| 1.843~5.21 GHz (3점) | 1.891 | — | — | 9.01× |

⭐ 타당성 단계가 예상한 "6.7~9.5배"는 우리 좁은대역 적합을 논문의 넓은대역 적합과 비교한 결과였다. 같은 구간에서 다시 재면 2.07배(Das)· 1.38배(Yuan)다 ⟨`validate_measured_airframe.json : 8_comparison.B_band_slope.fitting_interval_matters`⟩. 우리 μ(f) 는 직선이 아니다(전대역 적합 R² = 0.738).

### §1.3 기전 — 자체 대조군이 같은 모양을 낸다

표적이 전기적으로 커질수록 측정 대비 오차가 줄어든다. 광학영역 아래에서 PO 가 지는 신호다. 같은 것을 정준 표적에서 직접 보였다 — Yuan 자신의 교정구다.

| 밴드 | ka (구 r = 17.8 cm) | 구: 정확 Mie 대비 [dB] | 기체: 봉투 대비 [dB] |
|---|---|---|---|
| LTE 1.843 GHz | 6.88 | -0.851 | -4.43 |
| 5G 3.5 GHz | 13.06 | -0.375 | -1.20 |
| WiFi 5.21 GHz | 19.44 | -0.420 | +0.00 |

부호가 같고 모양이 같으며 크기만 약 5배 작다. 방위 **평균**은 이 결함에 가장 불리한 통계다 — 정반사에서 벗어난 자세가 지배하고, 그 자세에서 되돌아오는 것은 모서리 전류인데 PO 가 세는 것은 조명면 위의 표면 전류뿐이다.

### §1.4 기울기 예산 — 좁은대역 1.843~5.21 GHz

두 항은 **측정했고**(가정이 아니다), 나머지가 잔차다.

| 항 | [dB/GHz] | 어떻게 얻었나 |
|---|---|---|
| Das 대비 메워야 할 격차 | +1.681 | 우리 3점 적합 − Das 계수 |
| 기하 이상화 — 실물 0.4 mm 레이저 스캔 빼기 우리 파라메트릭 메쉬(양쪽 PEC) | -0.519 | 실측 스캔 3,086,679 삼각형 대 우리 15,156, 둘 다 \|Γ\|=1 로 몰아 기하만 남겼다 |
| 방위 섹터 규약 — 전방 반구(−90:2:90) 대신 전원 360° | -0.514 | 같은 메쉬에서 두 규약을 각각 평균했다 |
| **미설명 잔차** | **+0.648** | 격차 39% — 남은 기전은 부재한 PTD 모서리항뿐 |
| (참고) 재질 브래킷 양측 불확도 | 0.430 | V1/V2/V3 |

⚠ 잔차 0.648 는 재질 브래킷 0.430 의 1.5 배뿐이다. PTD 지목은 **지시적**이지 확정이 아니다. 방위섹터 항은 규약 정렬이지 모델 결함이 아니며, 두 논문 모두 자기 방위 평균이 반구인지 전원인지 적지 않았다.

### §1.5 오차막대

| 항 | 크기 [dB] | 기울기 영향 [dB/GHz] | 읽는 법 |
|---|---|---|---|
| 교정 스케일 — 커널 대 정확 Mie, Yuan 자신의 표준구 | 0.851 | — | 주파수에 대해 거의 평탄하다. 이 연습에서 가장 좁은 막대 |
| 메쉬 기하 충실도 — 실물 0.4 mm 스캔 대 우리 파라메트릭 | 1.959 | -0.519 | 같은 기체 계열에서 재질을 빼고 쟀으니 기하만 분리된다 |
| 재질 표 — 방어 가능한 브래킷 V1/V2/V3 | 1.136 | 0.430 | 모든 gamma_po 가 주파수 평탄이라 레벨만 움직인다 |
| 광선격자 산포 — div=16 | 1.782 | — | jitter=2 가 2×2 하위셀을 평균하므로 잔차는 이보다 작다. 규모 참고용 |
| 편파 — 우리 스칼라 커널 대 그들의 VV | 미유계 | — | 부호가 정해져 있다: 우리 값이 VV 측정보다 **낮게** 나온 것을 편파로는 설명하지 못한다 |
| 측정 자신의 방위 산포 ε | 5.265 | — | Das Table III Phantom 3: ε(f)=0.03f+5.16 dB — μ 는 매우 넓은 분포의 평균이다 |
| 문헌 봉투 — 측정 쪽만으로 | 3.587 | — | 한 데이터셋의 두 처리본이 우리가 등장하기도 전에 이만큼 벌어진다 |
| 형상 대조군 — 우리 P4 대 우리 P3, 재질·커널 동일 | 0.586 | — | ⚠ 구조상 작다 — P3 메쉬가 P4 셸 법칙을 상속한다. 실제 적용한 차이만 유계한다 |

### §1.6 이 대조가 세우지 못하는 것

- 우리 절대 σ 가 옳다는 것 — 기체 1종 · 측정 체인 1개, 그리고 같은 원자료의 두 처리본이 서로 2.26~2.39 dB 어긋난다
- 우리 생산 기체 5종에 대해 σ 가 옳다는 것 — Phantom 3 는 고정팔 350 mm 쿼드에 일체형 셸이고, Mini 5 Pro · Matrice 4E · S1000+ 는 산란 위상이 다르며 크기전이 지수가 미해결이다
- 편파 · 바이스태틱 기하 · 회전 프로펠러에 관한 것 전부
- 우리 Phantom 3 **셸**이 맞다는 것 — 저장소에 P3 사진이 없어 셸 법칙은 P4 사진감사에서 전이된 것이다

(원문 목록: `validate_measured_airframe.json : 8_comparison.G_what_this_cannot_establish`)

---

## §2. 리포트 순서 — 제안

**질문**: 어떤 RCS 검증 기체가 있는 기체를 우리 방식대로 mesh 를 만들어서 면밀하게 비교하는 작업을 앞쪽 레포트에 작성해서 레포트 순서를 변경하는 것은 어떻게 생각하니?

**답**: 찬성한다. 다만 앞으로 옮기는 것은 한 편이 아니라 **한 편 + 그 앞의 교정구 절**이고, 앵커는 반대로 뒤로 민다.

### §2.1 논거 — 검증은 신뢰성 게이트다

검증은 신뢰성 게이트다. 엔진이 측정된 기체를 재현하면 그 뒤의 모든 편이 그 위에 설 수 있고, 재현하지 못하면 그 사실이 더 짓기 전에 알려져야 한다. 검증을 뒤에 두면 논리가 뒤집힌다 — 이미 다 지은 다음에 바닥을 검사하는 꼴이다.

### §2.2 새 순서

| # | 편 | 무엇이 바뀌나 | 이 자리인 이유 |
|---|---|---|---|
| 01 | 선행연구 — 무엇이 가능하고 무엇이 아닌가 | 변동 없음 | 게재본 중 드론 메쉬에서 산란을 계산한 전례가 0편임을 먼저 확정해야, 뒤 편들이 무엇을 새로 하는지가 정의된다. |
| 02 | 표적 모델 — 메쉬와 산란 커널 | 현 02 에서 §4 앵커·§5 σ오차를 뺀 것 | 대조에 넣을 물건을 먼저 짓고, 커널은 해석해(구·이면각·kr 스윕)로만 게이트한다. 외부 측정은 아직 안 부른다. |
| 03 | ⭐ 측정 기체 대조 — DJI Phantom 3 | 신설. validate_measured_airframe.json + validation_feasibility.json | 신뢰성 게이트. 교정구로 절대 스케일을 닫고, 실제로 측정된 기체를 우리 방식대로 지어 σ(f) 를 대조하고, 그 판정 위에 앵커를 세운다. |
| 04 | 조명원 — 세 파형과 그 대가 | 현 03 이 번호만 이동 | 점유·대역·PRF·λ² 는 σ 와 무관하게 정확한 양이다. 03 의 판정과 독립이므로 뒤에 와도 잃는 것이 없다. |
| 05 | 검출기 — ECA · 거리도플러 · CFAR 교정 | 현 04 가 번호만 이동 | 경험 Pfa 는 σ 와 독립으로 측정된다. 여기까지가 03 의 결과에 영향받지 않는 구간이다. |
| 06 | 검출 결과 — 모노/바이 × 근/원거리 | 현 05 + 현 02 §5(σ 오차 강건성) | 03 의 앵커와 05 의 교정문턱을 함께 쓰는 유일한 편. 03 이 만든 밴드별 오차막대를 그대로 진다. |
| 07 | 실측 계획과 검증 | 현 06 이 번호만 이동, 03 의 잔차를 계약 항목으로 받는다 | 03 이 닫지 못한 항(편파·바이스태틱·FR1 저역 잔차)을 무엇을 재면 닫는지 결정표로 적는다. |

### §2.3 지금 내용이 어디로 가나

| 현재 | → | 비고 |
|---|---|---|
| 현 02 §1.1~§1.3 — 사진 대조 · 형상 검사 3종 · 부품별 재질 | 새 02 §1 | §1.3 재질 절만 본 문서 §3 의 3층 표로 교체한다 |
| 현 02 §2 — 엔진(광선 가림 + 재질 PO), §2.1 바이스태틱 출사 가시성 | 새 02 §2 | 변경 없음 |
| 현 02 §3 — 기준해 셋(해석 PO 구 · Mie · PEC 이면각) | 새 02 §3 | 교정구(r = 17.8 cm) 대조는 새 03 §1 로 승격 — 같은 커널이지만 그쪽에서는 '측정 체인과의 절대 대조'라는 다른 일을 한다 |
| 현 02 §4 — 앵커(§4.1 모드 · §4.2 세 인자 · §4.3 원장 · §4.4 통제·잔여) | 새 03 §6 | ⭐ 대조 **뒤**로. 앵커는 처방이고 처방은 증거보다 앞설 수 없다 |
| 현 02 §5 — σ 오차를 넣어도 파형 순위가 서는 범위 | 새 06 여는 절 | σ 불확도가 실제로 소비되는 곳 |
| 현 03 · 04 · 05 · 06 | 새 04 · 05 · 06 · 07 | 내용 변경 없이 번호만 이동 |

### §2.4 ⚠ 02편의 처분 — 검증은 자기 편인가, 02 의 절인가

**⭐ 자기 편(새 03)이다. 02 의 절이 아니다.**

| 이유 | 근거 |
|---|---|
| 분량 규약이 강제한다 | 03 이 답해야 하는 질문이 8개다 — 교정구, 레벨 봉투, 전대역 μ(f), 기울기 예산, 형상 충실도, 재질 브래킷, ε 대조, 방위섹터 규약. 하우스 규약 §5.7 의 상한이 편당 그림 8개이고, 02 의 메쉬·엔진 그림과 합치면 넘는다. §5.7 자신이 '넘치면 내용을 줄이지 말고 편을 쪼개라'고 정한다 |
| 주장의 종류가 다르다 | 02 = '우리가 무엇을 지었나', 03 = '외부 측정과 얼마나 맞나'. 한 편 = 한 주장 규약을 지키면 갈라진다 |
| 순환을 막는다 | 앵커는 대조 결과를 처방으로 바꾸는 절이다. 02 안에 다 넣으면 §1 메쉬 → §4 앵커 → 대조 순서가 되어 처방이 증거보다 앞선다 ⟨validation_feasibility.json : 7_report_ordering_recommendation.what_must_not_move_forward⟩ |
| 판정이 실패다 | 1.843 GHz 에서 봉투 밖 -4.43 dB. 실패를 자기 편으로 세우면 읽는 사람이 그것을 찾을 수 있다. 02 의 소절로 넣으면 같은 편의 앵커가 그 실패를 가리려 고른 것처럼 읽힌다 |

편 수는 6편 → 7편 으로 늘어난다. 하우스 규약 §5.7 은 '6편 골격 유지'를 적었다. 이 제안은 그것을 한 편 넘긴다. 넘기는 이유는 분량이 아니라 주장의 종류가 하나 늘었기 때문이다 — '외부 측정과의 대조'는 새 주장 종류이고, 기존 6편은 각각 다른 주장을 이미 하나씩 지고 있다.

⚠ 앞으로 옮기면 **안 되는 것**: 지금의 sigma_anchor 재보정(re-levelling) 편. 그건 대조가 아니라 대조 '이후'의 처방이다. 대조편보다 앞에 오면 순환논증으로 읽힌다.

---

## §3. 재질 — 무엇이 표준이고 무엇이 문헌이고 무엇이 선언인가

**질문**: mesh 의 형상뿐만 아니라 재질도 현실적인 재질이니? 실제로 조사해서 기입한거야?

**답**: 절반은 그렇고 절반은 아니다. eps_r 은 우리 대역을 덮는 측정 문헌으로 출처가 붙었고, gamma_po 실효값 5개는 출처가 없다 — 그 5개는 선언된 모델링 선택이고 총 σ 영향이 유계다. 그리고 셸 실효값 0.28 은 불확실한 정도가 아니라 부호가 틀렸다.

### §3.1 브리핑의 두 가지 오인

- **materials.py 에 battery eps_r 1.4 sigma 1.2 가 있다** → FALSE. eps_r=1.4, sigma=1.2 는 **absorber**(챔버 전파흡수체) 항목이다. battery 그룹은 DRONE_GROUP_MAT 에서 **ITU metal** 로 간다 — tier-2 가 아니라 tier-1.
- **FR-4 값이 모델에 쓰인다** → FALSE. pcb 그룹은 ITU metal + gamma_po=0.80 이다. FR-4 의 eps_r/tan_delta 는 코드 어디에서도 읽히지 않는다.

### §3.2 1층 — 표준 (ITU-R P.2040)

| 항목 | 값 | 출처 |
|---|---|---|
| metal | ITU-R P.2040 metal (1.0, 1e7 S/m) | ITU-R P.2040 Table 3 — Sionna 2.0.1 설치본 소스에서 직접 확인 |
| concrete_light/dark | ITU-R P.2040 concrete (5.24, 0.0462 f^0.7822) | ITU-R P.2040 Table 3 |
| battery → metal | ITU metal 로 사상 | ⭐ **계산으로 정당화됨**(문헌 불필요): 파우치 셀 Al 배리어층은 업계 규격 15-70 um, 우리 대역 표피깊이는 1.18-1.98 um → 최소 두께 15 um 에서도 7-13 표피깊이, 편도 감쇠 >65 dB. 두께 규격 출처가 약해도 결론은 견고하다. |
| pcb 기저 | ITU metal (구리 그라운드플레인 지배) | FR-4 의 eps_r/tan_delta 는 우리 모델이 읽지 않는다 |

### §3.3 2층 — 문헌 출처가 붙은 값

| 항목 | 출처(저자 · 학회 · 연 · DOI) | 측정 대역 | 문헌이 보고한 값 | 판정 |
|---|---|---|---|---|
| plastic eps_r = 2.7 | J. Zechmeister, J. Lacik, 2019 Conference on Microwave Techniques (COMITE), pp. 1-4, 2019, doi:10.1109/COMITE.2019.8733590 | 1~10 GHz | eps_r' 2.55~2.95 (ABS 포함 5종) | 우리 2.7 은 그 구간의 한가운데. 교차확인: ITU-R P.2040 plasterboard 2.73 |
| carbon sigma | G. Artner, P. K. Gentner, J. Nicolics, C. F. Mecklenbrauker, International Journal of Antennas and Propagation, vol. 2017, Art. 6152651, 11 pp., 2017, doi:10.1155/2017/6152651 | 4~6 GHz | shred-CFRP ~1e4 S/m (각도에 따라 10배 변동); 5.9 GHz 에서 1e4-8e4 S/m; twill-CFRP 는 1e3-1e6 S/m 축에 걸침 | ⚠ 현행 3000 S/m 는 출처가 없고 대역 내 측정치보다 낮다. 제안 패치 3.0e3 → 1.0e4 — 총 RCS 영향은 무시 가능하고, 바꾸는 이유는 정확도가 아니라 출처가 생기기 때문이다 |
| plastic sigma = 0.02 S/m | ⚠ 출처 없음 — 가장 가까운 표준 대용은 ITU-R P.2040 plasterboard 식 (0.0085 f^0.9395 S/m) | — | 문헌 ABS/PC tan_delta 0.005~0.008 | ⚠ **출처 없음**. tan_delta 로 0.026-0.072 (밴드에 따라) — 문헌 ABS/PC 0.005-0.008 보다 4-14배 크다. ITU plasterboard 식(0.0085 f^0.9395)이 가장 가까운 표준 대용. \|Gamma\| 영향 <0.01 dB, 셸 흡수 0.03-0.10 dB → 총 RCS 영향 무시 가능 |
| absorber eps_r 1.4 / sigma 1.2 | ⚠ 출처 없음 — note 가 이미 모델값이라 선언한다 | — | — | 챔버 흡수체 전용 · 드론 σ 와 무관하다 |

⭐ 플라스틱 eps_r 의 출처는 **우리 세 밴드를 전부 덮는 측정**이다 — 1~10 GHz. 탄소 도전율의 출처도 마찬가지로 대역 안이다 — 4~6 GHz 도파관 NRW.

### §3.4 3층 — 선언된 모델링 선택 (출처 없음, 영향 유계)

이 다섯은 문헌값이 아니다. **PO 경로에 들어가는 실효 반사계수**이고, 각각 총 σ 를 얼마나 움직이는지를 GPU 스윕으로 재서 유계로 만들었다.

| 항목 | 총 σ 최대 영향 [dB] | 어디서 | 상태 |
|---|---|---|---|
| plastic gamma_po = 0.28 (셸) | 1.102 | phantom4 · LTE 1.843 GHz · plastic=0.460 | ⭐ 불확실한 것이 아니라 **부호가 틀렸다** |
| carbon gamma_po = 0.90 | 1.210 | s1000plus · LTE 1.843 GHz · carbon=0.7000 | 선언된 선택 |
| camera_assembly gamma_po = 0.85 | 1.809 | mavic4pro · LTE 1.843 GHz · camera=0.50 | ⭐ tier-3 중 가장 큰 항 |
| pcb gamma_po = 0.80 | 0.048 | phantom4 · 5G 3.5 GHz · pcb=0.50 | 선언된 선택 |
| prop_plastic gamma_po = 0.25  (0.25→0.28 만 보면 0.033 dB) | 0.244 | mavic4pro · WiFi 5.21 GHz · prop=0.460 | 선언된 선택 · 근거 문장은 반증됨 |

**셸 0.28 — 부호가 틀렸다.** 1~3 mm ABS 셸의 정면입사 박막 |Γ| 는 0.033~0.254 로 벌크 프레넬 0.244 **아래**다. 0.28 이 나오려면 셸이 9.7/5.1/3.4 mm 여야 한다. 박막 유도값으로 통째로 갈아끼우면 총 σ 는 최대 0.854 dB 움직인다 ⟨`material_sources.json : impact_sweep.by_drone`⟩ — 즉 파라미터는 **선언하고 유계로 두면** 방어된다. 두께 자체는 출처가 없다: DJI 는 셸 두께도 수지 종류도 공개하지 않고, 공개 티어다운에도 벽 두께 실측이 없다.

**우선순위가 뒤집혀 있었다.** note 분량이 가장 두꺼운 프로펠러 파라미터는 스윕 전 구간(0.10~0.46)에서 총 σ 를 0.244 dB 움직이고, 논쟁의 대상이던 0.25 → 0.28 변경만 보면 0.033 dB 다. 같은 성격으로 출처가 없는 camera_assembly 는 스윕 전 구간(0.50~1.00)에서 1.809 dB — 프로펠러 전 구간의 7.4 배, 0.25 → 0.28 항의 54 배다. 실물에서 먼저 확인할 부품은 짐벌·카메라 조립체다.

**프로펠러 note 의 근거 문장은 우리 CAD 자신이 반증한다.** note 는 '날개가 셸(1~3 mm)보다 얇다'를 전제로 0.25 를 골랐는데, drone_cad 의 두께 법칙에서 유도한 날개는 시위평균 0.80~1.99 mm, s1000plus 최대 4.15 mm 로 셸을 넘어선다 ⟨`material_sources.json : propeller.per_drone`⟩. 값 0.25 의 결론은 살아남고 근거가 바뀐다 — 이제 그것은 유도값이 아니라 **선언된 상수**다.

**탄소 행이 0.000 dB 인 이유.** 주 스윕의 세 기체(phantom4 · mavic4pro · mini5pro)에 carbon 그룹이 아예 없다. 실제로 쓰는 기체로 다시 재면 1.210 dB 다 (s1000plus · LTE 1.843 GHz · carbon=0.7000). '무해'가 아니라 '해당 없음'이었다.

### §3.5 재질은 레벨을 정하고, 기울기는 재질 밖에서 정해진다

| 양 | 값 |
|---|---|
| 방어 가능한 브래킷(V1 벌크 프레넬 / V2 0.10 / V3 0.45)의 레벨 진폭 | 1.136 dB |
| 같은 브래킷의 3밴드 기울기 진폭 | 0.430 dB/GHz |

재질은 레벨을 정하고, 밴드 의존성은 재질 밖에서 정해진다. 모든 gamma_po 가 주파수 평탄한 실수 상수이기 때문이다 ⟨`engine_validation_verdict.json : 3_materials.bounded_total`⟩.

제안 패치 6건은 `outputs/material_sources.json : proposed_patch` 에 있다. src/materials.py 는 손대지 않았다 — 패치 6건은 제안으로만 있다.

---

## §4. 대조가 성립하는가 — 성립한다

성립한다 — 다만 **측정 체인 1개가 잰 기체 1종에 대한 정합성 검사**이지 절대 σ 의 검증이 아니다. 그 문장은 각주가 아니라 편 제목에 들어간다 ⟨`validation_feasibility.json : 5_verdict.is_a_valid_comparison_possible`⟩.

타당성 단계의 차단 질문(‘Phantom 2 의 수치가 근거리장이면 비교가 무너진다’)은 **불필요했다**. Das Table I 을 350 dpi 렌더에서 직접 읽으면 1.8~18.2 GHz · 2801점 · 원거리장 · 무향은 **Phantom 3** 열이고, Phantom 2 는 11~26 GHz · 2001점 · 근거리장 2.6 m 다. Phantom 2 는 대역 밖이라 애초에 후보가 아니었다 ⟨`validation_feasibility.json : 0_corrections_to_the_workflow_brief`⟩.

남은 미통제 항:

- Yuan 이 잰 Phantom 3 변형(Standard / 4K / Advanced / Professional)이 어느 것인지
- CATR 정숙영역 지름과 진폭·위상 테이퍼 — 미발표. Phantom 3 의 원거리장 성립은 두 논문의 선언으로 남아 있다
- 표적 거치대 — 두 논문 모두 기체가 무엇 위에 있었는지(스티로폼 기둥·파일런), 게이팅 후 그 기여가 얼마인지 적지 않았다
- 측정 당시 프로펠러의 방위 위상
- Das 의 Phantom 3 행이 Yuan 의 동일 원자료를 재적합한 것인지, 참고문헌 [7](Wang et al., China Commun., 2026) 경로로 재처리된 것을 적합한 것인지 — 2.5 dB 규약·처리 모호성이 이 두 논문만으로는 풀리지 않는 이유
- 1.8~5.2 GHz 에서 Phantom 급 기체의 교차편파 응답 — 스칼라 커널의 VV 대비 편향을 유계하는 데 필요한데, 발표된 곳을 찾지 못했다
- Phantom 3 앵커를 다른 기체로 옮길 때의 크기전이 지수 n ∈ {2, 4} — Phantom 3 를 직접 지은 덕분에 이 대조에서는 정할 필요가 없었다

(원문 목록: `validation_feasibility.json : 8_open_and_uncontrolled_terms`)

이 대조로도 닫히지 않는 것을 닫는 측정은 하나다 — **우리 기체 1종의 자체 모노스태틱 RCS 측정** (1.8~5.3 GHz, 정확 Mie σ 를 우리가 직접 계산하는 PEC 구로 교정, 배경 차감과 거리 게이팅 포함). 그것이 저장소의 모든 '앵커된' 주장을 '측정된' 주장으로 바꾼다 ⟨`validation_feasibility.json : 5_verdict.if_this_is_not_enough_what_measurement_is_needed`⟩. 설계는 `outputs/measurement_plan.json` 에 이미 있다.

---

## §5. 비용

| 단계 | SBR 실행 [건] | GPU [s] | 어떻게 셌나 |
|---|---|---|---|
| sphere+band | 15 | 296 | 로그 실측(구 3건은 초 미기록) |
| sweep 1.8-18.2 GHz 21점 | 21 | 2303 | 로그 실측 |
| group 분해 | 57 | 762 | 광선율 유도 k=6.014e-04 s/(ray·az) |
| material bracket | 12 | 160 | 광선율 유도 k=6.014e-04 s/(ray·az) |
| scan 대조(3.09 M tri) | 6 | 170 | 벽시계 상한(로그 mtime 차) |
| **대조 단계 합** | **111** | **3692** (1.03 h) | |
| 재질 단계 — impact sweep | 216 | 626 | 로그 실측 · 건수는 JSON 셀 수 |
| 재질 단계 — carbon 보충 | 36 | 397 | 로그 실측 · 건수는 JSON 셀 수 |
| 타당성 단계 | 0 | 0 | PDF 판독 · 회귀 · 규약 감사 |
| **전체** | **363** | **4715** (1.31 h) | |

카드 0·1 은 타 사용자가 점유 중이라 2·3 만 썼다. 광선율 모델은 sweep 로그의 (rays/az, 초) 21쌍에 원점 통과 최소제곱을 세운 것이다 — k = 6.014e-04 s/(ray·az).

### §5.1 두 번째 기체는 얼마인가

| 항목 | 값 |
|---|---|
| 재사용되는 것 | 교정구 대조(측정 체인 공통), 문헌 회귀, 규약 감사 |
| 새로 드는 것 | 메쉬 제작 · 밴드 3점 · 전대역 21점 · group 분해 · 재질 브래킷 |
| 한계 GPU 비용 | 3522 s (0.98 h) |
| scan 단계 | 실물 레이저 스캔이 있는 기체에만 붙는다 — 현재 저장소에 있는 스캔은 Phantom 4 하나뿐이다 |

**그러나 후보가 없다.** ⚠ FR1 에서 우리 5기종에 대응하는 공개 측정이 없다. Das 의 나머지 세 기체는 11~27 GHz 다. 즉 두 번째 대조 기체는 GPU 시간으로 사는 것이 아니라 측정으로만 산다. 두 번째 대조 기체를 결정하는 근거는 GPU 예산이 아니라 07편의 측정 설계다.

---

## §6. 다음 단계

| 다음에 할 일 | 그러면 결정되는 것 | 어디서 |
|---|---|---|
| rcs_anchor.json 재생성 | 현재 저장값이 세 번째 독립 하네스와도 어긋난다 — -2.686~-1.658 dB. 03 의 그림을 그리기 전에 닫아야 하는 유일한 선행 작업 | benchmark/rcs_anchor.py |
| PTD(모서리 회절) 항을 커널에 넣는다 | 좁은대역 기울기 잔차 0.648 dB/GHz (39%) 가 닫히는지. ⚠ 잔차는 재질 브래킷 0.430 dB/GHz 의 1.5 배뿐이라 지목은 지시적이지 확정이 아니다 | src/rcs_sbr.py |
| Phantom 3 를 DRONES 레지스트리에 등록 | 03 을 재현 가능한 편으로 만든다. 패치 문자열은 validate_measured_airframe.json : 1_airframe.registration_patch 에 있다 | src/drones.py · src/drone_cad.py · benchmark/rcs_anchor.py |
| 복소 Γ 를 커널에 넣는다 | 박막 실효 Γ 를 도입하려면 위상이 짝이다 — 금속 대비 상대위상이 70° 어긋난다 ⟨material_sources.json : proposed_patch⟩ | src/rcs_sbr.py · src/rcs_po.py |
| camera_assembly 를 실물에서 확인 | tier-3 중 총 σ 를 가장 크게 움직이는 항 — 스윕 전 구간 0.50~1.00 에서 1.809 dB (mavic4pro · LTE 1.843 GHz · camera=0.50). 프로펠러 전 구간의 7.4 배 | 실물 티어다운 |
| 우리 기체 1종의 자체 모노스태틱 RCS 측정 (1.8~5.3 GHz, PEC 구 교정) | FR1 에서 우리 5기종에 대응하는 공개 측정이 없다 — 두 번째 대조 기체는 문헌이 아니라 측정에서만 나온다 | 07편(실측 계획) · outputs/measurement_plan.json |

⭐ 첫 줄이 선행 조건이다. `outputs/rcs_anchor.json` 은 2026-07-30 자 파일이고, 저장된 phantom4 값이 이후 두 개의 독립 하네스와 -2.686~-1.658 dB 어긋난다 ⟨`validate_measured_airframe.json : 8_comparison.I_crosschecks`⟩. 드리프트의 방향이 사정을 **나쁘게** 만든다(레벨은 더 낮고 기울기는 더 가파르다). 03편의 그림을 그리기 전에 재생성한다.

